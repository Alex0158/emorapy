# CJ Batch 3 治理/共用/架構對碼核對記錄（2026-04-18）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：治理記錄
**來源時間**：2026-04-18
**上下文**：非現行 SSOT；僅作文檔治理歷史記錄
**SSOT 屬性**：非現行 SSOT（僅作證據/歷史/治理參考）
**最後核驗 Commit**：`c4b2d90`
**最後核驗日期**：`2026-04-19`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 範圍

本次核對 Batch 3 三個子域：

1. `03-管理端與平台治理/*`
2. `04-共用機制/*`
3. `05-工程架構與共享層/*`

## 取證入口

1. `frontend/src/router/index.tsx`
2. `frontend/src/utils/adminEntry.ts`
3. `backend/src/middleware/adminAuth.ts`
4. `backend/src/routes/admin.routes.ts`
5. `frontend/src/services/request.ts`
6. `frontend-admin/src/services/request.ts`
7. `package.json`
8. `frontend/tsconfig.app.json`
9. `frontend-admin/tsconfig.app.json`
10. `backend/tsconfig.json`
11. `mobile/tsconfig.json`
12. `scripts/check-docs-truth.mjs`

## 本輪發現與處置

| 編號 | 發現 | 影響 | 處置 |
|---|---|---|---|
| B3-01 | `03/04/05` 子域中的高價值聲明（AdminRedirect 邊界、RBAC 清單、request 雙堆棧、workspace/alias 現況）尚未形成獨立機器守衛 | 後續改動可能只改代碼不改文檔，漂移難及時暴露 | 在 `scripts/check-docs-truth.mjs` 新增 Batch 3 真實性校驗，失配即 CI 失敗 |
| B3-02 | tsconfig 使用 JSONC（含註釋），不能直接 `JSON.parse` | 守衛腳本可能在讀取 tsconfig 時中斷 | 改為以文本規則檢測 alias 聲明，避免 JSONC 解析脆弱點 |
| B3-03 | `03/04/05` 正式文檔中的反引號測試/腳本路徑尚未納入存在性校驗 | 文檔可長期保留失效 `*.test.ts(x)/*.e2e.ts` 或 `scripts/*` 引用而不被 CI 擋下 | 新增 Batch3 正式文檔反引號路徑存在性守衛（測試 + 腳本） |

## 守衛補強

本輪新增以下 Batch 3 真實性守衛：

1. root workspace 契約（僅 `frontend`、`frontend-admin`）與 `05` 子域文檔一致性。
2. `frontend` 已接入 `@cj/contracts/@cj/api-client` alias 的文檔一致性。
3. `frontend-admin` 仍為 `@/*` 本地 alias、尚未接入共享 alias 的文檔一致性。
4. `backend/mobile` 共享 alias 為「預留態」的文檔一致性。
5. Web/Admin request 雙堆棧（`frontend/src/services/request.ts` vs `frontend-admin/src/services/request.ts`）與 `04` 子域一致性。
6. `admin/* -> AdminRedirect -> VITE_ADMIN_LOGIN_URL(絕對 URL)` 邊界與 `03/04` 子域一致性。
7. RBAC 權限集合（`ops/config/users/reports/alerts/admin:all`）與 `03` 子域權限清單一致性。
8. `audit-logs`、`alerts/rules` 的 `requireAdminPermissionAll` 雙權限矩陣與 `03` 子域一致性。
9. `03/04/05` 正式文檔中的反引號測試/腳本路徑（`*.test.ts`/`*.test.tsx`/`*.e2e.ts`、`scripts/*`）存在性校驗。

## 2026-04-19 再核驗補記

1. 以 `f347125` 為基準完成 Batch 3 逐文件再核驗，`03/04/05` 全部 12 份文檔元數據已更新。
2. 本輪未發現新的正文語義漂移：Admin 邊界、RBAC、request 雙堆棧、workspace/alias 現況與現碼一致。
3. Batch 3 真實性守衛維持有效，無需新增降級規則或豁免。
4. 已新增 `03/04/05` 正式文檔反引號測試/腳本路徑存在性守衛，當前未檢出失效引用。

## 驗證結果

執行命令：

```bash
npm run docs:check
```

結果：

1. `docs:check:structure` PASS
2. `docs:check:truth` PASS（`126 endpoints / 30 frontend routes / 9 admin routes`）

## 結論

1. Batch 3 文檔與現碼主幹一致；本輪除元數據同步外，未發現需新增正文回寫的事實漂移。
2. Batch 3 關鍵聲明已轉為守衛條件，不再依賴人工口耳相傳。
3. 後續若改動 Admin 邊界、RBAC、request 堆棧或 alias/workspace 現況，未回寫核心文檔時會直接被 `docs:check:truth` 擋下。
