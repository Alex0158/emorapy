# App 跨端 Parity 落地待辦（2026-05-05）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：問題治理
**覆蓋範圍**：App 版承接跨端產品核心、共享 contracts / api-client、原生能力與 Web 基線 Parity 的待處理任務
**取證代碼入口**：`mobile/package.json`、`mobile/tsconfig.json`、`mobile/app/_layout.tsx`、`mobile/app/modal.tsx`、`mobile/src/platform`、`packages/contracts/src`、`packages/api-client/src`、`frontend/src/router/index.tsx`、`backend/src/routes`
**最後核驗 Commit**：`3890ba8`
**最後核驗日期**：`2026-05-08`
<!-- CORE_DOC_AUDIT_METADATA:END -->

**狀態**：進行中
**Owner**：Mobile / Frontend / Backend
**關聯核心文件**：`20-App端/00-App端總覽.md`、`20-App端/01-App導航與平台Adapter基線.md`、`50-跨端Mapping與Parity/00-跨端Parity總覽.md`、`50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md`、`08-測試規範與驗收/03-App測試與證據接入基線.md`

---

## 1. 背景

核心文件已把 Web 版與 App 版調整為同一產品核心的不同平台投影。當前 `mobile/` 已存在 Expo 專案骨架；2026-05-08 已移除 Expo template tabs，建立 CJ route group shell、provider / UI foundation、root error boundary、runtime config 與 API / SecureStore / SSE / upload / notifications / linking / lifecycle / telemetry adapter，並把 mobile bundler 接到 root workspace shared package resolution。M0 provider render、auth/session bootstrap、root error boundary 與 upload / notifications / lifecycle adapter branch tests 已有首輪 gate；2026-05-09 已新增 `routes:check`，固定 `(public)` / `(app)` / Quick / Auth / Case / Chat / Profile / Notifications / Repair / `modal` route topology 並阻止 template tabs 回流；`features:check` 已把 M0-M5 source coverage 釘到 route `testID`、screen action、shared API endpoint、SSE / Deep Link / Push / Upload / Telemetry adapter 與 Backend notification / telemetry / push route contract；`web:routes:smoke` 已把 16 條 App web export route 的 desktop + phone viewport 非空渲染 / page error / 主 CTA 樣式檢查納入 `release:preflight`。M1 Quick / Auth、同機 collaborative quick、quick submit session recovery、result access expired cleanup、result `case_judgment` stream replay、Quick result AppState reconnect RNTL gate、M2 Profile / Interview、M3 Chat、M4 Formal Case / Repair、M5 Notification / Deep Link / Upload / Telemetry 的 shared domain client 與 App screen / adapter 首輪接線已落地；M1 Quick result、M2 Interview、M3 Chat 與 M4 Repair replan 已接 App AI stream subscription hook，覆蓋 ready snapshot、`after_seq` replay、close/error retry 與 screen-level AppState recovery gate；AI stream persistence migration 已補齊 `ai_stream_sessions` / `ai_stream_events` / archive tables，AI stream backend 已補 subscription live race gate、SSE no-compression gate 與 live DB replay / snapshot fallback，M2 interview facts migrations 已補齊 `interview_sessions.collected_facts` / `interview_turns.extracted_facts`，M5 notification action metadata migration 已補齊 read / snooze / dismiss / act state 欄位；M0-M5 Maestro flow artifact static gate 已建立；M1-M5 true-service smoke harness 已建立 dry-run gate，可在顯式 `--run` 下跑 anonymous quick/chat 與 token-gated profile/formal case/notification probes，並已在本機隔離 Postgres + local backend + `--bootstrap-local-users` 下跑通 M1 claim-session、M1 `--m1-stream-replay` `case_judgment` SSE `after_seq` replay、local-only `--m1-inject-expired-session` 過期 submit recovery / result access reject / expired claim ignored、M2 `--deep` 5-turn interview + my-story completion、M3 `--request-ai` judgment、M4 `--request-ai` formal evidence upload / repair plan select / execution confirm / live replan stream persisted / backend restart 後 DB-backed replay、M5 notification state sync / evidence upload / telemetry ingest；Push device token registration / revoke 已新增 backend schema/API/client 與 App token sync，backend notification read / snooze / act / register / revoke 已補 service/controller/route contract gate，Expo push sender、pending push dispatch job 與 receipt polling job 已接線，App telemetry 已接 `POST /api/v1/telemetry/events` safe ingest、`POST /api/v1/telemetry/otlp/v1/traces` CJ OTLP JSON trace ingest、backend 二次清洗、structured log、最小化 persistence、Admin 聚合報表與 30d cleanup，App observability bootstrap 已接 session start、lifecycle transition、global JS fatal / unhandled promise、OpenTelemetry provider span export 與 native crash SDK configuration first pass，並新增 `telemetry:runtime:smoke` / `App-Telemetry-Runtime-*.json` 的 telemetry runtime evidence runner；backend crash-free 聚合已納入 JS fatal / unhandled promise，logout cleanup 已接本機清理前 revoke 補償，registration-time token rotation revoke 已接線，notification landing handler 與 non-notification Deep Link handler 已接 cold-start / foreground response target / post-login resume；普通用戶可見文案已完成 UI/UX 清理，Quick、Auth、Profile Interview、Chat、Repair、Notifications、連線狀態與 Error Boundary 不再暴露 `anonymous session`、`SecureStore adapter`、未完成 push route、`stream seq`、`solo_active`、`share_summary_only`、`all-visible user message`、`active/background` 等工程詞或 backend enum，並新增 `copy:check` 納入 `release:preflight` 防回歸；mock-backed backend smoke 已通過。App release-sensitive migrations 已有 release DB parity 清單覆蓋單測，且 local DB `ops:release-db:check` 已回報 14/14 required migrations applied；release / production DB parity 已有 non-local pass evidence。Telemetry runtime evidence 已於 2026-05-29 產出 release backend version 對齊、event ingest 與 OTLP ingest pass evidence；App 尚需按 M0-M6 補齊 native、contract、native crash runtime evidence、external tracing backend 與 flow 驗收。

這不是文檔問題，而是 App 版正式開發前需要閉環的代碼與工程問題。

## 2. 待處理範圍

待處理範圍已按 [../../20-App端/03-App完整版本開發Roadmap.md](../../20-App端/03-App完整版本開發Roadmap.md) 收斂為 M0-M6。原粗粒度任務仍成立，但後續執行、驗收與狀態更新一律按下表追蹤。

| Roadmap | 任務 | 優先級 | Owner 面 | 依賴文件 | 驗收 gate | 狀態 |
| --- | --- | --- | --- | --- | --- | --- |
| M0 Foundation | 替換 Expo template，建立 CJ App shell、route group、provider、error boundary、App smoke | P0 | Mobile / Frontend | `20-App端/01-App導航與平台Adapter基線.md`、`20-App端/03-App完整版本開發Roadmap.md` | App launch smoke、navigation inspection | 部分落地；shell / route group / providers / provider render / root error boundary / web smoke / adapter branch tests、M0-M5 Maestro flow artifact 與 29 個 selector static gate 已完成；以 Full Xcode 26.4.1 / iOS 26.4.1 / iPhone 17 simulator 已跑通 Release build 與 native Maestro 7/7 smoke；仍需 TestFlight / physical device / native side-effect 長鏈路證據 |
| M0 Foundation | 建立 App API adapter，支援 JWT、`X-Session-Id`、`X-Locale`、request id、FormData、typed error normalize | P0 | Mobile / Frontend / Backend | `50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md`、`06-接口描述/11-API契約與OpenAPI缺口台賬.md` | API adapter unit / header tests | 部分落地；adapter skeleton、header/error unit、FormData helper、upload web/native branch unit 與 M1 domain client 接線已完成，dev backend upload contract 待補 |
| M0 Foundation | 實作 SecureStore token / anonymous session / logout clear adapter | P0 | Mobile / Security | `20-App端/02-App完整版本工程PRD.md`、`04-共用機制/05-威脅建模與安全需求基線.md` | Platform adapter tests | 部分落地；SecureStore skeleton、auth/session bootstrap、logout clear 與 unit gate 已完成，iOS simulator native smoke 已補；physical device SecureStore evidence 待補 |
| M1 Quick + Auth | 承接 quick create / collaborative / result / AI stream replay / claim-session / login / register | P0 | Mobile / Frontend / Backend | `功能特性清單.md`、`業務流程整合.md`、`20-App端/02-App完整版本工程PRD.md` | Quick + Auth App smoke | 部分落地；quick session / quick case / 同機 collaborative quick / quick submit session recovery / result access expired cleanup / result fetch / pending polling / result `case_judgment` stream replay / login / register / claim-session 與 claim failure 不阻斷登入已接線，backend expired claim guard 已補，RNTL 最小互動、collaborative A/B handoff、stream persisted refetch、Quick result AppState background abort / foreground `afterSeq` reconnect、M0-M5 Maestro flow artifact / selector static gate、mock-backed backend smoke、`smoke:true-service` dry-run gate、本機隔離 Postgres `--run --scope=all --bootstrap-local-users` anonymous quick / judgment poll / claim-session、`--run --scope=m1 --bootstrap-local-users --m1-stream-replay` `case_judgment` true-service `after_seq` replay，以及 local-only `--run --scope=m1 --bootstrap-local-users --m1-inject-expired-session` 過期 submit recovery / result access reject / expired claim ignored 已通過；iOS simulator native quick/auth smoke 已通過，native reconnect 真機證據仍待補 |
| M2 Profile + Interview | 承接 profile shell、consent、interview SSE、result、my-story | P1 | Mobile / Frontend / Backend | `02-用戶端核心流程/00-用戶端核心流程總覽.md`、`08-測試規範與驗收/02-AI流式與Chat治理驗收基線.md` | SSE recovery smoke / profile regression | 部分落地；shared client、Profile/Interview/My Story screen、App AI stream subscription hook、ready snapshot / `after_seq` replay / close-error retry / Interview screen background abort / foreground `afterSeq` reconnect、Profile/Interview/Story auth-resume Maestro flow artifact、RNTL 最小測試與本機隔離 Postgres `--run --scope=m2 --bootstrap-local-users --deep` profile/consent/5-turn interview start/respond/background response/end/my-story completion 已完成；failed retry UI、partial-success read-only UI 與 My Story feedback-card summary 已補 RNTL gate；local-only `--m2-inject-failure` 已驗證 failed session readback / resume / retry accept，local-only `--m2-inject-partial-success` 已驗證 `partial_success=true` / retry reject；iOS simulator native auth-resume smoke 已通過，native lifecycle recovery evidence 仍待補 |
| M3 Chat | 承接 room、invite、message stream、chat-room AI draft、request judgment、auth handoff | P1 | Mobile / Frontend / Backend | `06-接口描述/07-chat.md`、`50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md` | Chat e2e / handoff smoke | 部分落地；shared client、Chat Home/Room/Invite screen、room event stream parser、chat-room AI draft replay/recovery hook、Chat Room background abort / foreground `afterSeq` reconnect、invite accept 未登入 auth resume、notification invite landing path 映射、Chat entry/auth-gate Maestro flow artifact、RNTL 最小測試、web route smoke 與本機隔離 Postgres `--run --scope=m3 --bootstrap-local-users --request-ai` room/message/invite/list/status、partner accept、request-judgment 已通過；iOS simulator native chat entry smoke 已通過，native stream reconnect 與真 push invite landing evidence 仍待補 |
| M4 Formal Case + Repair | 承接 pairing、case、judgment、reconciliation、execution、replan stream | P1 | Mobile / Frontend / Backend | `功能特性清單.md`、`業務流程整合.md`、`04-共用機制/06-狀態機與業務不變式治理基線.md` | Formal case + repair regression | 部分落地；shared client、Case/Repair screen、Case evidence 顯式 safety assertion handoff、formal case evidence upload true-service smoke、repair replan trigger、`repair_track` stream replay/recovery hook、Repair screen background abort / foreground `afterSeq` reconnect、Case/Repair auth-gate Maestro flow artifact、RNTL 最小測試、web route smoke 與本機隔離 Postgres `--run --scope=m4 --bootstrap-local-users --request-ai` pairing/case/evidence upload/judgment/execution dashboard/repair plan generation/plan select/execution confirm/live replan stream persisted 已通過；AI stream subscription race、SSE compression、live DB replay/snapshot fallback 與 notification key length 已補 backend gate；backend restart 後 DB-backed stream replay 已通過；iOS simulator native case/repair auth smoke、Android release APK native picker-cancel smoke 與 selected-media backend synthetic fixture upload/delete smoke 已通過，native reconnect、真機 picker-selected asset / profile media、repair 全狀態證據仍待補 |
| M5 Push + Deep Link | 裁決 device token registration / revoke、notification landing、read / snooze / act sync | P0 | Mobile / Backend / DB / Security | `06-接口描述/08-content-notification.md`、`05-工程架構與共享層/03-資料模型SchemaMigration與相容性治理基線.md` | Backend service/controller/route/job contract + Backend smoke + Deep Link smoke + native evidence 待補 | 部分落地；notification landing handler、post-login resume、notification/deep-link auth-resume Maestro flow artifact、read/snooze/dismiss/act sync、target resolver、Chat invite landing path、device token registration / revoke / logout cleanup、registration-time token rotation revoke 已完成，backend service/controller/route contract gate 已覆蓋 owned notification guard、state sync、non-sensitive token response 與 chat invite landing allowlist，Expo push sender / pending dispatch / receipt polling 已覆蓋 provider envelope、token redaction、ticket 保存、sent/failed/receipt 回寫，App-side `push-delivery:smoke` structured provider runner 已建立但尚未取得 pass JSON，local true-service `--scope=m5 --bootstrap-local-users` 已通過 push token register/revoke、notification state sync、evidence multipart upload/delete 與 telemetry ingest；iOS simulator custom-scheme native smoke 已通過，真 provider delivery、APNs sandbox、真機 cold-start notification response 與 provider lifecycle evidence 仍待補 |
| M5 Push + Deep Link | 實作 Deep Link auth failure fallback，不把 parse success 視為授權成功 | P0 | Mobile / Backend / Security | `04-共用機制/05-威脅建模與安全需求基線.md`、`08-測試規範與驗收/03-App測試與證據接入基線.md` | Deep Link fallback smoke | 首輪落地；backend target -> App route resolver、Quick child route preservation、notification response route handoff、non-notification Deep Link cold-start / foreground handler、pending href storage 與 `/auth?next` post-login resume 已測，真機 cold-start / foreground native evidence 待補 |
| M6 Release Hardening | 建立 iOS TestFlight、EAS Update policy、Android readiness、App evidence pack、telemetry report / retention / OTel baseline 裁決 | P0 | Mobile / Ops / QA | `03-管理端與平台治理/02-發布與回滾檢查表.md`、`08-測試規範與驗收/03-App測試與證據接入基線.md`、`06-接口描述/10-health-metrics.md` | EAS build smoke、device evidence、telemetry evidence | 部分落地；app identity、runtimeVersion、EAS profiles、release readiness script、Expo SDK 55 native dependency alignment gate、iOS native readiness gate、Android config readiness gate、Android native toolchain strict gate、Android emulator boot smoke、Android release APK install/launch smoke、Android Maestro 7/7 flow smoke、native upload picker-cancel smoke、selected-media backend upload/delete smoke、structured EAS iOS / TestFlight runner、structured EAS Android runner、structured physical device smoke runner、structured push provider delivery runner、structured native crash runtime runner、release DB parity dry-run、release evidence verifier、release evidence redaction check、release evidence sanitization check、external evidence status gate、external evidence status JSON contract gate、external evidence fixture contract gate、release completion audit gate、native Maestro execution runner、7 個 Maestro flow artifact / 29 個 selector static gate、iOS simulator Release build / install / launch evidence、native Maestro 7/7 evidence record、App telemetry safe ingest / CJ OTLP JSON trace ingest / minimized persistence / Admin report / 30d cleanup route/service/job/smoke gate、App observability bootstrap / OpenTelemetry provider span / JS fatal safe telemetry first pass、App-side OpenTelemetry provider evidence、native crash SDK configuration evidence、Android native toolchain evidence、Android emulator runtime evidence、Android app runtime evidence、Android full flow evidence、native ImagePicker evidence、AI stream persistence migration 與 release-blocking 清單已建立，App migration 清單覆蓋單測與 local DB `ops:release-db:check` 14/14 已通過；`release:evidence-redaction:check` 會掃描既有 `App-*.json` evidence pack，阻止 raw Bearer / push token / DB URL / JWT / API key / signed URL / EAS raw artifact URL 欄位；`release:evidence-sanitization:check` 會用受控假 secret 驗證 blocked evidence 不寫 raw Expo token / EAS build id / push token / access token / Sentry auth token / event id，並用 clean / leak prerequisite report 固化 `release:evidence-redaction:check -- --evidence-dir=<tmp>` 的 workflow runner temp 掃描正反驗收；`release:external-evidence:status` 會以不輸出 secrets / UDID / DB host 的方式列出 credential presence（含 push delivery token、Sentry runtime query credentials 與 native crash event id presence）、真機可見性、structured evidence candidate 與下一步命令；`release:external-evidence:status:contract` 會用受控 fake EXPO / Apple / ASC / push / Sentry / native crash event id / DB secrets 驗證 JSON / stdout / stderr 只輸出 presence booleans，不回洩 raw secret；`release:external-evidence:fixtures:check` 會用受控 fixture 復用 strict release audit validators，驗證 EAS iOS + TestFlight、EAS Android、physical device、provider delivery、native crash runtime 與 release DB parity pass / fail evidence 裁決；2026-05-08 本機 iOS physical connected=0 / offline=2、Android physical connected=0；EAS/TestFlight evidence、Android EAS evidence、physical device evidence、provider delivery evidence、native crash runtime evidence、telemetry runtime evidence 與 release-production DB parity evidence 均已收緊為 structured JSON，EAS iOS / TestFlight 必須由 `mobile/scripts/run-eas-ios-release-smoke.mjs` 證明 EAS finished iOS store production build + artifact HEAD/range + App Store Connect TestFlight build valid/not expired，Android EAS 必須由 `mobile/scripts/run-eas-android-release-smoke.mjs` 證明 EAS finished Android store production build + artifact HEAD/range + package/version/versionCode match，physical device 必須由 `mobile/scripts/run-physical-device-smoke.mjs` 證明真機 + native launch + M0 Maestro，provider delivery 必須由 `mobile/scripts/run-push-delivery-smoke.mjs` 證明 Expo ticket accepted + receipt ok，native crash runtime 必須由 `mobile/scripts/run-native-crash-runtime-smoke.mjs` 證明 Sentry event found + release / production environment match + native runtime + crash-like event，telemetry runtime 必須由 `mobile/scripts/run-telemetry-runtime-smoke.mjs` 對 non-local release API 先驗 backend `/version.commitSha` 對齊 runner 執行當下 `HEAD`，audit 另會拒絕 backend telemetry/version runtime 路徑已有後續漂移的舊 evidence，再產出 event + OTLP `blocked=false` 的 `App-Telemetry-Runtime-*.json`，DB parity 必須由 `backend/scripts/check-release-db-parity.ts` 產出 non-local `App-Release-DB-Parity-*.json`，`ops:release-db:dry-run` 只驗證 runner 入口且不能消除 blocker，local DB/raw console/markdown 不能消除 blocker；`npm --prefix mobile run release:completion:audit` 會把 EAS project id / Expo token / Apple credentials / TestFlight build artifact / Android EAS artifact / physical device evidence / APNs delivery / native crash runtime evidence 持續列為 blocker，release DB parity 與 telemetry runtime 目前已有 pass evidence，strict mode 未清零前不得通過；Android physical device evidence、EAS Android artifact、真機 picker-selected asset / profile media 仍待補 |
| M6 Release Hardening | 建立外部 release sign-off 一鍵編排入口 | P0 | Mobile / Ops / QA | `08-測試規範與驗收/03-App測試與證據接入基線.md`、`90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md` | `release:external-evidence:signoff`、`release:external-evidence:signoff:android-dry-run`、`release:external-evidence:validate`、`release:external-evidence:fixtures:check`、`release:external-evidence:run`、`.github/workflows/app-release-external-signoff.yml`、`release:completion:audit:strict` | 已建立 `mobile/scripts/run-release-external-evidence-signoff.mjs` 與手動 GitHub workflow；workflow mode 已支援 `dry-run` / `validate` / `run`；workflow 在 signoff 前跑 `release:evidence-redaction:check` / `release:evidence-sanitization:check`，signoff 後再以 `--evidence-dir="${APP_RELEASE_EXTERNAL_SIGNOFF_REPORT_DIR}"` 跑 generated evidence redaction，同時掃 repo evidence 與 runner temp prerequisite report 目錄，且只有 redaction step 通過才 upload artifact；workflow run-mode probe 會按 `physical_platform` 分別要求 iOS `APP_IOS_DEVICE_UDID` + `APP_IOS_DEVICE_APP_PATH` 或 Android `APP_ANDROID_DEVICE_SERIAL`；`release:external-evidence:signoff` 預設 dry-run 覆蓋 iOS physical branch，`release:external-evidence:signoff:android-dry-run` 覆蓋 Android physical-platform branch，兩者均已納入 `release:preflight` 作 orchestrator 入口回歸 gate；`release:external-evidence:fixtures:check` 已納入 `release:preflight`，用受控 fixture 驗證 strict release audit 對外部 evidence pass / fail JSON 的裁決；`release:external-evidence:validate` / run mode 會先做 prerequisite validation，缺 UUID 形狀 `extra.eas.projectId`、secrets、真機 inputs、`xcrun xctrace` / `adb devices -l` 真機可見性 / requested-device match 或 release DB 時在碰 EAS / provider / 真機安裝 / release DB 前失敗，並可輸出不含 secrets / UDID / serial 的 `App-External-Signoff-Prerequisites-*.json` prerequisite report artifact 供交接查看；orchestrator 支援 `--release-env-file=release.env.local`，只解析白名單內 `KEY=value` / `export KEY=value`，不 eval、不輸出 value，且 `REPLACE_WITH_...` placeholder 不算已配置，`NODE_OPTIONS` / 非白名單 key 會在 validation 前被拒絕；本地契約已驗證 validate-only、Android physical-platform validate、placeholder release-env-file validate、unsafe release-env-file key 與正式 run 的前置失敗都不泄漏 stdout/stderr/report，report 含 iOS / Android `device_visibility` counters 與 requested-device match，且 Android validate 在無 authorized physical device 時會前置失敗、正式 run 不開始 EAS / provider / 真機安裝 / release DB / strict audit steps；正式 `release:external-evidence:run` 不允許 `--skip`，不能跳過外部 evidence 或 strict audits；`release:external-evidence:run` / workflow run mode 在 secrets / 真機 / release DB 準備好後順序執行 EAS iOS + TestFlight、EAS Android、physical device、push provider、native crash runtime、release DB parity 與 strict audits。orchestrator / workflow / prerequisite report 不能替代各 structured pass evidence JSON，strict audit 未通過前仍不得宣稱完成；剩餘外部輸入與解除條件已拆到 `07-待處理問題與治理/待處理/App外部ReleaseSignoff待辦-2026-05-16.md` 單獨追蹤 |
| M6 Release Hardening | 固定 external handoff 的平台化真機驗收命令 | P0 | Mobile / Ops / QA | `90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md`、`20-App端/03-App完整版本開發Roadmap.md` | `release:external-evidence:handoff:check`、`release:external-evidence:handoff:contract` | 已把 handoff catalog 中的 physical device owner action 固定為 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排命令加底層 `physical-device:smoke` 命令；contract gate 會直接檢查 platform-specific validate / run command string，避免外部 release owner 繞過 prerequisite validation、status / handoff report、redaction 與 strict audit 編排 |
| M6 Release Hardening | 固定 external status 的平台化下一步命令 | P0 | Mobile / Ops / QA | `90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md`、`08-測試規範與驗收/03-App測試與證據接入基線.md` | `release:external-evidence:status`、`release:external-evidence:status:contract` | 已把 status next commands 固定為 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排入口、iOS signed app path、Android serial、底層 physical-device smoke 與 strict audit gate；contract gate 會直接檢查這些 command string，避免 status dashboard 與 handoff artifact 對外部 owner 指向不同路徑 |
| M6 Release Hardening | 建立 release completion audit 結構化 JSON contract | P0 | Mobile / Ops / QA | `90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md`、`90-證據與盤點/環境與發版驗證/App-Goal-Completion-Audit-2026-05-08.md` | `release:completion:audit -- --json`、`release:completion:audit:contract`、`release:preflight` | 已讓 release completion audit 輸出 `app-release-completion-audit` structured record，並用 contract gate 驗證 non-strict / strict JSON schema、required release blocker ids、頂層 `blocker_ids` 與 blocked checks 一致、check-level `handoff_blocker_ids` 只在 blocked check 非空、`checks[].handoff_catalog_ids` 保留 static catalog mapping、頂層 `handoff_blocker_ids` 與 external handoff catalog coverage 一致、summary 計數、strict exit-code 與 preflight wiring；用於 CI / dashboard / agent 交接，不替代任何外部 pass evidence |
| M6 Release Hardening | 對齊 ASC API credentials blocker 口徑 | P0 | Mobile / Ops / QA | `90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md`、`08-測試規範與驗收/03-App測試與證據接入基線.md` | `release:completion:audit`、`release:completion:audit:contract`、`release:external-evidence:status` | 已把 `app_store_connect_api_credentials` 納入 release completion audit required blocker ids，與 external status / handoff 對齊；缺 issuer id、key id、private key 或 private key path 時，strict completion audit 仍保持 not complete |
| M6 Release Hardening | 固定當前 release blocker / handoff blocker 快照 | P0 | Mobile / Ops / QA | `90-證據與盤點/環境與發版驗證/App-External-Evidence-Status-2026-05-29T17-17-48-569Z.json`、`90-證據與盤點/環境與發版驗證/App-External-Evidence-Handoff-2026-05-29T17-17-57-273Z.json`、`20-App端/03-App完整版本開發Roadmap.md` | `release:completion:audit -- --json`、`release:external-evidence:handoff:check -- --json`、`release:evidence:check`、`release:evidence-redaction:check` | 已落盤 secret-safe status / handoff 交接 JSON；當前 completion `blocker_ids` 固定為 EAS project id、Expo token、Apple submission credentials、App Store Connect API credentials、EAS iOS build artifact、TestFlight evidence、physical device evidence、EAS Android artifact、push provider delivery evidence、native crash runtime evidence。status 會以 `env_files.values_redacted=true` 與 `env_files.loaded[]` 交接 env-file provenance，不保存 secret value；handoff catalog 會把 10 個 current release completion blockers 標為 `release_completion_blocker=true`，另把 Sentry runtime query credentials、native crash event id 與 iOS / Android physical device visibility 標為 prerequisite-only blockers，便於外部 owner 分工但不替代 completion pass evidence；`release:evidence:check` 已把當前 status / handoff 的 App identity、known blocker ids、status/handoff 對齊、status env_files redaction / loaded provenance schema、當前 release completion audit `handoff_blocker_ids` handoff coverage、release/prerequisite classification、timestamp coherence、next commands / final gates 與文檔引用納入 preflight；release DB parity 與 telemetry runtime 已有 `candidate_pass` evidence 時，不再作為 completion blocker |
| M6 Release Hardening | 建立 `/goal` completion audit 與完成判定回寫 | P0 | Mobile / Ops / QA | `20-App端/03-App完整版本開發Roadmap.md`、`08-測試規範與驗收/03-App測試與證據接入基線.md`、`90-證據與盤點/環境與發版驗證/App-Goal-Completion-Audit-2026-05-08.md` | `goal:completion:audit`、`goal:completion:audit:contract`、`goal:completion:audit:strict`、`routes:check`、`features:check`、`web:routes:smoke`、`release:evidence:check`、`release:preflight` | 已建立 prompt-to-artifact checklist，覆蓋 stack、iOS-first / Android-compatible、M0-M5 artifact、route topology gate、feature coverage gate、web route smoke gate、UI/UX accessibility / copy gate、測試 gate、核心文檔回寫與 release sign-off；`goal:completion:audit -- --json` 在 `release_signoff` 未完成時會直接輸出 release completion audit 的 `blocker_ids` / `handoff_blocker_ids` / warnings / blockers；`goal:completion:audit:contract` 已固定 JSON schema / required ids / summary counts / strict exit-code contract，且要求 incomplete `release_signoff` caveat 包含 blocker/handoff ids，並要求 `tests_and_gates` 覆蓋 `routes:check`、`features:check`、`web:routes:smoke`、`release:evidence:check`、release evidence external status/handoff snapshot checker、current release completion audit `handoff_blocker_ids` handoff coverage、release/prerequisite classification、timestamp coherence 與 prerequisite report iOS / Android device-visibility / env-file placeholder / key allowlist contract；`goal:completion:audit` 與 contract gate 已納入 `release:preflight` 作日常盤點但仍輸出 not complete，`goal:completion:audit:strict` 依賴 `release:completion:audit:strict`，外部 release blockers 未清零前不得調用 `/goal` complete |

Web 配合任務：

| 任務 | 優先級 | 目標 |
| --- | --- | --- |
| 把 `frontend/src/services/api/*` 中平台無關 API shape 下沉到 `packages/api-client` | P0 | App 不長期複製 Web service |
| 把 AI stream event / snapshot reducer、`after_seq` replay helper 收斂為 shared pure logic | P1 | 2026-05-12 已把 Web draft reducer、App snapshot selector、terminal status/event helper 與 phase append helper 下沉至 `packages/api-client/src/aiStreamState.ts`；Web `frontend/src/utils/aiStreamState.ts` 改為 shared export，App `mobile/src/platform/sse/aiStreamState.ts` 保留 callbacks 型別並共用 shared helper；`npm --prefix packages/api-client run test:ai-stream-state` 作 Web / App shared pure-logic contract gate。平台 lifecycle / AbortController / header / token / locale / storage 仍留在各端 adapter |
| 規劃 packages/domain | P1 | 收斂 query key、status normalize、permission predicate、phase reducer |
| 統一 upload contract 與 error normalize | P1 | 部分完成；evidence upload 已進 `packages/api-client` 與 App platform FormData adapter，Case screen 已補顯式 safety assertion / picker cancel 非成功上傳 gate，Android release APK native picker-cancel evidence 與 selected-media backend synthetic fixture upload/delete evidence 已建立，profile media / 真機 picker-selected asset evidence 待補 |

## 3. 驗收口徑

1. `20-App端/00-App端總覽.md` 不再只描述 Expo 模板骨架，而能對應到 CJ App 首輪路由與能力。
2. `20-App端/01-App導航與平台Adapter基線.md` 中模板替換、SecureStore、Push、upload adapter、Deep Link 與 App smoke gate 的狀態可逐項更新，而不是只在總覽中描述。
3. `50-跨端Mapping與Parity/00-跨端Parity總覽.md` 中 App 狀態可從「待承接」轉為具體落地狀態。
4. `50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md` 中 Auth / Quick / Case / Chat / Profile / Notification / Media / Telemetry 的工程落點可逐項轉為可實作或已實作。
5. App 對 API / DB / Push / upload / Deep Link 的新增需求都有對應 backend 或 schema 待辦，不再只停留在平台想法。
6. App 不手寫分叉 DTO；共享 enum / response shape 以 `packages/contracts` 為準。
7. App 測試與 smoke gate 按 `08-測試規範與驗收/03-App測試與證據接入基線.md` 進入 `08-測試規範與驗收/`、`測試/` 或 `90-證據與盤點/` 的正式入口。

## 4. 風險

1. 若 App 直接複製 Web 頁面或 DTO，會形成第二套產品語義。
2. 若 Push / Deep Link / SecureStore 不先定義，容易在 auth/session、notification、case visibility 上產生跨端不一致。
3. 若 shared contracts / api-client 後續停在 M1-M5，App Release / Telemetry report / Push device token 仍可能分叉 API 與狀態枚舉；目前 safe telemetry ingest / CJ OTLP JSON trace ingest / Admin report / App observability bootstrap 只覆蓋最小事件入口、聚合報表、JS fatal first pass、native crash SDK configuration first pass、Android / iOS Sentry native 接線、Sentry runtime evidence runner 與 30d retention first pass，不覆蓋 pass 狀態 native crash runtime evidence / TestFlight crash-free sessions / external tracing backend。
4. `npm --prefix mobile run platform:check` 已能阻止 `mobile/app` 與非 platform 層 static / dynamic import 或 require SecureStore / ImagePicker / Notifications / Linking，並阻止直接使用 AppState，降低原生副作用散落風險；Android release APK native picker-cancel evidence 與 selected-media backend upload/delete contract evidence 已建立，但真機 picker-selected asset / profile media / physical device 驗收仍未閉環，仍不能把 adapter skeleton 視為真機能力完成。
5. 若 `mobile/app` 的 placeholder route 長期不接真實 API / auth gate，後續 App 開發容易把展示型 skeleton 誤當業務完成。
