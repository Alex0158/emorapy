# App Goal Completion Audit

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：證據文檔
**來源時間**：2026-05-08
**上下文**：`/goal` 完成狀態、M0-M6 App artifact、release sign-off blocker
**SSOT 屬性**：非現行 SSOT（僅作證據/歷史/治理參考）
**最後核驗 Commit**：`3890ba8`
**最後核驗日期**：`2026-05-08`
<!-- CORE_DOC_AUDIT_METADATA:END -->

**文檔版本**：v1.0
**最後更新**：2026-05-08
**對應範圍**：`mobile/scripts/check-app-goal-completion-audit.mjs`、`mobile/scripts/check-release-completion-audit.mjs`、`mobile/scripts/check-release-completion-audit-contract.mjs`、`mobile/scripts/run-release-external-evidence-signoff.mjs`、`mobile/package.json`、`docs/核心開發文件/20-App端/02-App完整版本工程PRD.md`、`docs/核心開發文件/20-App端/03-App完整版本開發Roadmap.md`、`docs/核心開發文件/08-測試規範與驗收/03-App測試與證據接入基線.md`、`docs/核心開發文件/07-待處理問題與治理/待處理/App跨端Parity落地待辦-2026-05-05.md`、`docs/核心開發文件/07-待處理問題與治理/待處理/App外部ReleaseSignoff待辦-2026-05-16.md`

外部 release sign-off 執行交接見 [App-External-Release-Signoff-Runbook-2026-05-08.md](./App-External-Release-Signoff-Runbook-2026-05-08.md)；當前 secret-safe 交接快照見 [App-External-Evidence-Status-2026-05-16T06-49-03-771Z.json](./App-External-Evidence-Status-2026-05-16T06-49-03-771Z.json) 與 [App-External-Evidence-Handoff-2026-05-16T06-49-04-222Z.json](./App-External-Evidence-Handoff-2026-05-16T06-49-04-222Z.json)。這些文件只把缺失外部證據轉成可執行步驟，不改變本文件的完成判定；handoff snapshot 會標明 11 個 current release completion blockers 與 4 個 prerequisite-only blockers（Sentry runtime query credentials、native crash event id、iOS / Android physical device visibility），避免把前置輸入或設備可見性誤讀成 release completion blocker。

---

## 1. Audit 目標

本文件把當前 `/goal` 拆成可核驗 checklist，避免用單一 green gate、已實作數量或文檔進度代替完成判定。

目標原文：

> 完成 CJ App 版全面開發：以 Expo + React Native + TypeScript、iOS 優先且 Android 兼容為主線，按 App PRD / M0-M6 Roadmap 逐步完成普通用戶 App 的 Quick、Auth/Session、Profile/Interview、Chat、Formal Case/Judgment/Repair、Push/Deep Link、Media Upload、Telemetry、UI/UX、測試驗收與核心文件/進度回寫。

## 2. 新增 Gate

| 命令 | 用途 | 完成口徑 |
| --- | --- | --- |
| `npm --prefix mobile run goal:completion:audit` | non-strict prompt-to-artifact audit，輸出每項目標對應 artifact 與缺口 | 可通過但仍可能輸出 `not complete`，用於日常進度盤點 |
| `npm --prefix mobile run goal:completion:audit:strict` | strict `/goal` 完成 gate | 只有所有 checklist 通過，且 `release:completion:audit:strict` 通過時才允許視為 goal 完成 |
| `npm --prefix mobile run goal:completion:audit -- --json` | 結構化 audit record | 供後續 CI 或 release dashboard 消費；當 `release_signoff` 未完成時，會在該 checklist caveat 與 `details` 中直接輸出 `release:completion:audit -- --json` 的 `blocker_ids`、`handoff_blocker_ids`、最新 external handoff snapshot、`prerequisite_only_blocker_ids`、warnings 與 blockers，避免只靠 stdout tail 或人工追檔造成外部交接缺項 |
| `npm --prefix mobile run release:completion:audit -- --json` | 結構化 release sign-off audit record | 輸出 `app-release-completion-audit`，供 CI / dashboard / agent 交接解析 release check、`blocker_ids`、`handoff_blocker_ids`、blocker、warning、failure 與 strict completion 狀態 |
| `npm --prefix mobile run release:completion:audit:contract` | release completion JSON contract gate | 驗證 non-strict / strict `app-release-completion-audit` JSON schema、required release blocker ids（含 App Store Connect API credentials）、頂層 `blocker_ids` 與 blocked checks 一致、頂層 `handoff_blocker_ids` 與 external handoff catalog coverage 一致、summary 計數、`complete` boolean、strict exit-code 一致性與 `release:preflight` wiring；已納入 `release:preflight`，不替代 pass evidence |
| `npm --prefix mobile run docs:check` / `docs:audit:dry-run:current` | core-doc preflight gate | 由 `mobile/package.json` 轉接根目錄 `docs:check` 與 current-only docs audit dry-run；已納入 `release:preflight`，用來阻止核心文件結構、truth check、過期 App 狀態守衛與 current SSOT metadata 漂移 |
| `npm --prefix mobile run goal:completion:audit:contract` | JSON contract gate | 驗證 non-strict / strict JSON audit 的 schema、required checklist ids、summary 計數、`complete` boolean、strict exit-code 一致性；當 `release_signoff` 未完成時，要求 caveat 與 `details` 明確包含 `blocker_ids`、`handoff_blocker_ids`、`prerequisite_only_blocker_ids`、latest external handoff path、`app_store_connect_api_credentials`、`release_db_parity_evidence`、`sentry_runtime_query_credentials`、`native_crash_event_id`、`ios_physical_device_visible` 與 `android_physical_device_visible`；並要求 `tests_and_gates` 覆蓋 shared external evidence policy、`routes:check`、`features:check`、`web:routes:smoke`、`docs:check`、`docs:audit:dry-run:current`、`release:evidence:check`、`release:completion:audit:contract`、`release:external-evidence:status:contract`、`release:external-evidence:fixtures:check`、`release:external-evidence:handoff:check`、`release:external-evidence:handoff:contract`、`release:external-evidence:prereq-report:check`、`release:external-evidence:workflow:check`、`release:external-evidence:env-template:check`、`release:external-evidence:signoff` 與 `release:external-evidence:signoff:android-dry-run`、M5 telemetry 證據使用 file-backed OTLP route label、不允許 malformed backend path label；同時要求 `ui_ux_accessibility` 包含 feature label module copy coverage，並直接檢查 `copy:check` source 仍掃 `src/features` / `labels.ts` / visible copy module / PsychDomain raw enum，直接檢查 `release:evidence:check` source 固定 external status / handoff snapshot、status-handoff blocker 對齊、current release completion audit `handoff_blocker_ids` 覆蓋檢查、release/prerequisite classification、timestamp coherence、platform-specific commands、final gates 與 docs references，直接檢查 release completion audit contract source 固定 `app-release-completion-audit` JSON、`blocker_ids` / `handoff_blocker_ids` consistency、external handoff coverage、required blocker ids、strict exit-code 與 preflight wiring，直接檢查 status contract source 固定 iOS / Android platform-specific validate / run next commands、signed app path、Android serial 與 strict audit gate，且直接檢查 prerequisite report contract source 固定 iOS / Android `device_visibility` schema、Android physical-platform validate、`android_physical_device_visible`、requested-device match、env-file placeholder guard 與 env-file key allowlist；已納入 `release:preflight` |
| `npm --prefix mobile run copy:check` | 普通用戶可見文案 regression gate | 掃描 `mobile/app`、`mobile/src/ui`、`mobile/src/features` route / UI `.tsx` 的 JSX text / visible props / JSX expression / visible state template，並把 `src/features/**/labels.ts` / `src/ui/**/labels.ts` 視為可見 copy module，防止工程詞、內部里程碑、未完成態、raw backend enum、raw internal id field、legacy English screen / field label、PsychDomain raw enum value 與 `.status` / `.priority` / `.phase` / `.mode` / `.code` / `.template_code` / `.id` / `.room_id` / `.case_id` / `.plan_id` / `.track_id` / `.judgment_id` / `.domains_touched` / raw route id 變數 / raw `feedState` / `.domains_touched.join(...)` 直接回到 App 可見文案；已納入 `release:preflight` 與 `/goal` UI/UX checklist |
| `npm --prefix mobile run routes:check` | App route topology static gate | 固定 `mobile/app` 的 `(public)` / `(app)` / Quick / Auth / Case / Chat / Profile / Notifications / Repair / `modal` route file set、layout registrations、authenticated deep route tab hiding，並阻止 Expo template `(tabs)` / `Tab One` / `Tab Two` 回流；已納入 `release:preflight` 與 `/goal` M0 / tests checklist |
| `npm --prefix mobile run features:check` | App feature coverage static gate | 固定 M0-M5 source contract：route `testID`、screen CTA / mutation、shared API endpoint、SSE / Deep Link / Push / Upload / Telemetry adapter、Backend notification / telemetry / push provider route；已納入 `release:preflight` 與 `/goal` tests checklist，用於降低只靠 artifact 存在造成的完成誤判 |
| `npm --prefix mobile run true-service:check` | true-service smoke contract gate | 固定 `mobile/scripts/app-true-service-smoke.mjs` 的 M1-M5 / `all` scope、local-only DB-state injection safety、`--bootstrap-local-users`、`--m1-stream-replay`、`--m1-inject-expired-session`、`--m2-inject-failure`、`--m2-inject-partial-success`、`--request-ai`、M1 `case_judgment` replay / expired session、M2 failed / partial-success、M3 request judgment、M4 repair replan stream、M5 notification / upload / `m5.telemetry_ingest` step contract，並檢查 `/goal` audit、Roadmap、測試基線與 `release:preflight` wiring；已納入 `release:preflight` 與 `/goal` tests checklist，不替代 local backend 實跑或外部 release evidence |
| `npm --prefix mobile run web:routes:smoke` | App web route smoke gate | 在 `smoke:web` 產物上啟動本地 static server，透過 Playwright 驗證 16 條 App route 在 desktop + phone viewport 掛載指定 screen marker、body 非空、無 page error，並檢查 public home primary CTA 基本樣式；已納入 `release:preflight` 與 `/goal` tests checklist，不替代 native navigation / Deep Link runtime evidence |
| `mobile/scripts/lib/release-evidence-policy.mjs` | 外部 evidence shared policy | status dashboard 與 strict completion audit 共用同一套 type-aware / identity-bound evidence 判定，避免 `/goal` audit 只看其中一個入口 |
| `npm --prefix mobile run release:external-evidence:status` / `release:external-evidence:status:contract` | 外部 evidence status contract | status next commands 固定 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排入口、iOS signed app path、Android serial、底層 physical-device smoke 與 strict audit gate；已納入 `release:preflight`，不替代外部 pass evidence |
| `npm --prefix mobile run release:external-evidence:fixtures:check` | 外部 evidence validator fixture contract | 以受控 fixture 驗證 strict release audit 對 EAS iOS + TestFlight、EAS Android、physical device、provider delivery、native crash runtime 與 release DB parity evidence 的正反裁決；已納入 `release:preflight`，不替代外部 pass evidence |
| `npm --prefix mobile run release:external-evidence:handoff:check` / `release:external-evidence:handoff:contract` | 外部 blocker handoff contract | handoff catalog 會把 physical device owner action 固定為 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排命令加底層 `physical-device:smoke`，contract gate 直接檢查 platform-specific validate / run command string；已納入 `release:preflight`，不替代外部 pass evidence |
| `npm --prefix mobile run release:external-evidence:signoff` / `release:external-evidence:signoff:android-dry-run` | 外部 release sign-off dry-run 編排 | 順序檢查 external status、EAS iOS + TestFlight、EAS Android、physical device、provider delivery、native crash runtime、telemetry runtime、release DB parity 與 completion audits 的 runner 可執行性；預設入口覆蓋 iOS physical branch，Android dry-run 入口覆蓋 `--physical-platform=android` orchestrator branch；兩者都已納入 `release:preflight` 作 orchestrator 入口回歸 gate |
| `npm --prefix mobile run release:external-evidence:workflow:check` | 外部 release sign-off workflow contract | 讀取 `.github/workflows/app-release-external-signoff.yml`，固定 mode / runner / run-mode secret probe / 外部 sign-off 前 App `release:preflight` / preflight step-level `APP_RELEASE_EXTERNAL_SIGNOFF_RUN=false` / generated handoff / `App-Goal-Completion-Audit-*.json` / generated redaction / artifact upload / prerequisite report artifact contract；已納入 `release:preflight`，不替代 GitHub workflow run 或 external pass evidence |
| `npm --prefix mobile run release:external-evidence:github-secrets:strict -- --json` | GitHub release secret-name readiness gate | 只讀 repository / environment secret names，不讀 secret values；缺任一 required release secret name 時 exit non-zero。當前 `missing_secret_name_count=16`、`ready_for_workflow_validate=false`，因此 workflow validate / run 仍不能作為 release sign-off evidence |
| `npm --prefix mobile run release:external-evidence:github-secrets:sync -- --json` | GitHub Environment secret sync dry-run | 從 gitignored `mobile/release.env.local` 讀取 release inputs，預設只做 redacted dry-run；加 `--apply` 才寫入 `Production` GitHub Environment secrets。工具拒絕 placeholder、不輸出 secret values，並把 `DATABASE_URL` 映射成 `APP_RELEASE_DATABASE_URL`、把 `APP_STORE_CONNECT_PRIVATE_KEY_PATH` 指向的 `.p8` 內容映射成 `APP_STORE_CONNECT_PRIVATE_KEY`。此命令只協助配置 CI secrets，不替代 workflow validate / run 或 external pass evidence |
| `npm --prefix mobile run release:external-evidence:env-template:check` | 外部 release env template contract | 讀取 `mobile/release.env.example` 與 sign-off runbook，固定 required env keys、placeholder-only secret values、`APP_RELEASE_EXTERNAL_SIGNOFF_RUN=false` dry-run-safe default、`--release-env-file=release.env.local` 交接命令與 runbook 交接說明；已納入 `release:preflight`，不替代 repository secrets 或 external pass evidence |
| `npm --prefix mobile run release:external-evidence:input-status` | 本機外部輸入 redacted 狀態檢查 | 只輸出 `mobile/release.env.local` required key 的 filled / placeholder / missing counts、key names、`extra.eas.projectId` 是否有效與 `ready_for_validate`；不輸出任何 value，也不連外部 provider 或裝置；已納入 `release:preflight`，用於正式 validate 前的本機自查，不替代 prerequisite validate 或 pass evidence |
| `npm --prefix mobile run release:external-evidence:validate` | 外部 release sign-off 正式 run 前置條件驗證 | 只檢查 `extra.eas.projectId`、成對 Apple submission credentials、ASC / Sentry / Push / DB / 真機 inputs 是否齊備；缺項時在碰 EAS / provider / 真機 / release DB 前失敗 |
| `npm --prefix mobile run release:external-evidence:run` | 外部 release sign-off 正式編排 | 在 secrets / 真機 / release DB 準備好後產生各 structured evidence，再跑 strict release / goal audit；不得用 orchestrator 本身替代各 evidence JSON |
| `.github/workflows/app-release-external-signoff.yml` | 外部 release sign-off 手動 CI 入口 | 預設 dry-run；正式 run 需 repository secrets / inputs 與可信 runner，iOS 真機證據需 self-hosted macOS 或等價設備環境；外部 sign-off 前先跑 App `release:preflight`，且 preflight step 會把 `APP_RELEASE_EXTERNAL_SIGNOFF_RUN` 固定為 `false` |

## 3. 2026-05-08 結論

`goal:completion:audit` 当前結果：

| 類別 | 狀態 | 證據 |
| --- | --- | --- |
| Tech stack | 已通過 | `expo`、`react-native`、`typescript`、`mobile/app.json`、`mobile/tsconfig.json` |
| iOS-first / Android-compatible | 已通過本地與模擬器證據 | `mobile/eas.json`、Android toolchain / emulator / app / Maestro evidence、iOS Release simulator evidence |
| M0 Foundation | 已通過 artifact presence gate | route group、route topology static gate、provider、platform adapter、platform boundary、Maestro selector gate |
| M1 Quick + Auth | 已通過 artifact + true-service contract gate | M1 feature files、RNTL tests、shared client、Maestro quick/auth flow；M1 true-service smoke harness contract 固定 quick case、claim-session、`case_judgment` replay 與 expired anonymous session recovery |
| M2 Profile + Interview | 已通過 artifact + true-service contract gate | M2 feature files、RNTL tests、shared client、Maestro profile/interview flow；M2 true-service smoke harness contract 固定 deep interview、failed-session retry 與 partial-success retry rejection；Profile / Interview / My Story legacy English screen label 與「回到 Profile」已本地化；Interview auth gate 已改為同步語義文案；My Story feedback history 已改用用戶語義 title，不顯示 `session_id`；Interview status panel 已用 PsychDomain label helper，不直接顯示 `domains_touched` raw enum；Interview sync status 不顯示 stream 段號 |
| M3 Chat | 已通過 artifact + true-service contract gate | M3 feature files、RNTL tests、shared client、Maestro chat flow；M3 true-service smoke harness contract 固定 room/message/invite accept/request judgment；Chat / Invite legacy English screen label 已本地化；Chat Home 已移除手填房間代號入口，改由新建對話、邀請連結與 deep link 承接，缺少對話上下文會引導回支援入口，Chat runtime 可見文案不再顯示「房間 / 房主 / 本機 session」等內部模型詞，Chat Room 訊息時間已統一格式化並補缺失時間 fallback |
| M4 Formal Case + Repair | 已通過 artifact + true-service contract gate | M4 feature files、RNTL tests、shared client、Maestro case/repair flow；M4 true-service smoke harness contract 固定 formal evidence upload、repair plan select、execution confirm 與 `repair_track` replan stream；Case / Repair legacy English screen label 已本地化；Case accept judgment -> Repair route context、Repair 方案卡選擇 / 開始與 dashboard replan 選取已覆蓋，Formal Case readiness helper 與 Repair check-in helper 已補，不再要求普通用戶手填判斷 / 方案 / 旅程識別 |
| M5 Push / Deep Link / Upload / Telemetry | 已通過 artifact + true-service contract gate | M5 feature files、notification/linking/upload/telemetry adapters、backend telemetry route、CJ OTLP JSON trace ingest、push service；M5 true-service smoke harness contract 固定 push token lifecycle、notification action sync、evidence upload/delete 與 `m5.telemetry_ingest` |
| UI / UX / Accessibility | 已通過 artifact presence gate | `mobile/src/ui`、`accessibility:check`、`copy:check`、shared UI tests；普通用戶可見文案已完成工程詞 / 內部里程碑 / 未完成態 / backend enum / read-only internal id / legacy English screen label / Auth 英文欄位標籤清理，覆蓋 Quick、Auth、Profile Interview、Chat、Formal Case、Repair、Notifications、App 狀態與 Error Boundary；Quick result ready fallback / Auth claim / Auth 電子郵件與密碼欄位本地化 / Profile Interview auth gate / Profile Interview sync status / Profile Interview `domains_touched` label mapping / `src/features/**/labels.ts` 可見 label module gate / Profile Story feedback history title / Chat room message time / Chat room AI draft sync status / Chat invite / Chat Home 無手填房間代號入口 / Chat runtime 不顯示「房間」模型詞 / App status 不顯示 raw service URL / missing conversation context / Formal Case readiness helper / Repair check-in helper / Repair dashboard / Repair plans / Repair replan / Notifications 已補 raw status / priority / feed state / template fallback / raw ISO time / internal id label mapping / legacy English eyebrow 本地化回歸測試 |
| Tests and gates | 已通過 wiring gate | Jest/RNTL、feature coverage contract、true-service smoke contract gate、Maestro、native readiness、Android readiness、core-doc checks、release evidence、release completion JSON contract、external status / handoff / fixture / prerequisite / workflow / env template contracts、release preflight |
| Core docs and progress | 已通過 document presence gate | App PRD、Roadmap、test baseline、release hardening、pending ledger |
| Release sign-off | 未完成 | `release:completion:audit:strict` 仍失敗 |

## 4. 未完成項

`goal:completion:audit:strict` 不會通過，直到以下 release sign-off blockers 清零：

1. `mobile/app.json` 尚未配置真實 `extra.eas.projectId`。
2. `EXPO_TOKEN` 不存在，無法非交互查詢 / 驗證 EAS production build metadata。
3. 完整 Apple submission credentials（`ASC_APPLE_ID` + `EXPO_APPLE_APP_SPECIFIC_PASSWORD`）不存在。
4. App Store Connect API credentials（issuer id、key id、private key 或 private key path）不存在。
5. 尚無 pass 狀態 `App-EAS-iOS-Release-*.json`。
6. 尚無 pass 狀態 TestFlight App Store Connect evidence。
7. 尚無 pass 狀態 `App-Physical-Device-*.json`；目前本機 iOS physical connected=0、offline=2，Android physical connected=0。
8. 尚無 pass 狀態 `App-EAS-Android-Release-*.json`。
9. 尚無 pass 狀態 `App-Push-Delivery-*.json`。
10. 尚無 pass 狀態 `App-Native-Crash-Runtime-*.json`。
11. 尚無 release / production non-local `App-Release-DB-Parity-*.json`。

當前 `release:completion:audit -- --json` 的 `blocker_ids` 必須保持為 `eas_project_id`、`expo_token`、`apple_submission_credentials`、`app_store_connect_api_credentials`、`eas_ios_build_artifact`、`testflight_evidence`、`physical_device_evidence`、`eas_android_build_artifact`、`apns_or_provider_delivery_evidence`、`native_crash_runtime_evidence`。`goal:completion:audit -- --json` 會把這些 completion blockers、對應 `handoff_blocker_ids`、latest `App-External-Evidence-Handoff-*.json` path 與 prerequisite-only blockers 一起放進 `release_signoff.details`；對外 handoff 會把 completion blockers 映射到 owner action，並在 external status / handoff report 與 `/goal` audit 中暴露 Sentry runtime query credentials、native crash event id 與 iOS / Android physical device visibility prerequisite blockers。release DB parity 已有 `candidate_pass` evidence 時，不再作為 release completion blocker。

以上缺口未消除前，不得調用 `/goal` complete，也不得宣稱完整 App 版已完成。

## 5. 最近核驗命令

| 命令 | 結果 |
| --- | --- |
| `npm --prefix mobile run goal:completion:audit` | 已通過 non-strict，輸出 `not complete` |
| `npm --prefix mobile run goal:completion:audit:strict` | 預期失敗，失敗點為 release sign-off blockers |
| `npm --prefix mobile run goal:completion:audit -- --json` | 已輸出 `complete=false`、`passed=11`、`missing_or_incomplete=1`；`release_signoff` caveat / `details` 直接列出 11 個 `blocker_ids`、11 個 `handoff_blocker_ids`、4 個 `prerequisite_only_blocker_ids`、latest external handoff path、7 個 warnings 與 11 個 blockers |
| `npm --prefix mobile run true-service:check` | 已通過；M1-M5 true-service smoke harness scope / flags / safety / key step contract、`release:preflight` wiring、`/goal` audit wiring、Roadmap 與測試基線同步有 gate |
| `npm --prefix mobile run goal:completion:audit:contract` | 已通過；JSON schema、strict exit-code、`--json --report-dir=...` 產出 / 讀回 `App-Goal-Completion-Audit-*.json`、UI/UX copy coverage、`docs:check` / `docs:audit:dry-run:current` preflight wiring、true-service smoke contract gate、release evidence external status/handoff snapshot checker、current release completion audit `handoff_blocker_ids` 覆蓋檢查、release/prerequisite classification、timestamp coherence、prerequisite report Android device-visibility、env-file placeholder、key allowlist、workflow `App-Goal-Completion-Audit-*.json` artifact contract 與 incomplete `release_signoff` blocker/handoff/prerequisite caveat contract 有 gate |
| `npm --prefix mobile run release:completion:audit -- --json` | 已輸出 `complete=false`、11 個 `blocker_ids` 與 11 個 `handoff_blocker_ids`，供外部 owner 交接 |
| `npm --prefix mobile run release:completion:audit:contract` | 已通過；驗證 JSON schema、`blocker_ids` / `handoff_blocker_ids` consistency、external handoff coverage、required blocker ids、strict exit-code 與 preflight wiring |
| `npm --prefix mobile run release:external-evidence:signoff` / `release:external-evidence:signoff:android-dry-run` | 已建立；預設 iOS branch 與 Android branch dry-run 均不觸碰 EAS / provider / 真機 / release DB |
| `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run release:preflight` | 已通過；覆蓋 Jest、accessibility、copy、routes、feature coverage、true-service smoke contract、platform boundary、docs structure / truth / current audit、typecheck、web export、web route smoke、Maestro static、native / Android readiness、release evidence、external status / handoff / fixture contracts、external signoff iOS + Android dry-run、prerequisite report contract、workflow goal-audit artifact contract、env template contract、redacted input status、release completion audit contract、goal audit contract 與 release baseline |
| `npm --prefix mobile run test` | 已通過；30 suites / 134 tests，含 UI 文案調整後的 M0-M5 screen tests |
| `npm --prefix mobile test -- --runInBand __tests__/m1-screens.test.js __tests__/m3-screens.test.js __tests__/m4-screens.test.js` | 已通過；38 tests，確認 Quick/Auth submit readiness helper、Quick/Auth/Chat/Formal Case/Repair legacy English eyebrow 本地化、Formal Case readiness helper、Case created-time stable label / missing timestamp fallback、Repair check-in helper、Quick result ready fallback / Auth claim / Chat room message time fallback / Chat Home 無手填房間代號入口 / Chat runtime 不顯示「房間」模型詞 / missing conversation context guidance / Formal Case / Repair dashboard / Repair plans / Repair replan 文案清理後 screen interactions、raw internal id hiding、Case accept judgment -> Repair route context、Repair 無手填判斷 / 方案 / 旅程識別與 label mapping 未破壞 |
| `npm --prefix mobile test -- --runInBand __tests__/m3-screens.test.js __tests__/m4-screens.test.js __tests__/m5-notifications.test.js src/ui/components.test.js` | 已通過；確認 Chat message time / Formal Case / Notifications 文案清理後 screen interactions、notification time fallback、raw enum label mapping 與 shared UI contract 未破壞 |
| `npm --prefix mobile run typecheck`、`npm --prefix mobile run accessibility:check`、`npm --prefix mobile run copy:check` | 已通過；確認 enum label mapping、TextInput accessibility contract 與普通用戶可見文案 regression gate 未破壞；copy gate 已覆蓋 `invite code`、`room id`、`deep link`、`pairing`、`API`、`apiBaseUrl`、raw service URL、`M0-M6`、`接入中`、`測試`、`Alpha`、`App 主線`、`結果頁骨架`、`判斷代號`、`方案代號`、`修復旅程識別碼`、`房間代號`、`房間`、`服務位址` 等內部口徑，並會攔截 `QUICK`、`AUTH`、`PROFILE`、`INTERVIEW`、`MY STORY`、`CHAT`、`INVITE`、`CASE`、`REPAIR`、`NOTICE`、`RESULT`、`CJ APP`、`APP`、`Email`、`Password` 等 legacy English screen / field label，以及 label / JSX text / visible state template / visible copy module 內的 raw `.status` / `.priority` / `.template_code` / `.id` / `.room_id` / `.case_id` / `.plan_id` / `.track_id` / `.judgment_id` / `.domains_touched` / route id / `feedState` / `.domains_touched.join(...)` / PsychDomain raw enum value 等 backend field |
| `.github/workflows/app-release-external-signoff.yml` | 已建立；只作手動外部 sign-off 編排入口，不替代 strict audit |
