# App 端總覽

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：App端規格
**覆蓋範圍**：Expo App 基線、原生能力邊界與 App 開發投影總覽
**取證代碼入口**：`mobile/package.json`、`mobile/tsconfig.json`、`mobile/app/_layout.tsx`、`mobile/app/modal.tsx`、`mobile/src/platform`、`packages/contracts/src`、`packages/api-client/src`
**最後核驗 Commit**：`3890ba8`
**最後核驗日期**：`2026-05-08`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 1. 當前 App 基線

App 版正式承載目錄為 `mobile/`。目前它是 Expo / React Native 專案，已完成 M0-M5 首輪普通用戶 App 工程接線，並進入 M6 外部發版證據與 strict sign-off 階段：

1. `mobile/package.json`：Expo、Expo Router、React Native、React Native Paper、React Query、Zustand、Axios、SecureStore、Notifications、ImagePicker 等依賴。
2. `mobile/app/_layout.tsx` 與 `mobile/app/modal.tsx`，以及 public / authenticated route group：已替換 Expo template tabs，建立 CJ route group、public quick/auth/result/collaborative screen、authenticated case/chat/profile/notifications/repair screen 與 App 狀態 modal；狀態頁不向普通用戶顯示 raw API URL。
3. provider / UI foundation：已建立 React Query、React Native Paper、safe area、auth/session bootstrap、root error boundary 與首輪 native UI 基線；shared button / panel 基線已修正 web export 下 CTA 樣式丟失問題，panel/card/input radius 收斂到 8px，寬屏 content 限制到 760px，authenticated tab label 已中文化，shared UI accessibility contract 已覆蓋 header role、button label/hint、busy/disabled state、48px touch target 與 FeatureRow 合併朗讀 label，App `TextInput` label / hint static gate 已建立。
4. runtime config：已建立 App runtime config，讀取 API base URL、locale、app version / build number 與 request timeout。
5. shared package resolution：App 端已從 `tsconfig` 直連 source alias 改為 package resolution，Metro 可解析 root workspace 的 `@cj/api-client` package exports；避免 typecheck 與 bundler 對 shared layer 使用不同入口。
6. `mobile/src/platform`：已建立 API client、SecureStore、SSE、upload、notifications、linking、lifecycle、telemetry 的 runtime adapter，並保留 storage / notifications / upload 型別邊界。

目前 App 已完成 Quick / Auth / Profile / Interview / Chat / Formal Case / Repair / Notification / Deep Link / Upload / Telemetry 的首輪接線。`mobile/app` 現在可被解讀為 CJ App M0-M5 普通用戶主流程實作入口；M0 provider render、auth/session bootstrap 與 root error boundary 已有首輪 gate，M1-M5 screen / adapter 已能消費 shared API client 與 App adapter，M1 Quick 已包含單人 quick、同機雙人協作 quick、過期 anonymous session recovery / result access cleanup、result pending polling、`case_judgment` stream `after_seq` replay / ready snapshot fallback、Quick result 共用 App AI stream subscription hook AppState background abort / foreground reconnect gate 與 claim-session handoff 的首輪 App route；M2 Interview、M3 Chat AI draft 與 M4 Repair replan 已接共用 App AI stream subscription hook，可處理 ready snapshot、`after_seq` replay、close/error retry 與 screen-level AppState 前後台恢復 gate；AI stream persistence migration 已補齊 `ai_stream_sessions` / `ai_stream_events` / archive tables，AI stream backend 已修正 replay 等待期間錯過 live terminal event 的 subscription race，SSE 端點已禁用 HTTP compression，memory/Redis 空白時可從 live DB events / sessions 做 replay / snapshot fallback，M2 interview facts migrations 已補齊 `interview_sessions.collected_facts` / `interview_turns.extracted_facts`，M5 notification action metadata migration 已補齊 read / snooze / dismiss / act state 欄位；M0-M5 Maestro flow artifact 與 29 個 selector static gate 已建立，且 iOS 26.4.1 / iPhone 17 simulator Release build 上 native Maestro 7/7 smoke 已通過；Android 端已完成 toolchain strict gate、Android 16 / API 36 arm64 AVD boot smoke、release APK build/install/foreground launch smoke、Android Maestro 7/7 flow smoke 與 native ImagePicker picker-cancel side-effect smoke；M1-M5 true-service smoke harness dry-run / safety gate 已建立，並已在本機隔離 Postgres + local backend + `--bootstrap-local-users` 下跑通 M1 claim-session、M1 `--m1-stream-replay` `case_judgment` SSE `after_seq` replay、local-only `--m1-inject-expired-session` 過期 submit recovery / result `SESSION_EXPIRED` reject / expired claim ignored、M2 `--deep` 5-turn interview start/respond/background response/end/my-story completion、M3 `--request-ai` partner accept/request judgment、M4 `--request-ai` formal evidence upload / repair plan select / execution confirm / `repair_track` replan stream persisted / backend restart 後 DB-backed replay、M5 push token register/revoke + notification state sync + evidence upload/delete + telemetry ingest，且 selected-media backend synthetic fixture upload/delete 已在臨時 local DB + local backend 通過；當前 `backend/.env` 仍可能指向 non-local DB，因此沒有顯式 `APP_SMOKE_BACKEND_DATABASE_URL` 時 run mode 仍會被 safety gate 阻擋。Deep Link resolver 已保留 `/quick/collaborative` 與 `/quick/result?caseId=...` 這類安全 Quick 子路由。Push device token registration / revoke / logout cleanup / registration-time token rotation revoke 已有 backend schema/API/client 與 App token sync 接線，backend Expo push sender / pending dispatch / receipt polling job 已接線，App telemetry 已接 non-blocking `POST /api/v1/telemetry/events` safe ingest、`POST /api/v1/telemetry/otlp/v1/traces` CJ OTLP JSON trace ingest baseline、backend 二次清洗、最小化 `app_telemetry_events` persistence、`GET /api/v1/admin/reports/app-telemetry` 聚合報表與 30d cleanup job；App observability bootstrap 已接 session start、lifecycle transition、global JS fatal / unhandled promise、OpenTelemetry provider span export 與 native crash SDK configuration status，payload 帶 app version / build number，backend crash-free 聚合已把 JS fatal / unhandled promise 納入 crash session 口徑；notification landing handler 已能處理 last notification response 與 foreground response target，非通知 Deep Link handler 已能處理 cold-start / foreground `cj://` target，且未登入受保護 target 可保存安全 App href 並在登入後 resume。release / production DB parity 與 telemetry runtime 已有 structured pass evidence；但在 physical device smoke、TestFlight / EAS build、APNs sandbox / 真 provider delivery、真機 picker-selected asset / profile media、Android EAS artifact、native crash runtime evidence / external tracing backend / physical-device OTLP runtime evidence 完成前，不能宣稱 App 已通過完整 release sign-off。

截至 2026-05-08，App 端已完成開發前工程 PRD 與 Roadmap 裁決，M0-M5 首輪普通用戶 App 工程已落地，M6 已進入外部發版證據階段。M0 已有 shell / adapter / provider render / auth-session bootstrap / root error boundary；M1 已有 shared Quick/Auth/Session domain client、App quick/auth screen 接線、同機雙人協作 quick route、過期 anonymous session recovery / result access cleanup、Quick result pending polling、`case_judgment` stream replay 接線、Quick result AppState reconnect gate、true-service quick harness 與 Maestro flow artifact；M2 已有 shared Profile/Interview client、Profile / Interview / My Story screen、Interview AI stream replay/recovery hook 接線、true-service token probe、Profile/Interview/Story protected deep-link auth-resume Maestro flow artifact，且 M2 deep true-service 已驗證 5-turn interview pipeline completion、profile narratives 與 feedback history card；M3 已有 Chat screen、room event stream parser、chat-room AI stream draft replay/recovery hook、invite accept 未登入 auth resume、true-service chat probe 與 Chat entry/auth-gate Maestro flow artifact；M4 已有 Case / Repair screen、Case evidence 顯式 safety assertion handoff、formal case evidence upload true-service smoke、repair replan trigger、`repair_track` stream replay/recovery hook、true-service formal case/repair/replan stream probe、backend restart 後 DB-backed replay probe 與 Case/Repair auth-gate Maestro flow artifact；M5 已有 Notification screen、Deep Link resolver、notification landing handler、non-notification Deep Link handler、post-login resume、registration-time token rotation revoke、notification / protected deep-link auth-resume Maestro flow artifact、evidence upload FormData adapter、Case upload 顯式聲明與 picker cancel 非成功上傳 gate、selected-media backend synthetic fixture upload/delete evidence、safe telemetry redaction 與 backend `POST /api/v1/telemetry/events` safe ingest、Push device token backend schema/API、Expo push sender / pending dispatch / receipt polling job 與 App token sync、notification action metadata migration 與 local true-service smoke；M6 已有 app identity、runtimeVersion、EAS profiles、release readiness script、Maestro static gate、iOS simulator Release build + native Maestro 7/7 發版證據入口、Android SDK/toolchain readiness、Android emulator boot smoke、Android release APK install/launch smoke、Android Maestro 7/7 flow evidence、Android native ImagePicker picker-cancel evidence、App telemetry 最小化 persistence / Admin report / 30d cleanup first pass、CJ OTLP JSON trace ingest baseline、App observability bootstrap / OpenTelemetry provider span / JS fatal safe telemetry first pass / native crash SDK configuration first pass、AI stream persistence migration、M2 interview facts migrations 與 M5 notification action metadata migration。已通過的本地 true-service 驗證包含 `--run --scope=all --bootstrap-local-users`、`--run --scope=m1 --bootstrap-local-users --m1-stream-replay`、`--run --scope=m1 --bootstrap-local-users --m1-inject-expired-session`、`--run --scope=m2 --bootstrap-local-users --deep`、`--run --scope=m3 --bootstrap-local-users --request-ai`、`--run --scope=m4 --bootstrap-local-users --request-ai`（含 `m4.evidence_upload`、repair plan select、execution confirm、`m4.repair_track_replan_stream` 與 backend restart 後 `after_seq=7` DB-backed replay）、`--run --scope=m5 --bootstrap-local-users` 與 `selected-media:upload:smoke -- --run`。尚未完成 EAS/TestFlight build、physical device evidence、push delivery / APNs sandbox、真機 picker-selected asset / profile media、Android EAS artifact、native crash runtime evidence / TestFlight crash-free sessions / external tracing backend。

## 2. App 版本產品定位

App 不是 Web 的截圖式重做，而是同一產品核心與同一組 `CJ-PRD-*` 通用級需求在移動端的承載：

| 能力組 | App 首輪承接原則 |
| --- | --- |
| 快速判斷 | 優先承接低門檻 quick flow，保留匿名 session / claim / quick case 語義 |
| 正式處理 | 承接登入、配對、案件、判決、修復旅程，但避免首輪一次塞入完整 Admin 能力 |
| 先聊再判 | 可作為 App 高頻入口，但 chat -> judgment / case 的後端歸屬與授權不能改寫 |
| 讓系統更懂你 | 適合承接心理訪談、個人檔案與關係檔案，但 schema 必須與 backend 保持一致 |
| 通知與提醒 | App 可使用 Push / Deep Link 提升到達率，但 notification 狀態與業務副作用仍由 backend 裁決 |

完整 App 版 scope 固定為：Quick、Auth / Session、Profile / Interview、Chat、Formal Case / Judgment / Repair Journey、Notification / Push / Deep Link、Media Upload、Telemetry。普通 App 不承接 Admin Web，只允許回傳必要 error / telemetry context。

## 3. 原生能力邊界

App 端可新增以下平台適配層：

1. SecureStore：保存 token / session 相關本地憑證，但不得替代 backend 授權。
2. Push notification：承接提醒與召回，但 notification 狀態以 backend 為準。
3. Deep Link：進入 case、chat、judgment、repair journey 等目標，但進入後仍需 backend gate。
4. Camera / ImagePicker：承接 evidence 或 profile media，但上傳授權與 media provider 仍以 backend 為準。
5. App lifecycle：處理冷啟動、恢復、前後台切換與 token refresh。
6. Telemetry：回傳 app version、platform、safe error context 與 request id，但不得上傳敏感 relationship / psych / prompt payload。

這些能力只屬平台投影。現碼已建立 SecureStore、Notifications、ImagePicker、SSE、Linking、Lifecycle、Telemetry 的 runtime adapter；其中 SSE transport 已補非 2xx typed error normalize / close callback，AI stream subscription hook 已接 AppState 前後台中斷後 `after_seq` 恢復入口，並已投用於 M1 Quick result、M2 Interview、M3 Chat AI draft 與 M4 Repair replan，對應 screen 均已有 background abort / foreground `afterSeq` reconnect RNTL gate；Notifications / Push backend token registration / revoke / logout cleanup / registration-time token rotation revoke / backend Expo push dispatch / notification landing handler / non-notification Deep Link handler / post-login resume 已完成首輪 schema/API/App 接線，Deep Link resolver 已保留 chat room、chat invite、profile interview/story、Quick collaborative / result 等安全 App 子路由，且 Chat invite notification path 只會落到 invite landing，不會直接觸發 accept side effect；upload 已改為按平台 lazy load ImagePicker，`pickImageWithStatus` 可區分 unsupported / permission denied / cancelled / selected，Case screen 上傳前要求用戶顯式提交 `safety_assertion` 且 picker cancel 不記成功，web/native branch unit、Android release APK native picker-cancel smoke 與 selected-media backend synthetic fixture upload/delete evidence 已建立，Telemetry adapter 已 non-blocking 送出 safe event envelope 到 backend `POST /api/v1/telemetry/events`，OpenTelemetry exporter 已送 `POST /api/v1/telemetry/otlp/v1/traces`，後端已二次清洗、寫 structured log、持久化最小事件摘要、提供 Admin 聚合報表並每日清理 30 天前事件；App observability bootstrap 已補 session start、lifecycle transition、global JS fatal / unhandled promise、OpenTelemetry provider span export first pass、CJ OTLP JSON trace ingest baseline 與 native crash SDK configuration first pass，但 SSE 真機 background interruption recovery、真機 picker-selected asset / profile media、真機 cold-start Deep Link、真 push delivery、native crash runtime evidence / TestFlight crash-free sessions / external tracing backend / physical device evidence 尚未驗收。若需要新增 API、DB 欄位或共享 enum，必須回寫跨端核心與 Parity 待辦。

## 4. 與 Web 的差異

| 類型 | Web 基線 | App 投影 |
| --- | --- | --- |
| 路由 | React Router / browser URL | Expo Router / Deep Link；M0-M5 已有 `(public)` / `(app)` / quick / auth / case / chat / profile / notifications / repair / modal screen 與首輪業務 flow / adapter，M6 仍等待外部發版證據 |
| 本地儲存 | localStorage / sessionStorage 類 adapter | SecureStore / native storage adapter |
| 通知 | Web notification / in-app notification | Push notification + in-app notification |
| 媒體 | Browser upload / media provider | ImagePicker / camera + media provider |
| 認證恢復 | Browser refresh / route guard | cold start restore / app lifecycle refresh |
| UI | Web responsive layout | native navigation / touch-first layout |

## 5. 待閉環事項

App 開發已完成 M0-M5 首輪工程落地，後續至少要按 Roadmap 補齊以下 M6 / 原生外部證據任務：

1. M0 Foundation：SecureStore / API adapter unit、provider render、auth/session bootstrap、root error boundary 與 upload / notifications / lifecycle adapter branch tests 已建立首輪 gate；iOS simulator Release launch + native Maestro 7/7 smoke、Android release APK native ImagePicker picker-cancel smoke 已補，仍需確認 SSE / selected-media upload / notification adapter 的 physical-device runtime 風險。
2. M1 Quick + Auth：已啟動 anonymous session、quick case、同機雙人 collaborative quick、quick submit 前過期 session recovery、result access 過期清理、result fetch / pending polling、`case_judgment` stream replay、Quick result AppState background abort / foreground `afterSeq` reconnect RNTL gate、login / register、claim-session App 接線；claim-session 失敗已改為不阻斷登入，過期 / 無效 anonymous session 會清理本機 session cache 並記 safe telemetry；backend `claim-session` 已拒絕過期 anonymous session claim，避免 cleanup job 尚未刪除時把 stale quick case 掛到登入用戶；本機隔離 Postgres `--run --scope=all --bootstrap-local-users` 已通過 anonymous quick / judgment poll / claim-session，`--m1-stream-replay` 已驗證真服務 `case_judgment` SSE `after_seq` replay 回放 terminal persisted event，local-only `--m1-inject-expired-session` 已驗證過期 submit recovery、result `SESSION_EXPIRED` reject 與 expired claim ignored；iOS simulator quick/auth native smoke 已通過，仍需 native stream reconnect 真機證據與真機錯誤狀態 UX。
3. M2 Profile + Interview：profile shell、consent、foreground stream interview、my-story、Interview AI stream ready snapshot / `after_seq` replay / close-error retry / AppState recovery hook 已開始接線，Interview screen background abort / foreground `afterSeq` reconnect RNTL gate 已建立，Profile / Interview / Story protected deep-link auth-resume Maestro flow artifact 已建立並在 iOS simulator native smoke 通過，本機隔離 Postgres `--run --scope=m2 --bootstrap-local-users --deep` 已通過 profile / consent / 5-turn interview start/respond/background response/end / my-story completion，local-only `--m2-inject-failure` 已通過 failed session readback / resume / retry accept，local-only `--m2-inject-partial-success` 已通過 completed / no-feedback readback、`partial_success=true` 與 retry reject，failed retry / partial-success UI 與 My Story feedback-card 摘要已有 RNTL gate；仍需真機 interruption recovery 與完整 native 分支驗證。
4. M3 Chat：room、invite、message stream、chat-room AI stream draft、request judgment、handoff 已開始接線，invite accept 在未登入時會保存邀請承接 href 並走 `/auth?next=...`，notification invite path 可映射到 `/chat/invite?code=...` 但不允許直接 accept，Chat entry / auth-gate Maestro flow artifact 已建立並在 iOS simulator native smoke 通過，本機隔離 Postgres `--run --scope=m3 --bootstrap-local-users --request-ai` 已通過 room/message/invite/list/status、partner accept 與 request judgment；仍需 native reconnect 與真 push invite landing 證據。
5. M4 Formal Case + Repair：pairing、case、judgment、reconciliation、execution 與 `repair_track` replan stream 首輪 App hook 已接線，Case evidence upload 已要求顯式 safety assertion，M4 formal case evidence upload、live replan stream 與 backend restart 後 DB-backed replay smoke probe 已通過 local true-service，Case / Repair auth-gate Maestro flow artifact 已建立並在 iOS simulator native smoke 通過，Android release APK 已通過 native picker-cancel side-effect smoke，本機隔離 Postgres `--run --scope=m4 --bootstrap-local-users --request-ai` 已通過 pairing/case/evidence upload/judgment/execution dashboard/repair plan generation/plan select/execution confirm/replan stream persisted；仍需 native reconnect、canonical dashboard native 證據、真機 selected-media upload 與 repair journey 全狀態證據。
6. M5 Push + Deep Link：device token、notification landing、read / snooze / act sync；App in-app notification / deep-link resolver / notification landing handler / non-notification Deep Link handler / post-login resume / registration-time token rotation revoke / notification/deep-link auth-resume Maestro flow artifact / upload / safe telemetry、backend `POST /api/v1/telemetry/events` safe ingest / `POST /api/v1/telemetry/otlp/v1/traces` CJ OTLP JSON trace ingest / minimized persistence / Admin report / 30d cleanup、backend registration / revoke / Expo push dispatch 與 logout cleanup 已落地，local true-service smoke 已通過 push token register/revoke、notification state sync、evidence upload/delete 與 telemetry ingest，iOS simulator custom-scheme native smoke 已通過；App observability bootstrap / JS fatal safe telemetry / native crash SDK configuration first pass 已接入；真 push delivery、真機 notification response landing 證據、native crash runtime evidence 與 external tracing backend 仍待驗收。
7. M6 Release Hardening：iOS TestFlight、EAS Update policy、Android readiness、App evidence pack；目前 iOS simulator Release / Maestro evidence、Android static config readiness gate、Android native toolchain strict gate、Android emulator boot smoke、Android release APK install/launch smoke、Android Maestro 7/7 flow smoke、native upload picker-cancel evidence、selected-media backend upload/delete evidence、release / production DB parity evidence 與 telemetry runtime evidence 已建立，仍缺 UUID 形狀 EAS project id、`EXPO_TOKEN`、Apple / ASC credentials、TestFlight / EAS build、physical device、Android EAS artifact、store artifact、真機 picker-selected asset / profile media、push provider delivery 與 native crash runtime evidence。

以上若影響 backend 或 Web，一律同步記錄到 `50-跨端Mapping與Parity/` 與 `07-待處理問題與治理/待處理/`。

## 6. 關聯正文

1. App navigation 與 platform adapter 的具體落點規則，見 [01-App導航與平台Adapter基線.md](./01-App導航與平台Adapter基線.md)。
2. 完整 App 版工程 PRD，見 [02-App完整版本工程PRD.md](./02-App完整版本工程PRD.md)。
3. 完整 App 版開發 Roadmap，見 [03-App完整版本開發Roadmap.md](./03-App完整版本開發Roadmap.md)。
4. App 與 Web / Backend / API / DB / shared packages 的一致性缺口總覽，見 [../50-跨端Mapping與Parity/00-跨端Parity總覽.md](../50-跨端Mapping與Parity/00-跨端Parity總覽.md)。
5. App 首輪 screen 到 Backend / API / DB / shared package 的工程落點，見 [../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md](../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md)。
6. App 測試、回歸、CI 與證據接入規則，見 [../08-測試規範與驗收/03-App測試與證據接入基線.md](../08-測試規範與驗收/03-App測試與證據接入基線.md)。
7. App 尚未落地的工程任務，見 [../07-待處理問題與治理/待處理/App跨端Parity落地待辦-2026-05-05.md](../07-待處理問題與治理/待處理/App跨端Parity落地待辦-2026-05-05.md)。
