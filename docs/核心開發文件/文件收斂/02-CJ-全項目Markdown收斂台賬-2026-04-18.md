# CJ 全項目 Markdown 收斂台賬（2026-04-18）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：文檔治理
**來源時間**：2026-04-18
**上下文**：非產品/工程 SSOT；承接當前文檔治理契約與台賬
**SSOT 屬性**：非現行 SSOT（僅作證據/歷史/治理參考）
**最後核驗 Commit**：`cbec3aa`
**最後核驗日期**：`2026-04-18`
<!-- CORE_DOC_AUDIT_METADATA:END -->

本文件是本輪全項目 Markdown 治理的唯一搬遷依據。

## 1. 最終存在區

本輪治理完成後，整個 `/Users/alex/Desktop/CJ` 只允許三個 Markdown 存在區：

1. `mother-bear-court/docs/核心開發文件/`
2. `mother-bear-court/docs/核心營銷文件/`
3. 根級 `歸檔/`

## 2. 四類分類規則

### A. 保留在核心開發文件

保留範圍：

1. 根層旗艦總覽
2. `01-06` 正式子域正文
3. `07/08/測試/90/99/文件收斂/` 中仍屬活躍治理、測試、證據或歷史入口的內容

### B. 保留在核心營銷文件

保留範圍：

1. 品牌定位
2. 行銷策略
3. campaign 文檔
4. 素材工作流與模板

### C. 先回寫再歸檔

本輪已回寫到核心開發文件的來源組包括：

1. repo 根歷史產品 / 開發總覽稿
2. repo 根 AI 流式重構藍圖稿
3. repo 根個人化判決升級稿
4. repo 根與 `backend/` 下的開發 / 部署 / API 說明稿
5. `backend/docs/` 下的 alerts、benchmark、migration、acceptance gate 稿
6. `docs/` 根層的本地環境、集成、快速啟動與命名規則稿
7. 舊前端設計、後端設計、發布指引、報告與歷史測試目錄中仍有穩定價值的內容
8. `frontend/docs/` 的樣式 token 規範

回寫落點：

1. `03-管理端與平台治理/01-03`
2. `04-共用機制/01`
3. `05-工程架構與共享層/01`
4. `08-測試規範與驗收/02`

### D. 直接歸檔或刪除

默認動作：

1. 非核心 Markdown 一律先移入根級 `歸檔/`
2. 空殼索引、重複鏡像、已被明確覆蓋的生成性報告可直接刪除

## 3. 目錄級搬遷規則

### 核心保留

1. `mother-bear-court/docs/核心開發文件/**`
2. `mother-bear-court/docs/核心營銷文件/**`

### 根級整體歸檔

1. `分析/**`
2. `OpenClaw任務/**`

### 倉庫內整體歸檔

1. repo 根歷史 Markdown
2. `.cursor/skills/**` 內的 Markdown
3. `backend/`、`backend/docs/`、`frontend/docs/`、`mobile/`、`packages/`、`e2e/` 內的旁路 Markdown
4. `mother-bear-court/docs/` 下除兩大核心目錄外的其餘 Markdown

## 4. 歸檔路徑規則

根級 `歸檔/` 保留原相對路徑鏡像，不改名、不扁平化：

1. `歸檔/mother-bear-court/...`
2. `歸檔/分析/...`
3. `歸檔/OpenClaw任務/...`

## 5. 驗收規則

1. 全倉 `rg --files /Users/alex/Desktop/CJ -g '*.md'` 只能落在三個允許區域
2. 核心開發文件剩餘正文不得再保留對舊旁路文檔的有效鏈接
3. 任何 dated 真相若未回寫正式正文，不得直接刪除
