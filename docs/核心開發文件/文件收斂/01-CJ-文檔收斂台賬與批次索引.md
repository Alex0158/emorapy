# Emorapy 文檔收斂台賬與批次索引

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：文檔治理
**來源時間**：2026-04-18
**上下文**：非產品/工程 SSOT；承接當前文檔治理契約與台賬
**SSOT 屬性**：非現行 SSOT（僅作證據/歷史/治理參考）
**最後核驗 Commit**：`3890ba8`
**最後核驗日期**：`2026-05-07`
<!-- CORE_DOC_AUDIT_METADATA:END -->

本文件記錄 `Emorapy` 當前文檔骨架與已完成的收斂批次。檔名中的 `CJ` 保留為 2026-04-18 legacy governance batch identifier，不代表現行產品名。

本文件本身屬於活躍治理台賬，不屬於產品/工程正文 SSOT。

## 活躍治理文件

| 文件 | 角色 |
| --- | --- |
| `README.md` | 說明本子域怎樣使用 |
| `00-CJ-文檔治理與同步規則.md` | 定義治理口徑與同步規則 |
| `01-CJ-文檔收斂台賬與批次索引.md` | 記錄收斂骨架與批次進度 |
| `02-CJ-全項目Markdown收斂台賬-2026-04-18.md` | 記錄三區 Markdown 收斂與遷移裁決 |
| `03-CJ-核心開發文件逐文件代碼校驗總台賬-2026-04-18.md` | 記錄核心開發文件逐文件審計狀態與取證入口 |

## 首輪收斂批次

1. 將 `核心開發文件/` 根層收斂為旗艦文檔與固定子目錄。
2. 將營銷正文收斂到 `docs/核心營銷文件/`。
3. 將 `06-接口描述/` 標準化為 `06-接口描述/`。
4. 將治理、證據、回歸與歷史材料移入 `07/`、`90/`、`測試/`、`99/`、`文件收斂/`。

## 2026-04-18 全項目收斂批次

1. 將整個 `/Users/alex/Desktop/CJ` 的 Markdown 存在區收斂為「核心開發文件 / 核心營銷文件 / 根級歸檔」三區。
2. 將 repo 根歷史說明稿、本地開發稿、部署稿、發布稿、告警 runbook、AI / chat gate 文檔回寫到 `03/04/05/08` 正式子域。
3. 將非核心 Markdown 整體移入根級 `歸檔/`，保留原相對路徑鏡像。
4. 移除所有指向舊旁路文檔的有效鏈接，剩餘正文只允許指向核心開發文件、核心營銷文件、代碼實體與必要歷史入口。

## 2026-04-18 逐文件代碼校驗批次

1. 以 `scripts/check-docs-structure.mjs` 與 `scripts/check-docs-truth.mjs` 恢復文檔基線守衛，並將 `docs:check` 擴展為「結構 + 真實性」雙層校驗。
2. 為 `docs/核心開發文件/` 全量文件補齊受控元數據塊，統一標記文檔類型、覆蓋範圍、取證入口、最後核驗 commit / 日期。
3. 新增 `03-CJ-核心開發文件逐文件代碼校驗總台賬-2026-04-18.md`，逐文件記錄核驗方法、狀態、差異摘要與是否屬於現行 SSOT。

## 2026-05-05 跨 Web / App 架構批次

1. 新增 `00-跨端產品核心/`，把產品能力、角色、流程、狀態、授權與一致性鐵律提升為 Web / App 共用核心。
2. 新增 `10-Web端/`，把當前 Web / Admin Web 作為已實作凍結基線，而不是 App 產品規則來源。
3. 新增 `20-App端/`，把現有 `mobile/` Expo 專案定義為 App 版正式承載位置，並標明尚未正式消費 shared contracts / api-client。
4. 新增 `50-跨端Mapping與Parity/`，把 Web / App / Backend / API / DB / shared packages 的一致性、差異與缺口集中追蹤。
5. 新增 `07-待處理問題與治理/待處理/App跨端Parity落地待辦-2026-05-05.md`，把 App 尚未承接主產品能力與原生 parity 策略列為代碼待閉環項。

## 2026-05-07 PRD / NFR / RTM 補強批次

1. 新增通用級 PRD 組：`00-跨端產品核心/01-產品PRD總章.md`、`02-用戶場景與假設台帳.md`、`03-成功指標與產品健康.md`、`04-PRFAQ與非目標.md`，補齊產品意圖、目標用戶、成功指標、非目標與 Web / App 分層。
2. 新增共用非功能需求：`04-共用機制/02-信任安全與非功能需求.md`，把信任安全、隱私、AI 可靠性、性能、成本與可觀測性放入正式規格。
3. 新增 `08-測試規範與驗收/04-需求驗證矩陣.md`，把 `CJ-PRD-*` / `CJ-NFR-*` 追到功能、流程、API、頁面、測試、證據與產品指標。
4. 回寫根層旗艦文檔、Web / App / Parity / 共用機制 / 測試索引與術語表，固定「通用級先於平台投影」與「App 不因 PRD 存在而宣稱已實作」的口徑。

## 2026-05-07 工程級 PRD 對標治理批次

1. 新增 `00-跨端產品核心/05-工程級PRD對標與治理缺口台賬.md`，把 ISO/IEC/IEEE 29148、RFC 2119、ISO/IEC 25010、OWASP ASVS / MASVS 轉化為本項目的需求屬性與治理缺口規則。
2. 回寫 `01-產品PRD總章.md`，補齊需求語句、強制程度、來源、理由、優先級、驗證方式與平台投影的最低欄位。
3. 回寫 `02-用戶場景與假設台帳.md`、`03-成功指標與產品健康.md`、`04-共用機制/02-信任安全與非功能需求.md`、`08-測試規範與驗收/04-需求驗證矩陣.md`，補清假設信心、指標資料來源、NFR 質量屬性、安全標準映射與驗證類型。
4. 回寫治理規則、術語表和逐文件總台賬，固定「外部標準只作參考對標，不構成合規聲明」的文件口徑。

## 2026-05-07 AI / API 工程治理深化批次

1. 新增 `04-共用機制/03-AI風險與安全治理基線.md`，把 NIST AI RMF、NIST AI RMF Generative AI Profile、ISO/IEC 42001 與 OWASP LLM Top 10 轉化為 CJ 的 AI asset inventory、風險矩陣、prompt / ledger / output gate 規則。
2. 新增 `06-接口描述/11-API契約與OpenAPI缺口台賬.md`，明確現有接口治理是 route truth guard + 主冊 + 模組詳規，不等於 OpenAPI / schema contract 完成。
3. 回寫工程級 PRD 對標台賬、NFR、RTM、共用機制、接口索引、根層入口與治理規則，把 AI / LLM 風險與 API contract 缺口納入變更 gate。
4. 本批次只治理文件與追溯口徑，不新增 runtime、OpenAPI 文件、schema contract test 或 App AI 能力。

## 2026-05-07 資料隱私與運維成熟度治理批次

1. 新增 `04-共用機制/04-資料治理與隱私風險基線.md`，把 NIST Privacy Framework、ISO/IEC 27701、NIST CSF 2.0 轉化為 CJ 的資料分類、consent/delete、retention、AI stream archive、log / release evidence 最小化與 App storage / telemetry 邊界。
2. 新增 `03-管理端與平台治理/06-SLO可觀測性與事故治理基線.md`，把 Google SRE SLO、NIST CSF 2.0、NIST SP 800-61r3 與 NIST SSDF 轉化為 SLI / SLO、告警門檻、事故分級、incident record、postmortem 與 release gate failure 口徑。
3. 回寫工程級 PRD 對標台賬、NFR、RTM、共用機制、管理端治理索引、術語表與文檔治理規則，固定「局部 delete / cleanup / health 200 / metrics 存在」不得被宣稱為完整隱私合規或 SLA。
4. 本批次只治理文件與追溯口徑，不新增 DSAR、OpenAPI、SLO target、incident detector、App telemetry 或 runtime 行為。

## 2026-05-07 架構決策與威脅建模治理批次

1. 新增 `04-共用機制/05-威脅建模與安全需求基線.md`，把 OWASP Threat Modeling、OWASP ASVS、OWASP MASVS、NIST SSDF 與 NIST CSF 2.0 轉化為 CJ 的信任邊界、STRIDE 分類、`CJ-SEC-*` 安全需求與 Web / App 安全投影缺口。
2. 新增 `05-工程架構與共享層/02-架構決策與ADR治理基線.md`，把 ISO/IEC/IEEE 42010、C4 model 與 NIST SSDF 轉化為 CJ 的架構視圖、ADR 最低格式、當前架構決策登記與架構變更 gate。
3. 回寫工程級 PRD 對標台賬、NFR、RTM、共用機制、工程架構索引、根層入口、術語表與文檔治理規則，固定「安全中間件存在不等於完整 threat model」與「repo 目錄說明不等於架構決策」的口徑。
4. 本批次只治理文件與追溯口徑，不新增 runtime、安全掃描、OpenAPI、App SecureStore/Push/Deep Link/upload adapter、正式 C4 圖集或 per-feature ADR 目錄。

## 2026-05-07 狀態機與錯誤契約治理批次

1. 新增 `04-共用機制/06-狀態機與業務不變式治理基線.md`，把 UML State Machines、W3C SCXML、BPMN 與 ISO/IEC/IEEE 29148 轉化為 CJ 的核心狀態、transition、guard、side effect、非法轉移、恢復策略與業務不變式治理口徑。
2. 新增 `06-接口描述/12-錯誤模型與ProblemDetails缺口台賬.md`，把 RFC 9110、RFC 9457、OpenAPI 與 ISO/IEC/IEEE 29148 轉化為 CJ 的 API failure contract、HTTP status、`error.code`、request id、retry policy 與 Problem Details 缺口。
3. 回寫工程級 PRD 對標台賬、NFR、RTM、共用機制、接口索引、根層入口、術語表與文檔治理規則，固定「enum 不等於完整狀態機」與「CJ envelope 不等於 RFC 9457 Problem Details」的口徑。
4. 本批次只治理文件與追溯口徑，不新增 runtime、statechart generator、OpenAPI error schema、Problem Details response、field-level validation schema、Retry-After header 或 App error UX。

## 2026-05-07 可訪問性與本地化治理批次

1. 新增 `04-共用機制/07-可訪問性本地化與內容設計治理基線.md`，把 WCAG 2.2、WAI-ARIA / APG、ISO 9241-210、W3C i18n、Unicode CLDR、Android / React Native Accessibility 轉化為 CJ 的 Web / Admin / App 可訪問性、本地化、`lang`、screen reader 與內容設計治理口徑。
2. 新增 `08-測試規範與驗收/05-可訪問性本地化驗收基線.md`，把 role / focus tests、keyboard-only smoke、axe / WCAG gap、i18n completeness、screen reader smoke、App native smoke 與內容審查接入驗收層。
3. 回寫工程級 PRD 對標台賬、NFR、RTM、共用機制、測試索引、根層入口、術語表與文檔治理規則，固定「i18n catalog / Radix / 局部 role test 不等於 WCAG 合規、雙語完整或 App accessibility 完成」的口徑。
4. 本批次只治理文件與追溯口徑，不新增 runtime、axe / WCAG 掃描、i18n completeness script、screen reader 證據、App native accessibility runtime 或 `html[lang]` 代碼修正。

## 2026-05-07 資料模型與 Schema Migration 相容性治理批次

1. 新增 `05-工程架構與共享層/03-資料模型SchemaMigration與相容性治理基線.md`，把 ISO/IEC 11179、Prisma migration history、Prisma expand-and-contract、PostgreSQL DDL、Google AIP-180 / 181 / 185 與 SemVer 轉化為 CJ 的資料模型、schema migration、DB parity、backfill、棄用與相容性治理口徑。
2. 新增 `08-測試規範與驗收/06-SchemaMigration與相容性驗收基線.md`，把 migration history、release DB parity、schema compatibility、backfill / dual-read、operational risk、deprecation 與 App parity 接入驗收層。
3. 回寫工程級 PRD 對標台賬、NFR、RTM、工程架構索引、測試索引、根層入口、術語表與文檔治理規則，固定「schema.prisma 更新 / migration commit / Dev DB 成功不等於 release DB parity、backfill 完成或 production-safe」的口徑。
4. 本批次只治理文件與追溯口徑，不新增 runtime、Prisma migration、schema diff checker、backfill script、release gate migration 清單、DB hotfix reconciliation 或 App schema parity smoke。

## 2026-05-07 SLO 可觀測性與事故演練驗收治理批次

1. 新增 `08-測試規範與驗收/07-SLO可觀測性與事故演練驗收基線.md`，把 Google SRE Alerting on SLOs、OpenTelemetry Signals、Prometheus Alerting 與 NIST SP 800-61r3 轉化為 CJ 的 health / metrics / logs / request id / release evidence / incident drill 驗收口徑。
2. 回寫 `03-管理端與平台治理/06-SLO可觀測性與事故治理基線.md`，補清 SLI 計算、信號裁決權重、metrics / logs / traces 缺口、incident drill 與不可宣稱邊界。
3. 回寫工程級 PRD 對標台賬、NFR、RTM、管理端治理索引、測試索引、根層入口與文檔治理規則，固定「`/metrics` 存在、request id 存在、Prometheus rule 文件存在或 release gate pass 不等於 SLA / error budget / tracing / incident drill 完成」的口徑。
4. 本批次只治理文件與追溯口徑，不新增 runtime、OpenTelemetry collector、trace id、Alertmanager、burn-rate alert、incident evidence 實例或 App telemetry runtime。

## 2026-04-18 Batch 5-6 收口結果

1. `07-待處理問題與治理` 已將已收口的代碼級缺陷移入 `已處理/`，活躍區只保留剩餘發版風險與治理項。
2. `08-測試規範與驗收` 與 `測試/` 已按現有測試樹校正取證入口，並區分活躍執行入口與歷史結果回看。
3. `90-證據與盤點`、`99-歷史降級索引`、`文件收斂`、`頁面HTML快照` 已統一收口為非正文層，明確“非 SSOT、只作證據/歷史/治理參考”的定位。
4. `Batch 5` 已將活躍場景案例數、回歸入口、未登入直連清單、Repair Journey 矩陣與實際路由/測試檔關係納入 `docs:check:truth` 守衛。

## 2026-04-18 Batch 6 人工抽樣覆核

1. 對 `90/99/文件收斂` 做 15 份樣本抽查，覆蓋 `md/json/html` 三類。
2. 抽樣結果已回寫 `04-CJ-Batch6-抽樣核對記錄-2026-04-18.md`，本輪樣本 `15/15 PASS`。
3. 抽樣覆核與 `docs:check` 機械守衛共同構成 Batch 6 收口證據。

## 2026-04-18 守衛加固（手動回歸一致性）

1. `scripts/check-docs-truth.mjs` 已新增手動回歸一致性守衛：對 `latest-date` 批次逐條比對 `summary.md` 與 `P01-P05/record.md` 的狀態。
2. 若任一流程出現 `summary != record` 或缺少狀態行，`npm run docs:check` 直接失敗，不再允許人工“只改 summary”。
3. 活躍口徑固定為：先更新 `record.md`，再重建/核對 `summary.md`，最後執行 `manual-regression:check:strict` + `docs:check`。

## 2026-04-18 全倉最終驗收

1. 新增 `10-CJ-全倉最終驗收記錄-2026-04-18.md`，集中落盤三區收斂、舊引用清零、守衛綠燈與抽樣追溯證據。
2. 驗收結果確認：`CJ` 全倉 `.md` 僅存在於 `核心開發文件 / 核心營銷文件 / 歸檔` 三區，核心開發文件內舊路徑引用為 0。
3. 最終守衛狀態：`npm run docs:check`、`npm run docs:check:truth` 均 PASS，並包含 Batch 6 metadata 與 HTML 快照一致性檢查。

## 歷史治理文件

以下文件保留回看價值，但不構成活躍治理正文：

1. `文檔封板後-代碼與測試對齊清單.md`
2. `文檔封板後-對齊執行記錄-2026-03-09.md`
3. `審計記錄-2026-03-06.md`
