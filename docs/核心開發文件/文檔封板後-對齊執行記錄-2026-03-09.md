# 文檔封板後 - 對齊執行記錄

**執行日期**：2026-03-09  
**執行範圍**：依 `文檔封板後-代碼與測試對齊清單.md`，完成本輪封板後 `F04`、`/profile/pairing`、`F07 canonical session_id` 與 chat invite accept 身份口徑之代碼、測試、文檔對齊。  
**執行方式**：靜態取證 + 定向修正 + 定向測試驗證；本輪已直接執行前後端單測與 chat E2E 專項。  

---

## 結論摘要

- `acceptInvite` 已正式收斂為 `User only`，核心 SSOT、鏡像文檔、後端實作與 E2E 測試口徑一致。
- `/profile/pairing` 的 F06 訪談入口已限制在配對成功態，與 `F08 主責 / F06 次責` 的封板描述一致。
- `F07` 匿名 owner 的 `canonical session_id` 規則已補齊前端、後端測試矩陣。
- `F04` 的 chat -> login -> judgment handoff 已補上專項回歸測試。

**總判定**：`本輪涉及範圍內，文檔 / 代碼 / 測試已達成一致。`

---

## 已完成修正

### 1. Chat invite accept 收斂為 `User only`

- 核心文檔已統一把 `POST /api/v1/chat/invites/:inviteCode/accept` 收斂為 `User only`。
- 二級映射文檔與快速入口文檔也已同步，避免再出現 `User/Session` 舊口徑。
- `frontend/e2e/chat/chat-flow.e2e.ts` 已改為「已登入 B 以邀請碼加入」。
- `backend/src/services/chat.service.ts` 既有 `acceptInvite()` 的登入要求維持不變，現已與文檔一致。

判定：`已對齊。`

### 2. `/profile/pairing` 僅在配對成功態承接 F06

- `frontend/src/pages/Profile/Pairing/index.tsx` 已把訪談 trigger 與 consent 後續流程綁定在 `active pairing`。
- `frontend/src/pages/Profile/Pairing/index.test.tsx` 已新增「非配對成功態不顯示 trigger」驗證，原有訪談相關測試也已改為顯式使用 active pairing。
- `docs/核心開發文件/頁面清單.md`、`docs/核心開發文件/業務流程整合.md`、`docs/前端設計/08-接口一覽表.md` 等口徑維持為「配對成功態可作 F06 次責入口」。

判定：`已對齊。`

### 3. `canonical session_id` 規則補齊測試矩陣

- `frontend/src/pages/Chat/Room/index.test.tsx` 已補三條匿名 owner 分支：
  - `room.session_id === canonical session_id` 可建立 invite
  - `room.session_id !== canonical session_id` 時按鈕禁用
  - 缺少 session 時按鈕禁用
- `backend/tests/unit/services/chat.service.test.ts` 已補三條 `createInvite()` 分支：
  - 匿名房主 session 匹配可建立 invite
  - 匿名房主 session 不匹配回 `FORBIDDEN`
  - 缺少 session 回 `SESSION_ID_REQUIRED`
- E2E helper 仍只使用 `cj_session_id`，未新增其他匿名身份來源。

判定：`已對齊。`

### 4. F04 chat -> login -> judgment handoff 補回歸

- `frontend/src/pages/Chat/Room/index.test.tsx` 已補未登入用戶從 chat 發起判決後，被 `ProtectedRoute` 導向 `/auth/login`，且保留 `/judgment/:id` 回跳目標的專項測試。
- 既有 `ProtectedRoute` 與 `Login` 回跳能力未改壞。

判定：`已對齊。`

---

## 測試驗證

已執行：

1. `frontend`: `npm test -- src/pages/Profile/Pairing/index.test.tsx src/pages/Chat/Room/index.test.tsx`
2. `backend`: `npm run test:unit -- tests/unit/services/chat.service.test.ts`
3. `frontend`: `npm run test:e2e -- chat/chat-flow.e2e.ts`

結果：

- 前端定向單測：`129 passed`
- 後端單測集合：`1090 passed`
- chat E2E 專項：`2 passed`

備註：

- 前端單測仍有既有警告輸出，如 `useForm` 未連接提示與部分 router navigation warning；本輪未新增失敗。

---

## 本輪判定

- 已收斂衝突：`acceptInvite` 身份口徑、`/profile/pairing` 訪談入口範圍。
- 已補齊缺口：匿名 owner `canonical session_id` 測試矩陣、chat judgment handoff 回跳測試。
- 已完成驗證：相關前後端單測與 chat E2E 專項均通過。

**總結**：`本輪封板後的對齊修正已完成，且在本輪涉及範圍內沒有再觀察到未處置衝突。`
