# 文檔代碼對齊審計記錄

**審計日期**：2026-03-06  
**審計範圍**：docs 下「關鍵 + 設計類」文檔與代碼對照，以代碼為準修正過時或矛盾內容；參考文件與 reports 僅標記為非權威。

---

## 對照結論摘要

| 階段 | 文檔/目錄 | 結論 | 主要修改 |
|------|-----------|------|----------|
| 階段1 | 頁面清單.md | 已對齊 | 文檔版本 v2.2、最後更新 2026-03-06 |
| 階段1 | 全接口清單-主文檔.md | 已對齊 | 文檔版本 v2.3、最後更新 2026-03-06 |
| 階段1 | 功能特性清單.md | 已對齊 | 文檔版本 v2.2、最後更新 2026-03-06 |
| 階段1 | 接口-功能-頁面-Mapping.md | 已對齊 | 文檔版本 v1.2、最後更新 2026-03-06 |
| 階段1 | 業務流程整合.md | 已修正 | 新增「路徑均為 /api/v1 下」說明；文檔版本 v2.2、最後更新 2026-03-06 |
| 階段1 | 術語表.md | 已對齊 | 文檔版本 v1.1、最後更新 2026-03-06 |
| 階段1 | 維護規範.md | 已對齊 | 文檔版本 v1.3、最後更新 2026-03-06 |
| 階段1 | 接口描述/01～10 + README | 已對齊 | 01 錯誤碼 EMAIL_ALREADY_EXISTS → EMAIL_EXISTS；全員最後更新 2026-03-06、文檔版本 v2.2 |
| 階段2 | 發布指引/、DEPLOYMENT、STAGING、ENVIRONMENT、QUICK_START、INTEGRATION、11-開發環境配置、技術實現細節補充 | 已對齊 | 最後更新與版本 bump；發布指引與 DEPLOYMENT 關係已明確 |
| 階段3 | 前端設計/02、08、03、04；後端設計/03、11、01、04、05、06、07；02-產品設計、10-MVP | 已修正 | SSOT 連結改為核心開發文件路徑；11-部署和運維 新增與 DEPLOYMENT/發布指引關係說明；最後更新 2026-03-06 |
| 階段4 | 參考文件/ | 僅標記 | 新增 README.md：非 SSOT、現行規格以核心開發文件與代碼為準 |
| 階段4 | reports/ | 僅標記 | 新增 README.md：歷史快照、現行以發布指引與 ENVIRONMENT 為準 |

---

## 取證代碼路徑（審計基準）

- 路由/頁面：`frontend/src/router/index.tsx`、`frontend-admin` 路由與 pages
- API：`backend/src/app.ts`、`backend/src/routes/*.ts`
- 前端調用：`frontend/src/services/api`、`frontend/src/store`、頁面內調用
- 發布/環境：`scripts/`、發布指引、`backend/DEPLOYMENT.md`

---

## 複查記錄

- **2026-03-07**：頁面清單 ↔ `router/index.tsx`、全接口清單 ↔ `app.ts` + `routes/*.ts`（含 content、notification）複查通過；業務流程路徑說明、接口描述 01 錯誤碼與代碼一致。無需變更。

---

## 未登入直連優化驗證（2026-03-06）

依「未登入直連優化驗證計畫」執行：

- **§1 代碼完成度**：已對齊。ProtectedRoute 逾時分支與兩處 Navigate 皆帶 `state={{ from: location }}`；Login 使用 `VALID_REDIRECT_PREFIXES` 與 `navigate(from, { replace: true })`；AdminSectionLayout 內 `!tokenPresent || !tokenReady` 時重定向至 `/admin/login`；`admin/login` 為獨立 route 不經 Layout。ProtectedRoute 單元測試 7 則全過（含逾時必導向、逾時不依 isAuthenticated 渲染）。
- **§2 邊界與漏洞**：無新增問題。保護路由均用預設 requireAuth；回跳僅限站內前綴；redirect 目標與 replace 使用正確。
- **§3 回歸**：自動化部分已跑；手動情境見「未登入直連-回歸驗證清單」。
- **§4 文件**：頁面清單、術語表、業務流程整合、接口-功能-頁面-Mapping、功能特性清單、02-路由與頁面結構設計 均已含計畫要求之關鍵內容，Grep 抽查通過。

**結論**：已對齊。符合「真的完成、未發現問題、無漏洞、無不合理」標準。

---

## 後續建議

- 常規迭代：依 [維護規範.md](維護規範.md) 變更觸發矩陣同步文檔。
- 月度巡檢：核對候選廢棄接口與頁面清單 In/Out 是否需更新。
- 發版前：抽檢 5 條高風險 API 的「全接口清單 ↔ 接口描述 ↔ 前端證據」閉環。
