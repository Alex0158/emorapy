# 平台實際部署執行結果（2026-03-05）

本文件記錄「已由 CLI 實際執行」的動作與結果（非紙上規劃）。

## 已完成（實際執行成功）

- Railway
  - 已連結專案：`ingenious-commitment`（environment: `production`）
  - 已連結服務：`mother-bear-court`
  - 已修復部署狀態：`mother-bear-court` 由 `CRASHED` 轉為 `SUCCESS`
  - 已確認網域：`https://mother-bear-court-production.up.railway.app`
  - 已驗證：
    - `GET /health` -> healthy
    - `GET /health/ready` -> ready
- Vercel（frontend）
  - 已連結專案：`frontend`
  - 已覆蓋 production env：
    - `VITE_API_BASE_URL=https://mother-bear-court-production.up.railway.app/api/v1`
    - `VITE_ADMIN_LOGIN_URL=https://frontend-admin-sigma-virid.vercel.app/admin/login`
  - 已成功 production 部署並 alias 到：`https://frontend-lilac-three-52.vercel.app`
- Vercel（frontend-admin）
  - 已連結專案：`frontend-admin`
  - 已覆蓋 production env（同上）
  - 遠端建置因 monorepo alias 限制失敗（`@/` 指向 `../frontend/src`）
  - 已改用 `prebuilt` 本機建置再部署，並成功 alias 到：
    - `https://frontend-admin-sigma-virid.vercel.app`

## 已驗證的跨系統連通

- 前台站點可訪問：`https://frontend-lilac-three-52.vercel.app`（HTTP 200）
- 管理台登入頁可訪問：`https://frontend-admin-sigma-virid.vercel.app/admin/login`（HTTP 200）
- 後端 API 在帶前台 Origin 時可建立 quick session（CORS 通過）
- 管理台 Origin 打 admin API 回應 `401`（符合未登入預期，而非 CORS 阻擋）

## 仍需你手動確認（平台權限/商務配置）

- 自訂網域與 DNS 綁定（若要使用 `app/admin/api.<domain>`）
- Railway / Vercel 上 secrets 長期輪替策略（`JWT_SECRET`、`ADMIN_JWT_SECRET`、`OPENAI_API_KEY`）
- Supabase 本地 `link`（需要 DB password 才能在 CLI 完整連結）

## 風險與建議

- `frontend-admin` 目前依賴 shared code（`../frontend/src`），在 Vercel remote build 可能失敗；穩定解法：
  - 保持 `prebuilt` 流程，或
  - 將 admin 專案 Root Directory/建置策略改為 monorepo-aware 配置。
- package `engines.node` 使用 `>=20.19.0`，平台可能升到新 major；建議後續鎖定 major（如 `20.x`）避免行為漂移。
