# 部署和運維

**文檔版本**：v2.1  
**最後更新**：2026-03-06  

> **與其他文檔的關係**：本文件為高階概述；環境變數、遷移、監控、Railway/Docker/PM2 等細則以 [docs/backend/DEPLOYMENT.md](../backend/DEPLOYMENT.md) 為權威；發布步驟以 [docs/發布指引/發佈流程指引.md](../發布指引/發佈流程指引.md) 及同目錄 Vercel/Railway 指引為準。

---

## 🚀 部署概述

### 部署環境

- **開發環境**：本地開發
- **測試環境**：Railway/Render測試實例
- **生產環境**：Railway/Render生產實例

### 部署平台

- **推薦**：Railway（免費額度，易於使用）
- **備選**：Render（免費版，功能完整）

---

## 📦 環境配置

### 環境變量

**文件**：`.env.example`

```env
# 服務器配置
NODE_ENV=production
PORT=3001

# 數據庫配置
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT配置
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# OpenAI配置
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=2000
OPENAI_DAILY_LIMIT=1000

# 郵件配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-password
EMAIL_FROM=noreply@motherbearcourt.com

# 文件存儲配置（實現以 UPLOAD_DIR、FILE_BASE_URL 為準，見 backend/.env.example）
UPLOAD_DIR=./uploads
FILE_BASE_URL=https://your-backend-domain.com

# 前端URL
FRONTEND_URL=https://motherbearcourt.com

# 允許的來源（CORS）；若前台為 Vercel 預設網域（如 https://mother-bear-court.vercel.app），須將該網址加入
ALLOWED_ORIGINS=https://motherbearcourt.com,https://www.motherbearcourt.com

# 日誌級別
LOG_LEVEL=info

# v2.0 訪談與分析（詳見下方「v2.0 部署注意事項」及 docs/ENVIRONMENT.md）
# OPENAI_INTERVIEW_MODEL=gpt-4o-mini
# OPENAI_ANALYSIS_MODEL=gpt-4o
# INTERVIEW_MAX_TURNS=25
# INTERVIEW_SOFT_TARGET=15
# INTERVIEW_TURN_INTERVAL_MS=3000
# INTERVIEW_START_RATE_LIMIT=3
# INTERVIEW_DAILY_SESSION_LIMIT=5
# REDIS_URL=redis://localhost:6379
```

---

## 🏗️ 部署流程

### 1. 準備工作

```bash
# 1. 確保代碼已提交到Git
git add .
git commit -m "Ready for deployment"
git push origin main

# 2. 運行測試
npm run test

# 3. 構建項目（後端使用 build:prod）
cd backend && npm run build:prod
```

### 2. Railway部署

**步驟**：

1. 在Railway創建新項目
2. 連接GitHub倉庫
3. 配置環境變量
4. 部署

**Railway配置**：

```json
{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### 3. Render部署

**步驟**：

1. 在Render創建新Web服務
2. 連接GitHub倉庫
3. 配置構建命令：`npm install && npm run build`
4. 配置啟動命令：`npm start`
5. 配置環境變量
6. 部署

---

## 🗄️ 數據庫部署

### Supabase設置

1. 創建Supabase項目
2. 獲取數據庫連接字符串
3. 運行遷移：

```bash
# 設置數據庫URL
export DATABASE_URL="postgresql://..."

# 運行遷移
npx prisma migrate deploy
```

### 數據庫遷移

```bash
# 開發環境：創建遷移
npx prisma migrate dev --name migration_name

# 生產環境：應用遷移
npx prisma migrate deploy
```

---

## 📊 監控和日誌

### 日誌管理

**本地日誌**：
- 錯誤日誌：`logs/error.log`
- 所有日誌：`logs/combined.log`

**生產環境**：
- Railway/Render提供內建日誌查看
- 可集成Sentry進行錯誤追蹤

### 性能監控

**關鍵指標**：
- API響應時間
- 數據庫查詢時間
- AI服務調用時間
- 錯誤率

**監控工具**：
- Railway/Render內建監控
- 可選：New Relic、Datadog

---

## 🔧 運維任務

### 日常維護

1. **日誌檢查**：每日檢查錯誤日誌
2. **性能監控**：監控API響應時間
3. **成本監控**：監控OpenAI API使用量
4. **備份檢查**：確認數據庫備份正常（v2.0 新增 5 表：`interview_sessions`、`interview_turns`、`profile_narratives`、`profile_insights`、`profile_snapshots` + 4 ENUM，需確認備份範圍涵蓋）

### 定期任務

1. **數據清理**：清理過期數據
2. **緩存清理**：清理過期緩存
3. **安全更新**：更新依賴包
4. **性能優化**：根據監控數據優化

---

## 🚨 故障處理

### 常見問題

**1. 服務無法啟動**

```bash
# 檢查日誌
railway logs

# 檢查環境變量
railway variables

# 檢查數據庫連接
npx prisma db pull
```

**2. 數據庫連接失敗**

- 檢查DATABASE_URL是否正確
- 檢查數據庫是否運行
- 檢查網絡連接

**3. AI服務錯誤**

- 檢查OPENAI_API_KEY是否有效
- 檢查API限額
- 檢查網絡連接

**實現對齊**：後端構建為 `npm run build:prod`；環境變量以 `backend/.env.example` 為準（含 UPLOAD_DIR、FILE_BASE_URL）；完整流程見 [發佈流程指引](../發布指引/發佈流程指引.md)、[ENVIRONMENT](../ENVIRONMENT.md)。

---

## 🧠 v2.0 部署注意事項

### 新增環境變量

| 變量 | 必需 | 說明 |
|------|------|------|
| `OPENAI_INTERVIEW_MODEL` | 否（默認 gpt-4o-mini） | 訪談對話模型 |
| `OPENAI_ANALYSIS_MODEL` | 否（默認 gpt-4o） | 分析/提取模型 |
| `INTERVIEW_MAX_TURNS` | 否（默認 25） | 訪談最大 turn 數 |
| `INTERVIEW_SOFT_TARGET` | 否（默認 15） | AI 建議結束 turn 數 |
| `INTERVIEW_TURN_INTERVAL_MS` | 否（默認 3000） | turn 最小間隔 |
| `INTERVIEW_START_RATE_LIMIT` | 否（默認 3） | start 端點每用戶每小時上限（express-rate-limit，防濫用，不計 turn 數） |
| `INTERVIEW_DAILY_SESSION_LIMIT` | 否（默認 5） | 每用戶每天最多 substantive session 數（業務邏輯，僅計 ≥ 3 輪） |
| `REDIS_URL` | 否 | 用於 session mutex lock（不配置則 fallback 到 DB advisory lock） |

### 數據庫遷移

```bash
npx prisma migrate deploy  # v2.0 新增 5 表 4 ENUM
```

### SSE 與反向代理

若使用 Nginx 反向代理，需禁用 SSE 端點的 buffering。後端 `interview.controller.ts` 已對 SSE 回應設定 `X-Accel-Buffering: no` 標頭，Nginx 據此自動關閉 buffering；但建議仍在 Nginx 層顯式配置：

```nginx
location /api/v1/interview/ {
  proxy_buffering off;
  proxy_cache off;
  proxy_read_timeout 120s;  # 訪談 SSE 可能持續 60s+
  # 後端已設 X-Accel-Buffering: no，此處為雙重保護
}
```

### 異步管線部署

異步管線（`AsyncPipelineService`）在 `POST /interview/:id/end` 後由同一進程內的異步任務執行（非獨立 worker queue）。部署注意事項：

- **無需獨立 worker**：管線以 `Promise` 形式在 Express 進程內非阻塞運行；不佔用 HTTP 線程，但會佔用 Node.js event loop
- **進程重啟容錯**：若進程在管線執行期間重啟，session 將停留在 `processing` 狀態。定時任務（每 30 分鐘）掃描 `processing` 超過 10 分鐘的 session 並標記為 `processing_failed`，用戶可從「我的故事」頁面觸發重試
- **水平擴展**：多實例部署時，`session-level mutex`（Redis 或 DB advisory lock）確保同一 session 的管線不會被多個實例同時處理
- **資源預估**：每個 session 的管線執行約 3-5 個 AI calls（GPT-4o-mini × 2 + GPT-4o × 1-2），預計耗時 15-45 秒，峰值記憶體增量 <50MB

### 監控要點

- **AsyncPipelineService**：監控 `processing_failed` session 數量（告警閾值：>5/hour）；監控 `processing` 超 10 分鐘的 session（可能是管線卡住）
- **SSE 超時**：監控 respond 端點的 P99 延遲（告警閾值：>30s）
- **AI 費用**：每月追蹤 OpenAI API 用量（預算：$10-50/月 基於 1000 session）
- **管線步驟耗時**：按 `pipeline_step` 維度追蹤各步驟耗時，識別瓶頸（通常是 step 3 摘要+洞察提取）

---

## 📚 相關文檔

- [後端架構設計](./01-後端架構設計.md)
- [數據庫設計](./02-數據庫設計.md)
- [發佈流程指引](../發布指引/發佈流程指引.md)

---

**文檔版本**：v2.0  
**最後更新**：2026-02-21（v2.1：補充頂部 env 示例 v2.0 變量、備份範圍涵蓋 v2.0 表、X-Accel-Buffering 說明）
