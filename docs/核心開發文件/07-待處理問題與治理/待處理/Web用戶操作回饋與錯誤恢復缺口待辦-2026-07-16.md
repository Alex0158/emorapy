# Web 用戶操作回饋與錯誤恢復缺口待辦（2026-07-16）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：問題治理
**覆蓋範圍**：Consumer Web、Admin Web 的操作回饋、表單錯誤、狀態可見性與恢復路徑
**取證代碼入口**：`frontend/src/App.tsx`、`frontend/src/pages`、`frontend/src/components/common`、`frontend/src/services/request.ts`、`frontend/src/utils/apiError.ts`、`frontend-admin/src/pages`
**最後核驗 Commit**：`d4f1a92`
**最後核驗日期**：`2026-07-16`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 結論

2026-07-16 對 Consumer 30 routes、Admin 9 routes 及其共用互動層完成靜態盤點，並在 Production `https://emorapy.com/auth/login` 重現「登入失敗後頁面完全沒有提示」。本輪共確認 **7 類可執行缺口**；其中首項是系統級根因，令 Consumer Web 46 個 production source files 中的 233 個 Sonner feedback emissions 全部沒有 renderer，不能把它們誤算成 233 個獨立頁面缺陷。首輪修復已由 PR #17 發布至 Production；live browser 證明 persistent alert 與單一 toast 已恢復，但同時發現 `INVALID_CREDENTIALS` 的 401 toast 被 status-only mapping 誤報為「登入已過期」。此項屬 WF-07 的 feedback ownership／語意完整性，不另膨脹成新類別；candidate 已改為復用共享 error-code mapping，並保留 `EMAIL_NOT_VERIFIED` 由 Login recovery page 單獨提示。

## 缺口清單

| ID | 等級 | 表面 | Current behavior | Target behavior | 狀態 |
| --- | --- | --- | --- | --- | --- |
| WF-01 | P0 | Consumer Web 全站 | `toast.*` 已被 46 個檔案使用 233 次，但 `App.tsx` 未掛載 `Toaster`，成功／失敗／警告／狀態提示全部不可見 | 根節點唯一掛載共用 `Toaster`，所有既有 emissions 恢復可見 | Production renderer 已驗證 |
| WF-02 | P1 | Consumer Login | credential/API failure 只有 toast，且缺持久、可聚焦語義的 form-level error | toast 作即時通知；同一受控訊息以 `role=alert` 留在表單，修改輸入或重試時清除 | Production persistent alert 已驗證 |
| WF-03 | P1 | Consumer Register | 發碼、驗碼、註冊 proof／API failure 只有 toast；驗證碼不完整亦沒有持久提示 | 多步驟共享一個 form feedback region，錯誤跟隨當前步驟並可重試 | Production code 已發布；全量 tests／gates 通過 |
| WF-04 | P1 | Consumer Forgot Password | 發信、驗證碼及重設 failure 只有 toast | 多步驟共享持久 form feedback，成功畫面和既有 redirect 不變 | Production code 已發布；全量 tests／gates 通過 |
| WF-05 | P1 | Admin Login | Admin 有 `Toaster`，但錯誤只短暫顯示，使用者仍需回到表單修正 | form-level `role=alert` 與 toast 並存；輸入更新後清除舊錯誤 | Production code 已發布；Admin tests／gates 通過 |
| WF-06 | P1 | Profile Pairing | `navigator.clipboard.writeText()` 未 await/catch，瀏覽器拒絕時仍顯示成功 | 復用既有 clipboard fallback，只有實際成功才提示成功，失敗明確提示 | Production code 已發布；regression 通過 |
| WF-07 | P1 | Consumer Auth error ownership／語意 | transport 與 Auth page 可能重複 emit；首輪 Production 又證明 401 status-only fallback 把 `INVALID_CREDENTIALS` 誤報為 session expired | transport 保留單一、code-aware ephemeral toast；頁面承接 persistent inline error；`EMAIL_NOT_VERIFIED` recovery 由頁面單獨提示 | 重複提示已發布；code-aware candidate 已完成 118 項 focused regressions，待 Production 重驗 |

## 回饋決策規則

1. **Inline feedback**：登入、註冊、重設密碼等需要使用者修正／重試的阻塞錯誤，必須留在相關表單內，並提供 `role="alert"`／`aria-live`。
2. **Toast**：適合短暫成功確認、非阻塞警告及跨區域 action result；不得成為身份驗證等高風險失敗的唯一載體。同一 failure 只可由一層 emit：transport 已提示時，page catch 只更新 inline state。
3. **Page state**：初始 load failure、empty state、長時串流或離線狀態繼續使用既有 page-level state，不重複以全局 toast 取代。
4. **Truthful success**：clipboard、下載、分享、媒體等 browser API 必須等實際完成後才報成功；rejection 不得被當成 success。
5. **No blanket fallback**：storage、history mutation、optional telemetry 等不影響使用者主任務且已有明確 fallback 的 catch，不為了增加訊息數量而打擾使用者。

## 驗證與完成條件

- [x] `App` regression 證明 Consumer root 永久掛載 `Toaster`。
- [x] Login、Register、Forgot Password、Admin Login component regressions 證明 server failure 會留下可見 alert，且不阻斷重試。
- [x] Pairing regression 證明 clipboard rejection 不會顯示 success。
- [x] Consumer / Admin 全量 test、lint、build、A11Y／copy／docs gates 通過。
- [x] 本機 browser 以實際 failed request 驗證一個 toast + 一個 persistent alert，且無 layout regression；before／after 證據位於 `output/product-feedback-audit-2026-07-16/`。
- [x] 首輪 exact-main `d4f1a92` CI、Production deploy 與 live Login persistent feedback 已驗證。
- [ ] code-aware 401 candidate 經 exact-main CI／Production deploy 後，再以 live browser 證明 toast 與 inline alert 同為「帳號或密碼不正確」，且公開 `version.json` SHA 對齊。

## 發布與 Live 證據

1. PR #17 已 squash merge 為 `d4f1a92ad81345220cfcf8fa6471a84e3aad1276`。
2. exact-main CI run `29465915391` 全部通過；Production workflow run `29466015088` 的 preflight、rollback baseline、Railway、Vercel 與 release gate 全部成功。
3. 三端 version endpoint 曾同時回報 `d4f1a92`；Production Login browser 實測得到一個 `role=alert`「帳號或密碼不正確」與一個 toast，證明 renderer／persistent feedback 已生效。
4. 同次 live QA 發現 toast 文案為「登入已過期，請重新登入」；根因是 `getHttpErrorFallbackMessage` 只按 HTTP 401 status 選文案，沒有使用已存在的 `INVALID_CREDENTIALS` mapping。candidate 已讓 request layer 復用 `getApiErrorMessageForCode`，另釘住 `EMAIL_NOT_VERIFIED` 不重複 transport toast。

## 非本輪範圍

- App native 的 toast/banner contract；由 App parity 與 native accessibility 待辦獨立治理。
- 把所有 toast 機械轉成 modal 或 inline alert；這會製造訊息疲勞並破壞既有頁面狀態層級。
- optional browser capability、storage fallback、history mutation 或 telemetry failure 的全局報錯；只有影響使用者已發起任務時才升級為可見回饋。
