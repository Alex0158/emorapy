# App 測試與證據接入基線

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：測試規範
**覆蓋範圍**：App smoke / regression / CI / evidence 接入前置規則
**取證代碼入口**：`mobile/package.json`、`mobile/app`、`mobile/src/platform`、`mobile/tsconfig.json`、`packages/contracts/src`、`packages/api-client/src`、`backend/src/routes`、`backend/prisma/schema.prisma`
**最後核驗 Commit**：`1295216`
**最後核驗日期**：`2026-05-06`
<!-- CORE_DOC_AUDIT_METADATA:END -->

本文定義 App 測試進入正式測試體系前的基線。它不宣稱 App 已完成；已存在的 M0-M6 本地命令可證明 shell / web export / unit / iOS simulator smoke / Android static config / Android emulator release APK install-launch 層可用，且已建立 structured EAS iOS / TestFlight evidence runner 與 structured physical device evidence runner，但不能替代 TestFlight pass JSON、真機實跑、provider side-effect、full native flow 或 EAS build evidence。

當前現碼事實：

1. `mobile/` 已存在 Expo 專案。
2. `mobile/app` 已替換 Expo Router template tabs，建立 `(public)`、`(app)`、quick、quick/collaborative、auth、case、chat、profile、notifications、repair、modal route skeleton。
3. `mobile/src/platform` 已建立 API / SecureStore / SSE / upload / notifications / linking / lifecycle / telemetry runtime adapter。
4. App 已改用 package resolution 消費 shared packages；API adapter skeleton 已接 `@cj/api-client` error normalize，M1 Quick/Auth/Session、同機 collaborative quick、quick submit session recovery、result access expired cleanup、result `case_judgment` stream replay、Quick result AppState background abort / foreground `afterSeq` reconnect RNTL gate、M2 Profile/Interview、M3 Chat、M4 Case/Repair 與 M5 Notification/Upload domain client 已開始落地；SSE transport 已補非 2xx typed error / close callback，M1 Quick result、M2 Interview、M3 Chat 與 M4 Repair replan 已接 App AI stream subscription hook（ready snapshot、`after_seq` replay、close/error retry），且 M1-M4 對應 screen 均已有 AppState background abort / foreground `afterSeq` reconnect RNTL gate；Push device token registration / revoke / logout cleanup / registration-time token rotation revoke 已有 backend/schema/API/client 與 App token sync，notification landing handler、post-login resume、upload web/native branch、telemetry safe ingest / CJ OTLP JSON trace ingest / minimized persistence / Admin report / 30d cleanup route contract、lifecycle subscription 與 App observability bootstrap（session start、global JS fatal / unhandled promise、OpenTelemetry provider span、native crash SDK configuration status）已有首輪 unit / route smoke，delivery、native crash runtime evidence / external tracing backend 與真機 native landing 證據仍待補齊。
5. 倉庫現有自動化測試與手動回歸主要覆蓋 Backend、Web、Admin Web、跨站真服務與發布腳本；App 目前已新增 `mobile` typecheck / web export smoke、provider render、auth/session bootstrap、root error boundary tests、M0-M5 Maestro flow artifact static gate、iOS simulator native Maestro 7/7 smoke、Android static readiness gate 與 `npm --prefix mobile run smoke:true-service` 真服務 smoke harness dry-run / safety gate。當前 dry-run 可執行並暴露 non-local backend DB blocker；在本機隔離 Postgres + local backend 下，M1-M5 已可用 `--bootstrap-local-users` 產生 token-gated true-service evidence，包含 M1 claim-session、M1 `--m1-stream-replay` `case_judgment` SSE `after_seq` replay、M1 local-only `--m1-inject-expired-session` 過期 submit recovery / result access reject / expired claim ignored、M2 `--deep` 5-turn interview start/respond/background response/end/my-story completion、M3 partner invite accept + `--request-ai` judgment、M4 pairing/case/judgment/execution + formal evidence upload + repair plan select + execution confirm + live `repair_track` replan stream `stream.persisted` + backend restart 後 DB-backed SSE replay、M5 push token register/revoke + notification state sync + evidence multipart upload/delete + telemetry ingest；但尚未覆蓋 physical device、native stream reconnect、Push delivery 或 EAS/TestFlight build。

## 1. 進場條件

App smoke / regression / CI 進入 `08-測試規範與驗收/`、`測試/` 或 `90-證據與盤點/` 前，必須先滿足以下條件：

1. 對應能力已在 [../20-App端/01-App導航與平台Adapter基線.md](../20-App端/01-App導航與平台Adapter基線.md) 找到 screen、navigation 或 platform adapter 落點。
2. 對應 Backend / API / DB / shared package 影響已在 [../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md](../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md) 標清。
3. 若 API、DB、shared enum、Push token、Deep Link、upload 授權或 session restore 尚未閉環，必須更新 [../07-待處理問題與治理/待處理/App跨端Parity落地待辦-2026-05-05.md](../07-待處理問題與治理/待處理/App跨端Parity落地待辦-2026-05-05.md) 或新增更細待辦。
4. 測試名稱、證據名稱與 case id 必須使用跨端產品能力語義，不得沿用 `Tab One` / `Tab Two` 等模板名稱作正式測試主體。

## 2. 最小測試分層

App 測試落地後，至少分為四層：

| 層級 | 覆蓋對象 | 可進入條件 | 文件落點 |
| --- | --- | --- | --- |
| App smoke | App 啟動、root layout、auth/session restore、至少一條 CJ 主流程入口 | `mobile/app` 已替換模板主入口 | `測試/回歸與驗收/` |
| Unit / component tests | route helpers、adapter helper、screen state、form validation、stream reducer | 對應 App module 已有 runtime code | `mobile/` 測試入口，後續文檔回寫 |
| Platform adapter tests | SecureStore、Push token、upload/ImagePicker、Deep Link parse / fallback、lifecycle restore、telemetry safe payload | `mobile/src/platform/<domain>` 已有 runtime adapter | `08-測試規範與驗收/` 補規範，`測試/` 補案例 |
| Cross-contract tests | App API adapter 對 `@cj/contracts` / `@cj/api-client` 的消費 | App 不再長期手寫分叉 DTO | `50-跨端Mapping與Parity/` + `測試/` |
| E2E / flow tests | Quick + Auth、Profile + Interview、Chat、Formal Case + Repair、Notification landing | 對應 Roadmap milestone 已實作 | `測試/回歸與驗收/` |
| App evidence | 真機 / 模擬器截圖、錄屏、CI log、manual regression record、EAS build artifact | 對應測試已能重跑或有清楚環境說明 | `90-證據與盤點/` |

只有「App screen 可以啟動」不足以宣稱 App parity 完成；必須同時說明它對 API、DB、shared contract、platform adapter 與跨端產品語義的影響。

## 2.1 工具裁決

| 工具 | 用途 | 進入條件 |
| --- | --- | --- |
| `jest-expo` | App unit / adapter helper / pure logic test | M0 開始引入；不得改寫 tracked files 作為測試副作用 |
| React Native Testing Library | Screen / component interaction test | M0 / M1 screen 實作後進場 |
| Maestro | 真機 / 模擬器 E2E flow，覆蓋 Quick、Auth、Profile、Chat、Case、Repair、Notification landing | M0-M5 flow artifact 與 selector static gate 已建立；M6 前至少要有 iOS simulator / device 實跑 smoke |
| EAS Build smoke | iOS TestFlight / Android readiness build artifact 驗證 | M6 必須進場；Expo Go 成功不能替代 |
| Manual evidence | 截圖、錄屏、CI log、TestFlight build、device notes | 每個 milestone 完成前至少留一組可追溯證據 |

## 3. 不得替代

以下情況不能被視為 App 已驗收：

1. Web / Admin Web 單元測試、Playwright 或手動回歸通過。
2. `mobile/` Expo 模板可以啟動。
3. `mobile/src/platform` 只有型別檔存在。
4. App 使用本地 mock DTO 跑通，但沒有對齊 `packages/contracts` 或 `packages/api-client`。
5. App 截圖或錄屏沒有對應可追溯的能力分組、測試步驟、API / DB / shared 影響說明。
6. Expo Go 本機啟動成功，但沒有 simulator / device / EAS build smoke 或對應 Roadmap milestone 證據。
7. Web Playwright、Web unit tests 或 Admin Web release gate 通過，被用來替代 App native accessibility、SecureStore、Push、Deep Link、upload 或 lifecycle 驗收。

## 4. 證據命名與回寫

新增 App 證據時，必須同時標明：

1. 對應跨端能力組：Auth、Quick、Case、Chat、Profile、Notification、Media、Telemetry。
2. 對應 App screen / adapter。
3. 對應 Backend route / DB schema / shared package，若沒有影響需明確寫「無新增後端/DB/shared 影響」。
4. 測試方式：unit、integration、simulator、device、manual regression、CI。
5. 尚未閉環項：直接鏈接待辦，不得只在證據中口頭保留。

若 App 測試結果暴露產品語義或 schema/runtime 差異，必須回寫 `00-跨端產品核心/`、`20-App端/`、`50-跨端Mapping與Parity/` 或 `07-待處理問題與治理/待處理/`，不能只留在測試證據中。

## 5. Roadmap Gate

| Roadmap | 最小測試 / 證據 gate |
| --- | --- |
| M0 Foundation | App launch smoke、provider render、storage adapter unit、API adapter header test |
| M1 Quick + Auth | quick create -> result smoke、session restore、claim-session smoke、auth fallback |
| M2 Profile + Interview | consent gate、SSE respond / skip / end、my-story completion、result / retry、stream interruption inspection |
| M3 Chat | room create、invite accept/decline、invite accept auth resume、message stream、request judgment、auth handoff |
| M4 Formal Case + Repair | pairing -> case -> judgment -> repair regression、upload fallback、`repair_track` replan replay |
| M5 Push + Deep Link | device token registration / revoke inspection、notification landing、read / snooze / act sync、auth failure fallback、structured provider delivery smoke |
| M6 Release Hardening | EAS build smoke、iOS TestFlight evidence、Android readiness check、structured physical device smoke、structured push provider evidence、manual evidence pack |

### 5.1 True-Service Smoke Safety Gate

`npm --prefix mobile run smoke:true-service` 的預設模式是 dry-run。只有 `--run` 或 `APP_TRUE_SERVICE_SMOKE_RUN=true` 才會打 backend。run mode 必須先通過 safety gate：

1. API URL 必須是 localhost / 127.0.0.1 / ::1；若要打 staging API，必須顯式使用 `--allow-remote-api` 或 `APP_SMOKE_ALLOW_REMOTE_API=true`。
2. 當 API URL 是本機時，腳本會讀取 `backend/.env` 或 process env 的 `DATABASE_URL`，並拒絕 non-local DB；若確定是 staging/sandbox，才可顯式使用 `--allow-remote-db` 或 `APP_SMOKE_ALLOW_REMOTE_DB=true`。
3. dry-run report 中 `blocked: true` 是環境缺口，不是 flow 完成證據；不得用 dry-run 取代真服務 run evidence。
4. 本機真服務 smoke 的推薦資料庫是隔離 Postgres，例如 `postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev`；可用 `backend/docker-compose.postgres.yml` 或同等本機 Postgres 16 cluster 啟動，並以 `APP_SMOKE_BACKEND_DATABASE_URL` 明確傳入腳本，避免誤讀 `backend/.env` 的 non-local DB。
5. `--bootstrap-local-users` 只允許 local API + local DB，會透過 `/auth/register` 建立暫時 primary / partner smoke users 並在同一次 `--scope=all` run 內重用 token；不得對 staging / production 使用。
6. 若沒有啟動本機 Redis，local backend 可能輸出 metrics / AI stream Redis warning；這只代表 Redis-backed replay / metrics 未在該次 smoke 覆蓋，不可把該次 smoke 說成 production-like runtime parity。

## 6. 當前 M0 驗證狀態（2026-05-08）

| 驗證項 | 狀態 | 結論 |
| --- | --- | --- |
| `npm --prefix mobile run typecheck` | 已通過 | TypeScript 對 route shell、provider、platform adapter skeleton 無阻塞錯誤 |
| `npm --prefix mobile run smoke:web` | 已通過 | Expo web export 可生成 App route 靜態產物 |
| web export route smoke | 已建立 gate | `npm --prefix mobile run web:routes:smoke` 會用 `mobile/dist` 啟動本地 static server，透過 Playwright 在 desktop + phone viewport 打開 16 條 App 入口並驗證指定 screen `testID` 已掛載、body 非空且無 page error：`/`、`/quick`、`/quick/collaborative`、`/quick/result`、`/auth`、`/auth?next=%2Fnotifications`、`/profile`、`/profile/interview`、`/profile/story`、`/chat`、`/chat/room`、`/chat/invite`、`/case`、`/repair`、`/notifications`、`/modal`；已納入 `release:preflight`，不替代 native navigation / Deep Link runtime evidence |
| App route topology static gate | 已建立首輪 gate | `npm --prefix mobile run routes:check` 會固定 `mobile/app` 的 `(public)` / `(app)` / Quick / Auth / Case / Chat / Profile / Notifications / Repair / `modal` route file set、root/public/authenticated layout registrations、authenticated deep route `href: null` 與禁止 Expo template `(tabs)` / `Tab One` / `Tab Two` 回流；已納入 `release:preflight`，不替代 native navigation、Deep Link 或 auth resume runtime evidence |
| App feature coverage static gate | 已建立首輪 gate | `npm --prefix mobile run features:check` 會把 M0-M5 Roadmap 直接釘到 source contract：route `testID`、Quick/Auth/Profile/Interview/Chat/Formal Case/Repair/Notification screen actions、shared `packages/api-client/src/m1.ts` 至 `packages/api-client/src/m5.ts` endpoint、SSE / Deep Link / Push / Upload / Telemetry platform adapter、Backend notification / telemetry / push provider route；已納入 `release:preflight` 與 `/goal` tests checklist，用來避免只靠資料夾或測試檔存在就誤判功能覆蓋 |
| App true-service smoke harness contract | 已建立首輪 gate | `npm --prefix mobile run true-service:check` 會固定 `mobile/scripts/app-true-service-smoke.mjs` 的 M1-M5 / `all` scope、`--run` / `--dry-run`、`--bootstrap-local-users`、`--deep`、`--m1-stream-replay`、`--m1-inject-expired-session`、`--m2-inject-failure`、`--m2-inject-partial-success`、`--request-ai`、remote API / DB override、local-only DB-state injection safety、M1 `case_judgment` replay / expired session recovery、M2 failed / partial-success、M3 request judgment、M4 `m4.repair_track_replan_stream` 與 M5 `m5.telemetry_ingest` / upload / notification action step contract；已納入 `release:preflight` 與 `/goal` tests checklist，用來避免 true-service smoke harness 被簡化成只有入口腳本存在 |
| Playwright UI style smoke | 已建立 gate | `web:routes:smoke` 會驗證 public home 的 primary CTA 渲染為可見 button（teal background / white text / 48px min-height）；本地未啟 backend 時 telemetry endpoint 可能出現 send-failed console noise，不替代 connected-service smoke |
| App shared UI accessibility contract | 已建立首輪 gate | `mobile/src/ui/components.test.js` 覆蓋 screen / panel header role、LinkButton / ActionButton label + hint、busy / disabled state、48px touch target 與 FeatureRow 合併朗讀 label；不替代 native screen reader、dynamic type、VoiceOver/TalkBack 或真機 smoke |
| App TextInput accessibility static gate | 已建立首輪 gate | `npm --prefix mobile run accessibility:check` 會掃描 `mobile/app/**/*.tsx` 的 `TextInput`，缺少 `accessibilityLabel` 或 `accessibilityHint` 即 fail；已納入 `release:preflight`，不替代真機讀屏或內容可理解性審查 |
| App user-visible copy static gate | 已建立首輪 gate | `npm --prefix mobile run copy:check` 會掃描 `mobile/app`、`mobile/src/ui` 與 `mobile/src/features` route / UI 的 `.tsx` JSX text / visible props / JSX expression / visible state template，並把 `src/features/**/labels.ts` / `src/ui/**/labels.ts` 視為可見 copy module 掃描，禁止普通用戶可見文案回退到 `anonymous session`、`after_seq`、`stream seq`、`claim-session`、`solo_active`、`share_summary_only`、`invite code`、`room id`、`deep link`、`pairing`、`API`、`apiBaseUrl`、`http://`、`https://`、`M0-M6`、`接入中`、`測試`、`Alpha`、`App 主線`、`結果頁骨架`、`判斷代號`、`方案代號`、`修復旅程識別碼`、`房間代號`、`房間`、`服務位址` 等工程詞、內部里程碑、raw service URL 或 backend enum；同時阻止 `QUICK`、`AUTH`、`PROFILE`、`INTERVIEW`、`MY STORY`、`CHAT`、`INVITE`、`CASE`、`REPAIR`、`NOTICE`、`RESULT`、`CJ APP`、`APP`、`Email`、`Password` 等 legacy English screen / field label 作為完整可見標籤回流，並阻止 `.status`、`.priority`、`.phase`、`.mode`、`.code`、`.template_code`、`.id`、`.room_id`、`.case_id`、`.plan_id`、`.track_id`、`.judgment_id`、`.domains_touched`、raw route id 變數、raw `feedState`、`.domains_touched.join(...)` 與 `attachment` / `family_origin` / `life_events` / `belief_values` / `cultural_background` / `education_cognition` / `personality` / `relationship_history` 等 PsychDomain raw enum value 直接進入 label / JSX text / feedback template / visible copy module；已納入 `release:preflight`，不替代人工 UX writing review 或本地化審查 |
| App platform side-effect boundary static gate | 已建立首輪 gate | `npm --prefix mobile run platform:check` 會掃描 `mobile/app` 與 `mobile/src`，除 `mobile/src/platform` 外禁止 static / dynamic import 或 require `expo-secure-store`、`expo-image-picker`、`expo-notifications`、`expo-linking`，以及直接使用 `AppState`；已納入 `release:preflight`，不替代真機 native side-effect evidence |
| jest-expo / React Native Testing Library | 已建立首輪 gate | 已覆蓋 SecureStore、API adapter、provider render、auth/session bootstrap、root error boundary、Quick/Auth screen 最小互動、upload / notifications / lifecycle adapter branch；native launch 與 deep native side effects 仍待補 |
| SecureStore / API / SSE adapter unit | 已建立首輪 gate | storage、header、typed error normalize、SSE non-2xx open error、SSE close callback、upload FormData / ImagePicker branch + picker status、notification web branch、lifecycle subscribe 已有最小測試；SSE 真機 native interruption 分支仍待補 |
| Maestro flow artifact / selector static gate | 已建立首輪 gate | `mobile/maestro/00-public-navigation-smoke.yaml`、`mobile/maestro/10-quick-auth-form-smoke.yaml`、`mobile/maestro/20-chat-entry-auth-gate-smoke.yaml`、`mobile/maestro/30-notification-landing-auth-gate-smoke.yaml`、`mobile/maestro/40-profile-interview-auth-gate-smoke.yaml`、`mobile/maestro/50-case-repair-auth-gate-smoke.yaml` 與 `mobile/maestro/60-deep-link-auth-resume-smoke.yaml` 已覆蓋 public navigation、Quick form、Auth form、Profile / Interview / Story protected auth resume、Chat entry/invite/room auth gate、Case / Repair auth gate、notification protected auth resume、非通知 Deep Link auth resume selector；`npm --prefix mobile run maestro:check` 可校驗 appId 與 29 個必要 selector；native execution 另由 `maestro:native` 留證 |
| iOS simulator / device / Maestro native execution / EAS | 部分建立 | iOS simulator Release build、Maestro native 7/7 pass、Android release APK ImagePicker picker-cancel smoke、selected-media backend upload/delete contract smoke、`eas-ios-release:smoke` / `eas-android-release:smoke` structured runners 與 `physical-device:smoke` structured runner 已建立；仍不得宣稱 TestFlight / physical device、Push delivery、真機 picker-selected asset / profile media 或 release evidence pack 完整完成，除非產生 pass 狀態 `App-EAS-iOS-Release-*.json`、`App-EAS-Android-Release-*.json` 與 `App-Physical-Device-*.json` |

## 7. 當前 M1 驗證狀態（2026-05-08）

| 驗證項 | 狀態 | 結論 |
| --- | --- | --- |
| `npm run typecheck --workspace @cj/api-client` | 已通過 | M1 shared domain client 型別通過 |
| `npm --prefix mobile run test` | 已通過 | jest-expo + React Native Testing Library 覆蓋 SecureStore、API adapter、Quick/Auth screen 最小互動、同機 collaborative quick A/B handoff、quick submit session recovery、result access expired cleanup、Quick result pending polling、stream persisted refetch、Quick result ready fallback 用戶語義文案、Quick result AppState background abort / foreground `afterSeq` reconnect 與 claim failure 不阻斷登入 |
| `npm --prefix mobile run typecheck` | 已通過 | Quick/Auth screen 接入 shared client 後無 TypeScript 阻塞 |
| `npm --prefix mobile run smoke:web` | 已通過 | Metro 可解析 shared package，quick/collaborative/auth/result route 可 export；result screen 的 stream connector 只在 client effect 中運行 |
| `npm --prefix mobile run maestro:check` | 已通過 | M0-M5 Maestro flow artifact static gate 已通過；M1 Quick/Auth selector contract 已建立；不替代 iOS simulator / device 實跑 |
| `npm --prefix backend test -- --runInBand tests/integration/smoke.test.ts` | 已通過 | mock-backed backend smoke 已覆蓋 session / auth / quick endpoint 可達性；不替代真 DB / AI flow |
| `npm --prefix backend test -- --runInBand tests/unit/services/auth.service.test.ts` | 已通過 | `claim-session` 會忽略已過期 anonymous quick session、嘗試刪除 stale session 並回傳 `case_id: null`；不替代 App UI 或 native evidence |
| `npm --prefix mobile run smoke:true-service -- --dry-run --scope=all` | 已可執行 / blocked report | App true-service smoke harness 已建立；M1 run mode 可跑 anonymous quick session、quick case、case fetch、by-session、judgment poll，提供 token 時可跑 claim-session；`--m1-stream-replay` 會在 dry-run requirements 標記 stream replay，run mode 要求 judgment persistence 後探測 `/streams/case_judgment/:caseId` 與 `after_seq` replay；`--m1-inject-expired-session` 在 dry-run requirements 內標記 DB-state injection，run mode 只允許 local API + local DB；dry-run report 仍會暴露 `backend/.env` DB 非本機 blocker，不替代真服務 flow 證據 |
| quick create -> result -> claim-session 真服務 smoke | 已通過 local true-service probe | 在 local backend + `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev` 下，`npm --prefix mobile run smoke:true-service -- --run --scope=all --bootstrap-local-users --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 safety、version、anonymous session、quick case、case by id、case by session、judgment poll 與 claim-session；不替代 native stream reconnect 或 Maestro native execution |
| expired anonymous session true-service injection | 已通過 local true-service probe | `mobile/scripts/app-true-service-smoke.mjs --m1-inject-expired-session` 只允許 local API + local DB，並只寫 smoke-created quick sessions；在 local backend + local Postgres 下，`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev APP_SMOKE_JUDGMENT_POLL_ATTEMPTS=10 APP_SMOKE_JUDGMENT_POLL_INTERVAL_MS=500 npm --prefix mobile run smoke:true-service -- --run --scope=m1 --bootstrap-local-users --m1-inject-expired-session --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 `m1.expired_submit_session_recovered`、`m1.expired_result_access_rejected` (`401` / `SESSION_EXPIRED`) 與 `m1.expired_claim_session_ignored` (`case_id: null`)；不替代 native UX 或 Maestro native execution |
| true-service `case_judgment` stream replay / native reconnect | true-service replay 已通過 / native 待補 | App 已有 unit gate；Quick result screen 已改用共用 App AI stream subscription hook，`mobile/__tests__/m1-screens.test.js` 覆蓋 AppState background abort 當前 SSE、foreground 後以最後 `afterSeq` 重連；在 local backend + local Postgres 下，`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev APP_SMOKE_JUDGMENT_POLL_ATTEMPTS=30 APP_SMOKE_JUDGMENT_POLL_INTERVAL_MS=1000 npm --prefix mobile run smoke:true-service -- --run --scope=m1 --bootstrap-local-users --m1-stream-replay --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 `m1.case_judgment_stream_replay`，初次 SSE 見 `stream.persisted` lastSeq=9，並以 `after_seq=8` 回放 terminal persisted event；仍不得宣稱 native lifecycle / foreground-background reconnect 完整驗收 |
| Maestro | iOS simulator native 已通過 / 真機待補 | flow artifact / selector static gate 已有；`App-Native-Maestro-2026-05-08T16-03-15-803Z.json` 記錄 iOS simulator Release build native Maestro 7/7 通過；仍不得宣稱 physical device、TestFlight 或 Android E2E 自動化完成 |

## 8. 當前 M2 驗證狀態（2026-05-08）

| 驗證項 | 狀態 | 結論 |
| --- | --- | --- |
| `npm run test:m2 --workspace @cj/api-client` | 已通過 | Profile / Psych Profile / Interview shared client envelope 與錯誤處理已覆蓋 |
| `npm --prefix mobile run test` | 已通過 | Profile/Interview/My Story 最小互動、Profile / Interview / My Story legacy English eyebrow 與「回到 Profile」本地化、interview auth-gate 同步文案、interview stream parser、ready snapshot / `after_seq` replay、persisted refetch、failed retry UI、feedback-card summary、feedback history title label / `session_id` hiding、PsychDomain label helper 與 Interview `domains_touched` raw enum hiding、Interview sync status 不顯示 stream 段號、App stream subscription hook 首輪分支與 screen-level AppState background abort / foreground `afterSeq` reconnect 已覆蓋 |
| `npm --prefix mobile run typecheck` | 已通過 | M2 screen 與 stream adapter 型別通過 |
| `npm --prefix mobile run smoke:web` | 已通過 | `/profile`、`/profile/interview`、`/profile/story` 可 export |
| `npm --prefix mobile run maestro:check` | 已通過 | M2 Profile / Interview / Story protected deep-link auth-resume Maestro flow artifact 與 selector static gate 已建立；native execution 另由 `maestro:native` 留證 |
| `npm --prefix mobile run smoke:true-service -- --dry-run --scope=all` | 已可執行 / blocked report | M2 smoke scope 已有 token-gated profile / psych-profile / consent / interview-resume probe，`--deep` 可顯式跑 5-turn start/respond/end/my-story completion synthetic flow，`--m2-inject-failure` 可在 local-only run 內注入 failed session，`--m2-inject-partial-success` 可在 local-only run 內注入 completed / no-feedback partial-success session；dry-run 暴露缺 token 與 non-local DB blocker |
| profile -> consent -> interview -> stream -> end -> my-story 真服務 smoke | 已通過 local true-service probe | 在 local backend + `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev` 下，`npm --prefix mobile run smoke:true-service -- --run --scope=m2 --bootstrap-local-users --deep --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=30000` 已通過 profile、psych-profile、consent、resume、5-turn interview start/respond/background AI response/end、session `pipeline_step=6`、profile narratives 與 feedback history `feedback_card`；不替代 failed retry、native interruption 或 Maestro native execution |
| failed retry / partial-success / feedback-card summary component gate | 已通過 | `mobile/__tests__/m2-screens.test.js` 已覆蓋 Profile `failed_session_id` retry、Interview `processing_failed` 只讀 retry state、Interview `partial_success=true` completed read-only state、My Story feedback card JSON summary 與 history title 不顯示 `session_id`；不替代 native lifecycle |
| true-service failed-session injection | 已通過 local true-service probe | `mobile/scripts/app-true-service-smoke.mjs --m2-inject-failure` 只允許 local API + local DB，並只寫入 smoke-created session；在 local backend + local Postgres 下，`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev npm --prefix mobile run smoke:true-service -- --run --scope=m2 --bootstrap-local-users --m2-inject-failure --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 failed injection session start、local DB injection、`processing_failed` readback、resume `has_failed` 與 retry accept；不替代 native lifecycle |
| true-service partial-success injection | 已通過 local true-service probe | `mobile/scripts/app-true-service-smoke.mjs --m2-inject-partial-success` 只允許 local API + local DB，並只寫入 smoke-created session；在 local backend + local Postgres 下，`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev npm --prefix mobile run smoke:true-service -- --run --scope=m2 --bootstrap-local-users --m2-inject-partial-success --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 partial-success session start、local DB injection、`completed` / `pipeline_step=6` / `feedback_card=null` readback、`partial_success=true` 與 retry `VALIDATION_ERROR` reject；不替代 native lifecycle |
| native foreground/background interruption recovery / Maestro native execution | 部分建立 | AppState recovery hook、Interview screen background abort / foreground `afterSeq` reconnect RNTL gate 與 Maestro flow artifact 已接入 M2；iOS simulator native auth-resume smoke 已通過，但仍缺真機 / 模擬器 interview interruption lifecycle 證據，不得宣稱 App SSE lifecycle 或 Profile e2e 已完整驗收 |

## 9. 當前 M3 驗證狀態（2026-05-08）

| 驗證項 | 狀態 | 結論 |
| --- | --- | --- |
| `npm run test:m3 --workspace @cj/api-client` | 已通過 | Chat shared client 覆蓋 room、invite、messages、request judgment、judgment status、error envelope 與 path encode |
| `npm --prefix mobile run test` | 已通過 | Chat Home / Room / Invite 最小互動、Chat / Invite legacy English eyebrow 本地化、Chat Home 無手填房間代號入口、缺少對話上下文回到支援入口、Chat runtime 不顯示「房間 / 房主 / 本機 session」等內部模型詞、invite accept 未登入 auth resume、notification invite landing path mapping、room event stream parser、chat-room AI draft ready snapshot / `after_seq` replay、persisted refetch、協調草稿同步狀態不顯示 stream 段號、message time formatting / fallback 與 Chat Room screen AppState background abort / foreground `afterSeq` reconnect 已覆蓋 |
| `npm --prefix mobile run typecheck` | 已通過 | M3 screen 與 stream adapter 型別通過 |
| `npm --prefix mobile run smoke:web` | 已通過 | `/chat`、`/chat/room`、`/chat/invite` 可 export |
| web export route smoke | 已通過 | query-based Chat Room / Invite route 在無 token/session 時不裸連 backend，避免 hydration / console error |
| `npm --prefix mobile run maestro:check` | 已通過 | M3 Chat entry / invite / room auth-gate Maestro flow artifact 與 selector static gate 已建立；native execution 另由 `maestro:native` 留證 |
| `npm --prefix mobile run smoke:true-service -- --dry-run --scope=all` | 已可執行 / blocked report | M3 smoke scope 已有 anonymous room create、message、invite create、message list 與 judgment-status probe；invite accept 與 `--request-ai` 需要 partner auth；dry-run 會暴露 non-local DB blocker，`--request-ai` 缺 partner token 時會標 blocked |
| create room -> send message -> invite -> accept -> request judgment 真服務 smoke | 已通過 local true-service probe | 在 local backend + `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev` 下，`npm --prefix mobile run smoke:true-service -- --run --scope=m3 --bootstrap-local-users --request-ai --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=35000` 已通過 room create、message、invite create、messages list、judgment-status、partner invite accept、B 方 message、A 方 follow-up 與 request-judgment；不替代 native stream reconnect 或 Maestro native execution |
| native stream reconnect / Maestro | 部分建立 | AppState recovery hook、Chat Room screen background abort / foreground `afterSeq` reconnect RNTL gate 與 Maestro flow artifact 已接入 M3；iOS simulator native chat entry smoke 已通過，但仍缺真機 / 模擬器 stream reconnect 與真 push invite landing 證據，不得宣稱 Chat App e2e / lifecycle 完整完成 |

## 10. 當前 M4 驗證狀態（2026-05-08）

| 驗證項 | 狀態 | 結論 |
| --- | --- | --- |
| `npm run test:m4 --workspace @cj/api-client` | 已通過 | Pairing / Case / Judgment / Reconciliation / Execution shared client envelope、path、normalization 已覆蓋 |
| `npm --prefix mobile run test` | 已通過 | Case / Repair screen 最小互動、Formal Case readiness helper、Case accept judgment -> Repair route handoff、Repair route context 生成方案、方案卡選擇 / 開始、check-in notes helper、repair replan `repair_track` stream ready/event replay、Repair screen AppState background abort / foreground `afterSeq` reconnect、M5 upload adapter 消費、Case evidence 顯式 safety assertion / picker cancel 非成功上傳已覆蓋 |
| `npm --prefix mobile run typecheck` | 已通過 | M4 screen 與 shared client 型別通過 |
| `npm --prefix mobile run smoke:web` | 已通過 | `/case`、`/repair` 可 export |
| web export route smoke | 已通過 | Case / Repair 在無 token 時不裸連 backend，避免 auth-gated route 產生 console error |
| `npm --prefix mobile run maestro:check` | 已通過 | M4 Case / Repair auth-gate Maestro flow artifact 與 selector static gate 已建立；native execution 另由 `maestro:native` 留證，不替代 formal case / repair native e2e |
| `npm --prefix mobile run smoke:true-service -- --dry-run --scope=all` | 已可執行 / blocked report | M4 smoke scope 已有 two-token pairing create/join、formal collaborative case create、case list、judgment poll、execution dashboard probe；`--request-ai` 才生成 repair plans；dry-run 暴露缺雙 token 與 non-local DB blocker |
| pairing -> case -> evidence -> judgment -> plans -> select -> execution confirm -> replan stream 真服務 smoke | 已通過 local true-service probe | 在 local backend + Homebrew Postgres 16 local cluster + `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev` 下，`npm --prefix mobile run smoke:true-service -- --run --scope=m4 --bootstrap-local-users --request-ai --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=60000` 已通過 safety、bootstrap users、pairing create/join、case create、`m4.evidence_upload`、case list、judgment poll、execution dashboard、repair plan generation、`m4.repair_plan_select`、`m4.execution_confirm`、`m4.repair_track_dashboard`、`m4.repair_track_replan_accept` 與 `m4.repair_track_replan_stream`；不替代 native evidence upload、repair journey 全狀態或 Maestro native execution |
| true-service `repair_track` replan stream / native reconnect | 部分建立 | App hook、adapter unit、RNTL screen gate 與 local true-service live replan stream 已覆蓋 ready snapshot、`after_seq` replay、persisted refresh；Repair screen 已補 background abort / foreground `afterSeq` reconnect RNTL gate；backend 已修正 SSE compression、subscribe/replay race 與 live DB replay/snapshot fallback；backend restart 後 `after_seq=7` DB-backed SSE replay 已回到 `stream.persisted`；仍缺 native lifecycle 與 canonical dashboard native 證據，不得宣稱 Repair replan stream 完整驗收 |
| media upload / native evidence / Maestro | 部分建立 | M5 已補 evidence FormData adapter、Case screen 顯式 safety assertion upload handoff、M4 formal case evidence upload true-service smoke 與 M4 auth-gate Maestro flow artifact；iOS simulator native case/repair smoke 已通過，但尚未跑真機 picker / full repair journey native e2e，不得宣稱正式案件證據、repair journey native e2e 完整完成 |

## 11. 當前 M5 驗證狀態（2026-05-08）

| 驗證項 | 狀態 | 結論 |
| --- | --- | --- |
| `npm run test:m5 --workspace @cj/api-client` | 已通過 | Notification / media upload shared client envelope、path、normalization 已覆蓋 |
| `npm --prefix mobile run test` | 已通過 | Notifications screen、notification time formatting / fallback、M5 feature adapter、Deep Link resolver、Quick child route preservation、Chat invite landing path、notification landing handler、non-notification Deep Link handler、post-login resume、upload FormData / ImagePicker branch + picker status adapter、lifecycle adapter、telemetry redaction、endpoint submit failure 不阻塞、App observability bootstrap 與 OpenTelemetry provider span / OTLP JSON exporter 已覆蓋 |
| `npm --prefix mobile run typecheck` | 已通過 | M5 screen、adapter 與 shared client 型別通過 |
| `npm --prefix mobile run smoke:web` | 已通過 | `/notifications` 可 export，且 `expo-notifications` 不再於 Web static render 階段觸發 native side effect |
| web export route smoke | 已通過 | `/notifications`、`/case`、`/repair` 等 route 在無 token 時不裸連 backend，避免 auth-gated route 產生 console error |
| `npm --prefix mobile run maestro:check` | 已通過 | M5 notification / protected Deep Link auth resume Maestro flow artifact 與 selector static gate 已建立；不替代 push delivery、APNs sandbox 或 TestFlight evidence |
| non-notification Deep Link auth resume | 已建立首輪 | `mobile/src/features/m5/DeepLinkLandingHandler.test.js` 覆蓋 cold-start / foreground `cj://` target、public route 直達、protected route pending href + `/auth?next`；`mobile/maestro/60-deep-link-auth-resume-smoke.yaml` 已納入 static gate 並通過 iOS simulator native smoke；不替代真機 cold-start notification response evidence |
| `npm --prefix mobile run smoke:true-service -- --run --scope=m5 --bootstrap-local-users --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=30000` | 已通過 local true-service probe | 在 local backend + `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev` 下已通過 push token register/revoke、notification create/list/unread/read/snooze/dismiss/act/read-all、quick-session evidence multipart upload/delete 與 App telemetry ingest；不替代 APNs sandbox、真機 notification landing 或真機 selected asset / profile media |
| Backend notification state sync / push token route contract | 已建立首輪 | `npm --prefix backend test -- --runInBand tests/unit/services/notification.service.test.ts tests/unit/controllers/notification.controller.test.ts tests/unit/routes/notification.routes.test.ts` 已覆蓋 read / read-all / snooze / dismiss / act target、device token register / revoke、owned notification guard 與 non-sensitive token response；不替代真服務 e2e 或真 provider delivery |
| Chat invite notification landing allowlist | 已建立首輪 | `mobile/src/platform/linking/native.test.js`、`mobile/src/platform/notifications/native.test.js` 與 `backend/tests/unit/services/notification.service.test.ts` 已覆蓋 `/chat/invite/:code` / `/chat/invites/:code` -> `/chat/invite?code=...`，並拒絕 `/chat/invites/:code/accept` direct side effect；不替代真 push delivery / cold-start landing |
| Backend Expo push sender / dispatch / receipt polling job | 已建立首輪 | `npm --prefix backend test -- --runInBand tests/unit/services/push-notification.service.test.ts tests/unit/services/notification.service.test.ts tests/unit/jobs/cleanup.job.test.ts` 已覆蓋 Expo send / receipts endpoint envelope、provider error token redaction、pending push -> sent / failed 回寫、ticket 保存、receipt ok/error/pending 回寫、`dispatch_pending_push_notifications` 與 `poll_push_notification_receipts` cron job；不替代 APNs sandbox、真 provider delivery 或真機 cold-start landing |
| Push provider delivery structured runner | 已建立 / evidence pending | `npm --prefix mobile run push-delivery:smoke -- --dry-run` 已建立 Expo provider send / receipt polling runner；正式 run 必須用真 Expo push token 取得 accepted ticket + ok receipt，證據只保留 token hash，不保存原始 push token / access token；不替代真機 notification landing 或 action completion |
| Backend App telemetry safe ingest / persistence / report | 已建立首輪 | `npm --prefix backend test -- --runInBand tests/unit/services/app-telemetry.service.test.ts tests/unit/routes/app-telemetry.routes.test.ts tests/unit/controllers/admin.controller.test.ts tests/unit/routes/admin.routes.test.ts tests/unit/jobs/cleanup.job.test.ts tests/integration/smoke.test.ts` 已覆蓋匿名 App safe telemetry 202、OTLP JSON trace ingest 202、batch limit / invalid name validation、backend 二次 redaction、severity summary、structured log、minimized DB persistence、Admin aggregate report、JS fatal / unhandled promise crash session 口徑與 30d cleanup；不替代 native crash runtime evidence、external tracing backend、長期 crash-free baseline 或 release evidence |
| Push device token registration / revoke / logout cleanup / rotation revoke / landing handler / post-login resume | 已建立首輪 | backend schema/API、shared client、App token sync、清理前 revoke 補償、registration-time rotation revoke、notification response route handoff、Quick child route preservation、pending href storage 與 auth resume 已覆蓋；不得宣稱真 provider delivery 或真機 native push landing 完成 |
| Push provider lifecycle evidence | 部分建立 | Backend Expo push sender / dispatch / receipt polling job 與 App-side `push-delivery:smoke` runner 已補；未有 pass 狀態 `App-Push-Delivery-*.json`、APNs sandbox、真機 delivery / lifecycle 前不得宣稱 Push token lifecycle 閉環完成 |
| iOS simulator Deep Link / Maestro native execution | 已建立 iOS simulator smoke | iOS 26.4.1 / iPhone 17 simulator 上 Maestro native 7/7 flows 已通過，覆蓋 protected Deep Link auth resume 與 public Quick deep link；仍不得宣稱 APNs 真投遞、真機 notification landing 或 selected-media ImagePicker native upload 完成 |

## 12. 當前 M6 驗證狀態（2026-05-08）

| 驗證項 | 狀態 | 結論 |
| --- | --- | --- |
| `npm --prefix mobile run release:preflight` | 已通過 | 已串起 30 suites / 134 tests、`accessibility:check`、`copy:check`、`routes:check`、`features:check`、`true-service:check`、`platform:check`、`docs:check`、`docs:audit:dry-run:current`、typecheck、web export、`web:routes:smoke`、Maestro static gate、iOS native readiness、Android config readiness、release evidence verifier、external evidence status + JSON contract、external evidence handoff check + contract、external evidence fixture contract、external evidence runner dry-run、external sign-off orchestrator dry-run、prerequisite report contract、workflow contract、env template contract、release completion audit、`goal:completion:audit`、`goal:completion:audit:contract` 與 release readiness；App preflight 會同步阻止核心文件結構、truth check、過期 App 狀態與 current SSOT metadata 漂移，但 `goal:completion:audit` 在 preflight 中只作 prompt-to-artifact 進度盤點，仍不代表 TestFlight evidence 或 `/goal` 完成 |
| `npm --prefix mobile run release:check` | 已通過 | app identity、runtimeVersion、EAS profiles、permission plugin、repo-local shared dependency link、release / native readiness baseline 已建立；會以 warning / info 暴露 EAS CLI 是否在 PATH、是否可非互動 authenticated、`EXPO_TOKEN` 是否缺失與 EAS project id 是否仍未配置；`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` 可臨時消除 CLT active path warning |
| `npx expo install --check` | 已通過 | Expo SDK 55 native dependency set 已對齊 expected versions；本輪 iOS Release build 曾暴露 `expo-notifications@55.0.12` / RN 0.83 Swift compile failure，已以 SDK expected `expo-notifications@~55.0.22`、`react-native@0.83.6`、`react-native-worklets@0.7.4`、`@sentry/react-native@~7.11.0` 修正 |
| `npm --prefix mobile run native:check` | 已通過 | 可 non-strict 檢查 native launch / Maestro execution prerequisites；本機 Full Xcode 26.4.1、iOS 26.4.1 runtime、iPhone 17 simulator、Maestro CLI 2.5.1 / Java 17 已可由 script 使用；未設 `DEVELOPER_DIR` 時仍會 warning active developer dir 指向 CLT |
| `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run native:check:strict` | 已通過 | strict native readiness 已能作為 native build / Maestro 前置 gate；不替代 TestFlight / physical device evidence |
| `npm --prefix mobile run android:check` | 已通過 | Android package、versionCode、adaptive icon assets、push/upload permissions、Expo plugin、EAS Android profile、Android SDK root、Java 17、adb、emulator CLI 與 sdkmanager 已通過；仍 warning 無 connected Android device |
| `npm --prefix mobile run android:check:strict` | 已通過 | strict Android native toolchain gate 已通過；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-Toolchain-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Android-Toolchain-2026-05-08.md)，不得用它替代 Android App install / flow smoke、device smoke 或 build artifact |
| `npm --prefix mobile run android:emulator:smoke -- --avd=CJ_Pixel_9_API_36 --timeout-ms=240000` | 已通過 | `CJ_Pixel_9_API_36` AVD 已 boot 到 `sys.boot_completed=1`；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-Emulator-2026-05-08T10-27-04-283Z.json](../90-證據與盤點/環境與發版驗證/App-Android-Emulator-2026-05-08T10-27-04-283Z.json)，不得用它替代 Android App APK 安裝 / flow smoke、device smoke 或 build artifact |
| `npm --prefix mobile run maestro:native:dry-run` | 已通過 | native Maestro runner 已能展開 static gate、strict readiness 與 7 個 flow 的 `maestro test` 命令；不執行 native flow |
| `SENTRY_DISABLE_AUTO_UPLOAD=true DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npx expo run:ios --configuration Release --device "iPhone 17" --no-bundler` | 已通過 | iOS 26.4.1 / iPhone 17 simulator 上 Release-iphonesimulator `CJ.app` build / install / launch 通過；證據見 [../90-證據與盤點/環境與發版驗證/App-iOS-Release-Simulator-2026-05-08T12-43-46Z.json](../90-證據與盤點/環境與發版驗證/App-iOS-Release-Simulator-2026-05-08T12-43-46Z.json)，不替代 EAS/TestFlight、physical device、Sentry runtime 或 release DB parity |
| `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run maestro:native -- --evidence-dir=/tmp/cj-maestro-evidence` | 已通過 | iOS 26.4.1 / iPhone 17 simulator 上 7/7 Maestro flows passed；證據副本見 `docs/核心開發文件/90-證據與盤點/環境與發版驗證/App-Native-Maestro-2026-05-08T16-03-15-803Z.json` |
| `npm --prefix mobile run release:evidence:check` | 已通過 | 會校驗 native Maestro evidence 的 app id、7/7 flow、static gate、native readiness、flow exit code / duration，也會校驗 iOS Release simulator evidence 的 Release build / install / launch、Sentry phases 與“不替代外部證據”聲明；同時校驗最新 external status / handoff snapshot 的 App identity、known blocker ids、status-handoff blocker 對齊、status `env_files.values_redacted` / `env_files.loaded[]` provenance schema、當前 `release:completion:audit -- --json` 的 `handoff_blocker_ids` 已被 handoff items 覆蓋且標為 `release_completion_blocker=true`、prerequisite-only blockers 不被誤認為 completion blocker、status / handoff / source status / source release audit timestamp coherence、platform commands、final gates 與文檔引用；不替代重新跑 native flow 或 TestFlight / physical device evidence |
| `npm --prefix mobile run release:evidence-redaction:check` | 已通過 | 掃描已落盤 `App-*.json` release evidence pack；可用 `--evidence-dir=<path>` 追加掃描 workflow runner temp prerequisite report 目錄，阻止 raw Bearer token、Expo push token、DB URL、JWT、API key、basic-auth URL、signed URL query 與 EAS raw artifact URL 欄位進入 structured evidence / artifact；此 gate 不連外部 provider，不替代 pass evidence |
| `npm --prefix mobile run android:app:smoke` | 已通過 | `CJ_Pixel_9_API_36` AVD 上已完成 Android release APK `assembleRelease`、`adb install -r`、`com.cj.motherbearcourt/.MainActivity` 顯式 launch、foreground window 與 process id 驗證；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-App-2026-05-08T10-43-22-314Z.json](../90-證據與盤點/環境與發版驗證/App-Android-App-2026-05-08T10-43-22-314Z.json)，不得用它替代 Android full Maestro flow、physical device、EAS Android artifact 或 store artifact |
| `npm --prefix mobile run android:maestro:smoke -- --skip-build` | 已通過 | `CJ_Pixel_9_API_36` AVD 上已串起 Maestro static gate、Android strict readiness、Android app runtime gate 與 M0-M5 7/7 Maestro flows；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-Maestro-2026-05-08T16-22-39-948Z.json](../90-證據與盤點/環境與發版驗證/App-Android-Maestro-2026-05-08T16-22-39-948Z.json)，不得用它替代 Android physical device、EAS Android artifact 或 store artifact |
| `npm --prefix mobile run native:upload:smoke -- --skip-build` | 已通過 Android emulator smoke | 在 `CJ_Pixel_9_API_36` / Android release APK 上串接 static contract、`platform:check`、upload adapter unit、Android strict readiness、APK install/launch 與 `maestro-side-effects/70-native-upload-picker-smoke.yaml`，證明 ImagePicker 可原生打開並返回 App，cancel 不被記成成功上傳；證據見 [../90-證據與盤點/環境與發版驗證/App-Native-Upload-2026-05-08T11-18-22-284Z.json](../90-證據與盤點/環境與發版驗證/App-Native-Upload-2026-05-08T11-18-22-284Z.json)，不替代真機 picker-selected asset / profile media / physical device |
| `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://alex@127.0.0.1:5432/cj_app_upload_smoke_20260508193900?schema=public npm --prefix mobile run selected-media:upload:smoke -- --run --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=30000` | 已通過 isolated local true-service probe | runner 已在臨時 Homebrew PostgreSQL DB + local backend 上通過 `platform:check`、upload adapter unit、M5 true-service synthetic PNG upload/delete，證據見 [../90-證據與盤點/環境與發版驗證/App-Selected-Media-Upload-2026-05-08T11-39-17-168Z.json](../90-證據與盤點/環境與發版驗證/App-Selected-Media-Upload-2026-05-08T11-39-17-168Z.json)；臨時 DB 已刪除。此證據不替代真機 picker-selected asset、profile media authorization 或 physical device |
| `npm --prefix mobile run physical-device:smoke -- --platform=ios --device=<simulator-udid> --skip-install --skip-launch --evidence-dir=/tmp/...` | 已通過 negative smoke | runner 現在會在正式 run 前用 `xcrun xctrace` 驗證 explicit `--device` 是 connected physical device；simulator UDID / offline device 只能產生 `blocked=true` evidence，不能被 `release:completion:audit:strict` 接受為 physical device pass |
| `npm --prefix mobile run eas-ios-release:smoke -- --dry-run` | 已通過 dry-run | 已建立 EAS iOS / TestFlight structured evidence runner；正式 run 必須用 `EXPO_TOKEN` 查詢 EAS production store build metadata 並通過 artifact HEAD/range check，`--require-testflight` 另需 App Store Connect API credentials 查詢 matching TestFlight build。只有 pass 狀態 `App-EAS-iOS-Release-*.json` 可消除 EAS/TestFlight blocker |
| `npm --prefix mobile run eas-android-release:smoke -- --dry-run` | 已通過 dry-run | 已建立 EAS Android production artifact structured evidence runner；正式 run 必須用 `EXPO_TOKEN` 查詢 EAS Android production store build metadata，並通過 package / version / versionCode / artifact HEAD/range check。只有 pass 狀態 `App-EAS-Android-Release-*.json` 可消除 Android EAS artifact blocker |
| `npm --prefix mobile run native-crash:runtime:smoke -- --dry-run` | 已通過 dry-run | 已建立 Sentry native crash runtime structured evidence runner；正式 run 必須提供 Sentry org / project / auth token 與受控 native crash event id，並輸出 `App-Native-Crash-Runtime-*.json` 讓 audit 驗證 provider query、release / production environment match、native runtime signal、crash-like event 與 `blocked=false`；不保存 Sentry auth token 或 event id 原文 |
| `npm --prefix backend run ops:release-db:dry-run` | 已通過 dry-run | 只驗證 release DB parity runner CLI / query plan / evidence command 可執行；不連資料庫、不寫 `App-Release-DB-Parity-*.json`、不輸出 host 或 credentials |
| `npm --prefix mobile run device-discovery:check` | 已通過 | 已納入 `release:preflight`；用 fixture 固化 iOS `xctrace` / Android `adb devices -l` parser，避免把本機 Mac Studio 或 offline device 誤當 physical-device release evidence |
| `npm --prefix mobile run release:evidence-sanitization:check` | 已通過 | 用受控假 secret 觸發 EAS iOS / EAS Android / push delivery / native crash runtime runner 的 blocked evidence，驗證 evidence JSON 與 console output 不包含 raw Expo token、EAS build id、push token / access token、Sentry auth token 或 controlled event id；同時用 clean / leak 兩份臨時 `App-External-Signoff-Prerequisites-*.json` 驗證 `release:evidence-redaction:check -- --evidence-dir=<tmp>` 會掃到 workflow runner temp report 目錄，clean report 必須通過、含 Bearer / push token / DB URL 的 report 必須被擋且輸出不能回洩原文；此 gate 只防止 evidence / artifact 泄密回退，不替代任何 pass evidence |
| `npm --prefix mobile run release:external-evidence:status` | 已通過 | 外部 evidence 總控狀態入口已納入 `release:preflight`；只探測 credential presence（含 push delivery token、Sentry runtime query credentials 與 native crash event id presence）、env-file provenance counters、EAS CLI available / authenticated boolean、真機可見性、type-aware / identity-bound structured evidence candidate、normalized blockers 與下一步命令，不輸出 secret、Expo account、UDID、DB host、env value 或 provider URL；next commands 同時覆蓋 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排入口、iOS signed app path、Android `APP_ANDROID_DEVICE_SERIAL` 與底層 physical-device smoke；錯型、dry-run、wrong app identity、缺 redaction marker、缺 summary 欄位、migration count 不足或 blocked evidence 只能是 `invalid_candidate` / `blocked_candidate`，不得消除 blocker；status 與 completion audit 共用 `mobile/scripts/lib/release-evidence-policy.mjs`，確保 dashboard/status 和 strict gate 對外部 evidence 的核心判定一致；2026-05-08 本機 iOS physical connected=0、offline=2、Android physical connected=0，因此真機 evidence 仍 pending |
| `npm --prefix mobile run release:external-evidence:status:contract` | 已通過 / 已納入 `release:preflight` | 驗證 `release:external-evidence:status -- --json` 的 schema、credential boolean（含 Sentry runtime query credentials 與 native crash event id presence）、env-files redaction / loaded provenance schema、EAS CLI tool boolean、device counters、type-aware / identity-bound evidence candidate 狀態、normalized blockers、`App-External-Evidence-Status-*.json` report 寫入與 next commands；next commands 必須包含 iOS / Android platform-specific validate / run、iOS physical-device smoke、Android physical-device smoke、iOS signed app path、`APP_ANDROID_DEVICE_SERIAL` 與 strict audit gate；用受控 fake EXPO / Apple / ASC / push / Sentry / native crash event id / DB secrets 驗證 JSON / stdout / stderr / report 只輸出 presence booleans，不回洩 raw secret，且 controlled credential blockers 會消除；另以 EAS iOS、TestFlight、EAS Android、physical device、push delivery、native crash runtime、release DB parity valid / invalid fixture pairs 固定錯型 / dry-run / wrong app identity / redaction 缺失 / migration count 不足 evidence 不會被標為 pass |
| `npm --prefix mobile run release:external-evidence:handoff:check` | 已通過 / 已納入 `release:preflight` | 讀取外部 evidence status JSON，確認每個 normalized blocker 都有 owner surface、required env keys、正式命令（含 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排入口與底層 physical-device smoke）、accepted evidence 與 strict gate；`--report-dir=<path>` 會寫入 `App-External-Evidence-Handoff-*.json`，且 report 只包含交接資訊與 placeholder command，不包含 token、push token、DB URL、Sentry event id 或 private key |
| `npm --prefix mobile run release:external-evidence:handoff:contract` | 已通過 / 已納入 `release:preflight` | 專用 handoff contract gate 會驗證 `app-external-evidence-handoff` JSON schema、current release completion blockers / prerequisite-only blockers 分類計數、iOS / Android platform-specific validate / run command 固定、controlled secret env 不回洩、`App-External-Evidence-Handoff-*.json` report 寫入，以及 report 可通過 `release:evidence-redaction:check -- --evidence-dir=<tmp>`；不連外部 provider、不替代任何 pass evidence |
| `npm --prefix mobile run release:external-evidence:fixtures:check` | 已通過 / 已納入 `release:preflight` | 以受控 fixture 直接復用 `release:completion:audit:strict` 的 evidence validators，驗證 EAS iOS + TestFlight、EAS Android、physical device、provider delivery、native crash runtime 與 release DB parity 的 pass fixture 會通過、關鍵欄位失效 fixture 會被拒絕；不連 EAS / ASC / 真機 / provider / Sentry / DB，不產生 pass evidence |
| `npm --prefix mobile run release:external-evidence:dry-run` | 已通過 dry-run | 已納入 `release:preflight`，串接 EAS iOS / EAS Android / iOS physical-device / Android physical-device / push delivery / native crash / telemetry runtime / release DB parity runner 的 dry-run；只驗證 runner CLI / script 可執行，不查 provider、不連真機、不連 release DB、不消除 release blocker |
| `npm --prefix mobile run telemetry:runtime:smoke -- --dry-run --release-env-file=release.env.local` / `--run` | 已通過 dry-run / release pass | telemetry runtime runner 已支援 gitignored release env file，安全讀取 `APP_TELEMETRY_RUNTIME_API_BASE_URL` 且不輸出 raw URL；正式 `--run --release-env-file=release.env.local` 只接受 non-local release API，先查 release backend `/version` 並要求 `commitSha` 等於 runner 執行當下本地 `HEAD`，再送 event / OTLP，產出 `App-Telemetry-Runtime-*.json`。audit 允許後續只追加 docs / evidence commit，但若 evidence backend commit 後改過 backend telemetry/version runtime 路徑，舊 evidence 會失效。2026-05-29 canonical evidence `App-Telemetry-Runtime-2026-05-29T16-00-52-498Z.json` 已 `backend_version_passed=true`、event ingest / OTLP ingest passed，`release:completion:audit -- --json` 顯示 `telemetry_runtime_evidence` passed；CORS 合約仍限定只有 telemetry events / OTLP 兩個 App native endpoint 可無 `Origin`，非白名單瀏覽器 Origin 與其他 API 無 Origin 仍拒絕 |
| `npm --prefix mobile run release:external-evidence:signoff` / `release:external-evidence:signoff:android-dry-run` | 已建立 / dry-run / 已納入 `release:preflight` | 外部 release sign-off orchestrator 預設 dry-run 走 iOS physical-device branch，Android branch 由 `release:external-evidence:signoff:android-dry-run` 固定納入 preflight；兩者都會串接 external status、external handoff、EAS iOS + TestFlight、EAS Android、physical device、push provider delivery、native crash runtime、release DB parity、release completion audit 與 `/goal` completion audit；加 `--report-dir=<path>` 時會同時輸出 `App-External-Evidence-Status-*.json` 與 `App-External-Evidence-Handoff-*.json`；不帶 `--run` 不查外部 provider、不連真機、不連 release DB，也不替代任何 pass evidence |
| `npm --prefix mobile run release:external-evidence:validate` | 已建立 / 當前預期失敗 | 正式外部 sign-off prerequisite gate；只檢查 UUID 形狀的 EAS project id（`extra.eas.projectId`）、EAS CLI 可用性、Expo token、成對 Apple submission credentials、ASC / Sentry credentials、真機 ID / signed app path、`xcrun xctrace` / `adb devices -l` 真機可見性與 requested-device match、push token 與 release / production DB URL，不查 provider、不安裝 App、不連 release DB、不寫 pass evidence。缺項時應在外部動作前失敗 |
| `npm --prefix mobile run release:external-evidence:prereq-report:check` | 已通過 | 本地契約測試會用受控假 secret 與缺失 file path 分別觸發 `release:external-evidence:validate -- --report-dir=<tmp>`、`release:external-evidence:run -- --report-dir=<tmp>`、Android physical-platform validate、`--release-env-file=release.env.example` placeholder validate 與 unsafe release-env-file key 前置失敗，驗證只產出 `App-External-Signoff-Prerequisites-*.json`、report 標記 `report_contains_secrets=false`、含 EAS CLI availability、credential / device / signed app / push / Sentry / DB input presence booleans、iOS / Android 真機可見性 counters 與 requested-device match boolean、`REPLACE_WITH_...` placeholder 不會被當成已配置、`NODE_OPTIONS` / 非白名單 key 會在 validation 前被拒絕、每個 missing prerequisite 都有 secret-safe `resolution_hints`、report/stdout/stderr 不包含 token / DB URL / push token / UDID / serial / Sentry event id，且正式 `run` 在前置失敗時不開始 EAS / provider / 真機安裝 / release DB / strict audit steps；同時驗證正式 `run` 拒絕 `--skip`，並驗證 dry-run orchestrator 在 `--report-dir` 下會產出 `App-External-Evidence-Status-*.json` status report 與 `App-External-Evidence-Handoff-*.json` handoff report，且 status / handoff 必須包含 `sentry_runtime_query_credentials` 與 `native_crash_event_id` prerequisite-only item；此 gate 不替代任何 pass evidence |
| `npm --prefix mobile run release:external-evidence:workflow:check` | 已通過 / 已納入 `release:preflight` | 本地 workflow contract gate 會讀 `.github/workflows/app-release-external-signoff.yml`，確認 mode choices、`runner_json` / `fromJSON` runner、run-mode secret/input probe（iOS physical run 要求 `APP_IOS_DEVICE_UDID` + `APP_IOS_DEVICE_APP_PATH`，Android physical run 要求 `APP_ANDROID_DEVICE_SERIAL`）、外部 sign-off 前先跑 App `release:preflight`、且 preflight step 必須用 step-level `APP_RELEASE_EXTERNAL_SIGNOFF_RUN=false` 防止 workflow run mode 提前觸碰 EAS / provider / 真機 / release DB、redaction-before-upload、artifact path、status / handoff / prerequisite report artifact 與 `release:external-evidence:run` / `validate` / dry-run dispatch 仍存在；不連 GitHub、不替代任何 structured pass evidence |
| `npm --prefix mobile run release:external-evidence:env-template:check` | 已通過 / 已納入 `release:preflight` | 本地 env template contract gate 會讀 `mobile/release.env.example` 與 external signoff runbook，確認 release sign-off env keys 齊全、secret-like 欄位只保留 `REPLACE_WITH_` placeholder、`APP_RELEASE_EXTERNAL_SIGNOFF_RUN=false`、report dir 預設 `/tmp`，且 runbook 有 `--release-env-file=release.env.local` 與 Do not commit real secrets 交接說明；不驗證 repository secrets、真機或 provider 是否已準備 |
| `npm --prefix mobile run release:external-evidence:github-secrets:sync:contract` | 已通過 / 已納入 `release:preflight` | 本地 GitHub secret sync contract 用受控臨時 env-file 與 private key fixture 驗證 `github-secrets:sync -- --json` dry-run 是 local-only，不要求 `gh` / GitHub auth / network；同時固定 current completion / evidence refresh secret group 分層、`DATABASE_URL -> APP_RELEASE_DATABASE_URL`、ASC private key path -> `APP_STORE_CONNECT_PRIVATE_KEY`、controlled secret redaction 與 `--apply` 才依賴 GitHub Environment。此 gate 不寫 GitHub secrets，也不替代 workflow validate / run 或 external pass evidence |
| `npm --prefix mobile run release:external-evidence:run` | 已建立 / evidence pending | 正式外部 sign-off 一鍵入口；先執行與 `release:external-evidence:validate` 相同的 prerequisite validation，通過後才會跑 EAS / ASC / 真機 / provider / release DB steps。正式 `run` 不允許 `--skip`，必須執行完整外部 evidence steps 與 strict audits；該命令仍以各 runner 產生的 structured JSON 與 `release:completion:audit:strict` / `goal:completion:audit:strict` 作最終驗收，不接受 orchestrator 本身作替代證據 |
| `.github/workflows/app-release-external-signoff.yml` | 已建立 / manual workflow | 手動 CI 入口已接 `release:external-evidence:signoff` / `release:external-evidence:validate` / `release:external-evidence:run`；預設 dry-run，validate mode 只用 repository secrets / inputs 檢查 prerequisites 且不碰外部 provider / 真機 / release DB；workflow 在外部 sign-off 前會先跑 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run release:preflight`，並在該 step 將 `APP_RELEASE_EXTERNAL_SIGNOFF_RUN=false`，確保 workflow `mode=run` 不會讓 preflight 提前觸碰外部 provider；之後再跑 `release:evidence-redaction:check` / `release:evidence-sanitization:check` / `release:external-evidence:handoff:contract`，signoff 後生成 `App-External-Evidence-Handoff-*.json`，再以 `--evidence-dir="${APP_RELEASE_EXTERNAL_SIGNOFF_REPORT_DIR}"` 跑一次 `release:evidence-redaction:check`，同時覆蓋 repo evidence、runner temp status / handoff / prerequisite report 目錄，只有 redaction step 通過才上傳 evidence artifact；validate / run mode 會上傳 `App-External-Evidence-Status-*.json` status report artifact、`App-External-Evidence-Handoff-*.json` handoff artifact 與 `App-External-Signoff-Prerequisites-*.json` prerequisite report artifact，供查看 normalized blockers / owner action / 缺項但不作 pass evidence；正式 run 需要 repository secrets / inputs 與可信 runner，iOS physical run 必須提供 `APP_IOS_DEVICE_UDID` + `APP_IOS_DEVICE_APP_PATH`，Android physical run 必須提供 `APP_ANDROID_DEVICE_SERIAL`。iOS 真機證據仍需 self-hosted macOS runner 或等價可信設備環境，GitHub-hosted dry-run / validate / workflow success 不替代 structured evidence JSON |
| `npm --prefix mobile run release:completion:audit` | 已通過 / not complete report | 會校驗 release blockers 已被核心文檔明示；iOS simulator Maestro evidence、iOS Release simulator build/install/launch evidence、Android native toolchain、Android emulator runtime evidence、Android app runtime evidence、Android full flow evidence、native ImagePicker picker-cancel evidence、selected-media backend upload evidence、native crash SDK configuration evidence、iOS Sentry native prebuild configuration、OTel provider evidence、release / production DB parity evidence 與 telemetry runtime evidence 已可被 audit 驗證；EAS/TestFlight、physical device、push provider delivery 與 native crash runtime evidence 已收緊為 structured / file-backed gates；當前 completion blockers 為 UUID 形狀的 EAS project id（`extra.eas.projectId`）、`EXPO_TOKEN`、Apple credentials、App Store Connect API credentials、EAS/TestFlight artifact、physical device、APNs delivery、Android EAS artifact 與 native crash runtime evidence；strict mode 未清零前不得通過 |
| `npm --prefix mobile run release:completion:audit -- --json` / `release:completion:audit:contract` | 已通過 / 已納入 `release:preflight` | `--json` 會輸出 `app-release-completion-audit` structured record，包含 `complete`、strict mode、每個 release check 的 passed/blocked 狀態、頂層 `blocker_ids`、頂層 `handoff_blocker_ids`、check-level `handoff_blocker_ids` / `handoff_catalog_ids`、warnings、blockers、failures 與 verified evidence；contract gate 驗證 non-strict / strict JSON schema、required release blocker ids、`blocker_ids` 與 blocked checks 一致、check-level `handoff_blocker_ids` 只在 blocked check 非空、`checks[].handoff_catalog_ids` 與 external handoff catalog mapping 一致、頂層 `handoff_blocker_ids` 與 external handoff catalog coverage 一致、summary 計數、strict exit-code 一致性與 preflight wiring。此 gate 只讓 release sign-off 狀態可被 CI / dashboard / agent 穩定解析，不替代 EAS / TestFlight / 真機 / provider / release DB pass evidence |
| `app_store_connect_api_credentials` strict blocker | 已納入 `release:completion:audit` | App Store Connect API credentials 不再只被 TestFlight evidence 間接要求；strict completion audit 會單獨列出 issuer id / key id / private key 或 private key path 缺口，直到 `eas-ios-release:smoke -- --run --require-testflight` 可生成 pass 狀態 structured evidence |
| `npm --prefix mobile run goal:completion:audit` | 已通過 / not complete report / 已納入 `release:preflight` | 以 `/goal` 原文建立 prompt-to-artifact checklist，覆蓋 stack、iOS-first / Android-compatible、M0-M5 artifact、M1-M5 true-service smoke harness contract、UI/UX accessibility / copy gate、測試 gate、`docs:check` / `docs:audit:dry-run:current` core-doc gate、外部 evidence fixture contract、核心文檔回寫與 release sign-off；證據見 [../90-證據與盤點/環境與發版驗證/App-Goal-Completion-Audit-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Goal-Completion-Audit-2026-05-08.md)。preflight 只跑 non-strict 版本作進度盤點；`goal:completion:audit:strict` 依賴 `release:completion:audit:strict`，EAS/TestFlight、physical device、provider delivery 與 native crash runtime 等 current external blockers 未清零前不得通過；release / production DB parity 與 telemetry runtime 目前已有 structured pass evidence |
| `npm --prefix mobile test -- --runInBand __tests__/m1-screens.test.js __tests__/m3-screens.test.js __tests__/m4-screens.test.js` | 已通過 | 38 tests passed；覆蓋 Quick/Auth submit readiness helper、Quick/Auth/Chat legacy English eyebrow 本地化、Formal Case readiness helper、Case created-time stable label / missing timestamp fallback、Formal Case / Repair legacy English eyebrow 本地化、Repair check-in helper、Quick result ready fallback / Auth claim / Chat room message time fallback / Chat Home 無手填房間代號入口 / Chat runtime 不顯示「房間」模型詞 / missing conversation context guidance / Formal Case / Repair dashboard / Repair plan / Repair replan 不再把 read-only `case_id`、`roomId`、`track_id`、`plan_id` 直接顯示給普通用戶，並覆蓋 Case accept judgment -> Repair route context 與 Repair 不需手填判斷 / 方案 / 旅程識別 |
| `npm --prefix mobile run goal:completion:audit:contract` | 已通過 / 已納入 `release:preflight` | 驗證 `goal:completion:audit -- --json` 與 `goal:completion:audit:strict -- --json` 的 JSON schema、required checklist ids、summary 計數、`complete` boolean、strict 退出碼一致性，以及 `tests_and_gates` 必須包含 `routes:check`、`features:check`、`true-service:check`、`web:routes:smoke`、`docs:check`、`docs:audit:dry-run:current`、`release:evidence:check`、`release:external-evidence:fixtures:check`、`release:external-evidence:handoff:check`、`release:external-evidence:handoff:contract`、`release:external-evidence:prereq-report:check`、`release:external-evidence:workflow:check`、`release:external-evidence:env-template:check`、`release:external-evidence:github-secrets:sync:contract`、`release:external-evidence:signoff` 與 `release:external-evidence:signoff:android-dry-run`；同時直接檢查 `check-app-true-service-smoke-contracts.mjs` 固定 M1 `m1.case_judgment_stream_replay`、M2 `m2.partial_success_retry_rejected`、M3 `m3.request_judgment`、M4 `m4.repair_track_replan_stream`、M5 `m5.telemetry_ingest`、local-only DB-state injection safety 與 `release:preflight` wiring；同時要求 `release:evidence:check` source 固定 external status / handoff snapshot、status env_files redaction/provenance schema、status-handoff blocker 對齊、current release completion audit `handoff_blocker_ids` 覆蓋檢查、release/prerequisite classification、timestamp coherence、platform-specific commands、final gates 與 docs references，要求 `ui_ux_accessibility` audit evidence 包含 `mobile/src/features/m2/labels.ts visible copy module scan`，並直接檢查 `copy:check` source 已掃 `src/features`、`labels.ts`、visible copy module 與 PsychDomain raw enum；另直接檢查 prerequisite report contract source 已覆蓋 iOS / Android `device_visibility` schema、Android physical-platform validate、`android_physical_device_visible`、requested-device match、env-file placeholder guard 與 env-file key allowlist，也直接檢查 GitHub secret sync contract source 固定 local-only dry-run、secret group readiness、release DB / private key mapping、controlled secret redaction 與 apply-only GitHub dependency；當 external release blockers 未清零時，strict JSON audit 必須 `complete=false` 且 exit non-zero，且 incomplete `release_signoff` caveat 必須包含 `blocker_ids`、`handoff_blocker_ids` 與 `app_store_connect_api_credentials`；只有當 release DB parity 或 telemetry runtime 仍位於 `blocker_ids` 時，caveat 才必須包含 `release_db_parity_evidence` 或 `telemetry_runtime_evidence` |
| `npm --prefix mobile run typecheck` | 已通過 | release config 變更後 TypeScript 仍通過 |
| `npm --prefix mobile run smoke:web` | 已通過 | release config 變更後 Expo Web export 仍通過 |
| `npm --prefix mobile run maestro:check` | 已通過 | release preflight 已能檢查 M0-M5 的 7 個 Maestro flow artifact 與 29 個 selector contract；native execution 另由 `maestro:native` 留證 |
| Backend App telemetry report / retention first pass | 已建立 | `app_telemetry_events` migration、`GET /api/v1/admin/reports/app-telemetry`、`cleanup_app_telemetry`、`POST /api/v1/telemetry/otlp/v1/traces` 與 targeted backend tests 已通過；App JS fatal / unhandled promise 已納入 crash session 口徑，不代表 native crash runtime evidence / TestFlight crash-free sessions / external tracing backend 已完成 |
| App observability bootstrap / OpenTelemetry provider | 已建立首輪 | `mobile/src/platform/telemetry/observability.test.js`、`mobile/src/platform/telemetry/client.test.js` 與 `mobile/src/providers/AuthSessionBootstrap.test.js` 覆蓋 session start、lifecycle transition、global JS fatal、unhandled promise、build number payload、OpenTelemetry provider span export、OTLP JSON exporter 與 native crash provider status context；`App-OTel-Provider-2026-05-08.md` 已留證；不替代 native crash runtime evidence、device crash evidence 或 external tracing backend |
| EAS iOS / TestFlight structured evidence runner | 已建立 / evidence pending | `mobile/scripts/run-eas-ios-release-smoke.mjs` 已提供 dry-run 與正式 EAS / App Store Connect provider query；`.ipa` path、raw build id、blocked JSON、Sentry SDK config 或手寫 markdown 均不可替代 EAS/TestFlight provider evidence |
| EAS Android structured evidence runner | 已建立 / evidence pending | `mobile/scripts/run-eas-android-release-smoke.mjs` 已提供 dry-run 與正式 EAS provider query；本機 APK、emulator smoke、blocked JSON 或手寫 markdown 均不可替代 Android EAS artifact evidence |
| Native crash SDK configuration | 已建立首輪 | `mobile/src/platform/telemetry/nativeCrash.test.js` 與 `App-Native-Crash-SDK-2026-05-08.md` 覆蓋 `@sentry/react-native` dependency、Sentry Metro config、DSN-gated initialization、safe `beforeSend` redaction、缺 DSN 時不初始化、Android native `sentry.gradle` / `sentry.properties` 接線、iOS native `sentry-xcode.sh` bundle phase 與 `sentry-xcode-debug-files.sh` dSYM upload phase；不替代 Sentry DSN / source map upload / native crash runtime evidence / TestFlight crash-free sessions |
| Native crash runtime structured evidence runner | 已建立 / evidence pending | `mobile/scripts/run-native-crash-runtime-smoke.mjs` 已提供 dry-run 與正式 Sentry provider query；只有 pass 狀態、release / production environment match、native runtime signal 與 crash-like event 皆成立的 `App-Native-Crash-Runtime-*.json` 可消除 release blocker，markdown、blocked JSON、development/staging Sentry event、Sentry SDK config 或 TestFlight crash-free 摘要均不可替代 native crash runtime capture |
| Backend AI stream / Interview facts / Notification action metadata migration | 已建立 / local migrate 已驗證 | `20260508133000_add_ai_stream_persistence` 已新增 `ai_stream_sessions`、`ai_stream_events` 與 archive tables；`20260508143000_add_interview_collected_facts` 與 `20260508143500_add_interview_turn_extracted_facts` 已補齊 M2 deep 所需 `interview_sessions.collected_facts` / `interview_turns.extracted_facts`；`20260508150000_add_notification_action_metadata` 已補齊 M5 notification read/snooze/dismiss/act 所需欄位；四者均加入 release DB parity 清單，local Postgres `prisma migrate deploy` 已顯示 25 migrations up to date，M1/M2/M4/M5 true-service smoke 已觀察到對應 runtime 可寫入；AI stream live subscription race、SSE compression 與 live DB replay/snapshot fallback 已補 gate，且 M4 backend restart 後 `repair_track` DB replay smoke 已通過；不代表 native lifecycle 或 release DB parity 已完成 |
| App migration release DB parity list coverage | 已建立 / release pass | `backend/tests/unit/scripts/check-release-db-parity.test.ts` 已覆蓋 release-blocking 清單引用的 migration 目錄存在、App release-sensitive migrations 均進入清單、missing / failed / rolled back 會阻塞；local DB `ops:release-db:check` 已回報 14/14 required migrations applied；non-local release DB parity evidence `App-Release-DB-Parity-2026-05-16T06-23-53-463Z.json` 已通過並被 `release:completion:audit -- --json` 判為 passed。後續若新增 release-blocking migration，必須重新產生 fresh non-local structured evidence |
| App release evidence record | 已建立 | 見 [../90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md) |
| EAS project id / Expo token / Apple credentials / App Store Connect API credentials | 未建立 | readiness script 會以 warning 暴露；EAS project id 必須是 UUID 形狀的 `extra.eas.projectId`，不得用 `TODO` / placeholder 字串消除 blocker；Apple submission credentials 必須是 `ASC_APPLE_ID` + `EXPO_APPLE_APP_SPECIFIC_PASSWORD` 成對存在；不得宣稱 CI EAS build / TestFlight submit 或 `eas-ios-release:smoke --run --require-testflight` 已可非交互執行 |
| Native launch / Maestro prerequisites | 已閉環 iOS simulator | readiness script 仍會以 warning 暴露缺 UUID 形狀 EAS project id / Expo token / 完整 Apple credentials；iOS simulator native launch / Maestro native pass evidence 已建立 |
| Android readiness | 部分建立 | Android config static gate、strict native toolchain gate、Android emulator boot smoke、Android release APK install/launch smoke、Android Maestro 7/7 flow smoke 與 EAS Android structured runner dry-run 已通過；Android device smoke、pass 狀態 EAS Android artifact 與 store artifact 未建立 |
| EAS/TestFlight structured evidence / physical device evidence | 未建立 | 不得宣稱 M6 Release Hardening 完整完成 |
