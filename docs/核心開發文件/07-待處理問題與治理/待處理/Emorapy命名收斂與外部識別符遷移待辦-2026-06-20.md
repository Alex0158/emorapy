# Emorapy 命名收斂與外部識別符遷移待辦

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：問題治理
**覆蓋範圍**：Emorapy 正式命名後，App Store / native identifiers / Web / Backend / Ops / GitHub / package scope / 核心文件 / evidence 的舊名收斂與遷移邊界
**取證代碼入口**：`mobile/app.json`、`mobile/scripts`、`mobile/maestro`、`backend/src/services`、`frontend/src/assets/i18n`、`frontend-admin/index.html`、`.github/workflows`、`scripts`、`packages/*/package.json`、`docs/核心開發文件`
**最後核驗 Commit**：`b5f3146`
**最後核驗日期**：`2026-06-20`
<!-- CORE_DOC_AUDIT_METADATA:END -->

**狀態**：P0 決策已確認 / P1 App native identity 已實作 / EAS project 已重建並綁定 Emorapy / iOS Release simulator evidence 已刷新 / P2 對外可見品牌文案與核心營銷 SSOT / 素材識別符已收斂 / P3 shared package scope 已遷移到 `@emorapy/*` / App telemetry runtime diagnostics、OTLP scope fixture、release commit env alias contract、legacy production hostname compatibility gate、release contract temp fixture naming、controlled fixture org/path naming、CI/local true-service Postgres fixture naming、Supabase Dev source comment provenance、Web localStorage key migration、Admin Web locale storage key migration、App SecureStore key migration、App visible copy legacy brand guard 與 Sentry native crash project / release identity handoff 已收斂 / P5 docs audit ledger generator output naming、legacy governance ID namespace policy 已收斂 / P6 evidence/archive provenance 已建立 / 待 Apple Developer、App Store Connect、Sentry 外部資源實際建立 / 查詢憑證、release evidence、P4 repo/domain/env deprecation 與 P5 PRD ID migration mapping 收斂
**Owner**：Product / Mobile / Ops / Docs
**關聯核心文件**：`20-App端/00-App端總覽.md`、`20-App端/01-App導航與平台Adapter基線.md`、`20-App端/03-App完整版本開發Roadmap.md`、`50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md`、`07-待處理問題與治理/待處理/App外部ReleaseSignoff待辦-2026-05-16.md`、`03-管理端與平台治理/04-兩版本運作規範.md`、`03-管理端與平台治理/05-運維連接與調用Runbook.md`、`05-工程架構與共享層/05-Repo平台分層與共享規範.md`、`術語表.md`

---

## 問題

產品正式命名已收斂為 `Emorapy`，但 repo、release tooling、部署資源、文件與歷史 evidence 仍混用 `CJ`、`Mother Bear Court`、`mother-bear-court`、legacy `cj-mobile` / `com.cj.motherbearcourt` evidence、PRD ID 與兼容 env var。App native identity、EAS project binding、shared package scope、產品可見文案與 ops shell 可見文案已完成第一批收斂。

此差異已影響 App Store 建檔、Bundle ID / Android package 選型、EAS release identity、外部 release sign-off、用戶可見文案與核心文件可信度。不得在命名策略未定前建立 `CJ` App Store app record，也不得把舊名硬改成無審計邊界的全局替換。

## 當前排查證據

2026-06-20 針對 source / config / current docs / evidence / generated / lockfile 做分層掃描，確認舊命名不是單點文案問題，而是跨層遷移。

| 分層 | 主要殘留 | 代表入口 | 初步處理口徑 |
| --- | --- | --- | --- |
| App Store / Native release identity | 初始掃描為 `CJ`、`cj-mobile`、`com.cj.motherbearcourt`；現行 App config 已改為 Emorapy identity | `mobile/app.json`、`mobile/scripts/check-release-readiness.mjs`、`mobile/scripts/check-android-readiness.mjs`、`mobile/maestro/*.yaml` | App Store / Apple Developer 外部建檔仍待完成；release evidence 需以新 identity 重跑 |
| Mobile runtime / telemetry | 初始掃描為 `cj-mobile`、`CJ`；現行 telemetry / native crash release naming 已改為 `emorapy-mobile`，current console tag、OTLP scope fixture、telemetry runtime smoke User-Agent 與受控 Sentry org fixture 已改用 `emorapy-*` | `mobile/src/platform/telemetry/client.ts`、`mobile/src/platform/telemetry/nativeCrash.ts`、`mobile/scripts/run-telemetry-runtime-smoke.mjs`、`mobile/scripts/check-release-evidence-sanitization.mjs`、`backend/tests/*app-telemetry*`、`mobile/src/i18n/catalogs/*` | Sentry project / runtime evidence 仍需以 Emorapy identity 重新取證；歷史 evidence 原文不改 |
| Web / Admin / Backend 可見文案 | `CJ Platform`、`CJ 平台`、`CJ Reminder` | `frontend/src/assets/i18n/en-US.ts`、`frontend-admin/index.html`、`backend/src/services/email.service.ts`、`backend/src/services/notification.service.ts` | 產品可見面必須改為 Emorapy |
| Shared package namespace | 現行為 `@emorapy/api-client`、`@emorapy/contracts`；legacy `@cj/*` 只允許留在歷史文件 / evidence / 本遷移說明 | `packages/*/package.json`、`tsconfig.json`、imports、lockfile | P3 已完成；current source/config/active docs 由 `npm run naming:check` 防回流 |
| Ops / deployment / CI | `mother-bear-court`、`CJ_COMMIT_SHA`、`cj`；current App release contract temp fixture、受控 release DB fixture path 與 CI/local true-service Postgres fixture 已收斂為 `emorapy-*` / `/emorapy` / `emorapy_*` | `.github/workflows/*`、`scripts/*`、`mobile/scripts/*contract*.mjs`、`backend/docker-compose*.yml`、`AGENTS.md`、env examples、Railway / Vercel defaults | 需要兼容期；不得一次性破壞 release gate；本地 contract temp 目錄、受控 release DB fixture、CI/local Postgres fixture 不再主動產生 `cj-release-*`、`/cj` 或 `cj_*` |
| Docs current layer | `CJ`、`mother-bear-court`、PRD ID、legacy hostname / env var 說明；`@cj/*` 只可在歷史 / 遷移說明中出現 | `docs/核心開發文件/README.md`、`00-跨端產品核心`、`07/00`、`20-App端`、`50-跨端Mapping與Parity`、`08-測試規範與驗收` | 已建立命名政策與 current package-scope gate；仍需逐域分類 PRD ID、legacy hostname、env var 與 evidence |
| Evidence / archive | `CJ`、`MotherBear`、`com.cj.motherbearcourt` | `docs/核心開發文件/90-證據與盤點`、`99-歷史降級索引` | 不改寫原始歷史證據；新增 alias / policy，未來 evidence 用 Emorapy |
| Generated / lockfile | package lockfile 已更新為 `@emorapy/*`；native generated old IDs 只可由 rebuild / regenerate 更新 | `package-lock.json`、`mobile/package-lock.json`、`mobile/ios`、`mobile/android/app/.cxx` | lockfile 已由 install 更新；native generated 由 rebuild/regenerate 更新，不手改 |

抽樣掃描結果初始顯示，並已按 P0 / P1 進度更新如下：

1. 初始 `mobile/app.json` 曾設定 `expo.name="CJ"`、`expo.slug="cj-mobile"`、`ios.bundleIdentifier="com.cj.motherbearcourt"`、`android.package="com.cj.motherbearcourt"`；P1 已改為 `expo.name="Emorapy"`、`expo.slug="emorapy-mobile"`、`scheme="emorapy"`、`ios.bundleIdentifier="com.emorapy.app"`、`android.package="com.emorapy.app"`。
2. 初始 App release readiness / Android readiness / evidence sanitization 腳本曾把 `cj-mobile`、`com.cj.motherbearcourt`、`@cj/*` 當成正確值；P1 已把正式 App identity 改為 Emorapy，P3 已把 current shared package scope 改為 `@emorapy/*`，舊 native evidence 會被 release audit 視為 stale。
3. GitHub Actions / ops scripts 仍預設 `mother-bear-court` Vercel / Railway service、`CJ_COMMIT_SHA` 與 `mother-bear-court` production URL。
4. backend email / notification、Admin title、部分 Web i18n 與 mobile catalog 仍存在 `CJ` 可見文案。
5. `packages/contracts` 與 `packages/api-client` 已改為 `@emorapy/*` package scope；current source/config/active docs 由 naming gate 阻止回流 `@cj/*`。
6. current docs 內仍有 `CJ` 作為 PRD ID、治理台帳、legacy env var、legacy hostname 或歷史別名；其中 PRD ID 類舊名不可直接機械替換。
7. App Store Connect 曾開啟新建 App modal 並填入 `CJ` / `com.cj.motherbearcourt` 類值；該 record 尚未完成建立，應取消並等待本待辦 P0 決策。

## 命名政策待決策

實作前必須先裁定以下策略，並回寫到本待辦或正式命名政策文件：

| 項目 | 建議方向 | 決策狀態 |
| --- | --- | --- |
| 產品顯示名 | `Emorapy` | 2026-06-20 使用者確認 |
| App Store app name | `Emorapy` | 2026-06-20 使用者確認；待 App Store Connect 建檔 |
| App Store SKU | `emorapy-ios-app` | 2026-06-20 使用者確認；不得使用 `CJ` 或 `com.cj.motherbearcourt` |
| iOS Bundle ID | `com.emorapy.app` | 2026-06-20 使用者確認；待 Apple Developer explicit App ID 註冊 |
| Android package | `com.emorapy.app` | 2026-06-20 使用者確認；P1 改 `mobile/app.json` 與 Android readiness gate |
| Expo slug | `emorapy-mobile` | 2026-06-20 使用者確認；已綁定新 EAS project `@alexdev518/emorapy-mobile` |
| EAS project | 新建並重新連結 `@alexdev518/emorapy-mobile`，不再使用 legacy `@alexdev518/cj-mobile` 作 release binding | 2026-06-20 已執行；`project:info` 回 `ID=73ba39d3-6218-4748-ae36-8d3a93ba34ac` |
| Sentry project / release name | `emorapy-mobile` | 2026-06-20 使用者確認；待 Sentry project / env 對齊 |
| Package scope | P0 / P1 曾短期保留 `@cj/*` 作內部 legacy scope；P3 已遷移到 `@emorapy/contracts`、`@emorapy/api-client` | 2026-06-20 使用者確認並已實作 |
| Env var prefix | 後續新增 `EMORAPY_*`，舊 `CJ_*` dual-read 一段兼容期 | 2026-06-20 使用者確認；P4 專項治理 |
| Repo / folder / GitHub name | 後續從 `mother-bear-court` 遷移到 Emorapy 命名 | 2026-06-20 使用者確認；P4 專項治理 |
| Domains / deploy service names | 新增 Emorapy domain/service alias，再逐步切正式入口 | 2026-06-20 使用者確認；P4 專項治理 |
| 歷史別名 | `CJ`、`Mother Bear Court`、`mother-bear-court` 作 historical aliases | 2026-06-20 使用者確認；P5 docs allowlist / 術語治理 |

## P0 推薦決策草案（2026-06-20 使用者已確認）

本草案只裁定 App Store 建檔與 native release identity 的前置方向；2026-06-20 使用者已確認採用本節推薦值。確認後仍不得建立 `CJ` App Store record、不得註冊 `com.cj.motherbearcourt` 作正式新 app identity，也不得把 legacy EAS project / historical artifact 改寫成新 evidence。Apple Developer / App Store Connect / Sentry 外部建檔仍需逐項完成與取證；EAS project binding 已於 2026-06-20 以新 project 方式閉環。

### 外部規則依據

1. App Store Connect 新建 app record 需要 app name、primary language、bundle ID 與 SKU；因此建檔前必須先確定外部識別符，不能用 `CJ` 暫代後再補救。參考 Apple Developer Help：[Add a new app](https://developer.apple.com/help/app-store-connect/create-an-app-record/add-a-new-app/)。
2. Apple 明確 App ID 的 explicit bundle ID 需要與 app target 的 bundle ID 匹配；因此 Apple Developer 端註冊值必須與 `mobile/app.json` / native target 最終值一致。參考 Apple Developer Help：[Register an App ID](https://developer.apple.com/help/account/identifiers/register-an-app-id/)。
3. Apple 移除 app 後 SKU 不能在同一組織重用；若已上傳 build，bundle ID 也不能重用。因此錯建 `CJ` / `com.cj.motherbearcourt` 會造成長期成本。參考 Apple Developer Help：[Remove an app](https://developer.apple.com/help/app-store-connect/create-an-app-record/remove-an-app/)。
4. Expo EAS 的 `extra.eas.projectId` 是 EAS server 上識別 project 的值；本 repo 曾有真實但 legacy 的 `@alexdev518/cj-mobile` projectId，現已改為 `@alexdev518/emorapy-mobile`。參考 Expo Docs：[Configure a development build in cloud](https://docs.expo.dev/tutorial/eas/configure-development-build/)。
5. Expo `android.package` 是 Android standalone app 的 package name，需在 Play Store 唯一；因此如果 Android 未公開發佈，應趁現在與 iOS 一起改為 Emorapy identity。參考 Expo app config：[android.package](https://docs.expo.dev/versions/latest/config/app/#package)。

### 當前外部帳號與 project 證據

| 項目 | 當前證據 | P0 裁決 |
| --- | --- | --- |
| Expo / EAS login | `npx eas-cli@20.3.0 whoami --non-interactive` 回覆 `alexdev518`，email `uk.alex.wong.uk@gmail.com`；accounts 包含 `alexdev518` 與 `alexdev518s-team` owner 權限 | 可由 agent 查詢 EAS metadata；不得輸出 token |
| EAS project | 2026-06-20 先確認舊 projectId 對應 remote slug `cj-mobile`，本地 `mobile/app.json` slug 是 `emorapy-mobile`，因此不可作 Emorapy release binding；後續已移除舊 projectId，使用 `EXPO_TOKEN` 建立並連結 `@alexdev518/emorapy-mobile`，`ID=73ba39d3-6218-4748-ae36-8d3a93ba34ac` | EAS project binding 已閉環；`APP_EAS_PROJECT_FULL_NAME=@alexdev518/emorapy-mobile` 為 non-secret release env；EAS Android production artifact 已有 pass evidence，仍需重新產出 EAS iOS / TestFlight structured evidence |
| EAS CLI rename 能力 | `npx eas-cli@20.3.0 project --help` 只列 `project:info`、`project:init`、`project:new`、`project:onboarding`；`eas init --help` 支援 `--id` 連結既有 project，未暴露 rename / edit 命令。2026-06-20 曾實測在舊 projectId 存在時執行 `project:init --non-interactive --force` 會把本地 `expo.slug` 從 `emorapy-mobile` 改回 `cj-mobile`，需立即還原；後續移除舊 projectId 後再執行 `project:init --non-interactive --force`，此時語義變成建立新的 `@alexdev518/emorapy-mobile` project 並回寫新 projectId | 不得在舊 projectId 存在時用 `project:init --force` 當 rename workaround；安全路徑是先移除 legacy binding，再新建 / link 真正的 Emorapy project |
| App Store Connect | 使用者已能進入 Apps 頁；此前新建 App modal 曾填入 `CJ` / 舊 SKU 類值但未完成建立 | 必須取消舊 modal，待本 P0 決策確認後重新建 |
| Apple Developer Bundle ID | 尚未確認已註冊 `com.emorapy.app` | 需要使用者在 Apple Developer / Certificates, Identifiers & Profiles 配合註冊或允許 agent 代操作 |

### 推薦決策

| 決策項 | 推薦值 | 理由 | 後續落點 |
| --- | --- | --- | --- |
| App Store app name | `Emorapy` | 對外品牌唯一化；避免首個 app record 占用舊名 | App Store Connect app record、App Store metadata、future screenshots |
| Primary language | `English (U.S.)` 或 App Store Connect 可選的英文主語系 | 產品名與國際化方向是英文品牌；繁中可作 localization 補充 | App Store Connect app record；若使用者希望首發繁中市場，可在建檔前改為繁中主語系 |
| SKU | `emorapy-ios-app` | SKU 是內部識別、不面向用戶；不使用 Bundle ID，可避免把平台 package 與商務 SKU 混淆 | App Store Connect app record；一旦使用後不得重用 |
| iOS Bundle ID | `com.emorapy.app` | 短、穩定、品牌對齊、reverse-DNS；比 `com.emorapy.mobile` 更適合作長期主 app identity | Apple Developer App ID、`mobile/app.json`、EAS iOS build / submit |
| Android package | `com.emorapy.app` | 與 iOS 主 identity 對齊；Android 尚未作公開發佈時改名成本最低 | `mobile/app.json`、Android native build、Maestro appId、readiness gates |
| Deep link scheme | `emorapy` | 現在仍是 `cj`；若未有公開 deep links，應在 release 前改成品牌 scheme | `mobile/app.json`、deep link tests、notification landing evidence |
| Expo slug | `emorapy-mobile` | 比 `emorapy` 更明確是 native app project，減少未來 web / hosting alias 混淆 | `mobile/app.json`、EAS dashboard / project naming decision、release scripts |
| EAS project | 新建並連結 `@alexdev518/emorapy-mobile`，`projectId=73ba39d3-6218-4748-ae36-8d3a93ba34ac`；legacy `projectId=1a4308a8-70ff-4bbc-a5e0-3f2a060888bc` 只作歷史證據參照 | EAS CLI 20.3.0 未暴露安全 rename / edit 命令；舊 projectId 已被證明會把本地 slug 拉回 `cj-mobile`，不應作正式 Emorapy release binding | `mobile/app.json`、`APP_EAS_PROJECT_FULL_NAME=@alexdev518/emorapy-mobile`、external status/handoff 已更新；EAS Android production artifact evidence 已通過，仍需重新跑 EAS iOS / TestFlight evidence |
| Sentry project / release name | `emorapy-mobile` | native crash / release evidence 不應再落到 `cj-mobile` | `APP_SENTRY_PROJECT`、Sentry dashboard、native crash evidence runner |
| `@emorapy/*` package scope | P0 曾短期保留 `@cj/*`，P3 已改為 `@emorapy/contracts`、`@emorapy/api-client` | package scope 牽涉 workspace、imports、lockfile、docs truth gates；因此未與 P0 Bundle ID 同輪強改，而是 P3 專項完成 | P3 已完成；`@cj/*` 只允許留在歷史 / 遷移說明 |
| Env var prefix | 新增 `EMORAPY_*`，舊 `CJ_*` dual-read；P0 不移除舊名 | release gate / local dev 仍依賴 `CJ_COMMIT_SHA` 等值；直接改名會破壞 local / CI / production gate | P4 專項治理；先加 alias，再逐步 deprecate |
| GitHub repo / Vercel / Railway / domain | P0 不改；先新增 Emorapy target 策略與 future alias | repo rename / domain migration 會影響 remotes、webhooks、CI、deploy；不應阻擋 App Store native identity | P4 專項治理；先 plan，再做 redirect / alias |

### App Store 建檔前置執行包（P0 confirmed）

本節是 App Store Connect / Apple Developer 頁面操作時的唯一 P0 欄位包；若頁面顯示與本節衝突，先停下回報，不得用 `CJ`、`Mother Bear Court`、`cj-mobile` 或 `com.cj.motherbearcourt` 暫代。此欄位包只處理建檔前置，不代表 TestFlight / App Store submission 已可送審。

| Surface | 欄位 | 使用值 / 選項 | 狀態與注意事項 |
| --- | --- | --- | --- |
| Apple Developer / Identifiers | Identifier 類型 | `App IDs` -> `App` | 先建立 explicit App ID，才能在 App Store Connect new app dialog 選到正確 Bundle ID |
| Apple Developer / Identifiers | Description | `Emorapy iOS App` | Description 只是 Apple Developer 內部標籤；不得填 `CJ` |
| Apple Developer / Identifiers | Bundle ID type | `Explicit` | 不使用 wildcard；push / associated services 需要 explicit App ID |
| Apple Developer / Identifiers | Bundle ID | `com.emorapy.app` | 必須與 `mobile/app.json` 的 `expo.ios.bundleIdentifier` 完全一致 |
| Apple Developer / Capabilities | Push Notifications | Enable | App 已使用 `expo-notifications`，M6 provider delivery evidence 需要 push capability；APNs / Expo provider pass evidence 仍另行取得 |
| Apple Developer / Capabilities | Associated Domains / Sign in with Apple | 暫不作 P0 必開 | 目前 App 使用 custom scheme `emorapy`，未以 universal links / Sign in with Apple 作 release blocker；若後續產品決策需要，再另開治理任務 |
| App Store Connect / New App | Platforms | `iOS` | 目前先建 iOS record；Android / Play Store 不在本 P0 App Store Connect scope |
| App Store Connect / New App | Name | `Emorapy` | 首個正式 app record 必須使用正式產品名 |
| App Store Connect / New App | Primary Language | `English (U.S.)` | P0 採英文主語系；繁中可作 localization 補充，若要改主語系需先回寫本文件 |
| App Store Connect / New App | Bundle ID | `com.emorapy.app` | 只選 Apple Developer 已註冊的 explicit App ID；不得選 / 建 `com.cj.motherbearcourt` |
| App Store Connect / New App | SKU | `emorapy-ios-app` | 內部不可重用識別符；不得使用 `CJ`、Bundle ID 或舊 package 作 SKU |
| App Store Connect / New App | User Access | `Full Access` | 當前個人帳號 / 初始 app record 可先用 Full Access；若後續加團隊成員再收窄 |
| Sentry | Project name | `emorapy-mobile` | 若 Sentry 尚未有此 project，外部 owner 需建立；若已有，後續把 `APP_SENTRY_PROJECT=emorapy-mobile` 填入 ignored env / CI secrets |
| Expo / EAS | Project full name | `@alexdev518/emorapy-mobile` | 已建立並綁定；不得回退 legacy `@alexdev518/cj-mobile` |
| GitHub / Vercel / Railway / domain | P0 行動 | 不改名，只記 future migration | repo / deployment / domain rename 屬 P4，不能阻塞 App Store native identity，也不能在本輪破壞 release gate |
| Package scope | P0 行動 | 當時保留 `@cj/*`；已由 P3 遷移到 `@emorapy/*` | P0 不把 package scope 放進 App Store / external metadata；P3 已完成 current source/config/docs 遷移 |
| Env prefix | P0 行動 | 新增用 `EMORAPY_*`，舊 `CJ_*` dual-read | 不移除舊 env；避免 local / CI / release gate 斷裂 |

#### 建檔順序與停止條件

1. 先在 Apple Developer 註冊 `com.emorapy.app` explicit App ID，啟用 Push Notifications。
2. 再到 App Store Connect / Apps / New App 建立 record，填入 `Emorapy`、`English (U.S.)`、`com.emorapy.app`、`emorapy-ios-app`。
3. 建立後才補 `APP_STORE_CONNECT_APP_ID` / `ASC_APP_ID` 到 ignored `mobile/release.env.local` 或 CI secret；不得把 numeric App Store Connect app id 寫入公開文件。
4. 若頁面提示 name / bundle id / SKU 已被使用、agreement 未簽、developer name / trader status / tax banking agreement 未完成，先停下並記錄實際頁面訊息；不得改用舊名繞過。
5. 若 App Store Connect 已存在任何 `CJ` 或 `com.cj.motherbearcourt` record，不能把它當正式 App；先確認是否可刪除 / abandon，並保留 SKU / bundle reuse 風險說明。

### 不建議方案

| 方案 | 不採用原因 |
| --- | --- |
| 繼續用 `CJ` 建 App Store app record | 會把舊名固定到首個外部 app record，且 SKU 不可重用；與正式品牌 Emorapy 衝突 |
| 使用 `com.cj.motherbearcourt` 作正式 Bundle ID / Android package | 舊名會進入不可逆或高成本 native identity；和 App Store / Play Store / crash / push / deep link 長期識別符不一致 |
| P0 同輪把 `@cj/*` package scope 全改 | 風險大且不阻擋 App Store 建檔；會牽涉 shared packages、imports、lockfile、docs truth scripts 與大量測試 |
| P0 同輪改 GitHub repo / Vercel / Railway domain | 會增加 release gate 與 production deploy 風險；應在 native identity 確認後另開 ops migration |
| 直接刪除或重建 EAS project | 原則上不應直接刪除歷史 project；本次採「新建 Emorapy project + 保留 legacy project/evidence lineage」而不是刪除舊 project，因 EAS CLI 未提供安全 rename 且舊 projectId 會把本地 slug 拉回 `cj-mobile` |

### 使用者需要確認或配合

1. 確認 App Store app name 使用 `Emorapy`。
2. 確認 SKU 使用 `emorapy-ios-app`。
3. 確認 iOS Bundle ID / Android package 使用 `com.emorapy.app`。
4. 確認 Expo slug 使用 `emorapy-mobile`。
5. 在 Apple Developer / Certificates, Identifiers & Profiles 註冊 explicit App ID：`com.emorapy.app`；初始 capabilities 啟用 Push Notifications，其他如 Sign in with Apple / Associated Domains 依後續功能再開。
6. 在 App Store Connect 新建 app record 時選用 `Emorapy`、`English (U.S.)`、`com.emorapy.app`、`emorapy-ios-app`；不要提交任何 `CJ` record。
7. EAS project binding 已由 agent 代查並完成新建 / relink；EAS Android production artifact evidence 已產出，後續仍需等待 EAS iOS / TestFlight release evidence 重新產出。
8. 若已建立 Sentry project，確認是否已有 `emorapy-mobile`；若沒有，後續由外部 owner 建立或提供 project name。
9. Expo / Apple / ASC / Sentry / push provider 類 credentials 必須以 non-placeholder 真值進入 ignored env file 或 CI secrets；`REPLACE_WITH_*` 只代表待補模板值，不得解除 `release:completion:audit` blocker。2026-06-20 已把 Expo / Apple / ASC placeholder guard 納入 `release:completion:audit:contract`。

### P0 完成判定

P0 不是 App 完成，也不是 release sign-off 完成。P0 只在以下條件同時成立時可標為完成：

1. 使用者已確認上述推薦決策，或在本待辦中記錄替代決策與理由。
2. Apple Developer explicit App ID `com.emorapy.app` 已建立，或明確記錄仍待使用者外部操作。
3. App Store Connect app record 使用 Emorapy 正式資料建立，或明確記錄仍待使用者外部操作。
4. 下一輪 P1 的代碼改名 scope 已明確：`mobile/app.json`、release readiness scripts、Maestro appId、telemetry / native crash release naming、mobile catalog、docs / release sign-off references。
5. 不存在已完成建立的 `CJ` App Store record 被誤當正式 app。
6. 若 Apple / App Store / Sentry 外部操作尚未完成，不得把 P0 寫成 release identity 全部閉環，只能寫成「決策已確認，Apple/App Store/Sentry 外部建檔待完成」；EAS project binding、EAS Android artifact evidence、iOS Release simulator evidence、Android emulator / app / full-flow evidence 與 native ImagePicker evidence 已完成，但 EAS iOS / TestFlight、physical device、provider delivery 與 native crash runtime evidence 仍屬 release sign-off blocker。

## 分階段待處理任務

### P0：App Store 建檔前置 blocker

1. 取消 / 不提交任何 `CJ` App Store app record。
2. 確認 App Store app name、SKU、iOS Bundle ID、Android package、Expo slug、EAS project 策略。
3. 在 Apple Developer 註冊正確 Bundle ID；不得註冊 `com.cj.motherbearcourt` 作正式新 app identity。
4. 更新 `App外部ReleaseSignoff待辦-2026-05-16.md` 中 EAS / Apple / ASC 輸入說明，避免外部 owner 繼續按 `cj-mobile` 判讀。
5. 更新 App release readiness 腳本的 expected values，讓舊 Bundle ID / slug 無法通過 release gate。

### P1：Mobile native / release identity 收斂

1. 已更新 `mobile/app.json` 的 `expo.name=Emorapy`、`expo.slug=emorapy-mobile`、`scheme=emorapy`、`ios.bundleIdentifier=com.emorapy.app`、`android.package=com.emorapy.app`、權限文案與 app display metadata。
2. 已更新 `mobile/maestro/*.yaml` 與 `mobile/maestro-side-effects/*.yaml` 的 appId 為 `com.emorapy.app`。
3. 已更新 `mobile/scripts/check-release-readiness.mjs`、`check-android-readiness.mjs`、release evidence sanitization、release completion audit、native crash / telemetry release naming；舊 `com.cj.motherbearcourt` evidence 會被 completion audit 視為 stale candidate，不會被改寫或誤當新 release pass evidence。
4. 已更新 `mobile/src/platform/telemetry/*` 的 OpenTelemetry `service.name=emorapy-mobile`、scope `emorapy.mobile.app`、Sentry release `emorapy-mobile@<version>+<build>` 與 Sentry tags。
5. 已更新 App catalog 內 public home eyebrow 為 `Emorapy`。
6. 未手工修改 generated native files；2026-06-20 已用 `SENTRY_DISABLE_AUTO_UPLOAD=true npx expo prebuild --platform ios --clean --no-install` 刷新 gitignored `mobile/ios`，生成 `Emorapy.xcodeproj`、`PRODUCT_BUNDLE_IDENTIFIER=com.emorapy.app`、`sentry-xcode.sh` 與 `Upload Debug Symbols to Sentry` phase；證據見 `90-證據與盤點/環境與發版驗證/App-iOS-Sentry-Prebuild-2026-06-20T08-43-59Z.json`，`sentry_ios_native_prebuild_configuration` 已從 release completion blocker 移除。
7. 已重新跑 App focused gates 與 release identity audit；`release:completion:audit -- --json` 現在 `failures=0`，但因外部憑證、EAS/TestFlight/真機/provider/native crash evidence 尚未完成，仍不是 release sign-off complete。

#### 2026-06-20 iOS Release simulator Emorapy identity evidence refresh

2026-06-20 已完成命名遷移後的 iOS Release simulator evidence refresh：先安裝 iOS 26.5 simulator runtime，啟動 iOS 26.5 `iPhone 17` simulator，再用 `SENTRY_DISABLE_AUTO_UPLOAD=true DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run ios:release-simulator:smoke -- --device="iPhone 17" --skip-prebuild` 驗證 Expo dependency alignment、Release build、Sentry Xcode bundle / dSYM phases、`com.emorapy.app` install / launch。

本次 pass evidence 為 `90-證據與盤點/環境與發版驗證/App-iOS-Release-Simulator-2026-06-20T14-04-07-291Z.json`，`release:completion:audit -- --release-env-file=release.env.local --json` 已把 `ios_release_simulator_evidence` 判定為 passed。該證據只解除本地 iOS Release simulator refresh blocker，不替代 EAS iOS production artifact、TestFlight、physical device、push provider delivery 或 native crash runtime evidence。

#### 2026-06-20 P1 驗證結果

| 命令 | 結果 | 備註 |
| --- | --- | --- |
| `npm --prefix mobile run platform:check` | 通過 | native side effects 仍只經 platform adapters |
| `npm --prefix mobile run release:check` | 通過 | release config baseline 已接受 Emorapy identity；仍 warning `EXPO_TOKEN`、Apple submission credentials 與 Xcode selection |
| `npm --prefix mobile run copy:check` | 通過 | App 可見文案 contract 通過 |
| `npm --prefix mobile run typecheck` | 通過 | TypeScript 無新增錯誤 |
| `npm --prefix mobile test -- --runInBand src/platform/telemetry/client.test.js src/platform/telemetry/nativeCrash.test.js` | 通過 | 2 suites / 9 tests passed |
| `npm --prefix mobile run release:completion:audit -- --json` | 未完成但無 hard failure | `failures=0`；舊 iOS / Android / telemetry / upload evidence 因 `com.cj.motherbearcourt` 被標記 stale ignored，需用 Emorapy identity 重新產出 |
| `npm --prefix mobile run goal:completion:audit -- --json` | 未完成 | `release_signoff` 仍 missing；App 目標不得標成完成 |
| `npm --prefix mobile run release:evidence-sanitization:check` | 通過 | 受控 secret redaction 檢查通過 |
| `npm --prefix mobile run release:external-evidence:status:contract` | 通過 | status/handoff schema 仍 identity-bound |
| `npm --prefix mobile run routes:check` | 通過（2026-06-20 追加修復） | gate 已改為 i18n-aware：route 使用 `appTabs.*` key，catalog 固定 `案件`、`對話`、`個人`、`提醒`、`修復` 與英文對應文案 |
| `npm --prefix mobile run features:check` | 通過（2026-06-20 追加修復） | gate 已改為 i18n-aware：M2/M3 route 使用狀態文案 key，catalog 固定 `同步狀態` / `協調草稿` 與英文對應文案 |

#### 2026-06-20 EAS project binding 追加治理

`npx eas-cli@20.3.0 project:info --non-interactive` 先暴露出比「命名待對齊」更直接的 release 前置問題：legacy `extra.eas.projectId=1a4308a8-70ff-4bbc-a5e0-3f2a060888bc` 對應的 remote slug 仍是 `cj-mobile`，而本地正式 slug 已是 `emorapy-mobile`，EAS CLI 因此拒絕 project info。已將 `release:completion:audit`、`release:external-evidence:input-status`、`release:external-evidence:status`、`release:external-evidence:signoff` 與 handoff catalog 調整為 `eas_project_id = UUID + Emorapy full name binding`，新增非 secret input `APP_EAS_PROJECT_FULL_NAME=@alexdev518/emorapy-mobile`，避免 release tooling 誤把 legacy `cj-mobile` 當成可用 Emorapy project。

2026-06-20 已分兩次實測 `cd mobile && npx eas-cli@20.3.0 project:init --non-interactive --force`：

1. 舊 projectId 仍存在時執行，命令會保留舊 projectId 並讓 `project:info` 暫時回到 `fullName @alexdev518/cj-mobile`，但代價是把本地 `mobile/app.json` 的 `expo.slug` 改回 `cj-mobile`，與 P0/P1 已確認的 Emorapy identity 衝突；該本地變更已還原，且不得把此流程當作 rename workaround。
2. 移除 legacy projectId 後執行，命令會建立並回寫新的 `@alexdev518/emorapy-mobile` project；當前 `mobile/app.json` 為 `extra.eas.projectId=73ba39d3-6218-4748-ae36-8d3a93ba34ac`，`npx eas-cli@20.3.0 project:info --json` 回 `fullName @alexdev518/emorapy-mobile` / 同一 ID。`release:completion:audit` 帶本機 release env 已顯示 `eas_project_id` 與 `expo_token` passed。

當前 EAS project binding 與 Android artifact evidence 已閉環：Android production build `56720475-d1fe-447e-adf0-455719b8683b` 已完成，EAS 回 `appVersion=1.3.1`、`appBuildVersion=2`、`gitCommitHash=b5f3146746f33f3e20442b26a6041f85dfbd14e7`，artifact URL 存在且 `npm --prefix mobile run eas-android-release:smoke -- --run` 產出 pass evidence：`90-證據與盤點/環境與發版驗證/App-EAS-Android-Release-2026-06-20T09-25-12-330Z.json`。iOS EAS / TestFlight 仍待 Apple / ASC credentials 與 App Store 建檔。

### P2：產品可見文案與系統對外身份

2026-06-20 已完成本輪可安全收斂的對外可見文案；P4 相關 deployment / env / shell script 命名仍保留給 ops 兼容策略，不在本輪混改。

1. 已將 Backend email subject / from name 從 `CJ 平台` / `CJ Platform` 收斂為 `Emorapy`。
2. 已將 Backend notification fallback push title 從 `CJ 提醒` / `CJ Reminder` 收斂為 `Emorapy 提醒` / `Emorapy Reminder`。
3. 已將 Web / Admin title、i18n 與 backend package description 中的 visible `CJ Platform` / `CJ 平台` 改為 Emorapy。
4. 已將 ops Slack alert 對外標題改為 `Emorapy Ops Alert`。
5. 已將 AI positioning gate 中涉及產品名的檢查改用 Emorapy，並保留「不得假扮人類治療師」等核心語義。
6. 已更新 `術語表.md`：`Emorapy` 是正式對外產品名；`CJ`、`Mother Bear Court`、`mother-bear-court` 是歷史別名 / legacy internal identifier，不得作為新對外 copy 或 external metadata 使用。
7. 已反向掃描 `CJ Platform`、`CJ 平台`、`CJ Reminder`、`CJ 提醒`、`Admin Login - CJ`、`Ops Dashboard - CJ`、`CJ Platform Admin`、`CJ Platform Ops Alert`：Backend / Web / Admin / Mobile user-facing source 已無舊對外品牌文案；2026-06-20 P4 追加收斂 `scripts/*.sh` operator-visible banner / comment。

#### 2026-06-20 P2 驗證結果

| 命令 | 結果 | 備註 |
| --- | --- |
| `rg -n "CJ Platform\|CJ 平台\|CJ Reminder\|CJ 提醒\|Admin Login - CJ\|Ops Dashboard - CJ\|CJ Platform Admin\|CJ Platform Ops Alert" backend frontend frontend-admin docs/核心開發文件/術語表.md docs/核心開發文件/04-共用機制 --glob '!backend/tmp/**'` | 通過（P2 範圍） | Backend / Web / Admin / Mobile user-facing source 無舊品牌文案；shell operator-visible copy 由 P4 gate 承接 |
| `npm --prefix backend test -- --runInBand tests/unit/services/email.service.test.ts` | 通過 | 17 tests passed |
| `npm --prefix backend test -- --runInBand tests/unit/services/notification.service.test.ts` | 通過 | 47 tests passed |
| `npm --prefix backend run build` | 通過 | contracts build + backend `tsc` 通過 |
| `npx tsc -b` in `frontend/` and `frontend-admin/` | 通過 | 替代 `npm run build` 中被環境 `Killed: 9` 的 `vite build` 階段，用於證明本輪 i18n / title 改動無 TS 錯誤 |
| `npm --prefix frontend run lint`、`npm --prefix frontend-admin run lint` | 通過 | Biome lint 無問題 |
| `npm run ai:positioning:check` | 通過 | 仍阻止把 Emorapy / AI 描述為治療師 |
| `npm run docs:check`、`npm run docs:audit:dry-run:current` | 通過 | docs structure / truth / current metadata dry-run 通過 |
| `npm --prefix frontend run build`、`npm --prefix frontend-admin run build` | 未完成 | `tsc -b` 已通過，但 `vite build` 階段被本機環境 `Killed: 9` 終止；需在 CI 或更高資源環境再刷新 bundle build 證據 |

### P3：共享 package / source namespace 收斂

2026-06-20 裁決：正式 internal workspace package scope 改為 `@emorapy/contracts` 與 `@emorapy/api-client`；`@cj/*` 不再作 current source / config / active docs 的正式 package identity。`@cj/*` 只允許留在已處理歷史任務、歷史 evidence / archive、或本待辦的遷移說明中，且不得再被 release readiness、docs truth checker 或 App shared package resolution 當成現行正確值。

#### P3 工程級方案

| 面向 | 目標 | 實作邊界 | 驗收 |
| --- | --- | --- | --- |
| Package manifests | `packages/contracts/package.json`、`packages/api-client/package.json` 改名；root / frontend / admin / mobile dependency 與 workspace scripts 同步 | 不改 package 物理資料夾；不改 npm registry 發佈策略；不改 package version | `package-lock.json`、`mobile/package-lock.json` 產生 `node_modules/@emorapy/*` workspace / file links |
| Source imports | Backend / Web / Admin / Mobile / shared package imports 改用 `@emorapy/*` | 不改 API surface、DTO 名稱、domain client function name 或 runtime behavior | `npm run contracts:build`、`npm run api-client:build`、`npm --prefix mobile run typecheck`、frontend/admin TS check 通過 |
| Tool aliases | tsconfig、Vite/Vitest、Metro、Jest、mobile release readiness gate 改用 `@emorapy/*` | 保留現有 alias 指向 `packages/*`；不新增 dual alias，避免新舊 scope 同時成為可用入口 | `npm --prefix mobile run release:check` 不再要求 `@cj/*` |
| Docs truth | `scripts/check-docs-truth.mjs` 與 current 架構 / App / parity / testing docs 改成 `@emorapy/*` token | 已處理歷史文件不重寫；current docs 不再把 `@cj/*` 寫成現行依賴 | `npm run docs:check`、`npm run docs:audit:dry-run:current` 通過 |
| Naming gate | `npm run naming:check` 加入 current package scope guard | Guard 只掃 current source/config/active docs；不掃 historical evidence / archive | 若 current source/config 回流 `@cj/*`，gate 失敗 |

#### P3 風險與注意事項

1. 這是 TypeScript / npm workspace identity 改名，不是資料模型或 API contract 改名；不得同步改 DB schema、HTTP route、domain DTO 名稱或 PRD ID。
2. `packages/*/dist` 與 `packages/*/types` 是 build artifact，應由 `npm run contracts:build` / `npm run api-client:build` 重新產生，不手工逐行維護。
3. Root workspace 與 mobile repo-local install 各有 lockfile；必須同時刷新 root `package-lock.json` 與 `mobile/package-lock.json`。
4. 不保留 `@cj/*` alias 作兼容入口，因為本 repo 內部 consumers 可一次性改完；保留雙 alias 會削弱命名治理 gate。
5. 歷史文件中出現 `@cj/*` 只代表當時狀態，不得作 current implementation 依據。

#### 2026-06-20 P3 實作與驗證結果

已將 root workspace、frontend、frontend-admin、backend、mobile、shared packages、Vite / Vitest / Metro / Jest / TypeScript aliases、mobile release readiness gate、root / mobile lockfile、current 架構 / App / parity / testing docs 與 docs truth checker 從 `@cj/contracts`、`@cj/api-client` 遷移到 `@emorapy/contracts`、`@emorapy/api-client`。`npm run naming:check` 已加入 current package scope guard：current source/config/active docs 不得回流 `@cj/*`；只放過本待辦的遷移說明、治理腳本 forbidden pattern、已處理歷史任務、歷史 evidence / archive 與文檔收斂歷史。

| 命令 | 結果 | 備註 |
| --- | --- | --- |
| `npm run contracts:build && npm run api-client:build` | 通過 | shared packages 以 `@emorapy/*` workspace name build |
| `npm --prefix backend run build` | 通過 | backend tsconfig alias 已改為 `@emorapy/*` declaration artifact |
| `npx tsc -b` in `frontend/` and `frontend-admin/` | 通過 | Web / Admin imports、Vite/Vitest aliases 與 manifests 對齊 |
| `npm --prefix mobile run typecheck` | 通過 | mobile shared build + TypeScript resolution 通過 |
| `npm --prefix mobile run release:check` | 通過 | release readiness 已要求 `@emorapy/*` file dependency / lockfile link；仍只提示外部憑證與 Xcode selection warning |
| `npm run docs:check`、`npm run docs:audit:dry-run:current` | 通過 | docs structure / truth / current metadata dry-run 通過 |
| `npm run test:m1/m2/m3/m4/m5/test:ai-stream-state --workspace @emorapy/api-client` | 通過 | shared domain client tests 全部通過 |
| `npm --prefix mobile test -- --runInBand src/platform/api/client.test.js src/platform/sse/client.test.js src/features/m1/api.test.js src/features/m2/api.test.js src/features/m3/api.test.js src/features/m4/api.test.js src/features/m5/api.test.js` | 通過 | App API adapter / SSE / M1-M5 mocks 已用 `@emorapy/*` |
| `npm --prefix frontend run lint`、`npm --prefix frontend-admin run lint` | 通過 | Biome lint 無新增問題 |
| `npm --prefix mobile run release:completion:audit -- --release-env-file=release.env.local --json` | 未完成但無 hard failure | `complete=false`、`failures=0`；blocker 仍是 Apple / ASC / EAS iOS / TestFlight / 真機 / push provider / native crash runtime |
| `npm --prefix mobile run goal:completion:audit -- --release-env-file=release.env.local --json` | 未完成 | 只剩 `release_signoff` missing/incomplete；不得宣稱 App complete |
| `git diff --check` | 通過 | diff hygiene 無 whitespace 問題 |

#### 2026-06-20 P3 npm manifest identity 收斂

已將 root `package.json` / `package-lock.json` 的 npm package identity 從 `cj-platform-root` 改為 `emorapy-root`，並將 `backend/package.json` / `backend/package-lock.json` 從 `cj-platform-backend` 改為 `emorapy-backend`。這只改 npm manifest / lockfile 顯示身份與工具輸出，不改 repo 物理資料夾、GitHub remote、backend source path、Railway service、Vercel domain 或 deploy hostname。

`npm run naming:check` 已新增 manifest name guard，防止 root / backend package name 回流舊名。`frontend`、`frontend-admin`、`mobile` 的 generic package name 暫不改，因為它們不是 `CJ` / `Mother Bear Court` 舊品牌身份。

本輪完成判定：

1. root npm identity 為 `emorapy-root`。
2. backend npm identity 為 `emorapy-backend`。
3. root / backend lockfile 與 manifest 一致。
4. `npm --prefix backend run build`、`npm run naming:check`、`npm run docs:check` 通過。
5. release completion audit 仍保持 `complete=false`、`failures=0`，不得因 manifest 改名宣稱 release complete。

### P4：Ops / deployment / repo / domain 收斂

1. GitHub repo `mother-bear-court` 是否改名，需與 remotes、GitHub Actions、Vercel / Railway integration、badges、webhooks 一起規劃。
2. Vercel main domain `mother-bear-court.vercel.app`、Railway service / domain `mother-bear-court-production.up.railway.app` 應新增 Emorapy 正式入口，再安排 redirect / compatibility。
3. `CJ_COMMIT_SHA`、`CJ_DEV_REDIS_DIR`、`CJ_RELEASE_GATE` 類 env var 應採 `EMORAPY_*` 新名 dual-read，確認 CI / local / release gate 都通過後再 deprecate 舊名。2026-06-20 已完成第一批兼容：backend 版本 manifest 在 Railway runtime 首選 `RAILWAY_GIT_COMMIT_SHA`，再 fallback `EMORAPY_COMMIT_SHA` 與 `CJ_COMMIT_SHA`，避免 Railway auto deploy 被 stale manual env var 誤標成舊 commit；frontend / admin 版本 manifest 優先讀 `EMORAPY_COMMIT_SHA` 再 fallback `CJ_COMMIT_SHA`；`start-dev.sh` 同時注入新舊 commit env；`ops:release:gate` 同時設定 `EMORAPY_RELEASE_GATE=1` 與 `CJ_RELEASE_GATE=1`；AI pricing checker 同時識別新舊 release gate；production deploy workflow 會同時寫入 Railway `EMORAPY_COMMIT_SHA` 與 `CJ_COMMIT_SHA`。`CJ_DEV_REDIS_DIR` 只作 legacy fallback，local Redis 預設資料夾已收斂為 `/tmp/emorapy-redis-dev`。**2026-06-21 更新**：兼容期結束，`CJ_COMMIT_SHA` / `CJ_RELEASE_GATE` / `CJ_DEV_REDIS_DIR` 的 dual-read fallback 已全部移除（見下方「2026-06-21 P4 env deprecation」），naming gate 已翻轉為禁止回流；`CJ_ANDROID_AVD_NAME` 因對應本機 AVD profile 另案處理。
4. Supabase dev project、test DB/user、local Redis dir、scripts 中的 `cj` 命名需按 dev / release parity 風險分批處理；不得因改名誤連 production DB 或破壞本機基線。
5. 更新 `AGENTS.md`、ops runbooks、platform map 與 release status commands，使 agent / human 不再以舊域名作唯一正式入口。

#### 2026-06-20 P4 ops shell 可見文案收斂

已完成低風險 operator-visible copy 收斂：`scripts/validate-env.sh`、`scripts/deploy.sh`、`scripts/health-check.sh`、`scripts/verify-deployment.sh`、`scripts/setup-production.sh` 的 banner / comment 已從 `CJ 平台` 改為 `Emorapy`；`scripts/setup-git.sh` 的預設初始 commit message 已改為 `Initial commit: Emorapy MVP`。此輪不改 GitHub repo name、Vercel / Railway hostname、Railway service、Supabase project、production domain、`CJ_COMMIT_SHA` fallback 或現存 legacy env var；`scripts/health-check.sh` 的 PM2 process regex 新增 `emorapy-backend`，但保留 `cj-platform|mother-bear-court` 兼容既有本機 / legacy PM2 進程名。

`npm run naming:check` 已追加掃描 `scripts/**/*.sh` 的 operator-visible copy；current ops helper 不得回流 `CJ Platform`、`CJ 平台`、`CJ Reminder`、`CJ 提醒`、`Admin Login - CJ`、`Ops Dashboard - CJ`、`CJ Platform Admin` 或 `CJ Platform Ops Alert`。此 gate 仍不把 `CJ_COMMIT_SHA`、`CJ_DEV_REDIS_DIR`、legacy hostnames、historical evidence 或 PRD ID 當成本輪 blocker，因為它們屬兼容 / 外部資源 / 歷史分類。

本輪完成判定：

1. shell helper 的產品可見 banner / comment 不再使用 `CJ 平台`。
2. PM2 建議新名為 `emorapy-backend`，但 health checker 仍可找到 legacy PM2 進程。
3. `npm run naming:check` 與 `npm run docs:check` 通過。
4. release completion audit 仍保持 `complete=false`、`failures=0`，不得因 copy 收斂宣稱 release complete。

#### 2026-06-20 P4 ops env alias 與 observability default

已完成第二批低風險兼容層：`ops:release:status`、`ops:release:gate`、`ops:release:gate:evidence`、`ops:release:smoke`、`wait-railway-deploy.sh` 仍保留 `MAIN_WEB_URL`、`ADMIN_WEB_URL`、`BACKEND_BASE_URL`、`FRONTEND_BASE_URL`、`ADMIN_BASE_URL`、`ORIGIN`、`ADMIN_ORIGIN`、`API_BASE_URL`、`RAILWAY_SERVICE_NAME` 舊名優先，但在舊名未設定時可讀取 `EMORAPY_MAIN_WEB_URL`、`EMORAPY_ADMIN_WEB_URL`、`EMORAPY_BACKEND_BASE_URL`、`EMORAPY_FRONTEND_BASE_URL`、`EMORAPY_ADMIN_BASE_URL`、`EMORAPY_ORIGIN`、`EMORAPY_ADMIN_ORIGIN`、`EMORAPY_API_BASE_URL`、`EMORAPY_RAILWAY_SERVICE_NAME`。GitHub production workflow 的 Railway deploy / previous deployment capture / wait step 亦可由 repository / environment variable `EMORAPY_RAILWAY_SERVICE_NAME` 覆蓋現存 legacy service default，三個步驟不再各自硬編 service name。

Production release gate workflow 的 live URL env 亦已改為 Emorapy alias 優先：`BACKEND_BASE_URL` 先讀 `EMORAPY_BACKEND_BASE_URL`，再 fallback `PRODUCTION_BACKEND_BASE_URL`；`MAIN_WEB_URL` 先讀 `EMORAPY_MAIN_WEB_URL`，再 fallback `PRODUCTION_MAIN_WEB_URL` / legacy Vercel hostname；`ADMIN_WEB_URL` 先讀 `EMORAPY_ADMIN_WEB_URL`，再 fallback `PRODUCTION_ADMIN_WEB_URL`。此輪只改 GitHub Actions env resolution，不新增或提交任何 GitHub secret / variable 值。

Backend logger default service 已從 `cj-platform-backend` 改為 `emorapy-backend`，並保留 `LOG_SERVICE_NAME` explicit override 與 `service_legacy=mother-bear-court-backend`。`npm run naming:check` 已防止 backend source 回流 `cj-platform-backend` default。此輪不改 `backend/package.json` package name、GitHub repo、Vercel / Railway hostname、Railway service 實名或 production domain；這些仍需外部平台 migration / redirect 設計後再做。

App 外部 signoff 的本地 report dir template 已收斂：`mobile/release.env.example` 現在預設 `APP_RELEASE_EXTERNAL_SIGNOFF_REPORT_DIR=/tmp/emorapy-app-signoff`，`release:external-evidence:env-template:check` 會拒絕回退到舊 `/tmp/cj-app-signoff`。此輪只治理未來本機 report 輸出目錄；`mobile/release.env.local`、GitHub runner temp path 與歷史 evidence artifact 不改寫。

#### 2026-06-20 P4 release commit env alias contract

已完成 current release commit / release-gate env alias contract 固定：`npm run naming:check` 現在會檢查 backend version manifest 的 commit 來源順序為 Railway runtime commit -> `EMORAPY_COMMIT_SHA` -> legacy `CJ_COMMIT_SHA`，Web / Admin version manifest 必須先讀 `EMORAPY_COMMIT_SHA` 再 fallback `CJ_COMMIT_SHA`，`start-dev.sh` 必須同時注入新舊 commit env，`ops:release:gate` 必須同時設定 `EMORAPY_RELEASE_GATE=1` 與 legacy `CJ_RELEASE_GATE=1`，AI pricing release gate checker 必須同時識別新舊 gate marker，production workflow 必須先寫入 Railway `EMORAPY_COMMIT_SHA` 再寫入 legacy `CJ_COMMIT_SHA`。

本輪只新增 static governance guard 與本待辦記錄，不改 production GitHub variables / secrets、不移除 `CJ_COMMIT_SHA` 或 `CJ_RELEASE_GATE` fallback、不改 Railway service / Vercel domain / repo name、不觸發 deploy，也不宣稱 env deprecation 完成。`CJ_*` 仍是兼容期 fallback；完成 env deprecation 前必須先證明 local / CI / release gate / production workflow 均可只依賴 `EMORAPY_*` 或平台注入值通過。

本輪完成判定：

1. 新 `EMORAPY_*` ops env alias 在不破壞舊 env 的情況下可用。
2. release gate evidence metadata 可記錄新 alias 對應的 effective URL，不記 secret 明文。
3. production workflow 的 Railway deploy、previous deployment capture 與 wait step 不再只能硬編 `mother-bear-court` service name。
4. production workflow 的 release gate URL 可由 `EMORAPY_BACKEND_BASE_URL`、`EMORAPY_MAIN_WEB_URL`、`EMORAPY_ADMIN_WEB_URL` 驅動，舊 `PRODUCTION_*` 仍可兼容。
5. observability default service 使用 `emorapy-backend`；legacy label 僅作過渡關聯。
6. release commit / release-gate env alias contract 已由 `npm run naming:check` 固定：new alias priority 與 legacy fallback 關係不得回退。
7. release completion audit 仍保持 `complete=false`、`failures=0`，不得因 alias 收斂宣稱 release complete。

#### 2026-06-21 P4 env deprecation（移除 CJ_COMMIT_SHA / CJ_RELEASE_GATE / CJ_DEV_REDIS_DIR）

兼容期目標已達成，現完成第二階段：移除 legacy commit / release-gate / local-redis env 的 dual-read fallback。先確認每個 reader 在 `CJ_*` 之後都還有平台注入或 git 兜底，移除不會產生 `unknown` 或破壞 release gate：

1. backend `backend/src/utils/version.ts`：commit 解析鏈移除 `CJ_COMMIT_SHA`；Railway runtime 一定有平台注入的 `RAILWAY_GIT_COMMIT_SHA`，其後仍有 `EMORAPY_COMMIT_SHA`、`VERCEL_GIT_COMMIT_SHA`、`GITHUB_SHA` 等。
2. `frontend/vite.config.ts`、`frontend-admin/vite.config.ts`：移除 `CJ_COMMIT_SHA`；其後有 Vercel 自動注入的 `VERCEL_GIT_COMMIT_SHA` 與 `git rev-parse HEAD` 兜底。
3. `scripts/start-dev.sh`：不再注入 `CJ_COMMIT_SHA`，Redis 目錄不再讀 `CJ_DEV_REDIS_DIR`（預設 `/tmp/emorapy-redis-dev`，可用 `EMORAPY_DEV_REDIS_DIR` 覆蓋）。
4. `scripts/ops-release-gate.sh`：不再 `export CJ_RELEASE_GATE=1`；`backend/scripts/check-ai-pricing-catalog.ts` 不再識別 `CJ_RELEASE_GATE`。
5. `.github/workflows/production-deploy-and-verify.yml`：Railway commit stamp 不再寫入 `CJ_COMMIT_SHA`，只寫 `EMORAPY_COMMIT_SHA`。

`npm run naming:check` 的 `checkCurrentOpsEnvAliasContract` 已從「要求 dual-read」翻轉為「要求 `EMORAPY_*` 存在且禁止 `CJ_COMMIT_SHA` / `CJ_RELEASE_GATE` / `CJ_DEV_REDIS_DIR` 回流」；新增 `requireAbsent` 輔助。`dev:redis-baseline:check` 同步斷言不再出現 `CJ_DEV_REDIS_DIR`。單元測試 `version.test.ts` 改為斷言「不再讀取已棄用的 `CJ_COMMIT_SHA`」、`check-ai-pricing-catalog.test.ts` 改為斷言 `CJ_RELEASE_GATE` 不再令其跳過 dotenv。current docs（環境基線、兩版本運作規範、運維 Runbook、共用機制總覽、本地開發基線、health-metrics 接口）同步移除 legacy CJ env 描述。

範圍邊界：本輪不動 `CJ_ANDROID_AVD_NAME`——它對應的是本機既有 Android emulator profile `CJ_Pixel_9_API_36`（real local AVD），不是 release identity；如要去名需先重建本機 AVD，屬獨立任務。歷史 evidence、`已處理/`、`90`/`99` 原文不改。

外部殘留清理（非 repo 內，需外部操作）：production Railway 上若仍存在歷史手動設定的 `CJ_COMMIT_SHA` variable，現已無人讀取（無害），可由運維在 Railway 後台 `railway variables --service <svc>` 確認後刪除；本輪不觸發 deploy、不刪 production variable。

本輪完成判定：

1. current source / config / CI / ops 腳本不再讀寫 `CJ_COMMIT_SHA` / `CJ_RELEASE_GATE` / `CJ_DEV_REDIS_DIR`。
2. 每個 commit reader 仍有平台注入或 git 兜底，移除後不會回 `unknown`。
3. `npm run naming:check`、`npm run dev:redis-baseline:check`、`npm run docs:check` 與相關 backend 單元測試通過。
4. release completion audit 仍保持 incomplete，不得因 env deprecation 宣稱 release complete。

#### 2026-06-20 P4 legacy production hostname compatibility gate

已完成 release helper legacy production hostname 的分類與防誤用 gate：`ops:release:status`、`ops:release:gate`、`ops:release:smoke` 與 `ops:release:gate:evidence` 仍保留 `https://mother-bear-court.vercel.app` 作為未配置 URL 時的 compatibility default，但 source 現在明確標註它只是 Emorapy domain migration 前的 legacy production default，且仍支援 `EMORAPY_MAIN_WEB_URL` override。GitHub production workflow 的 release gate URL priority 仍固定為 `EMORAPY_MAIN_WEB_URL` -> `PRODUCTION_MAIN_WEB_URL` -> legacy Vercel hostname。

本輪只改註釋、治理 gate 與 runbook / 待辦，不改 Vercel project、Railway service、GitHub repo、production domain、DNS、redirect、secret / variable，也不觸發 deploy。正式 Emorapy domain / redirect / platform alias 仍屬 P4 外部 platform migration；未取得新 domain release evidence 前，legacy hostname 不得被寫成正式新 external identity。

本輪完成判定：

1. release helper 中 legacy Vercel hostname 必須附帶 compatibility-default 註釋。
2. release helper 與 GitHub workflow 必須保留 Emorapy URL alias priority。
3. Runbook 必須明確說明 `mother-bear-court.vercel.app` 不是正式 Emorapy domain migration 完成證據。
4. `npm run naming:check`、`npm run docs:check` 與 App release / goal audit 仍需通過；release completion 仍保持 incomplete，不得因 hostname gate 宣稱 App release complete。

#### 2026-06-20 P4 GitHub repo alias for App release secret tools

已完成 App release GitHub secret 工具的 repo alias 兼容：`release:external-evidence:github-secrets:check` / `strict` 與 `release:external-evidence:github-secrets:sync` 解析 GitHub repo 時，優先使用 CLI `--repo=<owner/repo>`，其次讀取 `EMORAPY_GITHUB_REPO`，再 fallback `GITHUB_REPOSITORY`，最後才使用現存 legacy default `Alex0158/mother-bear-court`。這使未來 GitHub repo rename 或 fork / transfer 後，不必修改腳本 source 才能盤點或同步 App release secrets。

本輪不改 GitHub remote、repo name、GitHub Environment、GitHub secrets 或任何 secret value；`--apply` 仍是唯一會寫入 GitHub secrets 的模式。`release:external-evidence:github-secrets:sync:contract` 已用受控 `EMORAPY_GITHUB_REPO=Alex0158/emorapy-controlled` dry-run 驗證 repo alias 會進入 redacted JSON，且在 `PATH` 不含 `gh` 時仍不碰 GitHub。

本輪完成判定：

1. `github-secrets:check` / `strict` 與 `github-secrets:sync` 均支援 `--repo` > `EMORAPY_GITHUB_REPO` > `GITHUB_REPOSITORY` > legacy default。
2. dry-run 不需要 GitHub CLI / network，也不輸出 secret values。
3. `--apply` 仍需 GitHub CLI、GitHub auth 與明確 environment 存在，避免 repo alias 變成靜默寫入。
4. release completion contract 固定 repo alias 支援，防止未來回退到只硬編 `Alex0158/mother-bear-court`。

#### 2026-06-20 P4 App release secret sync fixture naming

已完成 App release GitHub secret sync contract 的受控 fixture 命名收斂：`mobile/scripts/check-release-github-secret-sync-contract.mjs` 的 fake signed iOS app path 從 `/tmp/controlled-signed-app-path-do-not-leak/CJ.app` 改為 `/tmp/controlled-signed-app-path-do-not-leak/Emorapy.app`，受控 release DB fixture path 從 `/cj` 改為 `/emorapy`，臨時目錄 prefix 從 `cj-github-secret-sync-contract-` 改為 `emorapy-github-secret-sync-contract-`。

本輪只改本地 contract fixture 與 release completion static guard，不改任何真實 signed app path、GitHub secret、release env、GitHub Environment 或外部 runner 設定；controlled values 仍必須保持 redacted，不可出現在 stdout / stderr / JSON。`release:completion:audit:contract` 已固定 `Emorapy.app` 與 `emorapy-github-secret-sync-contract-`，並拒絕 current contract script 回流 `CJ.app`。

#### 2026-06-20 P4 Android emulator AVD alias

已完成 Android emulator smoke runner 的 AVD 命名兼容：`mobile/scripts/run-android-emulator-smoke.mjs` 現在支援 `--avd`、`APP_ANDROID_AVD_NAME`、`EMORAPY_ANDROID_AVD_NAME` 與 legacy `CJ_ANDROID_AVD_NAME`；未指定時先自動尋找 `Emorapy_Pixel_9_API_36`，找不到才 fallback 到既有本機 / 歷史 evidence 使用的 `CJ_Pixel_9_API_36`。新 evidence 會記錄 `avd_name_source`、`default_avd_name` 與 `legacy_default_avd_name`，避免把 legacy AVD 名誤讀成 App release identity 回退。

本輪不重命名本機 Android AVD、不改歷史 evidence、不要求立即重跑 emulator，也不把 `CJ_Pixel_9_API_36` 從既有 2026-06-20 Android pass evidence 中改掉；該名稱只代表本機 emulator profile，不代表 App Store / Android package / Expo slug / release identity。正式 App identity 仍由 `com.emorapy.app`、`emorapy-mobile` 與 `@alexdev518/emorapy-mobile` 裁決。

#### 2026-06-20 P4 local Redis dir alias

已完成 local Redis 工作目錄預設命名收斂：`scripts/start-dev.sh` 現在仍按 `EMORAPY_DEV_REDIS_DIR` > legacy `CJ_DEV_REDIS_DIR` > default 的優先序解析 Redis 資料目錄，但 default 已從 `/tmp/cj-redis-dev` 改為 `/tmp/emorapy-redis-dev`。這只影響本機 `start-dev.sh` 在沒有任何 override 時啟動的 Redis appendonly 資料目錄，不改 `REDIS_URL=redis://127.0.0.1:6379`、不改 release Redis、Railway Redis、production env 或 DB。

`dev:redis-baseline:check` 已固定 Emorapy default 與 legacy env fallback，並拒絕 `start-dev.sh` 回退到 `/tmp/cj-redis-dev` default。本輪同步更新本地開發基線、兩版本運作規範與運維 Runbook；若開發者需要沿用舊本機 Redis appendonly data，可暫時明確設定 `CJ_DEV_REDIS_DIR=/tmp/cj-redis-dev` 或改用 `EMORAPY_DEV_REDIS_DIR=<existing-path>`，但新文件與預設值不再主動產生 `cj` 命名目錄。

#### 2026-06-20 P4 Web localStorage key migration

已完成 Web current browser storage key 命名收斂：quick session current key 從 `cj_session_id` 改為 `emorapy_session_id`，locale current key 從 `cj_locale` 改為 `emorapy_locale`。`cj_session_id` / `cj_locale` 與更早 `mbc_session_id` / `mbc_locale` 只作 legacy lazy migration source；讀到舊值時會寫入 Emorapy key 並清理舊 key，後續 set / remove 也會清理 legacy key，避免既有瀏覽器 session 或語言偏好在改名時丟失。

本輪只改 Web browser storage adapter、i18n storage、E2E / manual evidence seed、單測、Web / auth 核心文件與 naming governance gate；不改 API header、DB session model、App SecureStore key、auth token key、case-session map key、production domain 或 historical evidence。App native session 仍由 App SecureStore / platform adapter 承接，不直接使用 Web localStorage key。

本輪完成判定：

1. `frontend/src/utils/constants.ts` current session key 為 `emorapy_session_id`，legacy list 保留 `cj_session_id` / `mbc_session_id`。
2. `frontend/src/utils/i18n.ts` current locale key 為 `emorapy_locale`，legacy list 保留 `cj_locale` / `mbc_locale`。
3. Web tests / E2E / manual a11y evidence generator 不再把 `cj_session_id` 或 `cj_locale` 當 current seed。
4. `npm run naming:check` 固定 Web storage key migration contract，防止 current key 回退到 `cj_*`。
5. `npm --prefix frontend test -- --runInBand src/utils/constants.test.ts src/utils/storage.test.ts src/utils/i18n.test.ts`、`npm run docs:check` 與 App release / goal audit 仍需通過；release completion 仍保持 incomplete，不得因 Web storage key 收斂宣稱 App release complete。

#### 2026-06-20 P4 Admin Web locale storage key migration

已完成 Admin Web current locale browser storage key 命名收斂：`frontend-admin/src/utils/i18n.ts` 的 current key 從 `cj_locale` 改為 `emorapy_admin_locale`。`cj_locale` 與更早 `mbc_locale` 只作 legacy lazy migration source；初始化或 `setLocale` 時會寫入 Admin current key 並清理 legacy key，避免既有 Admin Web 語言偏好在改名時丟失。

本輪只改 Admin Web i18n storage adapter、Admin i18n 單測、Web 凍結基線與 naming governance gate；不改 Admin token key、Admin API `X-Locale` header、主站 Web `emorapy_locale`、App SecureStore、DB session model、production domain 或 historical evidence。`emorapy_admin_locale` 與主站 `emorapy_locale` 分開，是因 Admin Web 是獨立治理入口，不應讓管理後台語言偏好與普通用戶主站偏好互相污染。

本輪完成判定：

1. `frontend-admin/src/utils/i18n.ts` current key 為 `emorapy_admin_locale`，legacy list 保留 `cj_locale` / `mbc_locale`。
2. `frontend-admin/src/utils/i18n.test.ts` 覆蓋 `setLocale` 寫入新 key、清理 legacy key，以及初始化時從 `cj_locale` lazy migration。
3. `npm run naming:check` 固定 Admin Web locale storage key migration contract，防止 `frontend-admin` 回退到 `cj_locale` current key。
4. `npm --prefix frontend-admin run test -- src/utils/i18n.test.ts`、`npm --prefix frontend-admin run lint`、`npx tsc -b frontend-admin`、`npm run docs:check` 與 App release / goal audit 仍需通過；release completion 仍保持 incomplete，不得因 Admin Web browser key 收斂宣稱 App release complete。

#### 2026-06-20 P4 App SecureStore key migration

已完成 App SecureStore current key 命名收斂：`mobile/src/platform/storage/secureStore.ts` 的 current keys 從 `cj.auth.token`、`cj.session.id`、`cj.device.meta`、`cj.navigation.pendingLandingHref`、`cj.locale` 改為 `emorapy.auth.token`、`emorapy.session.id`、`emorapy.device.meta`、`emorapy.navigation.pendingLandingHref`、`emorapy.locale`。舊 `cj.*` keys 只作 legacy lazy migration source；讀到舊值時寫入 Emorapy key 並清理 legacy key，後續 set / clear / `clearAppStorage` 也會清理 legacy key，避免已安裝 App 在改名後失去 token、anonymous session、pending landing、locale 或 device metadata。

本輪只改 App platform storage adapter、App API adapter test fixture、SecureStore 單測、App adapter / Parity 核心文件與 naming governance gate；不改 backend auth/session model、不改 API header、Web localStorage、Admin token key、DB、EAS/App Store/Sentry 外部資源或 historical evidence。`cj.*` SecureStore values 只作本機已安裝 App 的 migration source，不再作 current runtime storage contract。

本輪完成判定：

1. `mobile/src/platform/storage/secureStore.ts` current keys 為 `emorapy.*`，legacy list 保留對應 `cj.*`。
2. `getToken` / `getSessionId` / `getPendingHref` / `consumePendingHref` / `getLocale` / `getDeviceMetadata` 支援 lazy migration；set / clear / `clearAppStorage` 會清理 legacy key。
3. `mobile/src/platform/storage/secureStore.test.js` 覆蓋 current key 寫入 / 清理與 legacy token/session lazy migration；`mobile/src/platform/api/client.test.js` 不再把 `cj.auth.token` / `cj.session.id` 當 current fixture。
4. `npm run naming:check` 固定 App SecureStore key migration contract，防止 current key 回退到 `cj.*`。
5. `npm --prefix mobile test -- src/platform/storage/secureStore.test.js src/platform/api/client.test.js --runInBand`、`npm --prefix mobile run platform:check`、`npm run docs:check` 與 App release / goal audit 仍需通過；release completion 仍保持 incomplete，不得因 App local SecureStore key 收斂宣稱 TestFlight / 真機 token restore evidence 已完成。

#### 2026-06-20 P4 App telemetry runtime diagnostics naming

已完成 current App telemetry runtime 診斷命名收斂：`mobile/src/platform/telemetry/client.ts` 的 safe console tag 從 `cj-app-telemetry` / `cj-app-otel` 改為 `emorapy-app-telemetry` / `emorapy-app-otel`，`mobile/scripts/run-telemetry-runtime-smoke.mjs` 的 release telemetry smoke User-Agent 從 `cj-app-telemetry-runtime-smoke/1.0` 改為 `emorapy-app-telemetry-runtime-smoke/1.0`。這只改 current App runtime diagnostics 與 structured release evidence request metadata，不改 telemetry payload schema、backend ingest endpoint、OTLP resource service name、Sentry project、release env secret 或歷史 evidence。

`npm run naming:check` 已加入 current App telemetry runtime tag / User-Agent guard，防止 `mobile/src` 或 `mobile/scripts` 回流 `cj-app-*` 診斷識別；`mobile/src/platform/telemetry/client.test.js` 同步固定新 tag。Sentry 外部 project / production native crash runtime evidence 仍是獨立 M6 blocker，不因本輪診斷命名收斂而視為完成。

#### 2026-06-20 P4 App OTLP telemetry scope fixture naming

已完成 current App OTLP telemetry scope fixture 收斂：backend integration / route / service tests 中的 OTLP `scope.name` 與 persisted `instrumentationScope` expectation 已從 `cj.mobile.app` 改為 `emorapy.mobile.app`，與 `mobile/src/platform/telemetry/client.ts` 的 current tracer name、`mobile/scripts/run-telemetry-runtime-smoke.mjs` 的 release runtime fixture 以及 App identity 文件一致。

本輪只改 current backend / mobile 測試與 release runner fixture，不改 backend OTLP ingest 行為、不拒絕外部傳入其他 instrumentation scope、不改 `app_telemetry_events` schema、不改 Admin report、不改 release env / Sentry / external tracing backend，也不改歷史 evidence。`npm run naming:check` 已加入 current telemetry fixture guard，防止 backend telemetry tests、mobile telemetry tests 或 telemetry runtime runner 回流 `cj.mobile.app`。

#### 2026-06-20 P4 App release contract temp fixture naming

已完成 current App release / external evidence contract 的本地 temp fixture 命名收斂：`check-release-evidence-sanitization.mjs`、`check-release-external-evidence-status-contract.mjs`、`check-release-external-evidence-handoff-contract.mjs`、`check-release-external-signoff-prerequisite-report.mjs`、`check-release-completion-audit.mjs` 與 `check-app-goal-audit-contract.mjs` 會建立的受控臨時目錄，已從 `cj-release-*` / `cj-goal-*` prefix 改為 `emorapy-release-*` / `emorapy-goal-*`。

該輪只改本機 contract / dry-run fixture 目錄名稱，不改正式 structured evidence 檔名、不改 `cj.app.ios_release_simulator_evidence.v1` 歷史 schema、不改 `APP_SENTRY_ORG` fixture 值、不改 GitHub / Vercel / Railway / App Store / Sentry 外部資源，也不改任何 secret / release env。`npm run naming:check` 已加入 current App release/audit contract temp prefix guard，防止 `mobile/scripts` 回流上述 `cj-release-*` / `cj-goal-*` temp prefix；歷史 evidence 與 schema 命名仍按 P6 provenance 保留原文。

#### 2026-06-20 P4 App release controlled fixture org/path naming

已完成 current App release / external evidence contract 的受控假資料識別符收斂：`check-release-evidence-sanitization.mjs`、`check-release-external-evidence-status-contract.mjs`、`check-release-external-evidence-handoff-contract.mjs` 與 `check-release-external-signoff-prerequisite-report.mjs` 中的 fake Sentry org 從 `cj-org` 改為 `emorapy-org`，受控 release DB URL path 從 `/cj` 改為 `/emorapy`。

本輪只改本地 sanitization / contract / prereq report fixture，不改真實 `APP_SENTRY_ORG`、Sentry project、release DB、`DATABASE_URL`、GitHub / Vercel / Railway / App Store / Sentry 外部資源、CI secret 或 release env。`cj.app.ios_release_simulator_evidence.v1` 歷史 schema、`cj-mobile` rename workaround warning 與歷史 evidence 仍按 P6 provenance 保留原文。`npm run naming:check` 已加入 current mobile release scripts guard，防止 `cj-org` 與 `release-db.example.../cj` 類受控 fixture 回流。

#### 2026-06-20 P4 CI / local true-service Postgres fixture naming

已完成 CI / local true-service 的 ephemeral Postgres fixture 命名收斂：`.github/workflows/ci.yml` 的 quick-flow、production-like smoke 與 admin-e2e Postgres service user / password / DB name / healthcheck / `DATABASE_URL` 已由 `cj`、`cj_dev_pass`、`cj_flow_test`、`cj_smoke`、`cj_dev` 改為 `emorapy`、`emorapy_dev_pass`、`emorapy_flow_test`、`emorapy_smoke`、`emorapy_dev`；CI 內 production-like smoke 的 dummy JWT fixture prefix 亦改為 Emorapy。

本地輔助 docker compose 已同步改為 `emorapy-backend-dev-postgres`、`emorapy-backend-dev-postgres-data`、`emorapy-backend-dev-redis`、`emorapy-backend-dev-redis-data`，`backend/tests/setup.ts` 的 fallback test DB 改為 `emorapy_platform_test`，true-service smoke 與 seed guard unit tests 改用 `emorapy_dev`。`05-工程架構與共享層/01-本地開發與工作區基線.md` 已同步標準本機 true-service Postgres URL。

本輪只改 CI / local ephemeral fixture，不改 Supabase Dev project `Mother Bear Court Dev` / `lbukyqztkkkztfrfltlh`、production / release `DATABASE_URL`、Railway / Vercel / GitHub repo / domain、`CJ_COMMIT_SHA` / `CJ_RELEASE_GATE` / `CJ_DEV_REDIS_DIR` legacy fallback、release DB parity historical evidence 或任何 secret。若本機已存在舊 Docker volume，需要開發者按需要手動遷移或刪除舊 volume；新預設不再主動建立 `cj-backend-dev-*` container / volume。

`npm run naming:check` 已加入 current CI/local fixture guard，防止 `.github/workflows/ci.yml`、`backend/docker-compose*.yml`、backend test setup / true-service smoke / seed guard test 與本地工作區基線回流 `cj_*` local DB、`cj_dev_pass`、`postgresql://cj:*`、`cj-backend-dev-*` 或 `cjJwt*` 類受控 fixture。

#### 2026-06-20 P4 Supabase Dev source comment provenance

已完成 current backend source 中 Supabase Dev project 註釋的 provenance 收斂：`backend/src/utils/seed-guard.ts` 仍保留真實已知安全 dev project ref `lbukyqztkkkztfrfltlh`，但註釋已改為 `Legacy Supabase Dev project name: "Mother Bear Court Dev"`，明確該名稱是現存外部 Supabase Dev project name / legacy infrastructure identifier，不代表現行產品名或新的 App / release identity。

本輪只改 source comment 與 `npm run naming:check` guard，不改 seed guard 行為、不改 Supabase project ref、不改 `DATABASE_URL`、不改 production / release DB、不改 Supabase project name，也不自動建立新的 Emorapy Supabase Dev project。若後續要遷移 Supabase Dev project display name 或建立新 dev DB，需按 P4 platform migration 另行設計並確認 migration / seed / env / release DB parity 邊界。

#### 2026-06-20 P4 App visible copy legacy brand guard

已完成 App 可見文案 gate 的 legacy brand 防回流加固：`mobile/scripts/check-user-copy-contracts.mjs` 不再只精準禁止 `CJ APP`，而是將 `CJ App`、`CJ Platform`、`CJ 平台`、`CJ Reminder`、`CJ 提醒`、`Mother Bear Court`、`mother-bear-court`、`cj-mobile` 與 `com.cj.motherbearcourt` 納入 App visible copy banned terms。這會覆蓋 App screen JSX text、visible prop、visible copy module 與 accessibility-visible copy 的掃描路徑，避免舊品牌或舊 native identity 重新進入普通用戶 App UI。

本輪只改 copy gate 與 `npm run naming:check` marker，不改現有 i18n catalog、不改 App Store metadata、不改 native bundle id / Android package、不改 EAS project、不改歷史 evidence。若未來某處需要以歷史說明方式向用戶顯示舊名，必須另行登記產品場景與合規理由，不得直接繞過 `copy:check`。

#### 2026-06-20 P4 Sentry native crash project / release identity handoff

已完成 Sentry native crash project / release identity 的本地 handoff 收斂：App runtime adapter 的 Sentry release 維持 `emorapy-mobile@${runtime.appVersion}+${runtime.buildNumber}`，`mobile/app.json` 的 Sentry plugin note 明確要求 `SENTRY_PROJECT=emorapy-mobile`，release handoff / signoff 腳本的 Sentry command 示例也改為 `APP_SENTRY_PROJECT=emorapy-mobile`，不再給 generic `<project>` 作可誤用占位。

本輪只改 App config 註記、handoff / signoff owner action、command example、native crash runtime release identity guard 與 `npm run naming:check` marker；不建立 Sentry project、不設定 Sentry org / token / DSN、不查詢 Sentry、不生成 native crash runtime pass evidence，也不把 `native_crash_runtime_evidence` 標成完成。若 Sentry 尚未存在 `emorapy-mobile` project，外部 owner 仍需先建立並把 `APP_SENTRY_ORG`、`APP_SENTRY_PROJECT=emorapy-mobile`、`APP_SENTRY_AUTH_TOKEN` 與受控 `APP_NATIVE_CRASH_SENTRY_EVENT_ID` 放到 ignored release env / CI secret 後，再跑正式 runtime evidence。

### P5：核心文件與治理文件收斂

1. 先建立正式命名政策 / historical alias 口徑，再批量更新核心文件。
2. 根層 `README.md`、`07/README.md`、`07/00-活躍問題總覽.md`、`20-App端`、`50-跨端Mapping與Parity`、`03-管理端與平台治理`、`05-工程架構與共享層`、`08-測試規範與驗收` 必須同步新口徑。
3. PRD ID `CJ-PRD-APP-*` 不得未經決策直接改名；若改為 `EMO-PRD-*`，必須提供映射、更新 docs structure gate 與引用。
4. current docs 中的 `CJ` 必須分類為：正式產品名殘留、PRD ID、compat env var、legacy hostname / repo / service、歷史別名、引用歷史證據、不可改原文。`@cj/*` 不再屬 current docs allowlist，只允許出現在歷史文件、evidence、archive 或本遷移說明。
5. `npm run naming:check`、`npm run docs:check`、`npm run docs:audit:dry-run:current` 必須作為每輪 docs 收斂驗證；`docs:check` 已先執行 naming governance gate。

#### 2026-06-20 P5 入口文件收斂

已完成第一批 agent / SSOT 入口修正：repo-level `AGENTS.md` 改為 `Emorapy Agent Guide`，並明確 `CJ`、`Mother Bear Court`、`mother-bear-court`、`com.cj.motherbearcourt` 只作 legacy aliases 或現存 infrastructure identifiers；`docs/核心開發文件/README.md` 改為 `Emorapy 核心開發文件（唯一工程 SSOT）`，並加入舊名不得作為新對外文案、App Store metadata、native bundle ID 或 release identity 的規則；工程架構總覽與本地開發基線中描述 App route shell 的文字已從 `CJ App` / `CJ route group shell` 改為 `Emorapy App` / `Emorapy route group shell`。

本輪未改實際 GitHub repo、Vercel / Railway hostname、Supabase dev project name、PRD ID 或 historical evidence；shared package scope 已在 P3 遷移到 `@emorapy/*`，其餘仍需按 P4 / P5 / P6 allowlist 分批治理，不能用簡單搜尋替換。

2026-06-20 追加完成 current formal-doc product-name intro cleanup：根層旗艦文檔、`00-跨端產品核心`、`01-認證與會話`、`02-用戶端核心流程`、`03-管理端與平台治理`、`04-共用機制`、`05-工程架構與共享層`、`06-接口描述`、`08-測試規範與驗收`、`20-App端` 與 `測試/README.md` 中把 `CJ` 當正式項目名 / 產品名 / App route group 的 current 表述已改為 Emorapy；`scripts/lib/core-docs-audit.mjs` 的 App route group audit copy 亦已對齊。此輪不改 `CJ-PRD-*`、`CJ-NFR-*`、`CJ-RTM-*`、`CJ-ADR-*`、`CJ-OPS-GAP-*` 等歷史 ID，不改 legacy repo / domain / env var，也不改 `已處理/`、`90-證據與盤點/` 或 `99-歷史降級索引/` 的歷史證據原文。

#### 2026-06-20 P5 naming governance gate

已新增 `npm run naming:check`，入口為 `scripts/check-emorapy-naming-governance.mjs`，並將它接入 root `docs:check` 第一段。該 gate 目前固定三類規則：

1. `mobile/app.json` 必須維持 `expo.name=Emorapy`、`slug=emorapy-mobile`、`scheme=emorapy`、`ios.bundleIdentifier=com.emorapy.app`、`android.package=com.emorapy.app`。
2. `mobile/app.json`、`mobile/eas.json`、App readiness scripts、Maestro flows 不得回流 `com.cj.motherbearcourt`、`cj-mobile` 或 `cj://` 類舊 App identity；release scripts 中描述 legacy `cj-mobile` rename workaround 的 warning 文字保留，不納入機械清零。
3. Backend / Web / Admin / Mobile user-facing source 與 `scripts/**/*.sh` operator-visible copy 不得回流 `CJ Platform`、`CJ 平台`、`CJ Reminder`、`CJ 提醒`、`Admin Login - CJ`、`Ops Dashboard - CJ` 等舊對外品牌文案；`AGENTS.md`、核心 README 與術語表必須保留 Emorapy 正式名與 legacy alias policy marker。

此 gate 已處理 current package scope，不允許 current source/config/active docs 回流 `@cj/*`。它仍不處理 `CJ-PRD-*`、現存 `mother-bear-court` repo / deploy hostname、compat env var 或 historical evidence；這些仍按 P4 / P5 / P6 分批治理。

#### 2026-06-20 P5 naming gate 掃描盲區加固與 dev-facing 範本收斂

獨立多角度複查（source / config / user-facing 三向 + 盲區 grep）後，確認 `npm run naming:check` 大方向有效，但其 scan glob 未覆蓋三類 dev-facing 入口，導致以下「應改而漏改」殘留不在 approved allowlist 內，且未來回流也不會被擋：

1. `e2e/package.json` 與 `e2e/package-lock.json` 的 manifest `name` 仍是 `cj-admin-e2e`，與已收斂的 `emorapy-root` / `emorapy-backend` 同類，已改為 `emorapy-admin-e2e`。
2. `backend/.env.example`、`frontend/.env.example`、`frontend-admin/.env.example` 的首行品牌註釋仍是 `CJ 平台`，已改為 `Emorapy`；`frontend/.env.example` 的 `VITE_APP_TITLE` 預設值範例 `CJ 平台` 屬潛在用戶可見回流，已改為 `Emorapy`；`backend/.env.example` 的 DB 範例 `postgresql://cj:cj_dev_pass@localhost:55432/cj_dev` 與已遷移的 CI/docker `emorapy_*` 基線不一致，已改為 `emorapy:emorapy_dev_pass@.../emorapy_dev`。`.env.example` 內的 `mother-bear-court.vercel.app` 屬 legacy production hostname compatibility default，與 ops release helper 一致，本輪不改，待 P4 domain migration 統一處理。
3. `start-dev.bat` 與 `*.bat` operator 入口此前不在掃描範圍（現況無舊品牌文案，但屬未受保護盲區）。

`scripts/check-emorapy-naming-governance.mjs` 已加固：`EXPECTED_PACKAGE_MANIFEST_NAMES` 新增 `e2e/package.json`（expected name `emorapy-admin-e2e`）；`OPERATOR_VISIBLE_SCAN_PATTERNS` 新增 `scripts/**/*.bat` 與 `start-dev.bat`；新增 `checkEnvExampleIdentity()` 對所有 `**/.env.example` 套用 visible-copy 與 dev/CI fixture 規則，禁止 `CJ 平台` / `CJ Platform` 品牌與 `cj_dev` / `cj_dev_pass` / `postgresql://cj:` 類 dev DB fixture 回流，並刻意放行 legacy hostname。

本輪只改 dev-facing 範本 / manifest 顯示身份與 naming gate；不改 production `DATABASE_URL`、Supabase project、GitHub repo、Vercel / Railway hostname、`CJ_*` compat env、PRD ID 或 historical evidence。

本輪完成判定：

1. `e2e` manifest 身份為 `emorapy-admin-e2e`，root / backend / e2e 三個 manifest name guard 一致。
2. 三個 `.env.example` 不再出現 `CJ 平台` 品牌或 `cj_*` dev DB fixture 範例；legacy hostname 範例保留。
3. `npm run naming:check` 已覆蓋 `e2e/`、`*.env.example`、`*.bat`；負向測試確認注入 `CJ 平台` 到 `.env.example` 會被擋（`[cj-platform-zh]`），還原後通過。
4. `npm run naming:check`、`npm run docs:check` 通過；release completion 仍保持 incomplete，不得因本輪 dev-facing 收斂宣稱 release complete。

#### 2026-06-20 P5 current docs 舊名 allowlist 分類

已完成 current docs 舊名殘留分類：在排除 `07-待處理問題與治理/已處理/`、`90-證據與盤點/`、`99-歷史降級索引/`、`文件收斂/` 的 current docs 掃描後，舊名主要剩下以下 approved allowlist 類別。這些不是正式產品名殘留，不應用全局替換處理；若未來要改任一類，需另建映射與 migration gate。

| 類別 | 允許例子 | 邊界 | 後續策略 |
| --- | --- | --- | --- |
| Legacy requirement / governance IDs | `CJ-PRD-*`、`CJ-NFR-*`、`CJ-RTM-*`、`CJ-ADR-*`、`CJ-ERR-*`、`CJ-SEC-*`、`CJ-SCHEMA-*`、`CJ-AI-*`、`CJ-DATA-*`、`CJ-L10N-*`、`CJ-A11Y-*`、`CJ-CONTENT-*`、`CJ-MET-*` | 只作需求、驗收、ADR、風險與治理追溯 ID；不得作產品名或新 external identifier | 若改為 `EMO-*`，必須先建立 ID mapping、更新 docs structure / truth gates、引用與歷史索引 |
| Compat env vars / local runtime aliases | `CJ_ANDROID_AVD_NAME`（本機 emulator profile） | 只作本機 Android AVD profile fallback；不是 release identity | 獨立任務；去名需先重建本機 AVD。`CJ_COMMIT_SHA` / `CJ_RELEASE_GATE` / `CJ_DEV_REDIS_DIR` 已於 2026-06-21 P4 env deprecation 移除 |
| Legacy repo / deployment / domain identifiers | `mother-bear-court`、`Mother Bear Court Dev`、legacy Vercel / Railway hostname、GitHub remote | 只作現存 infra / repo / dev project / legacy hostname；不得作新 App Store metadata 或 native identity | P4 另做 repo/domain/platform migration；先建 alias / redirect / vars，再切正式入口 |
| Historical package-scope references | `@cj/*` | 只允許在本遷移說明、已處理歷史任務、歷史 evidence / archive 或文檔收斂歷史中出現；current source/config/active docs 不得重新依賴 | 已由 `npm run naming:check` 防回流；current scope 為 `@emorapy/*` |
| Historical evidence / archive wording | `CJ`、`cj-mobile`、`com.cj.motherbearcourt` 等 | 只代表當時快照，不代表 current release pass evidence | P6 已在 `90` / `99` 入口建立 provenance；需要 current release proof 時重跑新 evidence |

本輪修正 `術語表.md` 的 `CJ` 定義：`@cj/*` 現在明確只作歷史 package-scope 引用，不再被描述為可用 current package scope。`npm run naming:check` 已加入本節 marker，避免 P5 allowlist 分類被移除。

#### 2026-06-20 P5 legacy governance ID namespace policy

已完成 `CJ-*` governance ID namespace 的正式分類與防誤改 gate：`術語表.md` 與 `00-跨端產品核心/01-產品PRD總章.md` 現在明確 `CJ-PRD-*`、`CJ-NFR-*`、`CJ-RTM-*`、`CJ-ADR-*`、`CJ-ERR-*`、`CJ-SEC-*` 等只作需求 / 驗收 / ADR / 風險 / 治理追溯 ID，不代表當前產品正式名、App Store metadata、native bundle ID、EAS project、package scope 或 release identity。

本輪不改任何現有 `CJ-*` ID，也不新增 `EMO-*` namespace；因為這些 ID 已被 PRD、RTM、ADR、NFR、接口、App Roadmap、Parity 與 docs truth gate 廣泛引用。若未來要遷移為 `EMO-*` 或其他新前綴，必須先建立 ID mapping、引用遷移策略、更新 docs structure / truth / naming gate 與所有交叉引用，不得用搜尋替換直接改名。

`npm run naming:check` 已加入 governance ID namespace policy gate，固定術語表、PRD 主章與本待辦中的上述 policy marker。此 gate 的目的不是清零 `CJ-*` ID，而是防止它們被誤讀成 current product name 或被無 mapping 地改寫。

#### 2026-06-20 P5 current source design-token comment cleanup

已完成 current Web / Admin source comment 中的 legacy product identity 收斂：`frontend/src/index.css` 的 design token header 從 `Mother Bear Court - Design Token System` 改為 `Emorapy - Design Token System`，`frontend-admin/src/index.css` 從 `Mother Bear Court Admin - Design Token System` 改為 `Emorapy Admin - Design Token System`。

本輪只改 source comment 與 naming governance gate，不改 CSS token value、色彩系統、runtime UI、Vercel / Railway domain、GitHub repo、Supabase project 或 historical evidence。`npm run naming:check` 已新增 current source identity guard，防止 `Mother Bear Court - Design Token System` 類 current source comment 回流；legacy hostname / repo / dev project name 仍按 P4 / P5 allowlist 分類處理。

#### 2026-06-20 P5 current formal-doc lead-in cleanup

已完成第二批 current formal-doc lead-in 收斂：`01-認證與會話/00-認證與會話總覽.md`、`02-用戶端核心流程/00-用戶端核心流程總覽.md`、`03-管理端與平台治理/00-管理端與平台治理總覽.md`、`03-管理端與平台治理/01-環境與部署基線.md`、`03-管理端與平台治理/04-兩版本運作規範.md`、`04-共用機制/00-共用機制總覽.md`、`05-工程架構與共享層/Repo平台分層與共享規範.md` 與 `08-測試規範與驗收/01-測試文檔分層與使用規則.md` 不再用 `CJ` 或 `mother-bear-court` 作正式產品 / 文件主體。

本輪只改 current formal docs 的 lead-in 與 naming governance gate，不改 `CJ-PRD-*`、`CJ-NFR-*`、`CJ-RTM-*`、`CJ-ADR-*`、legacy env var、legacy repo / deployment / hostname、歷史 evidence 或 archive 原文。`npm run naming:check` 已新增固定文件 lead-in guard，防止上述 current docs 回流 `CJ` 產品主體句；legacy identifiers 仍可在已分類的需求 ID、兼容 env、infra、evidence 或本遷移說明中保留。

#### 2026-06-20 P5 core marketing SSOT naming cleanup

已完成核心營銷文件的 current brand naming 收斂：`docs/核心營銷文件/README.md` 現在明確本目錄是 `Emorapy` 全項目的唯一營銷 SSOT；原 `CJ產品定位與行銷策略分析.md` 與 `CJ專屬營銷分析報告-2026-03-29.md` 已改名為 `Emorapy產品定位與行銷策略分析.md` 與 `Emorapy專屬營銷分析報告-2026-03-29.md`，README 連結與閱讀順序同步更新；短影音策略、合規免責、腳本、生成式分鏡與 `campaigns/` 單案投放稿中的 current product / brand copy 已從 `CJ` / `CJ 平台` / `CJ Platform` 收斂為 `Emorapy`。

本輪只改 current marketing SSOT 與投放底稿，不改工程 SSOT、不改 App Store metadata / Bundle ID / Android package / Expo slug、不改 GitHub / Vercel / Railway / domain、不改 `90` / `99` 歷史 evidence 或 archive 原文，也不把工程 / ops 文件中的 `Mother Bear Court` legacy repo / deploy alias 視為本輪 blocker。`npm run naming:check` 已新增 `docs/核心營銷文件/**/*.md` guard，防止 current marketing copy 回流 `CJ`、`CJ 平台`、`CJ Platform`、`Mother Bear Court` 或 `mother-bear-court` 作對外品牌。

#### 2026-06-20 P5 marketing asset identifier cleanup

已完成核心營銷 current 素材識別符收斂：`campaigns/2026-03-21-E-alpha_counseling-cost-sketch.md` 的專案內部代號與匯出成片檔名已由 `CJ_*` 改為 `Emorapy_*`，修訂記錄不再引用 `cj-*` workflow identifier；`templates/storyboard-gemini-jimeng.csv` 的 `project_id` 範例已由 `CJ_AD_001` 改為 `Emorapy_AD_001`。

本輪只改 current marketing asset / workflow identifiers，不改 historical evidence、不改 App Store / native identity、不改 GitHub / Vercel / Railway / domain，也不把已歸檔的歷史 skill 名稱視為需要追溯改寫。`npm run naming:check` 已將 `docs/核心營銷文件/**/*.csv` 納入 marketing copy scan，並新增 `CJ_` / `CJ-` / `cj-*` token prefix guard，防止未來素材代號、匯出檔名或分鏡模板回流舊品牌前綴。

#### 2026-06-20 P5 docs audit ledger generator output naming

已完成活躍文檔治理台賬生成器的輸出命名收斂：`scripts/generate-core-docs-audit-ledger.mjs` 重新生成 `文件收斂/03-CJ-核心開發文件逐文件代碼校驗總台賬-2026-04-18.md` 時，標題會使用 `Emorapy 核心開發文件逐文件代碼校驗總台賬`，並在台賬頭部加入「檔名中的 `03-CJ-` 保留為 2026-04-18 legacy governance batch identifier；現行產品與工程 SSOT 名稱為 `Emorapy`」的 provenance 說明。

本輪只改活躍 generator output、現存 ledger 頭部、`文件收斂/README.md` 與 `01-CJ-文檔收斂台賬與批次索引.md` 的現行產品主體語句；不改 `文件收斂/` 下既有 dated 檔名、不移動歷史批次文件、不改 `scripts/check-docs-truth.mjs` 的 ledger path、不改 `CJ-PRD-*` / `CJ-NFR-*` / `CJ-ADR-*` 等 requirement / governance IDs。`npm run naming:check` 已加入 generator / ledger marker gate，防止活躍 docs audit ledger 重新輸出 `# CJ 核心開發文件...` 作現行標題。

### P6：Evidence / archive 邊界

1. `90-證據與盤點` 與 `99-歷史降級索引` 的歷史 artifact 不做機械改寫。
2. 新增 alias / provenance 說明：舊 evidence 裡的 `CJ` / `Mother Bear Court` / `com.cj.motherbearcourt` 是當時快照，不等於當前正式命名。
3. 後續所有新 release evidence、status、handoff、App Store / TestFlight / EAS artifact 必須使用 Emorapy 命名。
4. 若某個 evidence checker 目前要求舊名，應修改 checker expected values，而不是改歷史 JSON 來配合新名。

#### 2026-06-20 P6 evidence / archive provenance gate

已完成 P6 入口治理：`90-證據與盤點/README.md` 新增 `Emorapy 命名與歷史證據 provenance`，明確該目錄內 `CJ`、`Mother Bear Court`、`mother-bear-court`、`cj-mobile`、`com.cj.motherbearcourt`、`CJ Platform` 或 `@cj/*` 只代表歷史證據快照、legacy infrastructure identifier 或歷史別名，不代表當前正式命名、App Store metadata、Bundle ID、Android package、EAS project、package scope 或用戶可見文案；若舊證據需要作為 release / TestFlight / App Store submission 依據，必須按 Emorapy identity 重新產生新證據，不得改寫舊 artifact。

`99-歷史降級索引/README.md` 同步新增 `Emorapy 命名與歷史方案 provenance`，明確歷史方案中的 `CJ` / `cj-mobile` / `com.cj.motherbearcourt` / `@cj/*` 只代表當時方案、路徑、repo / deploy alias、legacy package scope 或歷史證據引用；若歷史方案仍要採用，必須先回寫到現行 `00-跨端產品核心/`、`20-App端/`、`50-跨端Mapping與Parity/`、`05-工程架構與共享層/` 或本治理目錄，再按當前 Emorapy identity 實作與取證。

`npm run naming:check` 已加入 P6 marker gate，要求 `90` 與 `99` 入口保留上述 provenance 標記。此 gate 只驗證入口政策存在，不掃描或清零歷史 evidence / archive 原文；舊名在 `90` / `99` 中仍按 approved allowlist 留存。

| 命令 | 結果 | 備註 |
| --- | --- | --- |
| `npm run naming:check` | 通過 | P6 provenance marker gate 已生效 |
| `npm run docs:check` | 通過 | core docs structure / truth / naming gate 均通過 |
| `npm run docs:audit:dry-run:current` | 通過 | current SSOT markdown dry-run 211 files；不改寫歷史 evidence |
| `npm --prefix mobile run release:completion:audit -- --release-env-file=release.env.local --json` | 未完成但無 hard failure | `complete=false`、`failures=0`；blocker 仍是 Apple / ASC / EAS iOS / TestFlight / 真機 / push provider / native crash runtime |
| `npm --prefix mobile run goal:completion:audit -- --release-env-file=release.env.local --json` | 未完成 | 只剩 `release_signoff` missing/incomplete |

### P4 GitHub repo / domain rename runbook（2026-06-21 規劃，外部執行）

repo 內現況已 rename-safe，因此本節是「外部平台改名 runbook + repo 內已就緒證據」，repo 內不需要再改 code 即可支援改名；真正 rename 是 GitHub / Vercel / Railway 後台操作。

#### repo 內就緒證據（已驗證）

1. App release GitHub secret 工具（`mobile/scripts/check-release-github-secret-names.mjs`、`mobile/scripts/sync-release-github-secrets.mjs`）解析 repo 的優先序為 `--repo=<owner/repo>` > `EMORAPY_GITHUB_REPO` > `GITHUB_REPOSITORY` > legacy default `Alex0158/mother-bear-court`；rename 後設定 `EMORAPY_GITHUB_REPO` 或 `--repo` 即可，不需改 source。
2. `.github/workflows/*.yml` 使用 `${{ github.* }}` context 與 repository / environment variables，未硬編 repo slug；repo rename 後 Actions 仍指向同一 repo。
3. Railway service 名與 main/admin web URL 已可由 `EMORAPY_RAILWAY_SERVICE_NAME`、`EMORAPY_MAIN_WEB_URL`、`EMORAPY_ADMIN_WEB_URL`、`EMORAPY_BACKEND_BASE_URL` 覆蓋 legacy default（見上方 P4 ops env alias 段）。
4. legacy hostname `mother-bear-court.vercel.app` / `mother-bear-court-production.up.railway.app` 已分類為 compatibility default，naming gate 與 runbook 已標註不是正式 Emorapy domain 完成證據。

#### 外部 rename 執行順序與停止條件

1. **先加 alias，不切主入口**：在 Vercel 為 main / admin project 加 Emorapy 自訂 domain；在 Railway 為 backend service 加 Emorapy domain。先確認新 domain 可服務並通過 `EMORAPY_MAIN_WEB_URL=<new> npm run ops:release:status`。
2. **設 repo / environment variables**：在 GitHub repo 設 `EMORAPY_RAILWAY_SERVICE_NAME`（若 Railway service 也改名）、`EMORAPY_MAIN_WEB_URL`、`EMORAPY_ADMIN_WEB_URL`、`EMORAPY_BACKEND_BASE_URL`、`EMORAPY_GITHUB_REPO`，讓 workflow 與 release gate 走 Emorapy 入口。
3. **GitHub repo rename**：`Settings → Rename`。GitHub 會自動對舊 slug 做 redirect，但本機 remote 要更新：`git remote set-url origin git@github.com:<owner>/<new-repo>.git`。確認 Vercel / Railway 的 GitHub integration 仍綁定同一 repo（GitHub repo id 不變，integration 通常自動跟隨）。
4. **驗證**：repo rename 後重跑 `npm run ops:release:status`、確認 Vercel / Railway deploy 仍觸發、main/admin/backend `/version` 仍回報正確 commit。
5. **切正式入口後**：把 legacy hostname 從 compatibility default 降級為純 redirect，更新 platform map 與 runbook；確認後才可在後續輪移除 legacy default。
6. **停止條件**：若 rename 後 Vercel / Railway integration 斷開、Actions 無法觸發、或 `/version` 回 `unknown`，先停下、回滾 variable 指向 legacy，記錄實際錯誤；不得邊壞邊改。

#### 完成判定（repo 內 vs 外部）

- repo 內：已就緒，無待改 code（本節作為證據與 runbook 留存）。
- 外部：需使用者在 GitHub / Vercel / Railway 後台執行上述步驟並取得新 domain / repo 的 release evidence 後，才可在 platform map 與 AGENTS.md 把正式入口從 legacy hostname 切到 Emorapy domain。在此之前不得宣稱 domain / repo migration 完成。

### P5 legacy governance ID namespace 遷移映射與策略（2026-06-21 規劃，gated 執行）

本節提供 `CJ-*` → `EMO-*` 的權威前綴映射與安全遷移策略。**這是策略與映射，不是執行**；本輪不改任何現有 `CJ-*` ID。實際改名是獨立 gated 步驟，因為這些 ID 被 PRD / RTM / ADR / NFR / 接口 / App Roadmap / Parity 與 `scripts/check-docs-truth.mjs` 廣泛交叉引用，一次搜尋替換會打爆 truth gate 並破壞追溯。

#### 範圍盤點（2026-06-21）

全庫共約 290 個 distinct `CJ-*` 完整 ID，涵蓋 23 個命名空間前綴。current docs 引用集中在 `00-跨端產品核心`、`04-共用機制`、`05-工程架構與共享層`、`06-接口描述`、`20-App端`、`50-跨端Mapping與Parity` 與根層旗艦文檔；另有歷史引用分佈於 `07/已處理`、`90`、`99`、`文件收斂`（依 P6 原文不改）。

#### 權威前綴映射（suffix 保持不變，僅換前綴 `CJ-` → `EMO-`）

| Legacy 前綴 | 目標前綴 | 語義 |
| --- | --- | --- |
| `CJ-PRD-*` | `EMO-PRD-*` | 產品需求（含 `-APP` / `-COM` / `-WEB` 子段保留） |
| `CJ-NFR-*` | `EMO-NFR-*` | 非功能需求 |
| `CJ-RTM-*` | `EMO-RTM-*` | 需求追溯矩陣 |
| `CJ-MET-*` | `EMO-MET-*` | 成功指標（含 `-APP`） |
| `CJ-SEC-*` | `EMO-SEC-*` | 安全需求 |
| `CJ-SCHEMA-*` | `EMO-SCHEMA-*` | schema / 相容性 |
| `CJ-AI-*` | `EMO-AI-*` | AI / LLM 風險（含 `-RISK` / `-ASSET` / `-GAP`） |
| `CJ-ADR-*` | `EMO-ADR-*` | 架構決策記錄 |
| `CJ-DATA-*` | `EMO-DATA-*` | 資料治理（含 `-GAP`） |
| `CJ-A11Y-*` | `EMO-A11Y-*` | 可訪問性 |
| `CJ-HYP-*` | `EMO-HYP-*` | 假設 |
| `CJ-GAP-*` | `EMO-GAP-*` | 缺口（PRD/OPS/APP 子段保留） |
| `CJ-OPS-*` | `EMO-OPS-*` | 運維（含 `-T` / `-GAP`） |
| `CJ-ERR-*` | `EMO-ERR-*` | 錯誤模型（含 `-GAP`） |
| `CJ-L10N-*` | `EMO-L10N-*` | 本地化 |
| `CJ-SCENE-*` | `EMO-SCENE-*` | 場景案例 |
| `CJ-SLI-*` | `EMO-SLI-*` | SLI / 可觀測性 |
| `CJ-TB-*` | `EMO-TB-*` | 威脅建模 |
| `CJ-CONTENT-*` | `EMO-CONTENT-*` | 內容設計（含 `-T`） |
| `CJ-API-*` | `EMO-API-*` | 接口缺口 |
| `CJ-NOGO-*` | `EMO-NOGO-*` | NoGo 邊界 |
| `CJ-STATE-*` | `EMO-STATE-*` | 狀態機（含 `-GAP`） |
| `CJ-INC-*` | `EMO-INC-*` | 事故（含 `-YYYYMMDD` 模板段） |

映射規則：只替換 `CJ-` 前綴為 `EMO-`，**完整保留**其後所有子段與數字編號（例如 `CJ-PRD-APP-012` → `EMO-PRD-APP-012`），確保一一對應、可逆、不丟追溯。

#### 安全執行策略（未來獨立任務，依序）

1. **凍結與快照**：執行前 freeze 一次 `git grep -oE "CJ-[A-Z0-9-]+" | sort -u` 產出完整 290 ID 清單作 mapping fixture，存為 evidence。
2. **gate 先行**：先改 `scripts/check-docs-structure.mjs` / `check-docs-truth.mjs` 中對 ID 格式的 regex / 引用校驗，使其接受 `EMO-*`（過渡期可同時接受 `CJ-*` 與 `EMO-*`）。
3. **current docs 批次替換**：只在 current SSOT 子域（`00`/`04`/`05`/`06`/`20`/`50` + 根層旗艦）做前綴替換；**排除** `07/已處理`、`90`、`99`、`文件收斂`（P6 歷史原文不改）。
4. **雙向引用核對**：替換後跑 `docs:check:truth` 確認交叉引用無斷鏈；對照 step 1 的 fixture 確認數量一一對應。
5. **naming gate 收尾**：把 `EMO-*` 設為現行、`CJ-*` 在 current docs 標為禁止回流（歷史層仍允許）；更新術語表與 PRD 主章的 namespace policy。
6. **不可一次硬改**：禁止 `sed -i 's/CJ-/EMO-/g'` 全庫掃替——會污染歷史證據、env、host、package 等非 ID 的 `CJ` 用法，且破壞 P6 邊界。

#### 完成判定（本輪 vs 執行輪）

- 本輪（2026-06-21）：映射表與策略已固定為治理依據；不改任何 ID。
- 執行輪（未來）：完成 step 1–5 且 `docs:check`、`naming:check` 全綠、290 ID 一一對應後，才可宣稱 PRD ID migration 完成；在此之前 `CJ-*` ID 仍是現行有效追溯 ID，不得當作殘留舊名清零。

## 邊界與注意事項

1. 此任務不是簡單搜尋替換；App identifier、GitHub repo、deployment service、package scope、env var、PRD ID 都有長期契約風險。
2. App Store Connect app record、iOS Bundle ID、Android package 是對外不可逆或高成本識別符，必須先決策再建立。
3. 不得提交 Apple / ASC / EAS / Sentry / push provider secret；文件只記 key name、presence、owner action 與 redacted 狀態。
4. historical evidence / archive 保持原文；改的是當前正式口徑與未來 evidence policy。
5. generated native files 與 lockfile 不手工逐字改；由配置、package manifest、install / regenerate 流程產生。
6. deployment / env var 改名採兼容期；新名未證明 local / release gate 通過前，不移除舊名。
7. local workspace folder 可最後再評估是否改名；先處理 release identity 與正式 docs，避免破壞本地路徑依賴。

## 驗收口徑

此任務完成時必須同時滿足：

1. App Store Connect 不存在錯誤建立的正式 `CJ` app record；新 app 使用 Emorapy 名稱與決策後的 SKU / Bundle ID。
2. `mobile/app.json`、App readiness scripts、Maestro、telemetry / native crash release naming 與 App catalog 均不再把 `CJ` / `cj-mobile` / `com.cj.motherbearcourt` 當正式 release identity。
3. Web / Admin / Backend user-facing copy 不再對用戶顯示 `CJ Platform`、`CJ 平台`、`CJ Reminder` 類舊產品名。
4. Ops / deployment / env var 新舊命名有明確兼容與 deprecation 策略，release gate 不因改名失效。
5. 核心文件有正式命名政策、historical alias、PRD ID / package scope / evidence allowlist。
6. 舊名掃描結果只剩 approved allowlist 類別：historical evidence、archive、legacy PRD ID、legacy hostname / repo / service 或兼容 env var；`@cj/*` 不得出現在 current source/config/active docs。
7. App release strict sign-off 文件與工具不再要求舊 App identity。

## 建議驗證命令

```bash
npm run naming:check
npm run docs:check
npm run docs:audit:dry-run:current
npm --prefix mobile run release:completion:audit -- --json
npm --prefix mobile run goal:completion:audit -- --json
npm --prefix mobile run release:completion:audit:strict -- --json
npm --prefix mobile run goal:completion:audit:strict -- --json
rg -n "Mother Bear Court|mother-bear-court|CJ|cj-mobile|com\\.cj\\.motherbearcourt|@cj/" AGENTS.md backend frontend frontend-admin mobile packages scripts .github docs/核心開發文件
```

掃描結果不得以總量清零作唯一目標；必須按 allowlist 分類後驗收。
