# 共用機制總覽

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：正式規格
**覆蓋範圍**：前後台共享元件、樣式 token、流式通用機制、AI request ledger、跨流 case 授權分類、威脅建模、安全需求、狀態機、業務不變式、可訪問性與本地化
**取證代碼入口**：`frontend/src/App.tsx`、`frontend/index.html`、`frontend/src/utils/i18n.ts`、`frontend/src/assets/i18n`、`frontend/src/hooks/useAccessibility.ts`、`frontend-admin/src/App.tsx`、`frontend-admin/index.html`、`frontend-admin/src/utils/i18n.ts`、`frontend-admin/src/assets/i18n`、`frontend/src/components/common`、`frontend/src/components/ui`、`frontend/src/components/business`、`frontend/src/services/request.ts`、`frontend/src/services/sseRequest.ts`、`frontend/src/services/aiStream.ts`、`frontend-admin/src/services/request.ts`、`backend/src/app.ts`、`backend/src/middleware/auth.ts`、`backend/src/middleware/adminAuth.ts`、`backend/src/middleware/rateLimiter.ts`、`backend/src/routes/metrics.routes.ts`、`backend/src/utils/constants.ts`、`backend/src/utils/case-classifier.ts`、`backend/src/services/case.service.ts`、`backend/src/services/judgment.service.ts`、`backend/src/services/clinical-quality.service.ts`、`backend/src/services/ai-request-ledger.service.ts`、`backend/src/services/ai-cost-pricing.service.ts`、`backend/src/services/ai.service.ts`、`backend/src/services/interview-ai-response-consumer.ts`、`backend/src/services/safety-routing.service.ts`、`backend/src/services/reconciliation.service.ts`、`backend/src/services/chat.service.ts`、`backend/src/services/chat-ai-orchestrator.service.ts`、`backend/src/services/interview.service.ts`、`backend/src/services/interview-end-session-persistence.ts`、`backend/src/services/async-pipeline-session-status.ts`、`backend/src/services/product-state-recovery-task.service.ts`、`backend/src/services/execution.service.ts`、`backend/src/config/env.ts`、`backend/src/utils/ai-ledger-source.ts`、`backend/src/utils/ai-prompt-version.ts`、`backend/.env.example`、`backend/src/controllers/evidence.controller.ts`、`backend/prisma/schema.prisma`、`mobile/app`、`mobile/src/platform`、`packages/contracts/src`、`packages/api-client/src`
**最後核驗 Commit**：`23e85ef`
**最後核驗日期**：`2026-05-31`
<!-- CORE_DOC_AUDIT_METADATA:END -->

本文件承接 `CJ` 橫跨主 Web、Admin token 體系、共享契約與部分跨端邏輯的共用產品規則與共享交互機制。

本文中的 Web request、Admin token、route guard、FormData、SSE helper 等例子以 Web/Admin/Backend 現碼為取證基準。App 端若要承接同一共用語義，必須通過 `mobile/src/platform` adapter、shared contracts / api-client 與 App navigation 設計落地；具體 gate 見 [../20-App端/01-App導航與平台Adapter基線.md](../20-App端/01-App導航與平台Adapter基線.md) 與 [../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md](../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md)。

## 1. 本子域回答什麼

本子域只回答以下問題：

1. 哪些規則不是單一功能流獨有，而是跨頁、跨流程、跨端共同遵守。
2. AI stream、通知、身份承接、證據上傳、錯誤恢復、冪等語義應如何統一理解。
3. 哪些信任安全、AI 可靠性、成本、性能與可觀測性需求屬跨端共用非功能需求。
4. 哪些 AI / LLM 風險、prompt injection、防過度依賴、AI output downstream gate、ledger 與 prompt version 規則屬共用治理基線。
5. 哪些資料分類、consent、delete、retention、AI stream archive、log minimization 與 App storage / telemetry 規則屬隱私治理基線。
6. 哪些信任邊界、STRIDE 威脅分類、ASVS / MASVS 安全需求、media / metrics / admin / App native 安全缺口屬威脅建模治理基線。
7. 哪些核心狀態、狀態轉移、非法轉移、恢復任務與業務不變式需要以狀態機方式治理，而不是只看 enum。
8. 哪些可訪問性、本地化、內容設計、情緒安全文案與 Web / App 差異屬共用治理基線。
9. 哪些共用語義應回寫到正式正文，而不是留在 dated 驗收記錄裡。

本子域不回答：

1. 單一產品流程的完整頁面與交互正文。
2. 某個 API 是否已使用、待承接、候選廢棄或已確認廢棄。
3. `packages/*` 的代碼落點與工程分層。
4. App native storage、Push、Deep Link、upload provider、permission 或 lifecycle 的 runtime adapter 實作。

## 2. 共用機制分組

| 分組 | 影響範圍 | 核心內容 |
| --- | --- | --- |
| 身份承接 | quick、chat、formal case | `session_id`、JWT、claim-session、回跳 |
| Case 分類與授權 | quick、collaborative、remote、chat-to-case | session-bound vs user-bound、正式 collaborative、mixed credential |
| 安全與修復資格 | judgment、chat、repair journey、notification | safety/crisis route、partner invite、co-repair、partner notification |
| 路由與守衛 | Web、Admin | `ProtectedRoute`、`PublicRoute`、`AdminRedirect`、admin token redirect |
| AI stream / request ledger | quick judgment、repair replan、interview、chat | phase、replay、timeout、shared stream state、request/scope/token ledger |
| 通知與再進場 | Repair Journey、個人中心 | unread、dismiss、snooze、act、notifications inbox |
| 證據與上傳 | quick case、formal case、execution checkin | multipart、可選照片、失敗降級 |
| 錯誤恢復與冪等 | session refresh、request-judgment、重調流程 | refresh/retry、競態、冪等保護 |
| AI 風險治理 | judgment、chat、interview、repair、Admin report | prompt fencing、prompt version、ledger、safety route、output gate、防過度依賴 |
| 資料治理與隱私 | account、case、psych profile、safety state、AI stream、logs/evidence、App storage | 資料分類、consent/delete、retention、archive、log redaction、Push/Deep Link/telemetry 最小化 |
| 威脅建模與安全需求 | Web、Admin、Backend、App、media、metrics、AI provider、release evidence | 信任邊界、STRIDE 分類、`CJ-SEC-*`、ASVS / MASVS 參考、App security 本地 gate 與 M6 release evidence 缺口 |
| 狀態機與業務不變式 | case、chat、interview、AI stream、repair/execution、recovery task | state / transition / guard / side effect / illegal transition / recovery，enum 不等於完整狀態機 |
| 可訪問性、本地化與內容設計 | Web、Admin、App、i18n、AI / safety 文案、error / empty / waiting state | WCAG / ARIA / i18n / screen reader / keyboard / `lang` / emotion-safe content，Web 證據不等於 App 證據 |

當前現碼並不是把所有共用機制都抽到單一 package。主 Web 與 Admin 仍各自維護 request / token 攔截與頁面組裝；共享真相主要落在 route guard 語義、部分 common 元件、`packages/contracts` 與既有 request / SSE / AI stream helper。

App 端已透過 `@emorapy/api-client` 與 `mobile/src/platform` 建立 M0-M5 request / token / stream / runtime adapter。後續若新增 shared API adapter、SecureStore token、Push notification、Deep Link、upload provider 或 telemetry 能力，必須先在 `20-App端/01-App導航與平台Adapter基線.md` 中固定平台 adapter，再在 `50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md` 中確認 Backend / API / DB / shared package 影響。

## 3. 身份承接與守衛

以下內容屬跨流共用規則：

1. `canonical session_id` 是匿名身份唯一正式依據，不以任意 storage 臨時值作準。
2. `ProtectedRoute`、`PublicRoute` 是前台守衛語義；`AdminRedirect` 與 admin token 守衛是後台語義，兩者不能混成一套。
3. quick、chat、evidence 上傳都可能同時受 session 與 JWT 影響；承接規則先看身份，再看具體功能。
4. `claim-session` 是登入後補綁，不是 auth 主結果本身。
5. 主 Web 的 `frontend/src/services/request.ts` 與 Admin 的 `frontend-admin/src/services/request.ts` 目前是兩條正式請求堆棧，不存在一條完全統一的全端 token/request 中樞。

### 3.1 Case 分類與授權 invariant

以下規則是跨 case、judgment、evidence、chat-to-case、repair 的正式共用 invariant：

1. `quick` 一律是 session-bound。
2. `collaborative + session_id 有值` 是快速雙人 / 匿名協作，走 session-bound。
3. `remote` 走 user/JWT participant 授權，即使請求帶了過期或無關的 `X-Session-Id`，也不得覆蓋 JWT 授權。
4. `collaborative + session_id = null` 走 user/JWT participant 授權，可承接正式雙方陳述或 chat-to-case，不得被誤判成匿名協作。
5. 後端不得在各 service/controller 中重新手寫 `mode` 分支；應使用 `backend/src/utils/case-classifier.ts`。
6. 產品入口、分析來源與轉換來源不能只靠 `Case.mode` 推斷；`ChatToCaseLink` 仍是當前識別「先聊再判」的正式來源。若未來新增 `entry_point / source_channel / product_flow` 等字段，必須建立 schema migration 待辦並同步 dev / release DB。

### 3.2 安全路由與修復資格 invariant

安全路由不是單頁提示，而是會影響判決、修復旅程、伴侶邀請與通知召回的硬 gate：

1. case scope active `RelationshipRiskState` 是修復旅程、伴侶邀請、伴侶通知與 execution dashboard 的優先來源；沒有 active state 或讀取失敗時，才回退到判決 route policy。
2. 判決生成階段的 `emotional_analysis.route` 是 route fallback 的首要來源，允許值為 `standard / safety_support / crisis_support`。
3. 若 stored route 缺失，可由 `judgment_content` 透過 `SafetyRoutingService.decideRoute` 做 fallback 判斷。
4. `safety_support` / `crisis_support` 一律視為 high-risk product route：
   - 默認修復 intent 為 `safety_support`。
   - 不允許生成一般 `repair` 共同修復方案。
   - 不允許伴侶邀請。
   - 不允許進入 `co_active` 共同修復。
   - 不觸發伴侶修復通知。
5. `standard` 才允許一般 repair、co-repair、partner invite 與 partner notification；但若 active `RelationshipRiskState` 已把同一 case 標為高風險，必須以 active state 為準。
6. 以上規則由 `backend/src/services/repair-eligibility.service.ts` 聚合 `SafetyAssessmentService` 與 product safety policy；其他 service、controller、前端不得自行重寫高風險判斷。
7. Chat invite 屬安全邊界，不是純 UX 行為。同一聊天室短時間重複邀請需由後端限流，避免撤回/重發造成騷擾。

認證細節回 [../01-認證與會話/00-認證與會話總覽.md](../01-認證與會話/00-認證與會話總覽.md)。

## 4. AI Stream 共用語義

以下 AI stream 規則不應各頁自行再定義一套：

1. quick judgment、repair replan、interview、chat 都屬統一 AI stream 家族。
2. phase、`after_seq`、重連、snapshot/replay、timeout fallback 應優先沿用既有 SSE / AI stream helper，不應再新增第二套協議語義。
3. 前台的 `thinking / streaming / persisting` 等命名應維持共享語義，不為單頁重命名。
4. UI 展示層目前仍保留頁面級組裝，但錯誤命名、phase 映射與重試語義不得分叉成互不兼容的第二套口徑。
5. 所有新 AI runtime 應接入 `AIRequestLedgerService` 或既有 `AIService` ledger options，記錄 `request_id / stream_id / scope / product_flow / source_channel / entry_point / model / prompt_version / token usage / status / retry_count / failure_reason`。Admin costs 讀取 ledger 時必須透過 `getAIRequestLedgerProductFlow()` 做產品流分桶；缺失或空白 `product_flow` 固定落入 `unknown`，不得在報表 service 內另寫 fallback 字串。
6. Ledger 的 schema 與現有 callers 不保存 prompt 原文；當前只應保存 prompt 長度、request kind、scope、集中登記的 prompt version 與必要 metadata。`AIRequestLedgerService.metadata` 尚未具備 service-level key 白名單 / redaction gate，新增 caller 不得把 prompt、messages、完整聊天文本或可重建私密敘事的 metadata 透傳進 ledger；該缺口由 `AIRequestLedgerMetadata白名單待補待辦-2026-05-31.md` 追蹤。主要 prompt version 由 `AI_PROMPT_VERSIONS` / `getAIPromptVersion()` 管理；落庫 `judgments.prompt_version` 由 `getStoredJudgmentPromptVersion()` 管理，並與 judgment draft ledger 版本共用同一基礎版本；判決品質指標分桶由 `getJudgmentMetricsPromptVersion()` 與 `getClinicalQualityCaseType()` 管理，legacy 判決缺失版本固定落入 `judgment-prompt-version-unknown`，legacy case 缺失或空白 type 固定落入 `unknown`，不得在 service 內另寫 fallback。`consumeInterviewAIResponseStream()` 在缺少上層 ledger input 時仍需補齊 `profile_interview / profile_interview / interview_ai_response` 與 `interview-ai-response@v1.0`，新增訪談 stream 調用方不得產生空 source tracking。不得在 runtime 中散落未登記版本字串；若要新增 cost allocation 或 retention 欄位，必須按 DB parity 鐵律另記 dev / release 同步。
7. `cost_usd` 只在 `AI_COST_PRICING_JSON` 顯式配置且 provider/model/token usage 命中時寫入；pricing 的 `source/version/model/rates` 需記入 ledger metadata，未命中時保持 `cost_usd=null` / `costSource=not_allocated`，不得把 organization 聚合成本假分攤到產品流，也不得在代碼中硬編過期模型價格。Release gate 需透過 `ops:ai-pricing:check` 確認 pricing catalog 可解析、帶 `source/version`，`version` 以 `YYYY-MM-DD` 開頭且未來/過期檢查通過，並覆蓋 `OPENAI_MODEL / OPENAI_INTERVIEW_MODEL / OPENAI_ANALYSIS_MODEL` 與可選 `AI_COST_REQUIRED_MODELS`；`EMORAPY_RELEASE_GATE=1` 或 legacy `CJ_RELEASE_GATE=1` 時 pricing 檢查不得載入本機 dotenv。

AI / LLM 風險的資產清單、prompt injection、防過度依賴、AI output downstream gate、App AI 待承接口徑與 NIST / OWASP 參考對照，統一回 [03-AI風險與安全治理基線.md](./03-AI風險與安全治理基線.md)。

資料分類、心理畫像 consent/delete、AI request ledger / stream archive retention、log / release evidence 最小化、App local storage / Push / Deep Link / telemetry 隱私邊界，統一回 [04-資料治理與隱私風險基線.md](./04-資料治理與隱私風險基線.md)。

信任邊界、威脅建模、ASVS / MASVS 參考、安全需求 ID、media / metrics / admin / App native security 缺口，統一回 [05-威脅建模與安全需求基線.md](./05-威脅建模與安全需求基線.md)。

核心狀態機、狀態轉移、非法轉移、恢復任務與業務不變式，統一回 [06-狀態機與業務不變式治理基線.md](./06-狀態機與業務不變式治理基線.md)。新增 enum、修改 service guard 或讓 App 承接 server state 時，不得只更新 schema / contract 而不補 transition、guard、side effect、恢復策略與 RTM。

可訪問性、本地化、內容設計、情緒安全文案、`html[lang]`、i18n key、ARIA、keyboard/focus、screen reader 與 App native accessibility，統一回 [07-可訪問性本地化與內容設計治理基線.md](./07-可訪問性本地化與內容設計治理基線.md)。不得用 Web role 測試或 i18n catalog 存在宣稱 WCAG 合規、雙語完整或 App accessibility 已完成。Backend-owned success / error message 必須經 `responseFormatter`、`translateBackendMessage()` 或 `translateErrorByCode()` 覆蓋所支援 locale；backend-owned stored visible content（例如 judgment content / summary / emotional analysis、interview seed question、feedback card、notification render payload、chat safety notice、chat-to-case title、repair journey context、repair plan content）必須在寫入前按 request locale 生成；Web / App 不應為後端 response message 或已儲存顯示內容建第二套端側翻譯表。

具體接口回：

1. [../06-接口描述/03-case.md](../06-接口描述/03-case.md)
2. [../06-接口描述/05-reconciliation-execution.md](../06-接口描述/05-reconciliation-execution.md)
3. [../06-接口描述/06-interview-psych-profile.md](../06-接口描述/06-interview-psych-profile.md)
4. [../06-接口描述/07-chat.md](../06-接口描述/07-chat.md)

## 5. 通知、再進場與 CTA

通知中心不是裝飾層，而是正式再進場機制：

1. Header bell、`/notifications`、未讀數、單條 read/read-all、dismiss、snooze、act 構成完整通知入口。
2. active repair journey 的次操作應優先 `snooze`，不應把 `dismiss` 當成通用「稍後再說」。
3. `journey_context.primary_cta / secondary_cta / presentation_bucket` 屬跨頁共用判斷依據，不應每頁重猜一套。

通知接口主文在 [../06-接口描述/08-content-notification.md](../06-接口描述/08-content-notification.md)。

## 6. 證據上傳與恢復語義

以下規則屬跨功能共用：

1. `POST /cases/:id/evidence` 同時服務 quick case、formal case、execution 補充照片。
2. 證據上傳失敗時，應允許保留頁面上下文與單檔重試，不得把整個流程判為失敗。
3. execution checkin 的照片是可選資料；照片失敗時，應可降級成純文字打卡。
4. 文件大小、類型、下載授權屬共用風險點，實際字段與錯誤碼回接口文檔。
5. Web request 攔截器遇到 `FormData` 必須移除預設 `Content-Type`，由瀏覽器自動注入 multipart boundary；這是頭像與證據上傳的共用前提。
6. 簽名文件 URL 的 same-site 判斷在開發環境視 `localhost/127.0.0.1/::1` 為同站，避免本地跨 loopback hostname 時頭像/證據簽名被誤判失效。

## 7. 冪等、限流與錯誤恢復

以下規則屬共用防線：

1. `request-judgment`、AI 重調、訪談回合提交都應按冪等與限流來理解，不以頁面按鈕狀態代替後端保護。
2. session refresh、chat 邀請過期、SSE 中斷、AI 失敗 retry 都屬正式恢復分支，不是「偶發例外」。
3. 弱入口與分享鏈路必須假設 URL 直訪、過期、狀態變更隨時會發生。
4. AI 超時與鎖時間屬跨功能防線：單次 OpenAI 請求 `OPENAI_REQUEST=90000ms`、判決流程總超時 `JUDGMENT_GENERATION=180000ms`、判決生成鎖 `LOCK_TTL.JUDGMENT_GENERATION=300s`；三者需同步調整，避免中途鎖過期導致重入。
5. Redis 失聯時，cache / lock 會在首次錯誤後直接停用當前 Redis client 並降級到 memory / simple-lock，後續不再每次請求都先做 Redis 重試；要恢復 Redis-backed runtime 需由運維重啟進程。

## 8. 需要回跳到哪裡

1. 單一功能主流程，回 [../02-用戶端核心流程/00-用戶端核心流程總覽.md](../02-用戶端核心流程/00-用戶端核心流程總覽.md)。
2. Admin token、RBAC、jobs、health/metrics，回 [../03-管理端與平台治理/00-管理端與平台治理總覽.md](../03-管理端與平台治理/00-管理端與平台治理總覽.md)。
3. 工程代碼應該落在哪個 package 或 app，回 [../05-工程架構與共享層/00-工程架構與共享層總覽.md](../05-工程架構與共享層/00-工程架構與共享層總覽.md)。
4. App native adapter、Deep Link、Push、SecureStore、upload 或 App shared API adapter，回 [../20-App端/01-App導航與平台Adapter基線.md](../20-App端/01-App導航與平台Adapter基線.md) 與 [../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md](../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md)。

## 9. 閱讀順序

1. 先用 [../功能特性清單.md](../功能特性清單.md) 與 [../業務流程整合.md](../業務流程整合.md) 找到自己涉及的業務流。
2. 再讀本總覽，判斷這是不是跨流共用規則。
3. 若涉及共享樣式 token、theme 邊界與視覺收斂，再讀 [01-樣式Token與共享視覺規範.md](./01-樣式Token與共享視覺規範.md)。
4. 若涉及信任安全、AI 可靠性、隱私、性能、可用性或成本治理，再讀 [02-信任安全與非功能需求.md](./02-信任安全與非功能需求.md)。
5. 若涉及 AI / LLM 風險、prompt、ledger 或 output gate，再讀 [03-AI風險與安全治理基線.md](./03-AI風險與安全治理基線.md)。
6. 若涉及資料分類、consent/delete、retention、archive、log/evidence 或 App telemetry / storage，再讀 [04-資料治理與隱私風險基線.md](./04-資料治理與隱私風險基線.md)。
7. 若涉及 threat model、trust boundary、Admin / media / metrics / App native security 或安全需求矩陣，再讀 [05-威脅建模與安全需求基線.md](./05-威脅建模與安全需求基線.md)。
8. 若涉及 Case / Chat / Interview / AI Stream / Recovery 狀態、enum、非法轉移或業務不變式，再讀 [06-狀態機與業務不變式治理基線.md](./06-狀態機與業務不變式治理基線.md)。
9. 若涉及 WCAG / ARIA、keyboard/focus、screen reader、HTML `lang`、i18n、文案安全或內容設計，再讀 [07-可訪問性本地化與內容設計治理基線.md](./07-可訪問性本地化與內容設計治理基線.md)。
10. 再進對應接口文檔與 [../術語表.md](../術語表.md)。
11. 若需要已核實的環境或回歸證據，再進 [../90-證據與盤點/README.md](../90-證據與盤點/README.md)。
