# 前端設計：路由與頁面結構（代碼對齊）

> **文檔分級說明（2026-03-05 起）**  
> 本文件為二級映射文檔；頁面與路由一級權威改為 `docs/核心開發文件/頁面清單.md`。

**文檔版本**：v3.1  
**最後更新**：2026-03-05  
**對齊基準**：`frontend/src/router/index.tsx`、`ProtectedRoute.tsx`、`PublicRoute.tsx`、`utils/adminEntry.ts`

---

## 0. 關聯 SSOT（導航）

- 功能邊界主清單：`docs/功能特性清單.md`
- 產品流程與邊界：`docs/02-產品設計.md`
- 前端接口主文檔：`docs/前端設計/08-接口一覽表.md`
- 後端 API 權威詳規：`docs/後端設計/03-API設計.md`

---

## 1. 路由架構總覽

前端採 `createBrowserRouter`，以三個布局分域：

- `AppLayout`：主站域（首頁、案件、判決、和好、執行、個人中心、訪談、聊天室、admin 轉導）
- `SimpleLayout`：快速體驗域（`/quick-experience/*`）
- `AuthLayout`：身份域（`/auth/*`）

全頁面均透過 `React.lazy + Suspense` 載入，fallback 為 `Loading`。

---

## 2. 實作路由矩陣（按布局）

## 2.1 `AppLayout` 子路由

## 公開或弱保護

- `/` -> `Home`
- `/chat/room` -> `Chat/Room`
- `/chat/room/:roomId` -> `Chat/Room`
- `/admin/*` -> `AdminRedirect`

## 受保護（`ProtectedRoute`）

- `/case/list` -> `Case/List`
- `/case/create` -> `Case/Create`
- `/case/:id` -> `Case/Detail`
- `/case/:id/review` -> `Case/Review`
- `/judgment/:id` -> `Judgment/Detail`
- `/reconciliation/:judgmentId` -> `Reconciliation/List`
- `/reconciliation/:judgmentId/:id` -> `Reconciliation/Detail`
- `/execution/dashboard` -> `Execution/Dashboard`
- `/execution/:planId/checkin` -> `Execution/CheckIn`
- `/profile/index` -> `Profile/Index`
- `/profile/settings` -> `Profile/Settings`
- `/profile/pairing` -> `Profile/Pairing`
- `/profile/my-story` -> `Profile/MyStory`
- `/interview/:sessionId` -> `Interview/Chat`
- `/interview/:sessionId/result` -> `Interview/Result`

## 2.2 `SimpleLayout` 子路由

- `/quick-experience` -> 轉導 `/quick-experience/create`
- `/quick-experience/create` -> `QuickExperience/Create`
- `/quick-experience/result/:id` -> `QuickExperience/Result`
- `/quick-experience/collaborative` -> `QuickExperience/Collaborative`

## 2.3 `AuthLayout` 子路由

- `/auth` -> 轉導 `/auth/login`
- `/auth/login` -> `Auth/Login`（`PublicRoute`）
- `/auth/register` -> `Auth/Register`（`PublicRoute`）
- `/auth/forgot-password` -> `Auth/ForgotPassword`（`PublicRoute`）

## 2.4 全局 fallback

- `*` -> `NotFound`

---

## 3. 路由守衛語義（實作細節）

## 3.1 `ProtectedRoute`

- 依 `authStore.isAuthenticated` 判斷是否允許進入。
- store hydration 未完成時顯示 loading（最多等待 5 秒）。
- 未登入時導向 `/auth/login`，並附帶 `state.from` 供登入後回跳。

## 3.2 `PublicRoute`

- 已登入用戶不應再停留在登入/註冊/忘記密碼頁。
- 預設導向目標：`/case/list`。
- 同樣具有 hydration 等待邏輯（最多 5 秒）。

## 3.3 重要邊界

- 前端沒有獨立 `ConsentGuard` 路由守衛；訪談 consent 由頁面流程觸發，後端 `requireConsent` 做最終保護。
- `chat/room*` 目前未包 `ProtectedRoute`，授權由頁內 API 調用與後端規則控制。

---

## 4. `admin/*` 轉導規則

`AdminRedirect` 行為：

- 讀取 `VITE_ADMIN_LOGIN_URL`（必須為絕對 `http/https` URL）。
- 若有合法 URL：`window.location.assign(...)` 導向管理台。
- 若未配置或不合法：回到首頁 `/`。

這代表主前台不承載管理頁本體，只做入口轉發。

---

## 5. 參數命名映射規則

- 前端訪談頁參數是 `:sessionId`。
- 後端訪談 API 使用 `:id`。
- 映射在前端 API 層完成（例如 `getSession(sessionId) -> /interview/:id`）。

其他典型映射：
- 前端 `:planId` 對應 execution `plan_id` query/body
- 前端 `:judgmentId` 對應 reconciliation judgment 主鍵

---

## 6. 已知「不存在」路由（避免誤寫）

- 無獨立 `/500` 前端路由
- 無獨立 `/judgment/:id/result` 路由（由 `Judgment/Detail` 承載）

---

## 7. 變更同步規則

路由變更時，至少同步：
1. `frontend/src/router/index.tsx`  
2. 本文件  
3. `docs/前端設計/03-核心頁面詳細設計.md` 或 `04-其他重要頁面設計.md`（按頁面歸屬）  
4. `docs/功能特性清單.md`（若影響能力邊界）  
