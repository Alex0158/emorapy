# App 語言選擇與本地化未全量承接待辦

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：問題治理
**覆蓋範圍**：App 使用者所選語言、UI 文案、API/SSE locale header、本地化 catalog 與驗收 gate
**取證代碼入口**：`mobile/app`、`mobile/src/config/runtime.ts`、`mobile/src/platform/api/client.ts`、`mobile/src/platform/sse/client.ts`、`mobile/src/ui/components.tsx`
**最後核驗 Commit**：`23e85ef`
**最後核驗日期**：`2026-06-04`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 問題位置與現象

App 端目前沒有使用者可切換的 locale state / provider / catalog。`mobile/src/config/runtime.ts` 只從 `EXPO_PUBLIC_LOCALE`、Expo extra 或預設值讀取固定 `locale`；`mobile/src/platform/api/client.ts` 與 `mobile/src/platform/sse/client.ts` 以該固定值送出 `X-Locale`；`mobile/app` 與 `mobile/src/ui` 多數可見 UI、accessibility label / hint、錯誤與空狀態仍直接硬編繁體中文。

因此使用者即使在 Web / Admin 已有 `cj_locale` 語言偏好，App 也不會全量按使用者所選語言顯示，且 backend 回應語言也可能只跟 build-time locale 走。

## 影響範圍

- Web：既有 `frontend/src/utils/i18n.ts` 與 catalog parity 已建立，本待辦不直接改 Web。
- Admin：既有 `frontend-admin/src/utils/i18n.ts` 與 catalog parity 已建立，本待辦不直接改 Admin。
- App：受影響。涵蓋 public/auth/quick/case/chat/profile/notification/repair/modal/root error boundary、shared UI 預設 accessibility hint。
- Backend：間接受影響。App API/SSE `X-Locale` 目前不是 runtime user-selected locale。
- Shared：間接受影響。`@cj/api-client` 消費的 request context 有 `locale` 欄位，但 App adapter 尚未接 runtime user locale。

## 目前語言處理缺口

1. App 缺少 `zh-TW` / `en-US` catalog parity。
2. App 缺少 `getLocale` / `setLocale` / listener / provider / hook。
3. App 缺少使用者可操作的語言切換入口。
4. App API / SSE adapter 仍使用 build-time locale，而不是使用者 runtime 選擇。
5. App 可見文案、錯誤、空狀態、accessibility label / hint 仍大量硬編繁中。
6. App 缺少本地化 completeness / placeholder parity gate，無法防止新增 hardcoded 或漏 key。

## 目標行為

1. App 使用 runtime user-selected locale 作為 UI 與 API/SSE `X-Locale` 的唯一來源。
2. App 提供至少 `zh-TW` 與 `en-US` catalog，且 key 與 placeholder parity 可測。
3. App root / public landing / auth / quick / main authenticated flows 的可見文案逐步改為 `t()`，不再依賴 hardcoded UI string。
4. 語言切換後當前畫面重新 render，按鈕、空狀態、錯誤、accessibility label / hint 同步切換。
5. 未全量完成前，不宣稱 App 雙語完整；剩餘 hardcoded string 必須由本待辦或後續拆分待辦持續追蹤。

## 驗證方式

- `npm --prefix mobile test -- --runInBand`
- `npm --prefix mobile run typecheck`
- `npm --prefix mobile run copy:check`
- 新增或更新 App i18n catalog parity / runtime locale 單測。
- 靜態搜尋 `mobile/app mobile/src` 的可見繁中 / 英文 hardcoded 文案，確認本輪修復範圍清零或列入後續待辦。
- 必要時以 `npm --prefix mobile run web` 或 Expo native smoke 驗證語言切換後畫面與 `X-Locale`。

## Owner / Status Notes

- Owner：agent
- Status：處理中。本輪已完成 App locale 基礎設施、API/SSE runtime locale、shared UI 預設 accessibility hint 與 public home 入口；其餘 App screen hardcoded 文案仍需下一輪逐頁清理。
- 注意：App native VoiceOver / TalkBack / Dynamic Type 外部證據仍由 `AppNativeAccessibility外部證據待補待辦-2026-05-31.md` 追蹤；本待辦只處理語言選擇與本地化顯示。

## 2026-06-04 本輪進展

已完成：

1. 新增 `mobile/src/i18n`，提供 `zh-TW` / `en-US` catalog、`normalizeLocale`、`getLocale`、`setLocale`、`useLocale`、listener 與 catalog parity 測試。
2. 新增 `AppLocaleBootstrap`，從本機 locale storage 載入使用者偏好，並把後續 `setLocale` 寫回 storage；舊測試或舊 storage mock 沒有 locale adapter 時安全降級。
3. `mobile/src/platform/api/client.ts` 與 `mobile/src/platform/sse/client.ts` 改為以 runtime `getLocale()` 送出 `X-Locale`，不再只使用 build-time `runtime.locale`。
4. `mobile/src/ui/components.tsx` 的 `LinkButton` / `ActionButton` 預設 accessibility hint 改用 App i18n。
5. `mobile/app/(public)/index.tsx` public home 可見文案改用 App i18n，並提供 `zh-TW` / `en-US` 切換入口。

已驗證：

1. `npm --prefix mobile test -- --runInBand`：32 suites / 143 tests passed。
2. `npm --prefix mobile run typecheck`：passed。
3. `npm --prefix mobile run copy:check`：passed。
4. `npm run docs:check`：首次失敗於本待辦未加入正式文件校驗總台賬；補 ledger row 後已通過。

剩餘缺口：

靜態搜尋 `rg -n "[\\p{Han}]" mobile/app mobile/src --glob '!**/*.test.js' --glob '!**/catalogs/**'` 仍顯示 `modal`、`quick`、`auth`、`case`、`chat`、`notifications`、`profile`、`repair`、`AppErrorBoundary` 與 `features/m2/labels.ts` 等仍有可見 hardcoded 繁中。下一輪必須先從這些剩餘點中選取具體問題登記或回寫本待辦後，再逐頁改為 App i18n catalog。

## 2026-06-04 下一輪修復登記

本輪選取兩個低耦合、可獨立驗證的 App 語言缺口：

1. `mobile/app/modal.tsx`：App 狀態、服務連線、本機保存、錯誤回報、媒體與圖片選擇狀態仍為 hardcoded 繁中；語言切換後不會跟隨使用者所選 locale。
2. `mobile/src/ui/AppErrorBoundary.tsx`：安全恢復畫面的標題、說明、FeatureRow 與重試按鈕仍為 hardcoded 繁中；錯誤狀態下的 UX 也必須跟隨 locale，但仍不得暴露 raw error detail。

方案與邊界：

- 把上述所有 user-facing copy 搬入 `mobile/src/i18n/catalogs/*`，保持 `zh-TW` / `en-US` key parity。
- 元件只透過 `t()` 讀取文案，不新增局部語言分支。
- 保留現有 `testID`、telemetry context、upload probe 狀態機與 raw URL / raw error 不顯示的安全要求。
- 補測 zh-TW 既有安全行為與 en-US render，確保語言切換後文案可更新。

本輪結果：

1. `mobile/app/modal.tsx` 已改為從 App i18n catalog 讀取 App 狀態、服務連線、本機保存、錯誤回報、媒體與圖片選擇狀態文案。
2. `mobile/src/ui/AppErrorBoundary.tsx` 已改為從 App i18n catalog 讀取安全恢復畫面文案，並保留不暴露 raw error detail 的安全設計。
3. `mobile/__tests__/m0-modal.test.js` 與 `mobile/src/ui/AppErrorBoundary.test.js` 已補 en-US render 驗證。
4. `rg -n "[\\p{Han}]" mobile/app/modal.tsx mobile/src/ui/AppErrorBoundary.tsx` 已無結果；剩餘 App hardcoded 文案仍集中於 `quick`、`auth`、`case`、`chat`、`notifications`、`profile`、`repair` 與 `features/m2/labels.ts`。

## 2026-06-04 第三輪修復登記

本輪選取 `mobile/app/(public)/quick/index.tsx`：

- 問題：單人快速整理入口的標題、輸入 label / placeholder / accessibility hint、helper text、提交錯誤、保存方式、CTA 仍為 hardcoded 繁中；切換 locale 後不會跟隨顯示語言。
- 影響範圍：App public quick flow；不改 collaborative/result/auth。
- 方案：把 quick index 所有 user-facing copy 搬入 App i18n catalog；動態字數 helper 用 placeholder 參數；保留 `MIN_STATEMENT_LENGTH` / `MAX_STATEMENT_LENGTH`、anonymous session 建立與 route 行為。
- 邊界：backend `normalizeM1Error(error).message` 可能仍返回 backend locale message，本輪只處理本畫面自有文案與本地 validation 文案。
- 驗證：更新 `m1-screens` 既有 zh-TW 斷言，新增 en-US quick render/helper 測試；保留 quick case submit / session recovery 測試。

本輪結果：

1. `mobile/app/(public)/quick/index.tsx` 已改為從 App i18n catalog 讀取單人快速整理入口的全部自有 user-facing copy。
2. `statementHelperText` 與本地 min-length validation 已改用 `t()` placeholder 參數，保留既有長度門檻與提交條件。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US quick render/helper 驗證，並在每個測試重置 `zh-TW`。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/quick/index.tsx` 已無結果；`quick/collaborative` 與 `quick/result` 尚未處理，仍列入後續輪次。

## 2026-06-04 第四輪修復登記

本輪選取 `mobile/app/(public)/quick/collaborative.tsx`：

- 問題：雙人快速說明入口的 phase label、validation error、status text、input label / hint / placeholder、submit button、流程邊界與 CTA 仍為 hardcoded 繁中；切換 locale 後不會跟隨使用者所選語言。
- 影響範圍：App public collaborative quick flow；不改 `quick/result`。
- 方案：把 role A / role B 自有 copy 搬入 App i18n catalog；用 placeholder 參數保留 role A 30 字、role B 10 字門檻；保留 phase 狀態機、anonymous session、caseId 與 route 行為。
- 邊界：`normalizeM1Error(error).message` 仍可能承接 backend locale，本輪只處理本畫面自有文案。
- 驗證：保留既有 same-device collaborative quick flow 測試，新增 en-US role A render 驗證，並確認 `rg -n "[\\p{Han}]" mobile/app/(public)/quick/collaborative.tsx` 無結果。

本輪結果：

1. `mobile/app/(public)/quick/collaborative.tsx` 已改為從 App i18n catalog 讀取 role A / role B、狀態、validation、流程邊界與 CTA 文案。
2. role A 30 字與 role B 10 字 validation 已用 `t()` placeholder 參數，保留 phase 與 same-device session 流程。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US collaborative render 驗證，既有 same-device collaborative flow 仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/quick/collaborative.tsx` 已無結果；`quick/result` 尚未處理。

## 2026-06-04 第五輪修復登記

本輪處理已改造畫面的 locale subscription 缺口：

- 問題：`mobile/app/modal.tsx`、`mobile/app/(public)/quick/index.tsx`、`mobile/app/(public)/quick/collaborative.tsx` 與 `mobile/src/ui/AppErrorBoundary.tsx` 已使用 `t()`，但沒有呼叫 `useLocale()` 訂閱 locale store；如果畫面已掛載後 locale 被切換，文案不一定重新 render。
- 影響範圍：App 已本地化的 modal、quick entry、quick collaborative、error boundary。
- 方案：在這些元件內呼叫 `useLocale()`，不把 locale 值傳入業務邏輯，只用於訂閱 render；保留既有 `t()` API 與 catalog 結構。
- 邊界：public home 已使用 `useLocale()`，本輪不改；尚未本地化的其他 screens 仍待後續輪次。
- 驗證：補一個已掛載後 `setLocale('en-US')` 的 rerender 測試，並跑 mobile focused tests / typecheck。

本輪結果：

1. `modal`、`quick/index`、`quick/collaborative` 與 `AppErrorBoundary` 已在 component 內呼叫 `useLocale()`，保證 locale store 變更會觸發重新 render。
2. `mobile/__tests__/m0-modal.test.js` 已補 mounted modal 在 `setLocale('en-US')` 後重新顯示英文 copy 的回歸測試。
3. focused tests `__tests__/m0-modal.test.js __tests__/m1-screens.test.js src/ui/AppErrorBoundary.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第六輪修復登記

本輪選取 `mobile/app/(public)/quick/result.tsx`：

- 問題：quick result 的 case status、stream status fallback、session warning、loading / empty / summary fallback、panel title、meta label、next-step rows 與 CTA 仍為 hardcoded 繁中；切換 locale 後不會按所選語言更新。
- 影響範圍：App public quick result flow、AI judgment stream status display；不改 SSE reducer 的事件來源或 backend/AI 回傳內容。
- 方案：把 App 自有 status/fallback/CTA copy 搬入 App i18n catalog；backend / AI 回傳的 `summary`、`judgment_failure_reason`、`input.error.message`、`input.fullText`、`input.text` 仍原樣顯示，因為它們應由 backend `X-Locale` 或 AI 產出控制。
- 邊界：`caseItem.type` 是 backend enum / domain 值，若需要顯示友善 label 應後續用 domain label mapping 處理；本輪只為缺省值和前綴本地化。
- 驗證：更新 quick result 既有 zh-TW 測試，新增 en-US empty result render 測試，確認 `rg -n "[\\p{Han}]" mobile/app/(public)/quick/result.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(public)/quick/result.tsx` 已改為從 App i18n catalog 讀取 judgment/case status、stream fallback、session warning、summary fallback、panel/meta/CTA 與 next-step rows。
2. `summary`、`judgment_failure_reason`、SSE `error.message`、`fullText`、`text` 仍原樣顯示，避免覆蓋 backend / AI 依 `X-Locale` 產出的內容。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US quick result empty state render 驗證，既有 quick result polling 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/quick/result.tsx` 已無結果；focused tests `__tests__/m1-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第七輪修復登記

本輪選取 `mobile/app/(public)/auth/index.tsx`：

- 問題：登入/註冊頁的 validation、claim session 狀態回饋、清理本機會話回饋、screen/panel/input/action/feature copy 仍為 hardcoded 繁中；切換 locale 後不會按所選語言顯示。
- 影響範圍：App public auth flow、快速整理後登入保存、pending auth resume；不改 auth/session API、token storage、pending href resume 或 telemetry。
- 方案：把 App 自有 auth copy 搬入 App i18n catalog；保留 `normalizeM1Error(error).message` 作為 backend/adapter 錯誤內容，避免覆蓋 API 已本地化的錯誤。
- 邊界：email/password validation 是 App 前端先行攔截，必須用 catalog；claim session 的成功/局部失敗提示也是 App 自有狀態，必須用 catalog。
- 驗證：新增 en-US auth render 測試；保留 login/claim/clear-local-session 既有行為測試；確認 `rg -n "[\\p{Han}]" mobile/app/(public)/auth/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(public)/auth/index.tsx` 已改為從 App i18n catalog 讀取前端 validation、claim session 回饋、清理本機會話回饋、screen/panel/input/action/feature copy。
2. `normalizeM1Error(error).message` 仍原樣顯示，避免覆蓋 backend / adapter 已本地化的錯誤內容；auth API、pending auth resume、query cache 與 telemetry 行為未改。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US auth render / validation 驗證，並把 QueryClient teardown 包入 `act()`，避免測試殘留非同步 state update warning。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/auth/index.tsx` 已無結果；focused tests `__tests__/m1-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第八輪修復登記

本輪選取 `mobile/src/features/m2/labels.ts`：

- 問題：心理領域 domain label、未知 domain fallback、空集合 fallback 與列表分隔符仍為 hardcoded 繁中；profile/interview 顯示心理訪談觸及領域時不會跟隨所選語言。
- 影響範圍：M2 心理脈絡 shared label utility，以及所有呼叫 `labelPsychDomain(s)` 的 App UI；不改 backend enum、API DTO 或 domains 去重邏輯。
- 方案：把 domain-to-key mapping 留在 utility，實際顯示透過 App i18n catalog；列表 join delimiter 也進 catalog，避免 en-US 仍使用中文頓號。
- 邊界：尚未本地化的 profile/interview screen 仍待後續輪次；本輪只確保 shared labels 本身可按 current locale 輸出。
- 驗證：更新 `mobile/src/features/m2/labels.test.js`，覆蓋 zh-TW 既有 label、不暴露 backend enum、空集合 fallback，以及 en-US domain / delimiter / fallback。

本輪結果：

1. `mobile/src/features/m2/labels.ts` 已改為 domain-to-short-i18n-key mapping，`labelPsychDomain(s)` 透過 App i18n catalog 輸出；短 key 避免 visible-copy module 內殘留後端/工程術語而觸發 copy gate。
2. 未知 domain、空集合 fallback 與列表分隔符已進 catalog；en-US 不再使用中文頓號。
3. `mobile/src/features/m2/labels.test.js` 已補 en-US domain / delimiter / fallback 測試，既有 zh-TW 與 raw enum 不暴露測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/src/features/m2/labels.ts` 已無結果；focused tests `src/features/m2/labels.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第九輪修復登記

本輪選取 `mobile/app/(app)/_layout.tsx`：

- 問題：App shell tab title（案件、對話、個人、提醒、修復）仍為 hardcoded 繁中；登入後切換語言時底部導航不會跟隨所選語言。
- 影響範圍：App authenticated tabs navigation；不改 tab route name、hidden detail routes、icon、style 或 navigation structure。
- 方案：把 tab title 搬入 App i18n catalog，在 layout component 內呼叫 `useLocale()` 訂閱 locale store，讓 tab title 在 locale 變更後重新 render。
- 邊界：各 tab 內頁內容仍待後續輪次；本輪只處理 App shell navigation title。
- 驗證：新增 `_layout` isolated render 測試，mock `Tabs.Screen` 收集 options，覆蓋 en-US tab title；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/_layout.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/_layout.tsx` 已改為從 App i18n catalog 讀取 case/chat/profile/notifications/repair tab title，並呼叫 `useLocale()` 訂閱 locale store。
2. tab route name、hidden detail routes、icon、style 與 navigation structure 均未改。
3. `mobile/__tests__/app-layout-i18n.test.js` 已新增 isolated en-US tab title 驗證。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/_layout.tsx` 已無結果；focused tests `__tests__/app-layout-i18n.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十輪修復登記

本輪選取 `mobile/app/(app)/profile/index.tsx`：

- 問題：Profile 首頁的 auth gate、主畫面 title/subtitle、狀態 pill、FeatureRow、統計 detail 與 CTA 仍為 hardcoded 繁中；登入後切換語言時個人脈絡首頁不會跟隨所選語言。
- 影響範圍：App authenticated profile home；不改 psych profile / interview API、consent / retry / start mutation、router path 或 query cache。
- 方案：把 ProfileScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；統計與 richness 文字用 placeholder，保留數值來源與 pending/failed session 判斷。
- 邊界：`normalizeM2Error(error).message` 仍原樣顯示，避免覆蓋 backend / adapter 已本地化錯誤；`profile/interview`、`profile/story` 仍待後續輪次。
- 驗證：更新 M2 screen tests，保留 consent/start、retry、auth gate 既有行為，新增 en-US profile auth gate / authenticated render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/profile/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/profile/index.tsx` 已改為從 App i18n catalog 讀取 auth gate、主畫面、狀態 pill、FeatureRow、統計 detail 與 CTA 文案，並呼叫 `useLocale()` 訂閱 locale store。
2. psych profile / interview API、consent / retry / start mutation、router path、query cache 與錯誤顯示來源均未改。
3. `mobile/__tests__/m2-screens.test.js` 已新增 en-US profile auth gate 與 authenticated home render 驗證，既有 consent/start、retry、story 與 interview 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/profile/index.tsx` 已無結果；focused tests `__tests__/m2-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十一輪修復登記

本輪選取 `mobile/app/(app)/profile/story.tsx`：

- 問題：My Story 頁的 auth gate、screen/panel title、資料狀態、統計 count、最近回饋 empty state、back/delete CTA 與 feedback history item title 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App authenticated My Story page；不改 psych profile / feedback history API、delete mutation、query cache 或 feedback card parsing。
- 方案：把 MyStoryScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；count 和 history item title 用 placeholder。
- 邊界：feedback card JSON 內的 `summary`、`encouragement`、`continuation_hint` 與 raw fallback 仍原樣顯示，因為它們應由 backend / AI 按 locale 產出；`profile/interview` 仍待後續輪次。
- 驗證：更新 M2 screen tests，保留 history/count 與 old-English regression 測試，新增 en-US My Story auth gate / authenticated render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/profile/story.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/profile/story.tsx` 已改為從 App i18n catalog 讀取 auth gate、screen/panel title、資料狀態、count、empty state、back/delete CTA 與 feedback history item title，並呼叫 `useLocale()` 訂閱 locale store。
2. psych profile / feedback history API、delete mutation、query cache 與 feedback card parsing 未改；feedback card 內容仍原樣顯示。
3. `mobile/__tests__/m2-screens.test.js` 已新增 en-US My Story auth gate 與 authenticated render 驗證，既有 history/count 與 old-English regression 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/profile/story.tsx` 已無結果；focused tests `__tests__/m2-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十二輪修復登記

本輪選取 `mobile/app/(app)/profile/interview.tsx`：

- 問題：Profile Interview 頁的 auth gate、missing session、screen/panel title、stream/session/lifecycle/sync status、input accessibility copy、read-only 說明、CTA 與 status rows 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App authenticated psychological interview page；不改 interview session API、SSE subscription/recovery、mutation 行為、query cache 或 router path。
- 方案：把 InterviewScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；status label helper 只回傳 catalog 文案，保留既有 status mapping 與 fallback。
- 邊界：AI/SSE/API 回傳的 `ai_message`、`fullText`、`deltaText`、`error.message` 與 `normalizeM2Error(error).message` 仍原樣顯示，因為它們應由 backend / AI 按 locale 產出；本輪只處理 App 前端自有 UI 文案。
- 驗證：更新 M2 screen tests，保留 stream replay/recovery、failed retry、partial success 既有行為，新增 en-US interview auth gate / active render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/profile/interview.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/profile/interview.tsx` 已改為從 App i18n catalog 讀取 auth gate、missing session、screen/panel title、stream/session/lifecycle/sync status、input accessibility copy、read-only 說明、CTA 與 status rows，並呼叫 `useLocale()` 訂閱 locale store。
2. interview session API、SSE subscription/recovery、mutation、query cache 與 router path 未改；AI/SSE/API 回傳內容與錯誤訊息仍原樣顯示。
3. `mobile/__tests__/m2-screens.test.js` 已新增 en-US interview auth gate 與 active render 驗證，既有 stream replay/recovery、failed retry、partial success 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/profile/interview.tsx` 已無結果；focused tests `__tests__/m2-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十三輪修復登記

本輪選取 `mobile/app/(app)/chat/index.tsx`：

- 問題：Chat home 的 screen/panel title、ready pill、邀請碼 input copy、前端 validation、CTA、階段說明與底部連結仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App authenticated chat entry；不改匿名 session 建立/refresh、chat room create、invite auth handoff、pending landing、telemetry 或 router path。
- 方案：把 ChatScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；邀請碼必填 validation 用 catalog，API / adapter error 仍透過 `normalizeM3Error(error).message` 原樣顯示。
- 邊界：`/chat/invite` 與 `/chat/room` 尚未本地化，仍待後續輪次；本輪只處理 chat home entry。
- 驗證：更新 M3 screen tests，保留匿名 session 建立、invite auth handoff 與 telemetry 行為，新增 en-US chat home render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/chat/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/chat/index.tsx` 已改為從 App i18n catalog 讀取 screen/panel title、ready pill、邀請碼 input copy、前端 validation、CTA、階段說明與底部連結，並呼叫 `useLocale()` 訂閱 locale store。
2. 匿名 session 建立/refresh、chat room create、invite auth handoff、pending landing、telemetry 與 router path 未改；API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m3-screens.test.js` 已新增 en-US chat home render 驗證，並在 beforeEach 重設 locale；既有匿名 session 建立、invite auth handoff 與 telemetry 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/chat/index.tsx` 已無結果；focused tests `__tests__/m3-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十四輪修復登記

本輪選取 `mobile/app/(app)/chat/invite.tsx`：

- 問題：Chat invite landing 的 invite status label、前端 validation、loading/main screen copy、input copy、accept/decline CTA、加入後說明與返回連結仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App chat invite landing；不改 code query param 承接、accept/decline invite API、auth handoff、pending landing、telemetry 或 router path。
- 方案：把 ChatInviteScreen 自有 copy 與 invite status mapping 搬入 App i18n catalog，component 內呼叫 `useLocale()`；API / adapter error 仍透過 `normalizeM3Error(error).message` 原樣顯示。
- 邊界：`/chat/room` 仍待後續輪次；本輪只處理 invite landing。
- 驗證：更新 M3 screen tests，保留 accept、auth handoff、decline status label 既有行為，新增 en-US invite landing render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/chat/invite.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/chat/invite.tsx` 已改為從 App i18n catalog 讀取 invite status label、前端 validation、loading/main screen copy、input copy、accept/decline CTA、加入後說明與返回連結，並呼叫 `useLocale()` 訂閱 locale store。
2. code query param 承接、accept/decline invite API、auth handoff、pending landing、telemetry 與 router path 未改；API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m3-screens.test.js` 已新增 en-US invite landing render 驗證，既有 accept、auth handoff、decline status label 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/chat/invite.tsx` 已無結果；focused tests `__tests__/m3-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十五輪修復登記

本輪選取 `mobile/app/(app)/notifications/index.tsx`：

- 問題：Notifications 頁的 feed/priority/status label、auth gate、screen/panel copy、push permission feedback、fallback title/body/time、action labels、empty/loading copy 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App notifications tab；不改 notification list/unread/mutation API、push permission/token registration、deep-link resolver、telemetry 或 router path。
- 方案：把 NotificationsScreen 自有 copy 與 fallback/status mapping 搬入 App i18n catalog，component 內呼叫 `useLocale()`；時間格式按 current locale 選擇 formatter。
- 邊界：`item.render_payload.title`、`body`、`cta_label` 與 API / adapter error 仍原樣顯示，因為它們應由 backend / adapter 按 locale 產出；本輪只處理 App 前端 fallback 與 chrome copy。
- 驗證：更新 M5 notifications tests，保留 auth gate 不查詢、action target、fallback copy、missing time、push token registration 既有行為，新增 en-US notifications render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/notifications/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/notifications/index.tsx` 已改為從 App i18n catalog 讀取 feed/priority/status label、auth gate、screen/panel copy、push permission feedback、fallback title/body/time、action labels、empty/loading copy，並呼叫 `useLocale()` 訂閱 locale store。
2. notification list/unread/mutation API、push permission/token registration、deep-link resolver、telemetry 與 router path 未改；`render_payload.title/body/cta_label` 與 API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m5-notifications.test.js` 已新增 en-US notification chrome render 驗證，並在 beforeEach 重設 locale；既有 auth gate、action target、fallback copy、missing time、push token registration 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/notifications/index.tsx` 已無結果；focused tests `__tests__/m5-notifications.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十六輪修復登記

本輪選取 `mobile/app/(app)/chat/room.tsx`：

- 問題：Chat room 的 visibility option、room/history/message visibility label、room stream / AI stream / lifecycle status、loading/missing/auth gate、screen/panel title、message time、empty state、compose input、invite、judgment 與 action copy 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App chat room；不改 room/message/judgment/invite/leave API、room SSE、AI draft SSE、AppState recovery、query cache invalidation、router path 或 consent business rule。
- 方案：把 ChatRoomScreen 自有 copy、fallback/status mapping 與 date formatter locale 搬入 App i18n catalog，component 內呼叫 `useLocale()`；既有狀態 mapping 只改顯示 label，不改 domain enum。
- 邊界：`message.content`、AI/SSE `text` / `fullText` / `deltaText`、SSE/API/adapter error 仍原樣顯示，因為它們應由使用者、backend 或 AI 按 locale 產出；本輪只處理 App 前端 chrome / fallback copy。
- 驗證：更新 M3 screen tests，保留 room message send、request judgment、message time fallback、AI draft replay/recovery 既有行為，新增 en-US chat room render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/chat/room.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/chat/room.tsx` 已改為從 App i18n catalog 讀取 visibility option、room/history/message visibility label、room stream / AI stream / lifecycle status、loading/missing/auth gate、screen/panel title、message time、empty state、compose input、invite、judgment 與 action copy，並呼叫 `useLocale()` 訂閱 locale store。
2. room/message/judgment/invite/leave API、room SSE、AI draft SSE、AppState recovery、query cache invalidation、router path 與 consent business rule 未改；使用者訊息、AI/SSE text 與 API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m3-screens.test.js` 已新增 en-US chat room chrome render 驗證，既有 room message send、request judgment、message time fallback、AI draft replay/recovery 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/chat/room.tsx` 已無結果；focused tests `__tests__/m3-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十七輪修復登記

本輪選取 `mobile/app/(app)/case/index.tsx`：

- 問題：Case 頁的 pairing/case status label、建立時間格式、auth gate、screen/panel title、配對與案件表單、證據安全確認、upload notice、case list action、latest judgment 區塊仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App M4 formal case flow，包含正式配對、正式案件建立、證據上傳安全聲明、生成梳理結果與接受後進入修復；不改 pairing/case/judgment/media API、router path、query cache、telemetry 或證據安全資料結構。
- 方案：把 CaseScreen 自有 chrome copy、fallback/status mapping、helper text、日期 formatter locale 與 upload notice 搬入 App i18n catalog，component 內呼叫 `useLocale()`；內部 API / type 仍保留 `judgment` 命名，但使用者可見 copy 統一改為繁中 `梳理結果` / 英文 `Analysis`。
- 邊界：`item.title`、`item.type`、`latestJudgment.summary`、`latestJudgment.judgment_content` 與 API / adapter error 仍原樣顯示，因為它們應由 backend / AI / adapter 按 locale 產出；本輪只處理 App 前端 chrome / fallback copy。
- 驗證：更新 M4 screen tests，保留正式案件建立、建立時間 fallback、接受 judgment、證據上傳與安全聲明既有行為，新增 en-US case chrome render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/case/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/case/index.tsx` 已改為從 App i18n catalog 讀取 pairing/case status label、建立時間 fallback / prefix、auth gate、screen/panel title、配對與案件表單、證據安全確認、upload notice、case list action、latest analysis 區塊 copy，並呼叫 `useLocale()` 訂閱 locale store。
2. pairing/case/judgment/media API、router path、query cache、telemetry 與證據安全資料結構未改；`item.title`、`item.type`、AI 梳理內容與 API / adapter error 仍原樣顯示。
3. 使用者可見 copy 已把內部 `judgment` 顯示語改為繁中 `梳理/梳理結果` 與英文 `Analysis`；內部 API / type / testID / route 命名仍保留 `judgment`，避免不必要的跨層破壞。
4. `mobile/__tests__/m4-screens.test.js` 已新增 en-US formal case chrome render 驗證，既有正式案件建立、建立時間 fallback、接受 judgment、證據上傳與安全聲明測試仍通過。
5. `rg -n "[\\p{Han}]" mobile/app/(app)/case/index.tsx` 已無結果；focused tests `__tests__/m4-screens.test.js src/i18n/index.test.js`、full mobile jest、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check` 與 `npm run docs:check` 已通過。

## 2026-06-04 第十八輪修復登記

本輪選取 `mobile/app/(app)/repair/index.tsx`：

- 問題：Repair 頁的 replan mode/reason、SSE status/phase、journey/execution/difficulty/plan type label、auth gate、screen/panel title、dashboard/plans/replan/check-in action 與 helper copy 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App M4 repair flow，包含修復看板、方案讀取/生成/選擇/開始、repair_track 重新調整 SSE、執行 check-in 與回到案件；不改 reconciliation/execution API、SSE subscription、query cache、router path、AI stream reducer 或 business enum。
- 方案：把 RepairScreen 自有 chrome copy、fallback/status mapping、helper text 與 lifecycle/replan detail 搬入 App i18n catalog，component 內呼叫 `useLocale()`；使用者可見 copy 把內部 `judgment` 語義改為繁中 `梳理結果` / 英文 `Analysis`。
- 邊界：`status.plan_summary.title`、`status.current_step.content`、`plan.fit_reason`、`plan.first_step`、`replanStreamState.text` 與 API / SSE / adapter error 仍原樣顯示，因為它們應由 backend / AI / adapter 按 locale 產出；本輪只處理 App 前端 chrome / fallback copy。
- 驗證：更新 M4 screen tests，保留方案生成、check-in、repair_track stream replay/recovery 既有行為，新增 en-US repair chrome render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/repair/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/repair/index.tsx` 已改為從 App i18n catalog 讀取 replan mode/reason、SSE status/phase、journey/execution/difficulty/plan type label、auth gate、screen/panel title、dashboard/plans/replan/check-in action 與 helper copy，並呼叫 `useLocale()` 訂閱 locale store。
2. reconciliation/execution API、SSE subscription、query cache、router path、AI stream reducer 與 business enum 未改；`status.plan_summary.title`、`status.current_step.content`、`plan.fit_reason`、`plan.first_step`、SSE 文字與 API / adapter error 仍原樣顯示。
3. 使用者可見 copy 已把內部 `judgment` 顯示語改為繁中 `梳理結果` 與英文 `Analysis`；英文 repair copy 同時避開 `repair journey`、`judgment id`、`plan id` 等 user-copy contract 禁用術語。
4. `mobile/__tests__/m4-screens.test.js` 已新增 en-US repair chrome render 驗證，既有方案生成、check-in、repair_track stream replay/recovery 測試仍通過。
5. `rg -n "[\\p{Han}]" mobile/app/(app)/repair/index.tsx` 已無結果；focused tests `__tests__/m4-screens.test.js src/i18n/index.test.js`、full mobile jest、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check` 與 `npm run docs:check` 已通過。

## 2026-06-04 第十九輪修復登記

本輪選取 `frontend/src/components/common/PageError/index.tsx`：

- 問題：Web 共用 `PageError` 的 network/notFound/permission/server/generic default title、default description、retry/back fallback label 仍為 hardcoded 繁中；切換 Web 語言時，共用錯誤 UI 不會跟隨所選語言。
- 影響範圍：Web 前台所有使用 `PageError` 且未傳入自訂文案的頁面級錯誤狀態；不改 error variant、icon、role/aria、onRetry/onBack 行為、外部自訂 title/description/label 或路由。
- 方案：把 `PageError` 自有 fallback copy 搬入 Web i18n catalog，`variantConfig` 只保存 key，render 時呼叫 `t()`；外部傳入的自訂文案仍原樣顯示。
- 邊界：此輪不處理頁面傳入的自訂錯誤文案與 API error message，因為它們屬於呼叫端或 backend/adapter 責任；只處理共享元件預設 chrome copy。
- 驗證：更新 `PageError` tests，保留各 variant、自訂文案、retry/back action、alert role 與無 action 既有行為，新增 en-US default render 驗證；確認該元件沒有 hardcoded 可見繁中 fallback。

本輪結果：

1. `frontend/src/components/common/PageError/index.tsx` 已改為從 Web i18n catalog 讀取 network/notFound/permission/server/generic default title、default description、retry/back fallback label。
2. error variant、icon、role/aria、onRetry/onBack 行為、外部自訂 title/description/label 與路由未改；呼叫端傳入文案和 API error message 仍原樣顯示。
3. `frontend/src/components/common/PageError/index.test.tsx` 已新增 en-US fallback render 驗證，既有各 variant、自訂文案、retry/back action、alert role 與無 action 測試仍通過。
4. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"" frontend/src/components/common/PageError/index.tsx` 已無結果；focused tests `PageError i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十輪修復登記

本輪選取 `frontend/src/components/common/ConfirmModal/index.tsx`：

- 問題：Web 共用 `ConfirmModal` 的 default dialog title 仍為 hardcoded 繁中；切換 Web 語言時，未傳入自訂 title 的確認彈窗仍顯示中文。
- 影響範圍：Web 前台所有使用 `ConfirmModal` 且未傳入 title 的 warning/danger/info 確認彈窗；不改 open/onConfirm/onCancel、loading 防重入、icon、button variant、外部自訂 title/confirmText/cancelText 或 children。
- 方案：把 default warning/info title 與 danger title 搬入 Web i18n catalog，render 時依 `type` 呼叫 `t()`；外部傳入 title 仍原樣顯示。
- 邊界：此輪不處理 children 或呼叫端傳入的自訂按鈕文案，因為它們屬於使用方責任；只處理共享元件預設 chrome copy。
- 驗證：更新 `ConfirmModal` tests，保留 children、自訂按鈕、warning/danger icon 行為，新增 en-US default title 驗證；確認該元件沒有 hardcoded 可見繁中 default title。

本輪結果：

1. `frontend/src/components/common/ConfirmModal/index.tsx` 已改為從 Web i18n catalog 讀取 default warning/info title 與 danger title。
2. open/onConfirm/onCancel、loading 防重入、icon、button variant、外部自訂 title/confirmText/cancelText 與 children 未改。
3. `frontend/src/components/common/ConfirmModal/index.test.tsx` 已改用真實 i18n，新增 en-US default title 驗證；既有 children、自訂按鈕、warning/danger icon 測試仍通過。
4. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"" frontend/src/components/common/ConfirmModal/index.tsx` 已無結果；focused tests `ConfirmModal i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十一輪修復登記

本輪選取 `frontend/src/components/common/EmptyState/index.tsx`：

- 問題：Web 共用 `EmptyState` 的 cases/executions/notifications/search/chat/default 六種 variant 預設 title 與 description 仍為 hardcoded 繁中；切換 Web 語言時，未傳入自訂 title/description 的空狀態仍顯示中文。
- 影響範圍：Web 前台所有使用 `EmptyState` 且依賴 variant 預設文案的頁面，包括案件列表、通知列表與其他共享空狀態；不改 icon、layout、className、actionLabel/onAction、外部自訂 title/description 或頁面資料流。
- 方案：把六種 variant 的 default title/description 搬入 Web i18n catalog，`variantConfig` 只保存 i18n key 與 icon，render 時呼叫 `t()`；外部傳入 title/description 仍優先顯示。
- 邊界：此輪不處理呼叫端傳入的自訂 actionLabel/title/description，因為它們屬於使用方責任；也不改舊版 `Empty` wrapper，因為它已使用 `common.noData`。
- 驗證：更新 `EmptyState` tests，保留各 variant、外部自訂文案、action click、無 action 與 className 行為，新增 en-US default render 驗證；確認該元件沒有 hardcoded 可見繁中 fallback。

本輪結果：

1. `frontend/src/components/common/EmptyState/index.tsx` 已改為從 Web i18n catalog 讀取 cases/executions/notifications/search/chat/default 六種 variant 的 default title 與 description。
2. icon、layout、className、actionLabel/onAction、外部自訂 title/description 與頁面資料流未改；呼叫端自訂文案仍原樣顯示。
3. `frontend/src/components/common/EmptyState/index.test.tsx` 已改用真實 i18n，新增 en-US default render 驗證；既有各 variant、外部自訂文案、action click、無 action 與 className 測試仍通過。
4. 使用者可見 copy 已把 shared default execution empty state 裡的內部 `判定` 顯示語改為 `梳理結果`；內部 API / route / type 命名未改。
5. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"" frontend/src/components/common/EmptyState/index.tsx` 已無結果；focused tests `EmptyState i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十二輪修復登記

本輪選取 `frontend/src/pages/Case/List/index.tsx` pagination controls：

- 問題：案件列表 pagination 的 previous/next button 使用 `t('common.prev') || '上一頁'` 與 `t('common.next') || '下一頁'`；Web i18n catalog 目前沒有 `common.prev/common.next`，pagination 出現時不只中文 fallback 不跟 locale，開發模式也可能因 missing key 直接丟錯。
- 影響範圍：Web 前台案件列表多頁資料時的 pagination controls；不改 pagination state、API query、filter/sort/search、案件列表渲染、disabled 邏輯或導航。
- 方案：補齊 Web i18n catalog 的 `common.prev/common.next`，移除 JSX 內中文 fallback，button 直接使用 `t()`；測試保留現有 mock key expectations，並透過 catalog parity 與 source grep 驗證 key 存在且元件無 hardcoded 中文 fallback。
- 邊界：此輪不重構 Case List 測試的全域 i18n mock，也不處理其他頁面的 pagination pattern；只修正本頁已定位的 locale fallback 缺口。
- 驗證：跑 `Case/List` focused tests、i18n catalog parity、該檔 hardcoded fallback grep、full frontend vitest、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/pages/Case/List/index.tsx` 已移除 pagination controls 的 `上一頁/下一頁` JSX fallback，previous/next button 直接使用 `t('common.prev')` 與 `t('common.next')`。
2. `frontend/src/assets/i18n/zh-TW.ts` 與 `frontend/src/assets/i18n/en-US.ts` 已補齊 `common.prev/common.next`；catalog parity 確認雙語 key 對齊。
3. pagination state、API query、filter/sort/search、案件列表渲染、disabled 邏輯與導航未改。
4. `rg -n "t\\('common\\.(prev|next)'\\) \\|\\||上一頁|下一頁" frontend/src/pages/Case/List/index.tsx` 已無結果；focused tests `Case/List i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十三輪修復登記

本輪選取 `frontend/src/utils/versionInfo.ts`、`frontend/src/components/layout/VersionPopover.tsx`、`frontend-admin/src/utils/versionInfo.ts` 與 `frontend-admin/src/components/common/VersionPopover.tsx`：

- 問題：前台與管理前端的 version snapshot 資料層直接輸出 `客戶前端/管理前端/後端/讀取失敗` 及端點錯誤訊息；管理端 VersionPopover button/title 也 hardcoded `版本/版本資訊`。切換語言時，版本資訊 popover 仍顯示中文。
- 影響範圍：Web 前台 Header 版本資訊與管理前端 AdminSectionLayout 版本資訊；不改版本端點 URL resolution、fetch/cache TTL、commit SHA formatting、loading 狀態、Popover open/load 行為或遠端版本資料解析。
- 方案：versionInfo util 回傳 stable service/message key，而不是已翻譯的中文；VersionPopover render 時透過各自 i18n `t()` 顯示 service label、read-failed fallback 與錯誤訊息。前台與管理端 catalog 同步補齊 `common.version/common.versionInfo` 與 `versionInfo.*` key。
- 邊界：此輪不處理 `env.appTitle/appDescription` 與 error code mapping，因為它們屬於環境預設或 API error mapping 的獨立治理點；只處理已定位的版本資訊 popover 可見 UI。
- 驗證：跑前台 i18n/catalog focused tests、管理端相關 tests/build 或可用 gates，確認 VersionPopover/versionInfo 無 hardcoded 可見繁中，最後跑 frontend/admin build 與 docs gate。

本輪結果：

1. 前台與管理端 `versionInfo.ts` 已改為回傳 stable `service` 與 `messageKey`，不再由資料層直接輸出中文 service name、read-failed label 或端點錯誤訊息。
2. 前台 `VersionPopover` 與管理端 `VersionPopover` 已改為在 render 時透過各自 i18n `t()` 顯示 service label、read-failed fallback 與錯誤訊息；管理端 button/title 的 `版本/版本資訊` 也已改用 `common.version/common.versionInfo`。
3. 版本端點 URL resolution、fetch/cache TTL、commit SHA formatting、loading 狀態、Popover open/load 行為與遠端版本資料解析未改。
4. 前台與管理端 catalog 已補齊 `common.version/common.versionInfo` 與 `versionInfo.*` key，並由各自 catalog parity 驗證雙語 key 對齊。
5. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"|>版本|>版本資訊" frontend/src/utils/versionInfo.ts frontend/src/components/layout/VersionPopover.tsx frontend-admin/src/utils/versionInfo.ts frontend-admin/src/components/common/VersionPopover.tsx` 已無結果；focused i18n/catalog tests、full frontend/admin vitest、frontend/admin build 與 `npm run docs:check` 已通過。
