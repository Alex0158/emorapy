# App 語言選擇與本地化未全量承接待辦

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：問題治理
**覆蓋範圍**：App 使用者所選語言、UI 文案、API/SSE locale header、本地化 catalog 與驗收 gate
**取證代碼入口**：`mobile/app`、`mobile/src/config/runtime.ts`、`mobile/src/platform/api/client.ts`、`mobile/src/platform/sse/client.ts`、`mobile/src/ui/components.tsx`
**最後核驗 Commit**：`23e85ef`
**最後核驗日期**：`2026-06-04`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 問題位置與現象

App 端目前沒有使用者可切換的 locale state / provider / catalog。`mobile/src/config/runtime.ts` 只從 `EXPO_PUBLIC_LOCALE`、Expo extra 或預設值讀取固定 `locale`；`mobile/src/platform/api/client.ts` 與 `mobile/src/platform/sse/client.ts` 以該固定值送出 `X-Locale`；`mobile/app` 與 `mobile/src/ui` 多數可見 UI、accessibility label / hint、錯誤與空狀態仍直接硬編繁體中文。

因此使用者即使在 Web / Admin 已有 `cj_locale` 語言偏好，App 也不會全量按使用者所選語言顯示，且 backend 回應語言也可能只跟 build-time locale 走。

## 影響範圍

- Web：既有 `frontend/src/utils/i18n.ts` 與 catalog parity 已建立，本待辦不直接改 Web。
- Admin：既有 `frontend-admin/src/utils/i18n.ts` 與 catalog parity 已建立，本待辦不直接改 Admin。
- App：受影響。涵蓋 public/auth/quick/case/chat/profile/notification/repair/modal/root error boundary、shared UI 預設 accessibility hint。
- Backend：間接受影響。App API/SSE `X-Locale` 目前不是 runtime user-selected locale。
- Shared：間接受影響。`@cj/api-client` 消費的 request context 有 `locale` 欄位，但 App adapter 尚未接 runtime user locale。

## 目前語言處理缺口

1. App 缺少 `zh-TW` / `en-US` catalog parity。
2. App 缺少 `getLocale` / `setLocale` / listener / provider / hook。
3. App 缺少使用者可操作的語言切換入口。
4. App API / SSE adapter 仍使用 build-time locale，而不是使用者 runtime 選擇。
5. App 可見文案、錯誤、空狀態、accessibility label / hint 仍大量硬編繁中。
6. App 缺少本地化 completeness / placeholder parity gate，無法防止新增 hardcoded 或漏 key。

## 目標行為

1. App 使用 runtime user-selected locale 作為 UI 與 API/SSE `X-Locale` 的唯一來源。
2. App 提供至少 `zh-TW` 與 `en-US` catalog，且 key 與 placeholder parity 可測。
3. App root / public landing / auth / quick / main authenticated flows 的可見文案逐步改為 `t()`，不再依賴 hardcoded UI string。
4. 語言切換後當前畫面重新 render，按鈕、空狀態、錯誤、accessibility label / hint 同步切換。
5. 未全量完成前，不宣稱 App 雙語完整；剩餘 hardcoded string 必須由本待辦或後續拆分待辦持續追蹤。

## 驗證方式

- `npm --prefix mobile test -- --runInBand`
- `npm --prefix mobile run typecheck`
- `npm --prefix mobile run copy:check`
- 新增或更新 App i18n catalog parity / runtime locale 單測。
- 靜態搜尋 `mobile/app mobile/src` 的可見繁中 / 英文 hardcoded 文案，確認本輪修復範圍清零或列入後續待辦。
- 必要時以 `npm --prefix mobile run web` 或 Expo native smoke 驗證語言切換後畫面與 `X-Locale`。

## Owner / Status Notes

- Owner：agent
- Status：處理中。本輪已完成 App locale 基礎設施、API/SSE runtime locale、shared UI 預設 accessibility hint 與 public home 入口；其餘 App screen hardcoded 文案仍需下一輪逐頁清理。
- 注意：App native VoiceOver / TalkBack / Dynamic Type 外部證據仍由 `AppNativeAccessibility外部證據待補待辦-2026-05-31.md` 追蹤；本待辦只處理語言選擇與本地化顯示。

## 2026-06-04 本輪進展

已完成：

1. 新增 `mobile/src/i18n`，提供 `zh-TW` / `en-US` catalog、`normalizeLocale`、`getLocale`、`setLocale`、`useLocale`、listener 與 catalog parity 測試。
2. 新增 `AppLocaleBootstrap`，從本機 locale storage 載入使用者偏好，並把後續 `setLocale` 寫回 storage；舊測試或舊 storage mock 沒有 locale adapter 時安全降級。
3. `mobile/src/platform/api/client.ts` 與 `mobile/src/platform/sse/client.ts` 改為以 runtime `getLocale()` 送出 `X-Locale`，不再只使用 build-time `runtime.locale`。
4. `mobile/src/ui/components.tsx` 的 `LinkButton` / `ActionButton` 預設 accessibility hint 改用 App i18n。
5. `mobile/app/(public)/index.tsx` public home 可見文案改用 App i18n，並提供 `zh-TW` / `en-US` 切換入口。

已驗證：

1. `npm --prefix mobile test -- --runInBand`：32 suites / 143 tests passed。
2. `npm --prefix mobile run typecheck`：passed。
3. `npm --prefix mobile run copy:check`：passed。
4. `npm run docs:check`：首次失敗於本待辦未加入正式文件校驗總台賬；補 ledger row 後已通過。

剩餘缺口：

靜態搜尋 `rg -n "[\\p{Han}]" mobile/app mobile/src --glob '!**/*.test.js' --glob '!**/catalogs/**'` 仍顯示 `modal`、`quick`、`auth`、`case`、`chat`、`notifications`、`profile`、`repair`、`AppErrorBoundary` 與 `features/m2/labels.ts` 等仍有可見 hardcoded 繁中。下一輪必須先從這些剩餘點中選取具體問題登記或回寫本待辦後，再逐頁改為 App i18n catalog。

## 2026-06-04 下一輪修復登記

本輪選取兩個低耦合、可獨立驗證的 App 語言缺口：

1. `mobile/app/modal.tsx`：App 狀態、服務連線、本機保存、錯誤回報、媒體與圖片選擇狀態仍為 hardcoded 繁中；語言切換後不會跟隨使用者所選 locale。
2. `mobile/src/ui/AppErrorBoundary.tsx`：安全恢復畫面的標題、說明、FeatureRow 與重試按鈕仍為 hardcoded 繁中；錯誤狀態下的 UX 也必須跟隨 locale，但仍不得暴露 raw error detail。

方案與邊界：

- 把上述所有 user-facing copy 搬入 `mobile/src/i18n/catalogs/*`，保持 `zh-TW` / `en-US` key parity。
- 元件只透過 `t()` 讀取文案，不新增局部語言分支。
- 保留現有 `testID`、telemetry context、upload probe 狀態機與 raw URL / raw error 不顯示的安全要求。
- 補測 zh-TW 既有安全行為與 en-US render，確保語言切換後文案可更新。

本輪結果：

1. `mobile/app/modal.tsx` 已改為從 App i18n catalog 讀取 App 狀態、服務連線、本機保存、錯誤回報、媒體與圖片選擇狀態文案。
2. `mobile/src/ui/AppErrorBoundary.tsx` 已改為從 App i18n catalog 讀取安全恢復畫面文案，並保留不暴露 raw error detail 的安全設計。
3. `mobile/__tests__/m0-modal.test.js` 與 `mobile/src/ui/AppErrorBoundary.test.js` 已補 en-US render 驗證。
4. `rg -n "[\\p{Han}]" mobile/app/modal.tsx mobile/src/ui/AppErrorBoundary.tsx` 已無結果；剩餘 App hardcoded 文案仍集中於 `quick`、`auth`、`case`、`chat`、`notifications`、`profile`、`repair` 與 `features/m2/labels.ts`。

## 2026-06-04 第三輪修復登記

本輪選取 `mobile/app/(public)/quick/index.tsx`：

- 問題：單人快速整理入口的標題、輸入 label / placeholder / accessibility hint、helper text、提交錯誤、保存方式、CTA 仍為 hardcoded 繁中；切換 locale 後不會跟隨顯示語言。
- 影響範圍：App public quick flow；不改 collaborative/result/auth。
- 方案：把 quick index 所有 user-facing copy 搬入 App i18n catalog；動態字數 helper 用 placeholder 參數；保留 `MIN_STATEMENT_LENGTH` / `MAX_STATEMENT_LENGTH`、anonymous session 建立與 route 行為。
- 邊界：backend `normalizeM1Error(error).message` 可能仍返回 backend locale message，本輪只處理本畫面自有文案與本地 validation 文案。
- 驗證：更新 `m1-screens` 既有 zh-TW 斷言，新增 en-US quick render/helper 測試；保留 quick case submit / session recovery 測試。

本輪結果：

1. `mobile/app/(public)/quick/index.tsx` 已改為從 App i18n catalog 讀取單人快速整理入口的全部自有 user-facing copy。
2. `statementHelperText` 與本地 min-length validation 已改用 `t()` placeholder 參數，保留既有長度門檻與提交條件。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US quick render/helper 驗證，並在每個測試重置 `zh-TW`。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/quick/index.tsx` 已無結果；`quick/collaborative` 與 `quick/result` 尚未處理，仍列入後續輪次。

## 2026-06-04 第四輪修復登記

本輪選取 `mobile/app/(public)/quick/collaborative.tsx`：

- 問題：雙人快速說明入口的 phase label、validation error、status text、input label / hint / placeholder、submit button、流程邊界與 CTA 仍為 hardcoded 繁中；切換 locale 後不會跟隨使用者所選語言。
- 影響範圍：App public collaborative quick flow；不改 `quick/result`。
- 方案：把 role A / role B 自有 copy 搬入 App i18n catalog；用 placeholder 參數保留 role A 30 字、role B 10 字門檻；保留 phase 狀態機、anonymous session、caseId 與 route 行為。
- 邊界：`normalizeM1Error(error).message` 仍可能承接 backend locale，本輪只處理本畫面自有文案。
- 驗證：保留既有 same-device collaborative quick flow 測試，新增 en-US role A render 驗證，並確認 `rg -n "[\\p{Han}]" mobile/app/(public)/quick/collaborative.tsx` 無結果。

本輪結果：

1. `mobile/app/(public)/quick/collaborative.tsx` 已改為從 App i18n catalog 讀取 role A / role B、狀態、validation、流程邊界與 CTA 文案。
2. role A 30 字與 role B 10 字 validation 已用 `t()` placeholder 參數，保留 phase 與 same-device session 流程。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US collaborative render 驗證，既有 same-device collaborative flow 仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/quick/collaborative.tsx` 已無結果；`quick/result` 尚未處理。

## 2026-06-04 第五輪修復登記

本輪處理已改造畫面的 locale subscription 缺口：

- 問題：`mobile/app/modal.tsx`、`mobile/app/(public)/quick/index.tsx`、`mobile/app/(public)/quick/collaborative.tsx` 與 `mobile/src/ui/AppErrorBoundary.tsx` 已使用 `t()`，但沒有呼叫 `useLocale()` 訂閱 locale store；如果畫面已掛載後 locale 被切換，文案不一定重新 render。
- 影響範圍：App 已本地化的 modal、quick entry、quick collaborative、error boundary。
- 方案：在這些元件內呼叫 `useLocale()`，不把 locale 值傳入業務邏輯，只用於訂閱 render；保留既有 `t()` API 與 catalog 結構。
- 邊界：public home 已使用 `useLocale()`，本輪不改；尚未本地化的其他 screens 仍待後續輪次。
- 驗證：補一個已掛載後 `setLocale('en-US')` 的 rerender 測試，並跑 mobile focused tests / typecheck。

本輪結果：

1. `modal`、`quick/index`、`quick/collaborative` 與 `AppErrorBoundary` 已在 component 內呼叫 `useLocale()`，保證 locale store 變更會觸發重新 render。
2. `mobile/__tests__/m0-modal.test.js` 已補 mounted modal 在 `setLocale('en-US')` 後重新顯示英文 copy 的回歸測試。
3. focused tests `__tests__/m0-modal.test.js __tests__/m1-screens.test.js src/ui/AppErrorBoundary.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第六輪修復登記

本輪選取 `mobile/app/(public)/quick/result.tsx`：

- 問題：quick result 的 case status、stream status fallback、session warning、loading / empty / summary fallback、panel title、meta label、next-step rows 與 CTA 仍為 hardcoded 繁中；切換 locale 後不會按所選語言更新。
- 影響範圍：App public quick result flow、AI judgment stream status display；不改 SSE reducer 的事件來源或 backend/AI 回傳內容。
- 方案：把 App 自有 status/fallback/CTA copy 搬入 App i18n catalog；backend / AI 回傳的 `summary`、`judgment_failure_reason`、`input.error.message`、`input.fullText`、`input.text` 仍原樣顯示，因為它們應由 backend `X-Locale` 或 AI 產出控制。
- 邊界：`caseItem.type` 是 backend enum / domain 值，若需要顯示友善 label 應後續用 domain label mapping 處理；本輪只為缺省值和前綴本地化。
- 驗證：更新 quick result 既有 zh-TW 測試，新增 en-US empty result render 測試，確認 `rg -n "[\\p{Han}]" mobile/app/(public)/quick/result.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(public)/quick/result.tsx` 已改為從 App i18n catalog 讀取 judgment/case status、stream fallback、session warning、summary fallback、panel/meta/CTA 與 next-step rows。
2. `summary`、`judgment_failure_reason`、SSE `error.message`、`fullText`、`text` 仍原樣顯示，避免覆蓋 backend / AI 依 `X-Locale` 產出的內容。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US quick result empty state render 驗證，既有 quick result polling 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/quick/result.tsx` 已無結果；focused tests `__tests__/m1-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第七輪修復登記

本輪選取 `mobile/app/(public)/auth/index.tsx`：

- 問題：登入/註冊頁的 validation、claim session 狀態回饋、清理本機會話回饋、screen/panel/input/action/feature copy 仍為 hardcoded 繁中；切換 locale 後不會按所選語言顯示。
- 影響範圍：App public auth flow、快速整理後登入保存、pending auth resume；不改 auth/session API、token storage、pending href resume 或 telemetry。
- 方案：把 App 自有 auth copy 搬入 App i18n catalog；保留 `normalizeM1Error(error).message` 作為 backend/adapter 錯誤內容，避免覆蓋 API 已本地化的錯誤。
- 邊界：email/password validation 是 App 前端先行攔截，必須用 catalog；claim session 的成功/局部失敗提示也是 App 自有狀態，必須用 catalog。
- 驗證：新增 en-US auth render 測試；保留 login/claim/clear-local-session 既有行為測試；確認 `rg -n "[\\p{Han}]" mobile/app/(public)/auth/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(public)/auth/index.tsx` 已改為從 App i18n catalog 讀取前端 validation、claim session 回饋、清理本機會話回饋、screen/panel/input/action/feature copy。
2. `normalizeM1Error(error).message` 仍原樣顯示，避免覆蓋 backend / adapter 已本地化的錯誤內容；auth API、pending auth resume、query cache 與 telemetry 行為未改。
3. `mobile/__tests__/m1-screens.test.js` 已新增 en-US auth render / validation 驗證，並把 QueryClient teardown 包入 `act()`，避免測試殘留非同步 state update warning。
4. `rg -n "[\\p{Han}]" mobile/app/(public)/auth/index.tsx` 已無結果；focused tests `__tests__/m1-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第八輪修復登記

本輪選取 `mobile/src/features/m2/labels.ts`：

- 問題：心理領域 domain label、未知 domain fallback、空集合 fallback 與列表分隔符仍為 hardcoded 繁中；profile/interview 顯示心理訪談觸及領域時不會跟隨所選語言。
- 影響範圍：M2 心理脈絡 shared label utility，以及所有呼叫 `labelPsychDomain(s)` 的 App UI；不改 backend enum、API DTO 或 domains 去重邏輯。
- 方案：把 domain-to-key mapping 留在 utility，實際顯示透過 App i18n catalog；列表 join delimiter 也進 catalog，避免 en-US 仍使用中文頓號。
- 邊界：尚未本地化的 profile/interview screen 仍待後續輪次；本輪只確保 shared labels 本身可按 current locale 輸出。
- 驗證：更新 `mobile/src/features/m2/labels.test.js`，覆蓋 zh-TW 既有 label、不暴露 backend enum、空集合 fallback，以及 en-US domain / delimiter / fallback。

本輪結果：

1. `mobile/src/features/m2/labels.ts` 已改為 domain-to-short-i18n-key mapping，`labelPsychDomain(s)` 透過 App i18n catalog 輸出；短 key 避免 visible-copy module 內殘留後端/工程術語而觸發 copy gate。
2. 未知 domain、空集合 fallback 與列表分隔符已進 catalog；en-US 不再使用中文頓號。
3. `mobile/src/features/m2/labels.test.js` 已補 en-US domain / delimiter / fallback 測試，既有 zh-TW 與 raw enum 不暴露測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/src/features/m2/labels.ts` 已無結果；focused tests `src/features/m2/labels.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第九輪修復登記

本輪選取 `mobile/app/(app)/_layout.tsx`：

- 問題：App shell tab title（案件、對話、個人、提醒、修復）仍為 hardcoded 繁中；登入後切換語言時底部導航不會跟隨所選語言。
- 影響範圍：App authenticated tabs navigation；不改 tab route name、hidden detail routes、icon、style 或 navigation structure。
- 方案：把 tab title 搬入 App i18n catalog，在 layout component 內呼叫 `useLocale()` 訂閱 locale store，讓 tab title 在 locale 變更後重新 render。
- 邊界：各 tab 內頁內容仍待後續輪次；本輪只處理 App shell navigation title。
- 驗證：新增 `_layout` isolated render 測試，mock `Tabs.Screen` 收集 options，覆蓋 en-US tab title；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/_layout.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/_layout.tsx` 已改為從 App i18n catalog 讀取 case/chat/profile/notifications/repair tab title，並呼叫 `useLocale()` 訂閱 locale store。
2. tab route name、hidden detail routes、icon、style 與 navigation structure 均未改。
3. `mobile/__tests__/app-layout-i18n.test.js` 已新增 isolated en-US tab title 驗證。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/_layout.tsx` 已無結果；focused tests `__tests__/app-layout-i18n.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十輪修復登記

本輪選取 `mobile/app/(app)/profile/index.tsx`：

- 問題：Profile 首頁的 auth gate、主畫面 title/subtitle、狀態 pill、FeatureRow、統計 detail 與 CTA 仍為 hardcoded 繁中；登入後切換語言時個人脈絡首頁不會跟隨所選語言。
- 影響範圍：App authenticated profile home；不改 psych profile / interview API、consent / retry / start mutation、router path 或 query cache。
- 方案：把 ProfileScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；統計與 richness 文字用 placeholder，保留數值來源與 pending/failed session 判斷。
- 邊界：`normalizeM2Error(error).message` 仍原樣顯示，避免覆蓋 backend / adapter 已本地化錯誤；`profile/interview`、`profile/story` 仍待後續輪次。
- 驗證：更新 M2 screen tests，保留 consent/start、retry、auth gate 既有行為，新增 en-US profile auth gate / authenticated render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/profile/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/profile/index.tsx` 已改為從 App i18n catalog 讀取 auth gate、主畫面、狀態 pill、FeatureRow、統計 detail 與 CTA 文案，並呼叫 `useLocale()` 訂閱 locale store。
2. psych profile / interview API、consent / retry / start mutation、router path、query cache 與錯誤顯示來源均未改。
3. `mobile/__tests__/m2-screens.test.js` 已新增 en-US profile auth gate 與 authenticated home render 驗證，既有 consent/start、retry、story 與 interview 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/profile/index.tsx` 已無結果；focused tests `__tests__/m2-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十一輪修復登記

本輪選取 `mobile/app/(app)/profile/story.tsx`：

- 問題：My Story 頁的 auth gate、screen/panel title、資料狀態、統計 count、最近回饋 empty state、back/delete CTA 與 feedback history item title 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App authenticated My Story page；不改 psych profile / feedback history API、delete mutation、query cache 或 feedback card parsing。
- 方案：把 MyStoryScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；count 和 history item title 用 placeholder。
- 邊界：feedback card JSON 內的 `summary`、`encouragement`、`continuation_hint` 與 raw fallback 仍原樣顯示，因為它們應由 backend / AI 按 locale 產出；`profile/interview` 仍待後續輪次。
- 驗證：更新 M2 screen tests，保留 history/count 與 old-English regression 測試，新增 en-US My Story auth gate / authenticated render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/profile/story.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/profile/story.tsx` 已改為從 App i18n catalog 讀取 auth gate、screen/panel title、資料狀態、count、empty state、back/delete CTA 與 feedback history item title，並呼叫 `useLocale()` 訂閱 locale store。
2. psych profile / feedback history API、delete mutation、query cache 與 feedback card parsing 未改；feedback card 內容仍原樣顯示。
3. `mobile/__tests__/m2-screens.test.js` 已新增 en-US My Story auth gate 與 authenticated render 驗證，既有 history/count 與 old-English regression 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/profile/story.tsx` 已無結果；focused tests `__tests__/m2-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十二輪修復登記

本輪選取 `mobile/app/(app)/profile/interview.tsx`：

- 問題：Profile Interview 頁的 auth gate、missing session、screen/panel title、stream/session/lifecycle/sync status、input accessibility copy、read-only 說明、CTA 與 status rows 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App authenticated psychological interview page；不改 interview session API、SSE subscription/recovery、mutation 行為、query cache 或 router path。
- 方案：把 InterviewScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；status label helper 只回傳 catalog 文案，保留既有 status mapping 與 fallback。
- 邊界：AI/SSE/API 回傳的 `ai_message`、`fullText`、`deltaText`、`error.message` 與 `normalizeM2Error(error).message` 仍原樣顯示，因為它們應由 backend / AI 按 locale 產出；本輪只處理 App 前端自有 UI 文案。
- 驗證：更新 M2 screen tests，保留 stream replay/recovery、failed retry、partial success 既有行為，新增 en-US interview auth gate / active render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/profile/interview.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/profile/interview.tsx` 已改為從 App i18n catalog 讀取 auth gate、missing session、screen/panel title、stream/session/lifecycle/sync status、input accessibility copy、read-only 說明、CTA 與 status rows，並呼叫 `useLocale()` 訂閱 locale store。
2. interview session API、SSE subscription/recovery、mutation、query cache 與 router path 未改；AI/SSE/API 回傳內容與錯誤訊息仍原樣顯示。
3. `mobile/__tests__/m2-screens.test.js` 已新增 en-US interview auth gate 與 active render 驗證，既有 stream replay/recovery、failed retry、partial success 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/profile/interview.tsx` 已無結果；focused tests `__tests__/m2-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十三輪修復登記

本輪選取 `mobile/app/(app)/chat/index.tsx`：

- 問題：Chat home 的 screen/panel title、ready pill、邀請碼 input copy、前端 validation、CTA、階段說明與底部連結仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App authenticated chat entry；不改匿名 session 建立/refresh、chat room create、invite auth handoff、pending landing、telemetry 或 router path。
- 方案：把 ChatScreen 自有 copy 搬入 App i18n catalog，component 內呼叫 `useLocale()`；邀請碼必填 validation 用 catalog，API / adapter error 仍透過 `normalizeM3Error(error).message` 原樣顯示。
- 邊界：`/chat/invite` 與 `/chat/room` 尚未本地化，仍待後續輪次；本輪只處理 chat home entry。
- 驗證：更新 M3 screen tests，保留匿名 session 建立、invite auth handoff 與 telemetry 行為，新增 en-US chat home render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/chat/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/chat/index.tsx` 已改為從 App i18n catalog 讀取 screen/panel title、ready pill、邀請碼 input copy、前端 validation、CTA、階段說明與底部連結，並呼叫 `useLocale()` 訂閱 locale store。
2. 匿名 session 建立/refresh、chat room create、invite auth handoff、pending landing、telemetry 與 router path 未改；API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m3-screens.test.js` 已新增 en-US chat home render 驗證，並在 beforeEach 重設 locale；既有匿名 session 建立、invite auth handoff 與 telemetry 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/chat/index.tsx` 已無結果；focused tests `__tests__/m3-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十四輪修復登記

本輪選取 `mobile/app/(app)/chat/invite.tsx`：

- 問題：Chat invite landing 的 invite status label、前端 validation、loading/main screen copy、input copy、accept/decline CTA、加入後說明與返回連結仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App chat invite landing；不改 code query param 承接、accept/decline invite API、auth handoff、pending landing、telemetry 或 router path。
- 方案：把 ChatInviteScreen 自有 copy 與 invite status mapping 搬入 App i18n catalog，component 內呼叫 `useLocale()`；API / adapter error 仍透過 `normalizeM3Error(error).message` 原樣顯示。
- 邊界：`/chat/room` 仍待後續輪次；本輪只處理 invite landing。
- 驗證：更新 M3 screen tests，保留 accept、auth handoff、decline status label 既有行為，新增 en-US invite landing render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/chat/invite.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/chat/invite.tsx` 已改為從 App i18n catalog 讀取 invite status label、前端 validation、loading/main screen copy、input copy、accept/decline CTA、加入後說明與返回連結，並呼叫 `useLocale()` 訂閱 locale store。
2. code query param 承接、accept/decline invite API、auth handoff、pending landing、telemetry 與 router path 未改；API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m3-screens.test.js` 已新增 en-US invite landing render 驗證，既有 accept、auth handoff、decline status label 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/chat/invite.tsx` 已無結果；focused tests `__tests__/m3-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十五輪修復登記

本輪選取 `mobile/app/(app)/notifications/index.tsx`：

- 問題：Notifications 頁的 feed/priority/status label、auth gate、screen/panel copy、push permission feedback、fallback title/body/time、action labels、empty/loading copy 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App notifications tab；不改 notification list/unread/mutation API、push permission/token registration、deep-link resolver、telemetry 或 router path。
- 方案：把 NotificationsScreen 自有 copy 與 fallback/status mapping 搬入 App i18n catalog，component 內呼叫 `useLocale()`；時間格式按 current locale 選擇 formatter。
- 邊界：`item.render_payload.title`、`body`、`cta_label` 與 API / adapter error 仍原樣顯示，因為它們應由 backend / adapter 按 locale 產出；本輪只處理 App 前端 fallback 與 chrome copy。
- 驗證：更新 M5 notifications tests，保留 auth gate 不查詢、action target、fallback copy、missing time、push token registration 既有行為，新增 en-US notifications render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/notifications/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/notifications/index.tsx` 已改為從 App i18n catalog 讀取 feed/priority/status label、auth gate、screen/panel copy、push permission feedback、fallback title/body/time、action labels、empty/loading copy，並呼叫 `useLocale()` 訂閱 locale store。
2. notification list/unread/mutation API、push permission/token registration、deep-link resolver、telemetry 與 router path 未改；`render_payload.title/body/cta_label` 與 API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m5-notifications.test.js` 已新增 en-US notification chrome render 驗證，並在 beforeEach 重設 locale；既有 auth gate、action target、fallback copy、missing time、push token registration 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/notifications/index.tsx` 已無結果；focused tests `__tests__/m5-notifications.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十六輪修復登記

本輪選取 `mobile/app/(app)/chat/room.tsx`：

- 問題：Chat room 的 visibility option、room/history/message visibility label、room stream / AI stream / lifecycle status、loading/missing/auth gate、screen/panel title、message time、empty state、compose input、invite、judgment 與 action copy 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App chat room；不改 room/message/judgment/invite/leave API、room SSE、AI draft SSE、AppState recovery、query cache invalidation、router path 或 consent business rule。
- 方案：把 ChatRoomScreen 自有 copy、fallback/status mapping 與 date formatter locale 搬入 App i18n catalog，component 內呼叫 `useLocale()`；既有狀態 mapping 只改顯示 label，不改 domain enum。
- 邊界：`message.content`、AI/SSE `text` / `fullText` / `deltaText`、SSE/API/adapter error 仍原樣顯示，因為它們應由使用者、backend 或 AI 按 locale 產出；本輪只處理 App 前端 chrome / fallback copy。
- 驗證：更新 M3 screen tests，保留 room message send、request judgment、message time fallback、AI draft replay/recovery 既有行為，新增 en-US chat room render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/chat/room.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/chat/room.tsx` 已改為從 App i18n catalog 讀取 visibility option、room/history/message visibility label、room stream / AI stream / lifecycle status、loading/missing/auth gate、screen/panel title、message time、empty state、compose input、invite、judgment 與 action copy，並呼叫 `useLocale()` 訂閱 locale store。
2. room/message/judgment/invite/leave API、room SSE、AI draft SSE、AppState recovery、query cache invalidation、router path 與 consent business rule 未改；使用者訊息、AI/SSE text 與 API / adapter error 仍原樣顯示。
3. `mobile/__tests__/m3-screens.test.js` 已新增 en-US chat room chrome render 驗證，既有 room message send、request judgment、message time fallback、AI draft replay/recovery 測試仍通過。
4. `rg -n "[\\p{Han}]" mobile/app/(app)/chat/room.tsx` 已無結果；focused tests `__tests__/m3-screens.test.js src/i18n/index.test.js` 與 `npm --prefix mobile run typecheck` 已通過。

## 2026-06-04 第十七輪修復登記

本輪選取 `mobile/app/(app)/case/index.tsx`：

- 問題：Case 頁的 pairing/case status label、建立時間格式、auth gate、screen/panel title、配對與案件表單、證據安全確認、upload notice、case list action、latest judgment 區塊仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App M4 formal case flow，包含正式配對、正式案件建立、證據上傳安全聲明、生成梳理結果與接受後進入修復；不改 pairing/case/judgment/media API、router path、query cache、telemetry 或證據安全資料結構。
- 方案：把 CaseScreen 自有 chrome copy、fallback/status mapping、helper text、日期 formatter locale 與 upload notice 搬入 App i18n catalog，component 內呼叫 `useLocale()`；內部 API / type 仍保留 `judgment` 命名，但使用者可見 copy 統一改為繁中 `梳理結果` / 英文 `Analysis`。
- 邊界：`item.title`、`item.type`、`latestJudgment.summary`、`latestJudgment.judgment_content` 與 API / adapter error 仍原樣顯示，因為它們應由 backend / AI / adapter 按 locale 產出；本輪只處理 App 前端 chrome / fallback copy。
- 驗證：更新 M4 screen tests，保留正式案件建立、建立時間 fallback、接受 judgment、證據上傳與安全聲明既有行為，新增 en-US case chrome render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/case/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/case/index.tsx` 已改為從 App i18n catalog 讀取 pairing/case status label、建立時間 fallback / prefix、auth gate、screen/panel title、配對與案件表單、證據安全確認、upload notice、case list action、latest analysis 區塊 copy，並呼叫 `useLocale()` 訂閱 locale store。
2. pairing/case/judgment/media API、router path、query cache、telemetry 與證據安全資料結構未改；`item.title`、`item.type`、AI 梳理內容與 API / adapter error 仍原樣顯示。
3. 使用者可見 copy 已把內部 `judgment` 顯示語改為繁中 `梳理/梳理結果` 與英文 `Analysis`；內部 API / type / testID / route 命名仍保留 `judgment`，避免不必要的跨層破壞。
4. `mobile/__tests__/m4-screens.test.js` 已新增 en-US formal case chrome render 驗證，既有正式案件建立、建立時間 fallback、接受 judgment、證據上傳與安全聲明測試仍通過。
5. `rg -n "[\\p{Han}]" mobile/app/(app)/case/index.tsx` 已無結果；focused tests `__tests__/m4-screens.test.js src/i18n/index.test.js`、full mobile jest、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check` 與 `npm run docs:check` 已通過。

## 2026-06-04 第十八輪修復登記

本輪選取 `mobile/app/(app)/repair/index.tsx`：

- 問題：Repair 頁的 replan mode/reason、SSE status/phase、journey/execution/difficulty/plan type label、auth gate、screen/panel title、dashboard/plans/replan/check-in action 與 helper copy 仍為 hardcoded 繁中；切換語言時不會跟隨所選語言。
- 影響範圍：App M4 repair flow，包含修復看板、方案讀取/生成/選擇/開始、repair_track 重新調整 SSE、執行 check-in 與回到案件；不改 reconciliation/execution API、SSE subscription、query cache、router path、AI stream reducer 或 business enum。
- 方案：把 RepairScreen 自有 chrome copy、fallback/status mapping、helper text 與 lifecycle/replan detail 搬入 App i18n catalog，component 內呼叫 `useLocale()`；使用者可見 copy 把內部 `judgment` 語義改為繁中 `梳理結果` / 英文 `Analysis`。
- 邊界：`status.plan_summary.title`、`status.current_step.content`、`plan.fit_reason`、`plan.first_step`、`replanStreamState.text` 與 API / SSE / adapter error 仍原樣顯示，因為它們應由 backend / AI / adapter 按 locale 產出；本輪只處理 App 前端 chrome / fallback copy。
- 驗證：更新 M4 screen tests，保留方案生成、check-in、repair_track stream replay/recovery 既有行為，新增 en-US repair chrome render 驗證；確認 `rg -n "[\\p{Han}]" mobile/app/(app)/repair/index.tsx` 無 App 自有 hardcoded 繁中殘留。

本輪結果：

1. `mobile/app/(app)/repair/index.tsx` 已改為從 App i18n catalog 讀取 replan mode/reason、SSE status/phase、journey/execution/difficulty/plan type label、auth gate、screen/panel title、dashboard/plans/replan/check-in action 與 helper copy，並呼叫 `useLocale()` 訂閱 locale store。
2. reconciliation/execution API、SSE subscription、query cache、router path、AI stream reducer 與 business enum 未改；`status.plan_summary.title`、`status.current_step.content`、`plan.fit_reason`、`plan.first_step`、SSE 文字與 API / adapter error 仍原樣顯示。
3. 使用者可見 copy 已把內部 `judgment` 顯示語改為繁中 `梳理結果` 與英文 `Analysis`；英文 repair copy 同時避開 `repair journey`、`judgment id`、`plan id` 等 user-copy contract 禁用術語。
4. `mobile/__tests__/m4-screens.test.js` 已新增 en-US repair chrome render 驗證，既有方案生成、check-in、repair_track stream replay/recovery 測試仍通過。
5. `rg -n "[\\p{Han}]" mobile/app/(app)/repair/index.tsx` 已無結果；focused tests `__tests__/m4-screens.test.js src/i18n/index.test.js`、full mobile jest、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check` 與 `npm run docs:check` 已通過。

## 2026-06-04 第十九輪修復登記

本輪選取 `frontend/src/components/common/PageError/index.tsx`：

- 問題：Web 共用 `PageError` 的 network/notFound/permission/server/generic default title、default description、retry/back fallback label 仍為 hardcoded 繁中；切換 Web 語言時，共用錯誤 UI 不會跟隨所選語言。
- 影響範圍：Web 前台所有使用 `PageError` 且未傳入自訂文案的頁面級錯誤狀態；不改 error variant、icon、role/aria、onRetry/onBack 行為、外部自訂 title/description/label 或路由。
- 方案：把 `PageError` 自有 fallback copy 搬入 Web i18n catalog，`variantConfig` 只保存 key，render 時呼叫 `t()`；外部傳入的自訂文案仍原樣顯示。
- 邊界：此輪不處理頁面傳入的自訂錯誤文案與 API error message，因為它們屬於呼叫端或 backend/adapter 責任；只處理共享元件預設 chrome copy。
- 驗證：更新 `PageError` tests，保留各 variant、自訂文案、retry/back action、alert role 與無 action 既有行為，新增 en-US default render 驗證；確認該元件沒有 hardcoded 可見繁中 fallback。

本輪結果：

1. `frontend/src/components/common/PageError/index.tsx` 已改為從 Web i18n catalog 讀取 network/notFound/permission/server/generic default title、default description、retry/back fallback label。
2. error variant、icon、role/aria、onRetry/onBack 行為、外部自訂 title/description/label 與路由未改；呼叫端傳入文案和 API error message 仍原樣顯示。
3. `frontend/src/components/common/PageError/index.test.tsx` 已新增 en-US fallback render 驗證，既有各 variant、自訂文案、retry/back action、alert role 與無 action 測試仍通過。
4. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"" frontend/src/components/common/PageError/index.tsx` 已無結果；focused tests `PageError i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十輪修復登記

本輪選取 `frontend/src/components/common/ConfirmModal/index.tsx`：

- 問題：Web 共用 `ConfirmModal` 的 default dialog title 仍為 hardcoded 繁中；切換 Web 語言時，未傳入自訂 title 的確認彈窗仍顯示中文。
- 影響範圍：Web 前台所有使用 `ConfirmModal` 且未傳入 title 的 warning/danger/info 確認彈窗；不改 open/onConfirm/onCancel、loading 防重入、icon、button variant、外部自訂 title/confirmText/cancelText 或 children。
- 方案：把 default warning/info title 與 danger title 搬入 Web i18n catalog，render 時依 `type` 呼叫 `t()`；外部傳入 title 仍原樣顯示。
- 邊界：此輪不處理 children 或呼叫端傳入的自訂按鈕文案，因為它們屬於使用方責任；只處理共享元件預設 chrome copy。
- 驗證：更新 `ConfirmModal` tests，保留 children、自訂按鈕、warning/danger icon 行為，新增 en-US default title 驗證；確認該元件沒有 hardcoded 可見繁中 default title。

本輪結果：

1. `frontend/src/components/common/ConfirmModal/index.tsx` 已改為從 Web i18n catalog 讀取 default warning/info title 與 danger title。
2. open/onConfirm/onCancel、loading 防重入、icon、button variant、外部自訂 title/confirmText/cancelText 與 children 未改。
3. `frontend/src/components/common/ConfirmModal/index.test.tsx` 已改用真實 i18n，新增 en-US default title 驗證；既有 children、自訂按鈕、warning/danger icon 測試仍通過。
4. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"" frontend/src/components/common/ConfirmModal/index.tsx` 已無結果；focused tests `ConfirmModal i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十一輪修復登記

本輪選取 `frontend/src/components/common/EmptyState/index.tsx`：

- 問題：Web 共用 `EmptyState` 的 cases/executions/notifications/search/chat/default 六種 variant 預設 title 與 description 仍為 hardcoded 繁中；切換 Web 語言時，未傳入自訂 title/description 的空狀態仍顯示中文。
- 影響範圍：Web 前台所有使用 `EmptyState` 且依賴 variant 預設文案的頁面，包括案件列表、通知列表與其他共享空狀態；不改 icon、layout、className、actionLabel/onAction、外部自訂 title/description 或頁面資料流。
- 方案：把六種 variant 的 default title/description 搬入 Web i18n catalog，`variantConfig` 只保存 i18n key 與 icon，render 時呼叫 `t()`；外部傳入 title/description 仍優先顯示。
- 邊界：此輪不處理呼叫端傳入的自訂 actionLabel/title/description，因為它們屬於使用方責任；也不改舊版 `Empty` wrapper，因為它已使用 `common.noData`。
- 驗證：更新 `EmptyState` tests，保留各 variant、外部自訂文案、action click、無 action 與 className 行為，新增 en-US default render 驗證；確認該元件沒有 hardcoded 可見繁中 fallback。

本輪結果：

1. `frontend/src/components/common/EmptyState/index.tsx` 已改為從 Web i18n catalog 讀取 cases/executions/notifications/search/chat/default 六種 variant 的 default title 與 description。
2. icon、layout、className、actionLabel/onAction、外部自訂 title/description 與頁面資料流未改；呼叫端自訂文案仍原樣顯示。
3. `frontend/src/components/common/EmptyState/index.test.tsx` 已改用真實 i18n，新增 en-US default render 驗證；既有各 variant、外部自訂文案、action click、無 action 與 className 測試仍通過。
4. 使用者可見 copy 已把 shared default execution empty state 裡的內部 `判定` 顯示語改為 `梳理結果`；內部 API / route / type 命名未改。
5. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"" frontend/src/components/common/EmptyState/index.tsx` 已無結果；focused tests `EmptyState i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十二輪修復登記

本輪選取 `frontend/src/pages/Case/List/index.tsx` pagination controls：

- 問題：案件列表 pagination 的 previous/next button 使用 `t('common.prev') || '上一頁'` 與 `t('common.next') || '下一頁'`；Web i18n catalog 目前沒有 `common.prev/common.next`，pagination 出現時不只中文 fallback 不跟 locale，開發模式也可能因 missing key 直接丟錯。
- 影響範圍：Web 前台案件列表多頁資料時的 pagination controls；不改 pagination state、API query、filter/sort/search、案件列表渲染、disabled 邏輯或導航。
- 方案：補齊 Web i18n catalog 的 `common.prev/common.next`，移除 JSX 內中文 fallback，button 直接使用 `t()`；測試保留現有 mock key expectations，並透過 catalog parity 與 source grep 驗證 key 存在且元件無 hardcoded 中文 fallback。
- 邊界：此輪不重構 Case List 測試的全域 i18n mock，也不處理其他頁面的 pagination pattern；只修正本頁已定位的 locale fallback 缺口。
- 驗證：跑 `Case/List` focused tests、i18n catalog parity、該檔 hardcoded fallback grep、full frontend vitest、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/pages/Case/List/index.tsx` 已移除 pagination controls 的 `上一頁/下一頁` JSX fallback，previous/next button 直接使用 `t('common.prev')` 與 `t('common.next')`。
2. `frontend/src/assets/i18n/zh-TW.ts` 與 `frontend/src/assets/i18n/en-US.ts` 已補齊 `common.prev/common.next`；catalog parity 確認雙語 key 對齊。
3. pagination state、API query、filter/sort/search、案件列表渲染、disabled 邏輯與導航未改。
4. `rg -n "t\\('common\\.(prev|next)'\\) \\|\\||上一頁|下一頁" frontend/src/pages/Case/List/index.tsx` 已無結果；focused tests `Case/List i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十三輪修復登記

本輪選取 `frontend/src/utils/versionInfo.ts`、`frontend/src/components/layout/VersionPopover.tsx`、`frontend-admin/src/utils/versionInfo.ts` 與 `frontend-admin/src/components/common/VersionPopover.tsx`：

- 問題：前台與管理前端的 version snapshot 資料層直接輸出 `客戶前端/管理前端/後端/讀取失敗` 及端點錯誤訊息；管理端 VersionPopover button/title 也 hardcoded `版本/版本資訊`。切換語言時，版本資訊 popover 仍顯示中文。
- 影響範圍：Web 前台 Header 版本資訊與管理前端 AdminSectionLayout 版本資訊；不改版本端點 URL resolution、fetch/cache TTL、commit SHA formatting、loading 狀態、Popover open/load 行為或遠端版本資料解析。
- 方案：versionInfo util 回傳 stable service/message key，而不是已翻譯的中文；VersionPopover render 時透過各自 i18n `t()` 顯示 service label、read-failed fallback 與錯誤訊息。前台與管理端 catalog 同步補齊 `common.version/common.versionInfo` 與 `versionInfo.*` key。
- 邊界：此輪不處理 `env.appTitle/appDescription` 與 error code mapping，因為它們屬於環境預設或 API error mapping 的獨立治理點；只處理已定位的版本資訊 popover 可見 UI。
- 驗證：跑前台 i18n/catalog focused tests、管理端相關 tests/build 或可用 gates，確認 VersionPopover/versionInfo 無 hardcoded 可見繁中，最後跑 frontend/admin build 與 docs gate。

本輪結果：

1. 前台與管理端 `versionInfo.ts` 已改為回傳 stable `service` 與 `messageKey`，不再由資料層直接輸出中文 service name、read-failed label 或端點錯誤訊息。
2. 前台 `VersionPopover` 與管理端 `VersionPopover` 已改為在 render 時透過各自 i18n `t()` 顯示 service label、read-failed fallback 與錯誤訊息；管理端 button/title 的 `版本/版本資訊` 也已改用 `common.version/common.versionInfo`。
3. 版本端點 URL resolution、fetch/cache TTL、commit SHA formatting、loading 狀態、Popover open/load 行為與遠端版本資料解析未改。
4. 前台與管理端 catalog 已補齊 `common.version/common.versionInfo` 與 `versionInfo.*` key，並由各自 catalog parity 驗證雙語 key 對齊。
5. `rg -n "'[^']*[\\p{Han}][^']*'|\\\"[^\\\"]*[\\p{Han}][^\\\"]*\\\"|>版本|>版本資訊" frontend/src/utils/versionInfo.ts frontend/src/components/layout/VersionPopover.tsx frontend-admin/src/utils/versionInfo.ts frontend-admin/src/components/common/VersionPopover.tsx` 已無結果；focused i18n/catalog tests、full frontend/admin vitest、frontend/admin build 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十四輪修復登記

本輪選取 `frontend/src/pages/Case/Detail/index.tsx` 的 case type info value：

- 問題：案件詳情頁上方 case type badge 已透過 i18n key 顯示，但資訊區 `案件類型` 的 value 仍直接使用 `case_.type`；當 `case_.type` 是既有中文 enum（例如 `生活習慣衝突`）且 Web 語言切到英文時，詳情資訊仍顯示中文資料值。
- 影響範圍：Web 前台案件詳情資訊區 case type value 與共用 case type lookup utility；不改 API case type enum、case detail fetch、status/type badge、InfoItem layout、sub type、mode/date 欄位或未知 type fallback。
- 方案：在 `caseType` utility 提供 `getCaseTypeI18nKey(type)`，Case Detail 使用該 key 透過 `t()` 顯示；`statusTags.getCaseTypeTag` 同步使用同一 key lookup，移除重複 mapping，降低後續遺漏風險。
- 邊界：未知或後端新增的 case type 仍原樣 fallback 顯示，避免資料不可讀；此輪不改資料模型或後端 enum。
- 驗證：更新 Case Detail / statusTags 或既有 focused tests，確認已知 case type 在 render path 使用 i18n key；跑 source grep、focused tests、full frontend vitest/build 與 docs gate。

本輪結果：

1. `frontend/src/utils/caseType.ts` 已新增 `getCaseTypeI18nKey(type)`，讓已知中文 enum 可以取得對應 `caseList.type*` i18n key，未知 type 仍回傳 `undefined` 供呼叫端原樣 fallback。
2. `frontend/src/pages/Case/Detail/index.tsx` 的資訊區 `caseDetail.caseType` value 已改為已知 type 透過 `t(caseTypeI18nKey)` 顯示，避免英文 locale 下直出中文 enum。
3. `frontend/src/utils/statusTags.tsx` 已同步使用同一 `getCaseTypeI18nKey`，移除重複 mapping；status/type badge 視覺、InfoItem layout、API case type enum、case detail fetch、sub type、mode/date 欄位與未知 type fallback 未改。
4. `frontend/src/pages/Case/Detail/index.test.tsx` 已新增資訊區不直出 `生活習慣衝突` 的回歸斷言；`frontend/src/utils/caseType.test.ts` 已新增已知/未知 i18n key lookup 測試。
5. `rg -n "value=\\{case_\\.type\\}|const i18nMap|生活習慣衝突': 'caseList\\.typeLife'" frontend/src/pages/Case/Detail/index.tsx frontend/src/utils/statusTags.tsx` 已無結果；focused tests `Case/Detail statusTags caseType i18n catalogParity`、full frontend vitest、`npm --prefix frontend run build` 與 `npm run docs:check` 已通過。

## 2026-06-04 第二十五輪修復登記

本輪選取 `frontend-admin/src/services/request.ts`：

- 問題：Admin 已有 `frontend-admin/src/utils/i18n.ts` 與語言切換狀態，但 Admin API client 只建立 `createHttpClient({ baseURL })` 與 response interceptor，沒有 request interceptor 注入 `X-Locale`。因此使用者在 Admin 選擇 `en-US` 時，backend `localeMiddleware` 仍只能從缺省或瀏覽器 `Accept-Language` 判斷語言，Admin 端成功/錯誤訊息可能不按 Admin 當前所選語言顯示。
- 影響範圍：Admin 所有經 `frontend-admin/src/services/api/admin.ts` 呼叫的 backend API，包含登入、健康檢查、配置、使用者、報表、通知與媒體 provider 管理；不影響 Web/App 既有 API client。
- 方案：在 Admin request client 增加 request interceptor，於每次請求送出前讀取 `getLocale()` 並寫入 `X-Locale` header；保留既有 response envelope 正規化、錯誤轉換、Blob 下載與 query/cache 行為。
- 邊界：backend `responseFormatter` / `errorHandler` 只能翻譯已在 `backend/src/i18n/index.ts` 覆蓋的 message；本輪只修「Admin 沒有把所選 locale 傳給 backend」這個傳遞缺口，不把 backend 字典漏項混在同一輪處理。
- 驗證：新增 Admin request interceptor 單測，覆蓋 `setLocale('en-US')` 後請求 config 帶出 `X-Locale: en-US`；同時跑 Admin focused test / build 或 typecheck，以及 `npm run docs:check`。

本輪結果：

1. `frontend-admin/src/services/request.ts` 已新增 request interceptor，於每次 Admin API 請求送出前以 `getLocale()` 寫入 `X-Locale`，讓 backend `localeMiddleware`、`responseFormatter` 與 `errorHandler` 可按 Admin 當前所選語言輸出成功/錯誤訊息。
2. `frontend-admin/src/services/request.test.ts` 已新增單測，覆蓋 `setLocale('en-US')` 後請求 config 帶出 `X-Locale: en-US`。
3. 已驗證：`npm --prefix frontend-admin test -- src/services/request.test.ts src/utils/i18n.test.ts`、`npm --prefix frontend-admin run build`、`npm run docs:check` 均通過。
4. 後續若仍發現 backend 英文未翻譯，應登記為 backend i18n dictionary coverage 缺口；本輪已解決 Admin selected locale 未傳遞至 backend 的跨端承接問題。

## 2026-06-04 第二十六輪修復登記

本輪選取 `backend/src/i18n/index.ts` 的 `directEnUSMap` 覆蓋：

- 問題：backend `localeMiddleware`、`responseFormatter` 與 `errorHandler` 已能按 `X-Locale` 翻譯 `message`，但 `directEnUSMap` 只覆蓋部分成功訊息；`chat.routes`、`interview.controller`、`reconciliation.controller`、`notification.controller`、`admin.controller`、`auth.controller` 等仍有多個繁中 success message 不在 map 內。當 Web/Admin/App 傳 `X-Locale: en-US` 時，這些 API response message 仍會原樣中文返回，若前端顯示 `message` 或錯誤回退使用該欄位，就不按所選語言顯示。
- 影響範圍：backend response envelope 的 `message` 欄位；間接影響 Web/App/Admin 可能顯示 API message 的 toast、status 或 adapter error display。
- 方案：補齊掃描到的 controller/routes 成功訊息英文 mapping，保留 `zh-TW` 原樣返回；不改 response envelope schema、不改 error code first 的 `translateErrorByCode` 策略、不改 AI / SSE 自身產生的內容。
- 邊界：本輪只處理可枚舉的 static response `message`；動態 service error、AI 生成文案、資料模型 enum、內部 log 與開發診斷訊息不混入同一輪。若後續發現未掃到的 backend message，仍需按同流程登記。
- 驗證：更新 `backend/tests/unit/middleware/responseFormatter.test.ts`，覆蓋 `req.locale='en-US'` 下 chat/admin/interview/notification 代表性 success message 被翻譯；跑 focused backend test、backend build 與 `npm run docs:check`。

本輪結果：

1. `backend/src/i18n/index.ts` 已補齊掃描到的 controller/routes 靜態中文 response `message` 英文 mapping，涵蓋 chat、case、interview、reconciliation、judgment、notification、admin、auth、profile 與 psych-profile 等成功訊息。
2. 使用者可見英文 copy 已把內部 `判決` 顯示語轉為 `Analysis`；backend 原始中文 key、API response envelope、錯誤 code first 翻譯策略、AI / SSE 內容與資料模型 enum 未改。
3. `backend/tests/unit/middleware/responseFormatter.test.ts` 已新增 `en-US` 代表性 success message 翻譯測試，覆蓋 chat、interview、notification、admin 與 analysis request。
4. 靜態 coverage 掃描 controller/routes 的 70 個中文 response message，`directEnUSMap` 漏項為 0。
5. 已驗證：`npm --prefix backend test -- tests/unit/middleware/responseFormatter.test.ts --runInBand`、`npm --prefix backend run build`、`npm run docs:check` 均通過。

## 2026-06-04 第二十七輪修復登記

本輪選取 `mobile/src/platform/api/client.ts` 與 `mobile/src/platform/sse/client.ts`：

- 問題：App API / SSE adapter 會把 `X-Locale` 傳給 backend，但當 backend 沒有返回 `error.message`（例如 network error、無 body 的 401/404/500、SSE open response 無 JSON body）時，adapter 仍直接使用 shared `statusToRequestMessage()` 或固定英文 `Network request failed` / `Unknown request error`。這些 fallback 會經 `normalizeM1Error` 至 `normalizeM5Error` 顯示在 App screen，導致 zh-TW 使用者看到英文錯誤。
- 影響範圍：App platform API adapter、SSE open error adapter，以及所有顯示 `normalizeM*Error(error).message` 的 App M1-M5 畫面；不改 backend message、shared client response envelope、DTO、SSE event payload 或 screen UI。
- 方案：在 App platform layer 新增 locale-aware fallback error message helper，API / SSE adapter 在沒有 backend body message 時使用 App i18n catalog；backend 已提供的 body message 仍原樣顯示，避免覆蓋 backend 按 `X-Locale` 產出的內容。
- 邊界：本輪只處理 transport fallback；`Error` 實例自身 message、AI / SSE event body 文字與 shared client domain invalid-response fallback 如需治理，後續另行登記。
- 驗證：補 App i18n catalog key parity；更新 API / SSE adapter tests，覆蓋 zh-TW network / HTTP fallback 與 en-US parity；跑 focused mobile tests、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check` 與 `npm run docs:check`。

本輪結果：

1. `mobile/src/platform/api/errorMessages.ts` 已新增 App locale-aware fallback error helper，`mobile/src/platform/api/client.ts` 與 `mobile/src/platform/sse/client.ts` 在沒有 backend body message 時改用 App i18n fallback。
2. backend 已返回的 `error.message` 仍原樣顯示；API / SSE header、request id、session/token、response envelope、DTO、SSE event payload 與 screen UI 均未改。
3. `mobile/src/i18n/catalogs/zh-TW.ts` 與 `mobile/src/i18n/catalogs/en-US.ts` 已補齊 `appApi.error.*` key，catalog parity 測試覆蓋 key 與 placeholder 對齊。
4. `mobile/src/platform/api/client.test.js` 已覆蓋 zh-TW HTTP/network/unknown fallback 與 en-US HTTP fallback；`mobile/src/platform/sse/client.test.js` 已覆蓋 zh-TW / en-US SSE open fallback。
5. 已驗證：`npm --prefix mobile test -- src/platform/api/client.test.js src/platform/sse/client.test.js src/i18n/index.test.js --runInBand`、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check`、`npm run docs:check` 均通過。

## 2026-06-04 第二十八輪修復登記

本輪選取 `mobile/src/platform/api/client.ts` 對 shared `@cj/api-client` domain contract fallback 的承接：

- 問題：`packages/api-client` 的 M1-M5 domain client 在 response envelope 或 DTO contract drift 時會丟出固定英文 `Invalid ... response from server`；App `normalizeM1Error` 至 `normalizeM5Error` 會把 `appApiClient.normalizeError(error).message` 直接顯示在畫面上。雖然上一輪已處理 HTTP / network / SSE open fallback，但這類 shared-client invalid-response fallback 仍不按 App 所選語言顯示。
- 影響範圍：App 所有透過 shared M1-M5 client 並顯示 `normalizeM*Error(error).message` 的畫面；不改 shared `@cj/api-client`、Web/Admin 消費端、backend envelope、DTO schema、SSE event payload 或 screen UI。
- 方案：在 App platform adapter 邊界識別 `code` 以 `INVALID_` 開頭且 message 符合 `Invalid ... response from server` 的 shared-client fallback，轉為 App i18n catalog 的通用「服務回應格式異常」訊息；保留原 code/details，方便診斷與 telemetry。
- 邊界：backend 回傳的業務錯誤或已本地化 `error.message` 仍原樣顯示，即使 code 也是 `INVALID_*`，只要 message 不符合 shared-client 固定英文 pattern 就不覆蓋；`Error` instance 自身 message、AI / SSE event body 文字與 shared package 自身英文診斷字串不在本輪改動範圍。
- 驗證：補 App i18n catalog key parity；更新 API adapter tests，覆蓋 zh-TW / en-US invalid-response fallback，以及 backend-like `INVALID_*` message 不被覆蓋；跑 focused mobile tests、typecheck、copy check 與 docs gate。

本輪結果：

1. `mobile/src/platform/api/errorMessages.ts` 已新增 locale-aware invalid-response fallback helper，`mobile/src/platform/api/client.ts` 在 App adapter 邊界把 shared-client 固定英文 `Invalid ... response from server` 轉為 App i18n catalog 文案。
2. 原始 `code` 與 `details` 仍保留；backend 回傳的業務錯誤或已本地化 `error.message` 不被覆蓋，避免破壞 backend 按 `X-Locale` 產出的內容。
3. `mobile/src/i18n/catalogs/zh-TW.ts` 與 `mobile/src/i18n/catalogs/en-US.ts` 已補齊 `appApi.error.invalidResponse`，catalog parity 由既有 i18n tests 覆蓋。
4. `mobile/src/platform/api/client.test.js` 已新增 zh-TW / en-US shared-client invalid-response fallback 測試，並覆蓋 backend-like `INVALID_*` message 不被覆蓋。
5. 已驗證：`npm --prefix mobile test -- src/platform/api/client.test.js src/i18n/index.test.js --runInBand`、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check`、`npm run docs:check` 均通過。

## 2026-06-04 第二十九輪修復登記

本輪選取 `frontend-admin/src/services/request.ts` 與 `frontend-admin/src/services/api/admin.ts`：

- 問題：Admin request client 已傳 `X-Locale`，但當 backend 沒有返回 `error.message` 或 Admin service 發現 login/me response contract drift 時，仍會丟出固定英文 `Request failed`、`Request canceled`、`Network error`、`Unknown error`、`statusToRequestMessage(status)`、`Invalid admin me response from server` 等 fallback。`AdminPermissionRoute` 會把 `adminMeQuery.error.message` 顯示在身份驗證失敗 UI，因此這些 fallback 會不按 Admin 所選語言顯示。
- 影響範圍：Admin 所有經 `frontend-admin/src/services/request.ts` 的 API fallback error，以及 admin login/me contract fallback；不改 backend message、response envelope schema、query key/cache、權限判斷、login submit UX 或 Web/App client。
- 方案：在 Admin i18n catalog 補齊 `adminApi.error.*` key，request adapter 在沒有 backend body message 時使用 `t()` 產生 locale-aware fallback；admin login/me response contract fallback 也改用 catalog 文案。backend 已提供的 `error.message` 仍原樣顯示。
- 邊界：不覆蓋 backend 按 `X-Locale` 回來的業務錯誤；不把前台 Web service/SSE/AI stream fallback 混入本輪；不改 Admin login 頁既有固定失敗 toast 策略。
- 驗證：更新 Admin request tests，覆蓋 zh-TW HTTP/network/cancel/unknown fallback、en-US HTTP fallback、backend message 保留，以及 admin me invalid response 本地化；跑 Admin focused tests/build 與 docs gate。

本輪結果：

1. `frontend-admin/src/services/request.ts` 已改為在沒有 backend `error.message` 時使用 Admin i18n catalog 產生 HTTP status、request failed、request canceled、network 與 unknown fallback message；backend 已提供的 message 仍優先保留。
2. `frontend-admin/src/services/api/admin.ts` 的 admin login/me response contract fallback 已改用 `adminApi.error.invalidAdminLoginResponse` / `adminApi.error.invalidAdminMeResponse`，避免 `AdminPermissionRoute` 顯示固定英文 invalid-response。
3. `frontend-admin/src/assets/i18n/zh-TW.ts` 與 `frontend-admin/src/assets/i18n/en-US.ts` 已補齊 `adminApi.error.*` key，catalog parity 驗證雙語 key 與 placeholder 對齊。
4. `frontend-admin/src/services/request.test.ts` 已覆蓋 zh-TW HTTP/network/cancel/unknown fallback、en-US HTTP fallback、backend-provided message 保留，以及 admin me invalid response 本地化。
5. 已驗證：`npm --prefix frontend-admin test -- src/services/request.test.ts src/utils/i18n.test.ts src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend-admin run build`、`npm run docs:check` 均通過。

## 2026-06-04 第三十輪修復登記

本輪選取 `frontend/src/services/aiStream.ts`、`frontend/src/services/api/chat.ts`、`frontend/src/services/api/chatApiUtils.ts` 與 `frontend/src/services/sseRequest.ts`：

- 問題：Web 前台 AI stream、Chat stream 與 interview SSE request 已傳 `X-Locale`，但當 backend stream open response 沒有 `error.message`、stream body 缺失或 reader 中斷時，前端仍產生固定英文 `HTTP ${status}`、`No stream body found`、`SSE disconnected unexpectedly`、`No response body` 等 fallback。這些 message 會進入 stream error state、toast 或頁面 error block，切換語言時不跟隨所選語言。
- 影響範圍：Web 前台 AI stream subscription、Chat room live stream、Chat stream HTTP error helper、interview SSE request；不改 stream URL、headers、token/session/locale 傳遞、SSE parser、AI/Chat event payload、重試策略或頁面 UI。
- 方案：新增 Web stream fallback i18n helper 與 `stream.error.*` catalog key；AI/Chat/SSE transport 層在沒有 backend body message 時使用 locale-aware fallback，並保留 backend 已提供的 `error.message`。
- 邊界：AI 生成文字、chat message content、backend/SSE event body 文字與開發診斷 log 不在本輪翻譯；HTTP error code 仍保留 `HTTP_${status}`，只改使用者可見 message。
- 驗證：更新 AI stream / Chat stream / SSE request focused tests，覆蓋 zh-TW fallback、en-US fallback、backend message 保留；跑 Web focused tests、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/services/streamErrorMessages.ts` 已新增 Web stream fallback helper，集中處理 HTTP status、stream body missing、stream disconnected 與 SSE response body missing 的使用者可見文案。
2. `frontend/src/services/aiStream.ts`、`frontend/src/services/api/chat.ts`、`frontend/src/services/api/chatApiUtils.ts` 與 `frontend/src/services/sseRequest.ts` 已改為在沒有 backend body message 時使用 `stream.error.*` catalog fallback；backend 已提供的 `error.message` 仍優先保留。
3. `frontend/src/assets/i18n/zh-TW.ts` 與 `frontend/src/assets/i18n/en-US.ts` 已補齊 `stream.error.*` key，包含 `{status}` placeholder parity。
4. 已更新 / 新增 `frontend/src/services/aiStream.test.ts`、`frontend/src/services/api/chat.test.ts`、`frontend/src/services/api/chatApiUtils.test.ts`、`frontend/src/services/sseRequest.test.ts`，覆蓋 zh-TW fallback、en-US fallback、backend message 保留、body missing 與 stream disconnected。
5. 已驗證：`npm --prefix frontend test -- src/services/aiStream.test.ts src/services/api/chat.test.ts src/services/api/chatApiUtils.test.ts src/services/sseRequest.test.ts src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend run build`、`npm run docs:check` 均通過。

## 2026-06-04 第三十一輪修復登記

本輪選取 `frontend/src/utils/apiError.ts`：

- 問題：Web 前台 `getErrorMessage(error, fallbackKey)` 會優先直出 `error.message`。因此 `frontend/src/services/api/user.ts`、`frontend/src/services/api/profile.ts` 與 shared `@cj/api-client` domain client 在 response contract drift 時丟出的固定英文 `Invalid ... response from server`，會直接進入多個頁面的 toast、store error 或 error state；切換英文/繁中時都不按 Web 所選語言處理。
- 影響範圍：Web 前台所有使用 `@/utils/apiError.getErrorMessage` 的頁面/狀態管理；不改 backend message、shared package、Web request interceptor、service DTO 驗證、頁面 UI 或現有 fallbackKey 策略。
- 方案：在 Web `apiError` 邊界識別 `Invalid ... response from server` 固定英文 pattern，轉為 Web i18n catalog 的通用 response contract fallback；backend 已提供的業務 message、一般 Error message 與 fallbackKey 行為仍維持原樣。
- 邊界：不翻譯所有英文 error message，只處理已定位的 shared/service contract fallback pattern，避免覆蓋瀏覽器、第三方或後端提供的具體錯誤；舊 `utils/errorMessages.ts` 目前無 production import，本輪不混入死碼整理。
- 驗證：更新 `apiError` tests 覆蓋 zh-TW / en-US invalid-response fallback、一般 direct message 保留、fallbackKey 行為不變；跑 focused Web tests、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/utils/apiError.ts` 已新增 visible message normalization，只在 message 符合 `Invalid ... response from server` pattern 時轉為 `apiError.invalidResponse`，其他 direct message、nested `error.message` 與 `Error.message` 仍按既有行為原樣返回。
2. `frontend/src/assets/i18n/zh-TW.ts` 與 `frontend/src/assets/i18n/en-US.ts` 已補齊 `apiError.invalidResponse`，讓 Web 前台 shared/service contract fallback 跟隨目前 locale。
3. `frontend/src/utils/apiError.test.ts` 已覆蓋 zh-TW / en-US invalid-response fallback、一般英文 direct message 保留、nested message 與 fallbackKey 行為不變。
4. `frontend/src/services/api/user.ts`、`frontend/src/services/api/profile.ts` 與 shared `@cj/api-client` 的固定英文 invalid-response 目前可由 `getErrorMessage()` 邊界統一承接；未改 DTO 驗證與 service 行為。
5. 已驗證：`npm --prefix frontend test -- src/utils/apiError.test.ts src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend run build`、`npm run docs:check` 均通過。

## 2026-06-04 第三十二輪修復登記

本輪選取 `frontend/src/store/interviewStoreUtils.ts`：

- 問題：Web 前台 interview store 的 `extractInterviewErrorInfo(err)` 在收到物件型 error 但沒有 `message` 時，會回退到固定英文 `Unknown error`。該 message 會被 `frontend/src/store/interviewStore.ts` 寫入 interview error state，進而可能在訪談頁面 error block 或 toast 顯示；繁中使用者或切換語言後仍會看到英文 fallback。
- 影響範圍：Web 前台 interview / AI 問答流程的 store error normalization；不影響 backend response envelope、stream/SSE transport、shared API client、App/Admin 或已由 backend / service 提供的具體 error message。
- 方案：在 Web store 邊界把缺失 `message` 的物件型 error fallback 改為既有 `common.unknownError` i18n key；若 error 已有非空字串 `message`，仍保留原訊息，避免覆蓋 backend 已按 `X-Locale` 產出的內容或具體業務錯誤。
- 邊界：本輪只治理已定位的 object-without-message fallback；非物件 error 仍維持既有 `String(err)` 行為，避免擴大改動到錯誤型別語義與 debug 訊息處理。若後續發現 `null` / `undefined` 或 retry fallback 進入 UI，需另行登記。
- 驗證：更新 `frontend/src/store/interviewStoreUtils.test.ts`，覆蓋 zh-TW fallback 改用 `common.unknownError`、en-US fallback 仍由 catalog 輸出，以及已有 message/code/status 的 error 不被覆蓋；跑 focused Web tests、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/store/interviewStoreUtils.ts` 已改為在物件型 error 缺失有效 `message` 時使用 `t('common.unknownError')`，讓 interview error fallback 跟隨 Web 當前 locale。
2. 已有非空 `message`、`code` 與 `status` 的 error 仍按原邊界保留；非物件 error 的 `String(err)` 行為未擴大改動，避免把具體錯誤來源混入本輪治理。
3. `frontend/src/store/interviewStoreUtils.test.ts` 已覆蓋 zh-TW fallback、en-US catalog fallback，以及標準 API error message/code/status 保留。
4. 已驗證：`npm --prefix frontend test -- src/store/interviewStoreUtils.test.ts src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend run build`、`npm run docs:check` 均通過。

## 2026-06-04 第三十三輪修復登記

本輪選取 `frontend/src/components/ui/dialog.tsx` 與 `frontend/src/components/ui/sheet.tsx`：

- 問題：Web 前台 Radix UI primitive 的 Dialog / Sheet close control 仍硬編碼英文 `Close`。其中 `DialogContent` / `SheetContent` 的 icon close button 以 `sr-only` 輸出，會被螢幕閱讀器讀出；`DialogFooter showCloseButton` 也可能產生可見 `Close` 按鈕。當 Web locale 為 zh-TW 時，基礎 UI 操作文案仍不按所選語言顯示。
- 影響範圍：Web 前台所有使用 `DialogContent`、`SheetContent` 與可選 `DialogFooter showCloseButton` 的 modal / sheet；包含 accessibility label 與少量可見 fallback close button。不影響業務 modal 自己傳入的按鈕文案、Radix state、focus trap 或 open/close 行為。
- 方案：新增通用 `common.close` i18n key；Dialog / Sheet primitive render 時以 `t('common.close')` 輸出 close label。此 key 屬於全局 UI 操作詞，不重用 `quickCreate.close` 這類場景 key，避免維護語義混淆。
- 邊界：本輪只處理 UI primitive 內建 close copy；不重構 shadcn/Radix 組件 API、不改既有 modal footer 排版、不改頁面層已本地化的取消/確認文案。
- 驗證：更新或新增 UI primitive tests，覆蓋 zh-TW / en-US close label；跑 Web focused tests、catalog parity、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/assets/i18n/zh-TW.ts` 與 `frontend/src/assets/i18n/en-US.ts` 已新增通用 `common.close`，作為全局 close 操作詞。
2. `frontend/src/components/ui/dialog.tsx` 的 icon close `sr-only` 與 `DialogFooter showCloseButton` 可見 fallback button 已改用 `t('common.close')`；`frontend/src/components/ui/sheet.tsx` 的 icon close `sr-only` 也已改用同一 key。
3. `frontend/src/components/ui/dialog-sheet-i18n.test.tsx` 已新增覆蓋 Dialog / Sheet 在 zh-TW 與 en-US 下的 close accessible name，確保 screen-reader 文案與可見 fallback close button 都按 locale 顯示。
4. 已驗證：`npm --prefix frontend test -- src/components/ui/dialog-sheet-i18n.test.tsx src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend run build`、`npm run docs:check` 均通過。

## 2026-06-04 第三十四輪修復登記

本輪選取 `frontend-admin/src/App.tsx`、`frontend-admin/src/components/ui/dialog.tsx` 與 `frontend-admin/src/components/ui/sheet.tsx`：

- 問題：Admin root Suspense fallback 仍硬編碼 `Loading...`，Admin Radix UI primitive 的 Dialog / Sheet close control 也仍硬編碼英文 `Close`。Admin 已有 locale state、catalog 與語言切換；這些基礎 UI fallback 卻不按 Admin 所選語言顯示。
- 影響範圍：Admin shell lazy route loading 畫面、所有 Admin modal / sheet icon close screen-reader label，以及可選 `DialogFooter showCloseButton` 可見 fallback close button；不影響 Admin 業務頁面已本地化的按鈕、toast、表格或 API 錯誤訊息。
- 方案：App Suspense fallback 改用既有 `common.loading`；Admin catalog 新增通用 `common.close`，Dialog / Sheet primitive render 時以 `t('common.close')` 輸出 close label。保持與 Web primitive 第三十三輪一致的 key 語義與維護策略。
- 邊界：不改 router、Suspense 邏輯、Radix focus trap、Dialog/Sheet API 或業務 modal footer 內容；本輪只處理基礎 UI 內建 fallback copy。
- 驗證：新增 Admin UI primitive i18n tests 覆蓋 zh-TW / en-US close label；更新 catalog parity；跑 Admin focused tests、Admin build 與 docs gate。

本輪結果：

1. `frontend-admin/src/App.tsx` 的 Suspense fallback 已改用 `t('common.loading')`，Admin lazy route loading 畫面跟隨目前 locale。
2. `frontend-admin/src/assets/i18n/zh-TW.ts` 與 `frontend-admin/src/assets/i18n/en-US.ts` 已新增 `common.close`；`frontend-admin/src/components/ui/dialog.tsx` 與 `frontend-admin/src/components/ui/sheet.tsx` 的內建 close label 已改用此 key。
3. `frontend-admin/src/components/ui/dialog-sheet-i18n.test.tsx` 已新增 Admin primitive i18n contract 測試，覆蓋 `DialogFooter showCloseButton` 的 zh-TW / en-US copy，並確認 Dialog / Sheet icon close 不再保留硬編碼英文 source。
4. 已驗證：`npm --prefix frontend-admin test -- src/components/ui/dialog-sheet-i18n.test.tsx src/utils/i18n.test.ts src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend-admin run build`、`npm run docs:check` 均通過。

## 2026-06-04 第三十五輪修復登記

本輪選取 `mobile/app/+not-found.tsx`：

- 問題：App Expo not-found fallback screen 仍硬編碼英文 `Oops!`、`This screen doesn't exist.` 與 `Go to home screen!`。App 已有 `useLocale()` / `t()` 與 zh-TW / en-US catalog；當使用者選擇繁中時，未知路由頁仍顯示英文 header、body 與返回首頁操作。
- 影響範圍：App 所有落入 Expo Router `+not-found` 的未知路由 / deep link fallback；不影響 Web/Admin not-found、App public home、deep link handler、navigation stack 或 route matching。
- 方案：新增 `notFound.title`、`notFound.body`、`notFound.home` App catalog key；`+not-found` 以 `useLocale()` 訂閱語言變更並使用 `t()` 輸出 Stack title、body 與 Link text。
- 邊界：不改 route fallback 行為、不改 link href、不改 layout/styling、不把 not-found copy 混入 API `appApi.error.notFound` 資料缺失錯誤文案。
- 驗證：新增 App not-found i18n render test，覆蓋 zh-TW / en-US 文案；跑 App focused tests、typecheck、copy check 與 docs gate。

本輪結果：

1. `mobile/src/i18n/catalogs/zh-TW.ts` 與 `mobile/src/i18n/catalogs/en-US.ts` 已新增 `notFound.title`、`notFound.body`、`notFound.home`。
2. `mobile/app/+not-found.tsx` 已改用 `useLocale()` + `t()` 輸出 Stack title、body 與返回首頁 link，未知路由 fallback 會跟隨 App 當前語言。
3. `mobile/__tests__/not-found-i18n.test.js` 已新增 render test，覆蓋 zh-TW / en-US body、link 與 Stack title。
4. 已驗證：`npm --prefix mobile test -- __tests__/not-found-i18n.test.js src/i18n/index.test.js --runInBand`、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check`、`npm run docs:check` 均通過。

## 2026-06-04 第三十六輪修復登記

本輪選取 `frontend/src/store/interviewStore.ts` 與 `frontend/src/store/interviewStoreUtils.ts`：

- 問題：Web interview 一般 API error 已透過 `extractInterviewErrorInfo()` 使用 i18n fallback，但 stream failure path 的 `applyStreamFailure(error)` 仍直接使用 `error.message || t('interview.respondFail')`。當 mirrored stream snapshot、`stream.failed` event 或 terminal SSE error 帶入固定英文 `Invalid ... response from server` / 空白 message 時，interview error state 可能繞過 `apiError` 邊界，直接顯示不按目前語言處理的訊息。
- 影響範圍：Web 前台 `/interview/:sessionId/chat` 串流失敗後的 error state 與 error block；不影響 backend stream event schema、AI 生成內容、SSE reconnect 策略、safety alert 內容或一般 respond/skip/end API error path。
- 方案：在 interview store util 增加 stream failure message normalization：非空且不符合 shared/service invalid-response pattern 的 message 原樣保留；符合 `Invalid ... response from server` 時改用既有 `apiError.invalidResponse`；空白/缺失 message 使用 `interview.respondFail`。
- 邊界：不翻譯所有英文 stream event message，避免覆蓋 backend / AI / domain event 已提供的具體內容；只治理已知 fixed fallback pattern 與空 message fallback。
- 驗證：更新 interview store / util tests 覆蓋 zh-TW / en-US invalid-response stream failure、具體 message 保留與空 message fallback；跑 focused Web tests、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/store/interviewStoreUtils.ts` 已新增 `getInterviewStreamFailureMessage()`，統一處理 interview stream failure 可見 message：保留具體 message、轉換 shared invalid-response fixed fallback、缺失時使用 `interview.respondFail`。
2. `frontend/src/store/interviewStore.ts` 的 `applyStreamFailure()` 已改用上述 helper，避免 mirrored snapshot、`stream.failed` event 或 terminal SSE error 直接把固定英文 fallback 寫入 error state。
3. `frontend/src/store/interviewStoreUtils.test.ts` 已覆蓋 zh-TW / en-US invalid-response stream failure、具體 message 保留與空 message fallback；`frontend/src/store/interviewStore.test.ts` 已新增 store action 回歸測試。
4. 已驗證：`npm --prefix frontend test -- src/store/interviewStoreUtils.test.ts src/store/interviewStore.test.ts src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend run build`、`npm run docs:check` 均通過。

## 2026-06-04 第三十七輪修復登記

本輪選取 App AI stream subscription 與 App API error normalization 的固定 fallback 承接：

- 問題：App 串流頁面已透過 `normalizeM1Error` 至 `normalizeM4Error` 顯示 stream connection / terminal error，但 `appApiClient.normalizeError(new Error(...))` 會直出 `Error.message`；因此 JSON parse failure、shared/service `Invalid ... response from server` 固定英文、以及既有 `SSE stream disconnected` 固定英文仍可能進入 quick result、profile interview、chat room、repair track 的錯誤狀態。`useAIStreamSubscription` 預設 normalizer 對非 Error fallback 也仍硬編英文斷線訊息。
- 影響範圍：App M1 quick analysis stream、M2 interview stream、M3 chat AI stream、M4 repair track stream，以及未來未傳自訂 `normalizeError` 的 App stream hook 使用者；不改 backend/SSE event schema、AI 生成內容、screen 狀態機、重連策略或具體 backend business message。
- 方案：在 App platform i18n helper 補 stream disconnected catalog key；`appApiClient.normalizeError()` 只把已知固定 fallback pattern 轉為 App locale-aware message，包括 `Invalid ... response from server`、`SyntaxError` JSON parse failure 與 `SSE stream disconnected`，其他一般 `Error.message` 原樣保留；`useAIStreamSubscription` 預設 normalizer 改用同一 stream disconnected helper。
- 邊界：不翻譯所有英文 Error，避免覆蓋第三方、backend 或 domain 層提供的具體診斷；不覆蓋 backend 已按 `X-Locale` 返回的 `error.message`；`onClose` 無 raw error 時仍只進入 recoverable reconnect，不新增使用者可見錯誤。
- 驗證：更新 App API adapter 與 stream hook focused tests，覆蓋 zh-TW / en-US fixed fallback、一般 local Error 保留、SyntaxError invalid-response 與 hook 預設斷線 fallback；跑 focused mobile tests、typecheck、copy check 與 docs gate。

本輪結果：

1. `mobile/src/platform/api/errorMessages.ts` 已新增 `getLocalizedStreamDisconnectedMessage()`，`zh-TW` / `en-US` App catalog 已補 `appStream.error.disconnected`。
2. `mobile/src/platform/api/client.ts` 已在 `Error` instance 邊界只轉換已知固定 fallback：`Invalid ... response from server`、`SyntaxError` JSON parse failure 與 `SSE stream disconnected`；一般 `Error.message` 與 backend-provided `RequestErrorLike.message` 仍原樣保留。
3. `mobile/src/platform/sse/useAIStreamSubscription.ts` 預設 normalizer 已改用 App locale-aware stream disconnected fallback，避免未來未傳自訂 normalizer 的 stream hook 使用者產生固定英文斷線訊息。
4. `mobile/src/platform/api/client.test.js` 與 `mobile/src/platform/sse/useAIStreamSubscription.test.js` 已覆蓋 zh-TW / en-US fixed fallback、SyntaxError invalid-response、stream disconnected 與一般 local Error 保留。
5. 已驗證：`npm --prefix mobile test -- src/platform/api/client.test.js src/platform/sse/useAIStreamSubscription.test.js src/i18n/index.test.js --runInBand`、`npm --prefix mobile run typecheck`、`npm --prefix mobile run copy:check` 均通過；靜態掃描 `SSE stream disconnected` / `Invalid ... response from server` 只剩 normalization pattern，沒有新增 user-facing hardcoded fallback。

## 2026-06-04 第三十八輪修復登記

本輪選取 backend 訪談 AI stream failure payload：

- 問題：`buildInterviewStreamFailedPayload()` 直接把 `Error.message` 寫入 `stream.failed.error.message`，而 stream event 不經 Express `responseFormatter`；Web / App reducer 會保留該 message，因此 `provider down`、`db write failed` 等 runtime 診斷可能不按所選語言直接顯示。HTTP respond / skip 入口雖有 `req.locale`，但背景 stream settlement 未承接 locale。
- 影響範圍：Backend `POST /interviews/:id/respond` / `skip` 啟動的背景訪談 AI stream、Web 訪談 chat stream failure state、App M2 profile interview stream failure state；不改一般 HTTP API response、SSE schema、AI 生成內容或重連策略。
- 方案：`buildInterviewStreamFailedPayload()` 增加可選 `locale`；已知 error code 用 `translateErrorByCode(locale, code)` 產生可見 message；未知 runtime / provider / DB 診斷回 stream 專用 generic fallback：`服務內部錯誤` / `Internal service error`；controller 將 `req.locale` 傳入背景 respond / skip 任務。
- 邊界：不翻譯所有英文 `Error.message`，避免誤把第三方/provider 診斷當產品文案；zh-TW 下未知 code 且已是中文業務訊息可保留；en-US 下只接受已知 backend message map 的翻譯，否則回 generic fallback；保留 `error.code` 作治理與調試線索。
- 驗證：更新 backend payload / settlement / service focused tests，覆蓋已知 code 本地化、未知英文診斷不外露、en-US fallback 與背景 locale 傳遞；跑 backend focused tests、backend build 與 docs gate。

本輪結果：

1. `backend/src/services/interview-stream-payload-utils.ts` 已讓 `buildInterviewStreamFailedPayload()` 支持 `locale`，已知 error code 使用 backend i18n code map，未知 runtime / provider / DB 診斷改為 `服務內部錯誤` / `Internal service error`。
2. `backend/src/services/interview-response-settlement.ts` 已把 locale 傳入 failed payload builder，並保留 `error.code` 與 `latestText` 語義。
3. `backend/src/services/interview.service.ts` 與 `backend/src/controllers/interview.controller.ts` 已把 respond / skip 的 `req.locale` 傳入背景 stream 任務，避免 async settlement 丟失使用者所選語言。
4. `backend/tests/unit/services/interview-stream-payload-utils.test.ts`、`backend/tests/unit/services/interview-response-settlement.test.ts`、`backend/tests/unit/services/interview.service.test.ts` 已覆蓋已知 code 本地化、未知英文診斷不外露、en-US fallback、latestText 保留與背景 locale 傳遞。
5. 已驗證：`npm --prefix backend test -- tests/unit/services/interview-stream-payload-utils.test.ts tests/unit/services/interview-response-settlement.test.ts tests/unit/services/interview.service.test.ts --runInBand`、`npm --prefix backend run build`、`npm run docs:check` 均通過。
6. 靜態復查確認 `stream.failed` payload 不再直接發布 raw `Error.message`；剩餘 `error.message` 只在受控 helper 內用於 code/map/generic fallback 判斷。

## 2026-06-04 第三十九輪修復登記

本輪選取 Admin Web request unknown error fallback：

- 問題：`frontend-admin/src/services/request.ts` 已對 HTTP status、network、cancel 與 backend-empty message fallback 使用 `t(...)`，但 unknown error branch 仍使用 `error.message || t('adminApi.error.unknown')`。當 adapter / runtime 丟出 `new Error('boom')` 這類非 Axios error 時，上層 Admin page 可能直接顯示 raw message，不按 Admin 當前語言處理。
- 影響範圍：Admin Web 所有使用全局 request service 的 API consumer；只影響無 `response`、無 `request`、非 cancel 的 unknown branch；不影響 backend 已按 `X-Locale` 回傳的 message、HTTP status fallback、network fallback 或 cancel fallback。
- 方案：unknown branch reject payload 固定使用 `t('adminApi.error.unknown')`，保留 `UNKNOWN_ERROR` code；補測試覆蓋 en-US 下 `new Error('boom')` 不外露 raw message。
- 邊界：不覆蓋 backend-provided message；不新增 page-level fallback map；raw runtime message 可留在開發者 log，但不作使用者可見文案。
- 驗證：跑 Admin request focused tests、Admin build 與 docs gate。

本輪結果：

1. `frontend-admin/src/services/request.ts` 的 unknown error branch 已改為固定使用 `t('adminApi.error.unknown')`，不再把 raw `error.message` 作 reject payload。
2. `frontend-admin/src/services/request.test.ts` 已新增 `new Error('boom')` 回歸測試，覆蓋 en-US 下不外露 raw runtime message。
3. 保留 HTTP response backend-provided message、status fallback、network fallback 與 cancel fallback 的既有語義。
4. 已驗證：`npm --prefix frontend-admin test -- src/services/request.test.ts src/utils/i18n.test.ts src/assets/i18n/catalogParity.test.ts`、`npm --prefix frontend-admin run build`、`npm run docs:check` 均通過。
5. 靜態復查確認 `frontend-admin/src/services/request.ts` unknown branch 只使用 `adminApi.error.unknown` catalog fallback。

## 2026-06-04 第四十輪修復登記

本輪選取 backend 非訪談 AI stream failure payload：

- 問題：訪談 `stream.failed` payload 已補 locale-aware 治理，但 M3 Chat AI 仍直接發布 `err.message`，M4 Repair replan 仍直接發布 `error.message` 或固定繁中 `AI 重調失敗`，M1 Judgment stream failure 雖用受控 `failureReason`，但固定繁中。這些 event 不經 Express `responseFormatter`，Web / App reducer 會直接使用 `event.error.message`。
- 影響範圍：M1 快速/正式案件梳理 stream failure、M3 Chat room AI response stream failure、M4 Repair track replan stream failure；不改 AI 生成內容、SSE schema、reconnect / persisted / cancelled 狀態機。
- 方案：新增共用 `buildAIStreamFailurePayload()`；已知 stream error code 使用 backend i18n code map；受控繁中 fallback message 在 en-US 下用 backend message map；未知 provider/runtime/DB 診斷回 generic fallback；controller/service 把 `req.locale` 傳入 judgment、chat AI 與 repair replan async stream。
- 邊界：不翻譯所有英文 `Error.message`；保留 `error.code`；Judgment DB 的 `judgment_failure_reason` 仍可保存既有繁中內部原因，本輪只治理 stream event 可見 payload。
- 驗證：新增/更新 backend helper、chat AI、chat judgment、execution、judgment focused tests；跑 backend build 與 docs gate。

本輪結果：

1. `backend/src/services/ai-stream-failure-payload-utils.ts` 已新增共用 `buildAIStreamFailurePayload()`，集中處理 AI stream failed 可見 payload；已知 code 使用 backend i18n code map，受控 backend message 使用 message map，未知 runtime/provider 診斷回 generic fallback。
2. `backend/src/services/judgment.service.ts`、`backend/src/services/chat-ai-orchestrator.service.ts`、`backend/src/services/execution.service.ts` 已改用共用 helper，M1 judgment、M3 chat AI、M4 repair replan 的 `stream.failed.error.message` 不再直接承接 raw `Error.message` 或固定繁中 fallback。
3. `backend/src/controllers/judgment.controller.ts`、`backend/src/controllers/case.controller.ts`、`backend/src/routes/chat.routes.ts`、`backend/src/controllers/reconciliation.controller.ts`、`backend/src/services/chat.service.ts` 已把 `req.locale` / request options locale 傳入對應 async stream 任務，避免背景任務丟失使用者所選語言。
4. `backend/tests/unit/services/ai-stream-failure-payload-utils.test.ts`、`backend/tests/unit/services/chat-ai-orchestrator.service.test.ts`、`backend/tests/unit/services/chat.service.test.ts`、`backend/tests/unit/services/execution.service.test.ts`、`backend/tests/unit/services/judgment.service.test.ts`、`backend/tests/unit/controllers/judgment.controller.test.ts`、`backend/tests/unit/routes/chat.routes.test.ts` 已覆蓋 code localization、未知 raw message 不外露、chat judgment locale 傳遞與 repair replan 背景任務 locale 傳遞。
5. 已驗證：`npm --prefix backend test -- tests/unit/services/ai-stream-failure-payload-utils.test.ts tests/unit/services/chat-ai-orchestrator.service.test.ts tests/unit/services/chat.service.test.ts tests/unit/services/execution.service.test.ts tests/unit/services/judgment.service.test.ts tests/unit/controllers/judgment.controller.test.ts tests/unit/routes/chat.routes.test.ts --runInBand` 通過 7 suites / 179 tests；`npm --prefix backend run build` 通過。

## 2026-06-04 第四十一輪修復登記

本輪選取 Web Chat Room action error feedback：

- 問題：`frontend/src/pages/Chat/Room/chatRoomUtils.ts` 的 `getSendMessageErrorFeedback()` / `getRoomMutationErrorFeedback()` 對一般錯誤使用 `getErrorMessage(error, fallbackKey)`，現有測試明確要求 `new Error("發送失敗")` / `new Error("建立邀請失敗")` 原樣顯示。這會讓 shared client、backend route 或 runtime 傳入的固定中文 / raw 診斷直接成為 toast，不按 Web 當前語言顯示。
- 影響範圍：Web Chat Room 送訊息、建立邀請、接受/拒絕邀請、發起梳理、離開房間、移除 B 方等 action failure toast；不改 Chat content、AI 生成內容、SSE schema、room 狀態機或 shared API transport。
- 方案：Chat Room action helper 改為 code-specific 分支優先，未知一般 message 回 caller 的 locale catalog fallback；已知 fixed diagnostic 仍可用 `getErrorMessage()` normalization；測試改掉“保留後端 message”舊契約，覆蓋 en-US / zh-TW raw message 不外露。
- 邊界：不全局修改 `getErrorMessage()`，避免影響其他頁面的既有受控 message 顯示；本輪只治理 Chat Room action toast。
- 驗證：更新 Chat Room helper focused tests，跑 frontend focused tests、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/pages/Chat/Room/chatRoomUtils.ts` 已新增 Chat action 專用錯誤文案邊界；一般 API/runtime `Error.message` 不再直接成為 action toast，改回 `chat.message.*` locale catalog fallback。
2. session / conflict / permission code-specific 分支維持原有專用文案；`Invalid ... from server` fixed diagnostic 仍交由 `getErrorMessage()` normalization，避免破壞既有前端資料契約錯誤處理。
3. `frontend/src/pages/Chat/Room/chatRoomUtils.test.ts` 已覆蓋 en-US / zh-TW raw message 不外露與 fixed diagnostic 本地化；`frontend/src/pages/Chat/Room/index.test.tsx` 已同步頁面 toast 舊契約，送訊息、建立邀請、發起梳理等 action failure 不再期待 raw message。
4. 已驗證：`npm --prefix frontend test -- src/pages/Chat/Room/index.test.tsx` 通過 1 file / 113 tests；`npm --prefix frontend test -- src/pages/Chat/Room/chatRoomUtils.test.ts src/pages/Chat/Room/index.test.tsx src/utils/apiError.test.ts src/assets/i18n/catalogParity.test.ts` 通過 4 files / 172 tests；`npm --prefix frontend run build` 通過。

## 2026-06-04 第四十二輪修復登記

本輪選取 Web 前台全局 request interceptor HTTP error toast / reject payload：

- 問題：`frontend/src/services/request.ts` 雖已帶 `X-Locale` 並有 `getHttpErrorFallbackMessage()`，但 400 / 401 / 403 / 404 / 409 / 422 / 413 / 429 / 503 / 500 toast 仍大量優先使用 `errorData?.message`，reject payload `message` 也仍是 `errorData?.message || fallbackMessage`。當後端固定繁中 message、shared adapter message 或 runtime diagnostic 進入 response body 時，Web 會蓋過目前所選語言的 catalog fallback。
- 影響範圍：Web 前台所有使用全局 request service 的 API consumer；直接影響全局 HTTP error toast，以及頁面/store 後續讀取 reject payload `message` 的可見錯誤。
- 方案：HTTP response 分支可見文案統一改為 status / code / request context 的 locale fallback；session refresh / expired 類保留刷新與 suppress 邏輯但不顯示 raw backend message；reject payload 預設使用 locale fallback，保留 `code` / `details` 給頁面分支。
- 邊界：不改 backend formatter、不改 Admin request service、不改 stream payload helper、不全局修改 `getErrorMessage()`；若未來需要顯示某些 backend 受控 message，必須建立顯式白名單。
- 驗證：更新 `frontend/src/services/request.test.ts` 的 raw message 不外露覆蓋，跑 frontend focused tests、frontend build 與 docs gate。

本輪結果：

1. `frontend/src/services/request.ts` 已把 `success=false` reject payload 改為 `common.requestFail` catalog fallback，不再把 response body `error.message` 作 UI fallback。
2. HTTP error toast 已統一使用 `getHttpErrorFallbackMessage()` 產出的 status / code / request context fallback；400 / 401 session refresh 分支保留 clear / refresh / suppress 行為，但不再顯示 backend message。
3. HTTP reject payload `message` 已改為 locale fallback，保留 `code` / `details`，讓頁面/store 可繼續做業務分支但不默認外露 raw message。
4. `frontend/src/services/request.test.ts` 已覆蓋 `success=false`、400 validation、400 session refresh、401 unauthorized、500 server error 的 raw backend message 不外露；靜態掃描確認 `frontend/src/services/request.ts` 不再有 `errorData?.message` 可見 fallback。
5. 已驗證：`npm --prefix frontend test -- src/services/request.test.ts src/assets/i18n/catalogParity.test.ts` 通過 2 files / 64 tests；`npm --prefix frontend run build` 通過。

## 2026-06-04 第四十三輪修復登記

本輪選取 App platform API client / SSE open error normalization：

- 問題：`mobile/src/platform/api/client.ts` 的 Axios error normalization 與 `mobile/src/platform/sse/client.ts` 的 SSE open error normalization 仍使用 `bodyError.message ?? getLocalizedStatusMessage(...)`。App M1-M5 feature API/SSE 共用這些入口，因此 response body 固定繁中 message 或 raw diagnostic 會蓋過 App 當前語言 fallback。
- 影響範圍：App M1 quick result / judgment API+SSE、M2 profile/interview API、M3 chat API+SSE、M4 repair API、M5 notification/telemetry 等 platform API consumer。
- 方案：App Axios response error 與 SSE open error 的可見 message 改用 status/network locale fallback；保留 `code` / `details`；shared invalid-response diagnostic 仍保留既有 normalization。
- 邊界：不改 backend formatter、App `X-Locale` header、feature API shape 或 stream event reducer；runtime `Error.message` 分支另行後續掃描。
- 驗證：新增 App platform API/SSE focused tests，跑 mobile focused tests、mobile typecheck 與 docs gate。

本輪結果：

1. `mobile/src/platform/api/client.ts` 的 Axios response error normalization 已改用 status / network locale fallback，不再直接承接 response body `message`。
2. `mobile/src/platform/sse/client.ts` 的 SSE open error normalization 已改用 status locale fallback；`code` / `details` 保留給 feature 層做業務分支。
3. `mobile/src/platform/api/client.test.js` 與 `mobile/src/platform/sse/client.test.js` 已覆蓋 body 固定繁中 message 不外露、code/details 保留、en-US fallback 顯示。
4. 已驗證：`npm --prefix mobile run test -- src/platform/api/client.test.js src/platform/sse/client.test.js --runInBand` 通過 2 suites / 10 tests；`npm --prefix mobile run typecheck` 通過；靜態掃描確認 platform API/SSE client 不再用 `bodyError.message` 作可見 fallback。

## 2026-06-04 第四十四輪修復登記

本輪選取 Admin Web request success=false / HTTP response message fallback：

- 問題：`frontend-admin/src/services/request.ts` 的 success=false envelope 分支仍使用 `err.message || t('adminApi.error.requestFailed')`，HTTP response 分支仍使用 `apiError.message || getLocalizedStatusMessage(status)`；現有測試明確要求 backend-provided message 優先於 Admin fallback。
- 影響範圍：Admin Web 所有透過全局 request service 的 API consumer；不影響 unknown runtime branch、network/cancel fallback 或 invalid Admin identity fixed diagnostic。
- 方案：success=false 可見 message 改為 `adminApi.error.requestFailed`；HTTP response 可見 message 改為 status-based Admin locale fallback；保留 `code` / `details`。
- 邊界：不改 backend formatter、不改 Admin `X-Locale` header、不新增 page-level 白名單；如需顯示 backend 受控 message，後續必須顯式白名單。
- 驗證：更新 Admin request focused tests，跑 Admin focused tests、Admin build 與 docs gate。

本輪結果：

1. `frontend-admin/src/services/request.ts` 的 success=false envelope 分支已改為 `adminApi.error.requestFailed` catalog fallback，不再直接承接 response body `message`。
2. Admin HTTP response error 分支已改為 status-based Admin locale fallback；`code` / `details` 保留給頁面做業務分支。
3. `frontend-admin/src/services/request.test.ts` 已把「backend-provided message 優先」舊契約改為 raw message 不外露，並新增 success=false envelope message 不外露測試。
4. 已驗證：`npm --prefix frontend-admin test -- src/services/request.test.ts src/assets/i18n/catalogParity.test.ts` 通過 2 files / 9 tests；`npm --prefix frontend-admin run build` 通過；靜態掃描確認 Admin request service 不再用 `err.message` / `apiError.message` 作可見 fallback。
