# 安全元資料 Schema 同步待辦（2026-05-03）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：問題治理
**覆蓋範圍**：安全元資料 schema migration、dev/release DB parity、發布前資料庫核驗
**取證代碼入口**：`backend/prisma/schema.prisma`、`backend/prisma/migrations/20260503224500_add_safety_metadata_columns/migration.sql`、`backend/src/controllers/evidence.controller.ts`、`backend/src/services/case.service.ts`、`backend/src/utils/product-safety-policy.ts`
**最後核驗 Commit**：`664ef1d`
**最後核驗日期**：`2026-05-03`
<!-- CORE_DOC_AUDIT_METADATA:END -->

**狀態**：待處理
**優先級**：P0，涉及 dev / release DB schema parity
**責任範圍**：Backend / Database / Release Ops

## 背景

後端已新增 migration：

- `backend/prisma/migrations/20260503224500_add_safety_metadata_columns/migration.sql`

此 migration additive 新增：

- `cases.safety_metadata JSONB`
- `evidences.safety_metadata JSONB`
- 對早期 `evidences.description` transitional JSON 做 best-effort backfill
- `cases_safety_metadata_idx`
- `evidences_safety_metadata_idx`

後端寫入路徑已改為：

- evidence upload assertion -> `Evidence.safety_metadata`
- formal case assertion -> `Case.safety_metadata`
- formal case assertion + `evidence_urls` -> 同批 `Evidence.safety_metadata`

## 必須同步的兩邊

1. Supabase Dev DB：本機開發版使用的資料庫。
2. Release / Production DB：Railway backend 實際連接的 Supabase/Postgres 資料庫。

## 驗證命令

在對應環境配置好 `DATABASE_URL` 後執行：

```bash
npm run ops:db:status
cd backend && npx prisma migrate status
```

套用後至少驗證：

```bash
cd backend && npm test -- --runInBand tests/unit/controllers/evidence.controller.test.ts tests/unit/services/case.service.test.ts tests/unit/utils/product-safety-policy.test.ts
cd backend && npm run build
npm run docs:check
```

## Release 注意事項

- 已提交 migration 不代表 release DB 已套用。
- 發布 Railway backend 前，必須確認 release DB migration 狀態。
- 若 release DB 尚未套用，`safety_metadata` 寫入會在 runtime 失敗。
- 不得用 production DB credentials 做本機開發；若需要查 production migration status，僅允許 read-only status command，且不得輸出密鑰。

## 後續

1. 確認 Supabase Dev DB 已套用 migration。
2. 確認 Release DB 已套用 migration。
3. 發布 backend 後做 evidence upload / formal case safety assertion smoke。
4. 完成後把本文件移入 `已處理/`，並在產品能力主線收斂待辦中更新狀態。
