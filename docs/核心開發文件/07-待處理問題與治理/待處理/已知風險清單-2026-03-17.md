# 已知風險清單（2026-03-17）

**文檔版本**：v1.1  
**最後更新**：2026-03-28  
**定位**：記錄本輪發版前仍需被明確知曉、但不再歸類為代碼級阻塞缺陷的剩餘風險。

---

## 1. 判定口徑

- 本清單只記錄「仍存在的發版風險或證據風險」。
- 已被自動化與真 DB 證據收斂的代碼缺陷，不再重複列入。
- 若後續有新增阻塞缺陷，應另行回寫到缺陷台帳，而不是混入本清單。

---

## 2. 已知風險

| 編號 | 風險 | 當前狀態 | 影響 | 建議處置 |
|---|---|---|---|---|
| R-01 | 發版前手動回歸結果尚未形成正式記錄 | 待補 | 發版評審仍缺最後一層人工操作證據 | 依 `發版前手動回歸執行版-2026-03-17.md` 完成 5 條主流程實操並回填結果；先跑 `npm run manual-regression:check`，收口前再跑 `npm run manual-regression:check:strict` |
| R-02 | 後端 flow integration 使用真 DB，但 AI 仍為 mock | 已接受 | 可驗證主業務鏈與資料一致性，但不是完整外部依賴整合 | 以本輪發版為 accepted risk，後續如需更高等級驗證再補 provider-level smoke |
| R-03 | `chat-failure-matrix` 曾有 1 條 Playwright skip 保留 | 已關閉（2026-03-28） | 已轉為正式用例並回歸通過（9 passed, 0 skip） | 已補 `test:e2e:critical-guard` 阻斷關鍵 e2e 回退為 skip |
| R-04 | 移動端/窄視口、空態/過期態仍缺最後一輪人工視覺抽檢 | 待補 | 不一定影響主功能，但可能影響臨門一腳的交付觀感 | 併入手動回歸包一起完成 |

---

## 3. 不屬於本清單的事項

以下事項目前不應再被描述為「已知未修復缺陷」：

- `F02 真 DB 驗證`
- `F10 RBAC / audit / config 治理證據`
- `高風險 API 五條抽檢`
- `README / package / SSOT / 分析稿` 的版本口徑統一

上述項目已於本輪補齊，應以相應證據文檔為準。

---

## 4. 發版建議

- 若要進入正式發版評審，應先關閉 `R-01`。
- `R-02` 與 `R-04` 可視為非代碼阻塞風險，但需在發版說明或評審會上明示。
