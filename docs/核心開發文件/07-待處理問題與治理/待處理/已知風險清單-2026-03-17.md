# 已知風險清單（2026-03-17）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：問題治理
**覆蓋範圍**：未落地設計、活躍治理項與需要追蹤的偏差：已知風險清單-2026-03-17
**取證代碼入口**：`backend/src`、`frontend/src`、`frontend-admin/src`、`docs/核心開發文件`
**最後核驗 Commit**：`1b007fa`
**最後核驗日期**：`2026-05-02`
<!-- CORE_DOC_AUDIT_METADATA:END -->

**文檔版本**：v1.6
**最後更新**：2026-05-02
**定位**：記錄本輪發版前仍需被明確知曉、但不再歸類為代碼級阻塞缺陷的剩餘風險。

---

## 1. 判定口徑

- 本清單只記錄「仍存在的發版風險或證據風險」。
- 已被自動化與真 DB 證據收斂的代碼缺陷，不再重複列入。
- 若後續有新增阻塞缺陷，應另行回寫到缺陷台帳，而不是混入本清單。

---

## 2. 已知風險

| 編號 | 風險 | 當前狀態 | 影響 | 建議處置 |
|---|---|---|---|---|
| R-01 | 後端 flow integration 使用真 DB，但 AI 仍為 mock | 已接受 | 可驗證主業務鏈與資料一致性，但不是完整外部依賴整合 | 以本輪發版為 accepted risk，後續如需更高等級驗證再補 provider-level smoke |
| R-02 | 移動端/窄視口、空態/過期態仍缺最後一輪人工視覺抽檢 | 已關閉 | 已以本地瀏覽器自動化覆蓋首頁、快速體驗建立頁、快速體驗 Session 過期頁、登入後案件列表空態 | 證據見「已於本輪關閉」補充 |

### 已於本輪關閉

- 原 `R-MR-01`（手動回歸結果尚未形成正式記錄）已於 `2026-04-18` 關閉：`P01-P05` 皆為 `PASS`。  
  證據：`docs/核心開發文件/90-證據與盤點/手動回歸證據/2026-04-18/summary.md`；可用 `npm run manual-regression:check:strict` 或 `npm run manual-regression:gate` 復核。
- `2026-04-19` 再次嚴格復核：`npm run manual-regression:check:strict -- --date 2026-04-18` 與 `npm run --workspace frontend test:e2e:critical-guard` 均為 `PASS`，未發現新的阻塞級回退。
- `R-MR-01` 為歷史退役編號，不再與現行風險編號 `R-01` 共用。
- `F02-BUG-002`（安全場景比例算法未拆出）已於 `2026-04-25` 以代碼級守衛關閉：`safety_support` 不再走一般比例 fallback，結果頁在 `safety_support/crisis_support` 下不展示比例卡。
  證據：`backend/src/services/ai.service.ts`、`backend/src/utils/judgment.ts`、`frontend/src/pages/QuickExperience/Result/index.tsx`；測試：`backend/tests/unit/services/ai.service.test.ts`、`backend/tests/unit/services/safety-routing.service.test.ts`、`backend/tests/unit/utils/judgment.test.ts`、`frontend/src/pages/QuickExperience/Result/index.test.tsx`。
- `R-02`（移動端/窄視口、空態/過期態最後一輪視覺抽檢）已於 `2026-05-02` 關閉：以 390x844 與 375x667 兩個手機視口覆蓋 `/`、`/quick-experience/create`、`/quick-experience/result/expired-case`、`/case/list` 空態。
  抽檢修正快速體驗 Session 過期頁在窄視口下文字直排與全域 toast 疊層問題；修正後截圖位於 `output/visual-r02/*-verified.png`。
  證據：`frontend/src/pages/QuickExperience/Result/index.tsx`、`frontend/src/pages/QuickExperience/Result/Result.less`、`frontend/src/services/request.ts`、`frontend/src/services/requestPolicy.ts`、`frontend/src/services/api/judgment.ts`；測試：`npm run test --workspace frontend`、`npm run build --workspace frontend`。

---

## 3. 不屬於本清單的事項

以下事項目前不應再被描述為「已知未修復缺陷」：

- `F02 真 DB 驗證`
- `F02-BUG-002 safety_support 比例算法拆分與安全場景比例卡展示`
- `F10 RBAC / audit / config 治理證據`
- `高風險 API 五條抽檢`
- `README / package / SSOT / 分析稿` 的版本口徑統一
- `chat-failure-matrix` 的關鍵 `.skip` 回退風險

上述項目已於本輪補齊，應以相應證據文檔為準。

---

## 4. 發版建議

- `R-01`（AI mock）屬非代碼阻塞風險，但需在發版說明或評審會上明示。
- `R-02` 已完成本地瀏覽器自動化抽檢並關閉；若需要真機等級驗收，可另開 device-lab 補充項，不再列為本輪發版風險。
