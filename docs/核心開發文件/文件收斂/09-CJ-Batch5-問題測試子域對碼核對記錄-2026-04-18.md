# CJ Batch 5 問題/測試子域對碼核對記錄（2026-04-18）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：治理記錄
**來源時間**：2026-04-18
**上下文**：非現行 SSOT；僅作文檔治理歷史記錄
**SSOT 屬性**：非現行 SSOT（僅作證據/歷史/治理參考）
**最後核驗 Commit**：`c1e0771`
**最後核驗日期**：`2026-04-19`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 範圍

本次核對 Batch 5 三個範圍：

1. `07-待處理問題與治理/*`
2. `08-測試規範與驗收/*`
3. `測試/*`（活躍場景與回歸入口）

## 取證入口

1. `scripts/check-docs-truth.mjs`
2. `frontend/src/router/index.tsx`
3. `frontend-admin/src/router.tsx`
4. `backend/tests/**`
5. `frontend/src/**/*.test.tsx`
6. `frontend/e2e/**/*.e2e.ts`
7. `e2e/admin/**/*.e2e.ts`
8. `scripts/run-manual-regression-gate.mjs`
9. `scripts/check-manual-regression-status.mjs`
10. `scripts/summarize-manual-regression.mjs`
11. `scripts/init-manual-regression-batch.mjs`

## 本輪發現與處置

| 編號 | 發現 | 影響 | 處置 |
|---|---|---|---|
| B5-01 | `測試/活躍場景案例/quick-experience/README.md` 聲明「5 份案例」，實際在目錄中有 6 份 | 案例索引口徑與實體案例數漂移，易誤導回歸覆蓋面 | 文檔回寫為 6 份，並新增案例數與表格條目一致性守衛 |
| B5-02 | `Repair Journey 2.3 場景驗收矩陣` 使用 `/reconciliation/:judgmentId/:planId`，與前台路由 `/reconciliation/:judgmentId/:id` 不一致 | 回歸路徑標註與實際路由契約漂移 | 文檔回寫為 `:id`，並新增矩陣路由 token 對碼守衛 |
| B5-03 | 活躍場景/回歸入口文檔對測試檔引用缺少機器守衛 | 測試檔改名、移位或入口過時時，文檔漂移難被即時發現 | 在 `docs:check:truth` 新增 Batch 5 守衛（案例表、回歸入口、手動回歸包引用、未登入直連清單、Repair Journey 測試引用） |
| B5-04 | Batch5 子域存在 `npm exec -- playwright ... e2e/chat/*` 舊命令回退風險，且已有兩份歷史文檔命中 | 命令可執行性依賴隱含工作目錄，容易在回歸時誤跑失敗 | 已修正命中歷史文檔為 `npm exec --workspace frontend -- playwright test -c e2e/playwright.config.ts ...`，並將守衛擴展為 `07/08/測試` 全子域掃描 |

## 守衛補強

本輪新增以下 Batch 5 真實性守衛：

1. `quick-experience/README.md` 的「包含 X 份」聲明必須與目錄實際案例檔數一致。
2. `quick-experience` / `chat-room` README 案例表首列檔名，必須與目錄實體 `.md` 文件一一對齊（缺失或過時均報錯）。
3. `測試/活躍場景案例/README.md` 必須保留 `quick-experience` 與 `chat-room` 活躍入口。
4. `chat-room/README.md` 必須保留 `chat-ai-orchestrator` 單元測試入口，且檔案存在。
5. `測試/回歸與驗收/README.md` 的活躍執行入口文件必須存在於磁碟。
6. `發版前手動回歸包-2026-03-17.md` 的核心自動化前置證據（E2E / integration / smoke）必須有文檔引用且文件存在。
7. `發版前手動回歸執行版-2026-03-17.md` 必須覆蓋 `manual-regression:check:strict / summarize / gate / mark` 現行命令入口。
8. `未登入直連-回歸驗證清單.md` 必須與現行 `frontend + admin` 路由存在性/守衛類型一致。
9. `Repair Journey 2.3 場景驗收矩陣.md` 必須覆蓋現行前台路由 token，且測試引用需對應實際測試文件（接受 `--prefix` 命令下的相對寫法）。
10. `07-待處理問題與治理/*`、`08-測試規範與驗收/*`、`測試/*` 任一文檔若包含 `e2e/chat/*` 的 Playwright 命令，必須使用 `npm exec --workspace frontend -- playwright test -c e2e/playwright.config.ts`，不得回退到 root 舊命令。

## 驗證結果

執行命令：

```bash
npm run docs:check
```

結果：

1. `docs:check:structure` PASS
2. `docs:check:truth` PASS（`126 endpoints / 30 frontend routes / 9 admin routes`）
3. `docs:check:truth` 已包含 `batch-5 scenario+regression semantics verified`

## 2026-04-19 補充復核

執行命令：

```bash
npm run manual-regression:check:strict -- --date 2026-04-18
npm run --workspace frontend test:e2e:critical-guard
```

結果：

1. `manual-regression:check:strict` PASS（`P01-P05` 全部 `PASS`）
2. `frontend test:e2e:critical-guard` PASS（關鍵 guard 檔案不存在 `.skip`）
3. `07/08/測試` 子域文檔未檢出新漂移，沿用上一輪核驗基準 `Commit 4d14e4f / 2026-04-19`

## 2026-04-19 再核驗（Batch5）

本次以 `Commit c1e0771` 對 Batch5 子域再核驗，重點確認：

1. `07-待處理問題與治理/*` 的活躍/已處理/不處理分層與現碼風險現況一致。
2. `08-測試規範與驗收/*` 內命令入口與腳本名仍與 `package.json`、`scripts/*.mjs` 一致。
3. `測試/*` 活躍場景與回歸入口對應測試檔仍存在，且關鍵 guard 未被跳過。
4. `07/08/測試` 全子域不存在 root playwright + `e2e/chat/*` 的回退命令。

再核驗結果：

1. `npm run docs:check` PASS（含 `batch-5 scenario+regression semantics verified`）。
2. `npm run manual-regression:check:strict -- --date 2026-04-18` PASS（`P01-P05` 全部 `PASS`）。
3. `npm run --workspace frontend test:e2e:critical-guard` PASS（6 個關鍵文件無 `.skip`）。
4. 已修正兩份命中文檔為 workspace 形式，並補入 `07/08/測試` 全子域防回退守衛。

## 結論

1. Batch 5（問題治理 + 測試規範 + 活躍測試入口）已完成一輪代碼真值化核對與回寫。
2. 本輪修正了兩個明確文檔漂移（案例數、Repair Journey 路由參數名）。
3. 後續若活躍場景案例、回歸入口、手動回歸流程或對應測試檔變更但未回寫，`docs:check:truth` 會直接失敗。
4. `2026-04-19` 已完成嚴格回歸再核驗，Batch 5 核驗結論保持有效。
