# App 完整版本開發 Roadmap

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：App端規格
**覆蓋範圍**：完整 App 版從開發前規格到 iOS TestFlight / Android readiness 的里程碑、完成定義、依賴與測試 gate
**取證代碼入口**：`mobile/package.json`、`mobile/tsconfig.json`、`mobile/app/_layout.tsx`、`mobile/app/modal.tsx`、`mobile/src/platform`、`packages/contracts/src`、`packages/api-client/src`、`frontend/src/services/api`、`backend/src/routes`
**最後核驗 Commit**：`3890ba8`
**最後核驗日期**：`2026-05-08`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 1. 定位

本文是完整 App 版開發 Roadmap。它把 [02-App完整版本工程PRD.md](./02-App完整版本工程PRD.md) 的需求拆成可以實作、驗收與留證的工程里程碑。

本文不把 milestone 的外部發版證據與首輪工程落地混為一談。當前 Roadmap 的狀態是「開發前裁決已完成，M0-M5 普通用戶 App 工程已完成首輪接線並通過本機 true-service / simulator / emulator gate，M6 仍等待外部 strict release sign-off」：`mobile/app` 已替換 Expo template tabs，`mobile/src/platform` 已建立 runtime adapter，M0 root error boundary 與 upload / notifications / lifecycle adapter branch tests 已接入，M1 已覆蓋 quick、quick collaborative、result、auth 的首輪 App route，並補 quick submit 前的過期 anonymous session recovery、result access expired cleanup、`case_judgment` stream `after_seq` replay / ready snapshot fallback，以及 Quick result 接入共用 App AI stream subscription hook 的 AppState background abort / foreground `afterSeq` reconnect unit gate；M2 Interview、M3 Chat AI draft 與 M4 Repair replan 已接共用 App AI stream subscription hook，覆蓋 ready snapshot、`after_seq` replay、close/error retry 與 screen-level AppState recovery gate；M2-M5 shared client 與對應 screen / adapter 已接入，M0-M5 已建立 Maestro flow artifact、selector contract static gate 與 iOS simulator native execution gate，`features:check` 已把 M0-M5 source coverage 釘到 route `testID`、screen action、shared API endpoint、SSE / Deep Link / Push / Upload / Telemetry adapter 與 Backend notification / telemetry / push route contract，push device token registration / revoke / logout cleanup / registration-time token rotation revoke 已有 backend schema/API/client 與 App token sync 接線，backend Expo push sender、pending dispatch job 與 receipt polling job 已接線，App telemetry 已接 non-blocking `POST /api/v1/telemetry/events` safe ingest 端點、`POST /api/v1/telemetry/otlp/v1/traces` CJ OTLP JSON trace ingest baseline、backend 二次清洗、最小化 persistence、Admin 聚合報表與 30d cleanup，App observability bootstrap 已接 session start、lifecycle transition、global JS fatal / unhandled promise、OpenTelemetry provider span export 與 native crash SDK configuration first pass，AI stream persistence、M2 interview facts 與 notification action metadata migrations 已補齊，AI stream live subscription 已修正 replay 等待期間錯過快速 terminal 事件的 race，SSE 端點已禁用 HTTP compression，AI stream runtime 已可在 memory/Redis 空白時從 live DB events / sessions 做 replay / snapshot fallback，notification landing handler 已有首輪 response route handoff 與 post-login resume，並通過 typecheck / jest-expo / React Native Testing Library / web export / route smoke / mock-backed backend smoke；M1-M5 true-service smoke 已可在本機隔離 Postgres + local backend 下以 `--bootstrap-local-users` 跑通 token-gated probes，其中 M1 `--m1-stream-replay` 已覆蓋 `case_judgment` SSE `after_seq` replay 回放 terminal persisted event，M1 `--m1-inject-expired-session` 已覆蓋過期 anonymous session submit recovery、result access `SESSION_EXPIRED` 與 expired claim-session ignored，M2 `--deep` 已覆蓋 profile / consent / interview 5-turn synthetic flow / background AI response / end / my-story completion artifacts，M3 `--request-ai` 已覆蓋 partner invite accept + request judgment，M4 `--request-ai` 已覆蓋 formal evidence upload、repair plan selection、execution confirm、`repair_track` replan stream `stream.persisted` 與 backend restart 後 `after_seq=7` DB-backed SSE replay，M5 已覆蓋 push token register/revoke、notification state sync、evidence multipart upload/delete 與 telemetry ingest；iOS 26.4.1 / iPhone 17 simulator Release build 已安裝啟動，Maestro native 7/7 flows passed；Android SDK root、Java 17、adb、emulator CLI 與 sdkmanager 已就緒，`android:check:strict` 已通過，`CJ_Pixel_9_API_36` Android 16 / API 36 arm64 AVD 已完成 emulator boot smoke，本機 Android release APK 已完成 build、install 與 `MainActivity` foreground launch smoke，Android Maestro 7/7 flows passed，Android release APK native ImagePicker picker-cancel side-effect smoke 已通過，且 selected-media backend synthetic fixture upload/delete 已在臨時 local DB + local backend 上通過。release DB parity 與 telemetry runtime pass evidence 已有 structured evidence；EAS / TestFlight、physical device、真 provider delivery / APNs sandbox、真機 picker-selected asset / profile media、Android EAS artifact、native crash runtime evidence / external tracing backend 仍待後續 milestone 驗收。

## 2. 里程碑總表

| Milestone | 名稱 | 目標 | App 狀態 |
| --- | --- | --- | --- |
| M0 | Foundation | workspace / shell / provider / API adapter / SecureStore / smoke | 首輪工程已落地；shell / adapters / web smoke / storage+API adapter unit / provider render / auth-session bootstrap / root error boundary / upload+notifications+lifecycle adapter branch tests、platform side-effect boundary static gate、M0-M5 Maestro selector contract static gate、iOS simulator Release build / native launch / Maestro native smoke 已完成；physical-device native side-effect evidence 待補 |
| M1 | Quick + Auth | anonymous session、quick case、collaborative quick、result、claim、login/register | 首輪工程已落地；shared domain client、App screen 接線、同機 collaborative quick route、quick submit session recovery、Quick result access expired cleanup、backend expired claim guard、`case_judgment` stream replay、pending polling、claim failure 不阻斷登入與 Quick/Auth Maestro flow baseline 已完成，RNTL 最小互動、Quick result AppState background abort / foreground `afterSeq` reconnect、mock-backed backend smoke、本機隔離 Postgres true-service quick flow + bootstrap claim-session、`case_judgment` stream `after_seq` replay 與 local-only expired-session submit/access/claim injection 已通過；native stream reconnect physical-device 證據待補 |
| M2 | Profile + Interview | profile shell、consent、SSE interview、result、my-story | 首輪工程已落地；shared client、Profile / Interview / My Story screen、AI stream subscription hook、ready snapshot / `after_seq` replay、screen-level AppState background abort / foreground reconnect RNTL gate、Profile/Interview/Story auth-gate 與 protected profile deep-link auth-resume Maestro flow artifact 已接線，本機隔離 Postgres true-service token probe 與 `--deep` 5-turn start/respond/background response/end/my-story completion 已通過；failed retry UI / partial-success read-only UI / My Story feedback-card summary、feedback history title label、PsychDomain label helper 與 Interview sync status label 已有 RNTL / unit gate，不再向普通用戶顯示 `session_id`、`domains_touched` raw enum 或 stream 位置段號；local failed-session 與 partial-success true-service injection 分支已驗收；iOS simulator Maestro native smoke 已通過；native lifecycle interruption physical-device evidence 待驗收 |
| M3 | Chat | room、invite、message stream、chat-room AI draft、request judgment、handoff | 首輪工程已落地；shared client、Chat Home / Room / Invite screen、room event stream parser、chat-room AI stream draft hook、screen-level AppState background abort / foreground reconnect RNTL gate、invite accept 未登入 auth resume、notification invite landing path 映射、Chat Home 手填房間代號入口已移除並改由新建對話 / 邀請連結 / deep link 承接，缺少對話上下文會引導回支援入口，Chat runtime 可見文案已把「房間 / 房主 / 本機 session」收斂為「對話 / 邀請方 / 本機會話」，Chat entry/auth-gate Maestro flow artifact 與首輪測試已完成，本機隔離 Postgres true-service room/message/invite/list/status、partner invite accept 與 `--request-ai` request-judgment 已通過；iOS simulator Maestro native smoke 已通過；native stream reconnect physical-device evidence 待驗收 |
| M4 | Formal Case + Repair | pairing、case、judgment、reconciliation、execution | 首輪工程已落地；shared client、Case screen、Repair screen、repair replan trigger、`repair_track` stream replay/recovery hook、screen-level AppState background abort / foreground reconnect RNTL gate、Case/Repair auth-gate Maestro flow artifact、Case evidence upload 顯式 safety assertion UI、正式案件 / check-in readiness helper 與首輪測試已完成，本機隔離 Postgres true-service pairing/case/formal evidence upload/judgment/execution dashboard/repair plan generation/plan select/execution confirm/replan stream persisted 與 backend restart 後 DB-backed replay 已通過；iOS simulator Maestro native smoke 已通過；native selected-media picker / lifecycle reconnect physical-device evidence 待驗收 |
| M5 | Push + Deep Link | device token、notification landing、read/snooze/act sync | 首輪工程已落地；shared notification/upload client、Notifications screen、deep-link resolver、notification landing handler、post-login resume、registration-time token rotation revoke、protected Deep Link auth-resume Maestro flow、evidence FormData adapter、Case screen 顯式 safety assertion handoff、safe telemetry redaction、backend `POST /api/v1/telemetry/events` safe ingest、backend `POST /api/v1/telemetry/otlp/v1/traces` CJ OTLP JSON trace ingest、backend App telemetry minimized persistence / Admin report / 30d cleanup、backend device token registration / revoke、Expo push sender、pending push dispatch job、receipt polling job、App token sync 與 logout cleanup 已完成，iOS simulator Deep Link Maestro native smoke 已通過，structured `push-delivery:smoke` provider runner 已建立；native push delivery、APNs sandbox、真機 cold-start notification landing 證據待補 |
| M6 | Release Hardening | iOS TestFlight、EAS Update policy、Android readiness、evidence pack | 部分落地；app identity、runtimeVersion、EAS profiles、release readiness script、iOS simulator Release build、native readiness strict gate、Android config / toolchain strict readiness gate、Android emulator boot smoke、Android release APK install/launch smoke、Android Maestro 7/7 flow evidence、Maestro native 7/7 evidence record、EAS iOS / TestFlight structured evidence runner、EAS Android structured evidence runner、telemetry runtime structured evidence runner、release evidence verifier 與 release completion audit gate 已建立；telemetry runtime canonical pass evidence 已落盤並通過 backend `/version` 對齊、event ingest 與 OTLP ingest；EAS project id、Expo/Apple credentials、pass 狀態 TestFlight / physical device build artifact、Android EAS artifact、push provider delivery 與 native crash runtime 待補 |

## 3. M0 Foundation

| 項目 | 裁決 |
| --- | --- |
| 目標 | 讓 `mobile/` 從 Expo template 變成 CJ App 可開發骨架 |
| 主要工作 | root workspace 裁決、Expo Router route group、QueryClient provider、auth/session bootstrap、API adapter、SecureStore adapter、error boundary、smoke command |
| Web 配合 | 抽出 API transport / error normalize / query key 的 shared shape，不改 Web 對外行為 |
| Backend / DB | 不新增 runtime API；若需要 app version / telemetry endpoint，先進待辦 |
| 測試 gate | App launch smoke、provider render test、storage adapter unit、API adapter header tests、platform side-effect boundary static gate |
| 完成定義 | `Tab One` / `Tab Two` / template modal 不再作主入口；CJ shell 可啟動並能讀寫 token/session sandbox |
| 不得宣稱 | 不能宣稱任何 CJ 業務流程已完成 |

### 3.1 M0 進度回寫（2026-05-08）

| 已落地項 | 證據 |
| --- | --- |
| Expo template tabs 已移除，新增 public / authenticated route group，以及 quick、auth、case、chat、profile、notifications、repair、modal screen | `mobile/app/_layout.tsx`、`mobile/app/modal.tsx`、`mobile/app` route tree；public home 入口顯示為「App 狀態」 |
| Provider / UI foundation 已建立 | provider layer、theme、shared UI components；shared `LinkButton` 已改為 `Pressable + router.push`，避免 Expo Web `Link asChild` 丟失 button 樣式，並把 panel/card/input radius 收斂到 8px、寬屏 content 限制到 760px、底部 tab label 中文化；shared UI accessibility contract 已覆蓋 header role、button label/hint、busy/disabled state、48px touch target 與 FeatureRow 合併朗讀 label，App `TextInput` label / hint static gate 與普通用戶可見文案 regression gate 已建立 |
| 普通用戶可見文案已完成 UI/UX 清理 | `mobile/app/(public)/auth/index.tsx`、`mobile/app/(public)/quick/index.tsx`、`mobile/app/(public)/quick/collaborative.tsx`、`mobile/app/(public)/quick/result.tsx`、`mobile/app/(public)/index.tsx`、`mobile/app/(app)/chat/index.tsx`、`mobile/app/(app)/chat/invite.tsx`、`mobile/app/(app)/chat/room.tsx`、`mobile/app/(app)/case/index.tsx`、`mobile/app/(app)/profile/interview.tsx`、`mobile/app/(app)/profile/story.tsx`、`mobile/app/(app)/repair/index.tsx`、`mobile/app/(app)/notifications/index.tsx`、`mobile/app/modal.tsx`、`mobile/src/ui/AppErrorBoundary.tsx` 已移除或替換 `anonymous session`、`SecureStore adapter`、`device token registration / revoke route 尚未存在`、`stream seq`、`solo_active`、`share_summary_only`、`all-visible user message`、`active/background`、`invite code`、`room id`、`deep link`、`pairing`、`API`、raw service URL、`M0-M6 / 接入中`、`Alpha`、`App 主線`、`結果頁骨架` 等工程 / 未完成態 / backend enum / 內部里程碑文案；Quick / Auth / Formal Case / Repair check-in 已補 submit readiness helper 與 disabled state，Auth 表單已把 `Email` / `Password` 可見欄位標籤、hint 與錯誤提示統一為「電子郵件 / 密碼」語義，Quick result（含 ready fallback 與背景恢復不顯示 stream 位置序號）、Profile Interview（含 auth gate 同步文案、心理領域 label helper 與同步狀態不顯示 stream 位置段號）、Profile Story、Chat room（含協調草稿不顯示 stream 位置段號，且可見模型詞已從「房間 / 房主 / 本機 session」收斂為「對話 / 邀請方 / 本機會話」）、App 狀態 modal（不顯示 raw API URL / 服務位址）、Formal Case、Repair dashboard / plans / replan 與 Notifications 已把 case / session / room / journey / plan / notification 的 read-only internal id、backend status、priority、feed state、raw ISO time、PsychDomain 或 template fallback 統一經 label helper 轉成用戶語義；App screen legacy English eyebrow / label（`QUICK`、`AUTH`、`PROFILE`、`INTERVIEW`、`MY STORY`、`CHAT`、`INVITE`、`CASE`、`REPAIR`、`NOTICE`、`RESULT`、`CJ APP`、`APP`）已本地化為「快速整理 / 帳號 / 個人脈絡 / 心理訪談 / 我的故事 / 對話 / 邀請 / 正式案件 / 修復 / 提醒 / 整理結果 / CJ / App」等用戶語義；Formal Case 列表的建立時間已改用穩定繁中日期 formatter 與缺失 fallback，不再受裝置 locale 或 raw ISO timestamp 影響；不再把 `case_id` / `session_id` / `roomId` / `track_id` / `plan_id` / `domains_touched` raw enum 作為結果、訪談回饋、訪談狀態、房間、案件卡、旅程看板或新方案提示直接顯示；Chat entry 已移除手填「房間代號」，改以建立新對話、邀請碼 / invite landing / deep link 承接已有對話，缺少對話上下文時引導回支援入口；Repair flow 已從手填「判斷代號 / 方案代號 / 修復旅程識別碼」改成 Case accept judgment route handoff、方案卡直接選擇 / 開始與旅程看板選取重新調整對象；`npm --prefix mobile run copy:check` 已新增並納入 `release:preflight`，防止 App screen / shared UI / `src/features/**/labels.ts` 可見文案回退到工程詞、raw backend status / priority / feed state / template code、visible state template enum、legacy English screen label、英文欄位標籤、PsychDomain raw enum value、PsychDomain raw join 或 raw internal id field；`npm --prefix mobile run test`、`npm --prefix mobile run typecheck`、`npm --prefix mobile run accessibility:check`、`npm --prefix mobile run copy:check`、`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run release:preflight` 已通過 |
| Auth/session bootstrap 已建立，啟動時從 SecureStore 預載 token / session id 到 React Query cache，登入與清理時同步更新 cache | `mobile/src/providers/AuthSessionBootstrap.test.js`、`mobile/__tests__/m1-screens.test.js` 已通過 |
| Root error boundary 已接入 Expo Router root layout，只顯示安全恢復文案並回傳不含 raw error message 的 telemetry context | `mobile/src/ui/AppErrorBoundary.test.js` 已通過 |
| Runtime config 已建立 | App runtime config |
| Platform adapter runtime 已建立 | `mobile/src/platform` 下的 API、storage、SSE、upload、notifications、linking、lifecycle、telemetry adapter |
| Native adapter branch unit 已補強 | `mobile/src/platform/upload/native.test.js`、`mobile/src/platform/notifications/native.test.js`、`mobile/src/platform/lifecycle/native.test.js` 已覆蓋 web skip、native picker permission / asset normalize、notification web branch 與 AppState subscribe / remove |
| Maestro flow artifact、selector contract static gate 與 iOS simulator native smoke 已建立 | `mobile/maestro/00-public-navigation-smoke.yaml`、`mobile/maestro/10-quick-auth-form-smoke.yaml`、`mobile/maestro/20-chat-entry-auth-gate-smoke.yaml`、`mobile/maestro/30-notification-landing-auth-gate-smoke.yaml`、`mobile/maestro/40-profile-interview-auth-gate-smoke.yaml`、`mobile/maestro/50-case-repair-auth-gate-smoke.yaml`、`mobile/maestro/60-deep-link-auth-resume-smoke.yaml`、`mobile/scripts/check-maestro-flows.mjs`、`npm --prefix mobile run maestro:check` 已通過，覆蓋 7 個 flow artifact / 29 個 selector id；`maestro:native` 在 iOS 26.4.1 / iPhone 17 simulator 上 7/7 flows passed |
| App test command 已建立，並覆蓋 SecureStore adapter、API adapter header/error normalize | `npm --prefix mobile run test` 已通過；30 suites / 134 tests |
| App accessibility static gate 已建立 | `npm --prefix mobile run accessibility:check` 已通過，且已納入 `release:preflight` |
| App user-visible copy static gate 已建立 | `npm --prefix mobile run copy:check` 已通過，且已納入 `release:preflight`；會掃描 `mobile/app`、`mobile/src/ui`、`mobile/src/features` route / UI 的 `.tsx` 可見 JSX text / visible props / JSX expression / visible state template，並把 `src/features/**/labels.ts` / `src/ui/**/labels.ts` 視為可見 copy module 掃描，阻止 `anonymous session`、`stream seq`、`after_seq`、`claim-session`、`solo_active`、`share_summary_only`、`invite code`、`room id`、`deep link`、`pairing`、`API`、`apiBaseUrl`、`http://`、`https://`、`M0-M6`、`接入中`、`測試`、`Alpha`、`App 主線`、`結果頁骨架`、`判斷代號`、`方案代號`、`修復旅程識別碼`、`房間代號`、`房間`、`服務位址` 等工程詞、內部里程碑、raw service URL 或 backend enum 出現在普通用戶可見文案，並阻止 `QUICK`、`AUTH`、`PROFILE`、`INTERVIEW`、`MY STORY`、`CHAT`、`INVITE`、`CASE`、`REPAIR`、`NOTICE`、`RESULT`、`CJ APP`、`APP`、`Email`、`Password` 等 legacy English screen / field label 作為完整可見標籤回流；同時阻止 `.status`、`.priority`、`.phase`、`.mode`、`.code`、`.template_code`、`.id`、`.room_id`、`.case_id`、`.plan_id`、`.track_id`、`.judgment_id`、`.domains_touched`、raw route id 變數、raw `feedState`、`.domains_touched.join(...)` 與 `attachment` / `family_origin` / `life_events` / `belief_values` / `cultural_background` / `education_cognition` / `personality` / `relationship_history` 等 PsychDomain raw enum value 直接進入 label / JSX text / feedback template / visible copy module |
| App route topology static gate 已建立 | `npm --prefix mobile run routes:check` 已通過，且已納入 `release:preflight`；會固定 `mobile/app` 的 `(public)` / `(app)` / Quick / Auth / Case / Chat / Profile / Notifications / Repair / `modal` route file set、layout registrations、authenticated deep route tab hiding，並阻止 Expo template `(tabs)` / `Tab One` / `Tab Two` 回流 |
| App web route smoke gate 已建立 | `npm --prefix mobile run web:routes:smoke` 已通過，且已納入 `release:preflight`；會在 `smoke:web` 產物上用 Playwright 驗證 16 條 App web route 在 desktop + phone viewport 掛載指定 screen marker、body 非空、無 page error，並檢查 public home primary CTA 基本樣式 |
| Platform side-effect boundary static gate 已建立 | `npm --prefix mobile run platform:check` 已通過，且已納入 `release:preflight`；會阻止 App screen / 非 platform 層 static / dynamic import 或 require `expo-secure-store`、`expo-image-picker`、`expo-notifications`、`expo-linking`，以及直接使用 `AppState` |
| 本地驗證已通過 | `npm --prefix mobile run test`、`npm --prefix mobile run accessibility:check`、`npm --prefix mobile run copy:check`、`npm --prefix mobile run routes:check`、`npm --prefix mobile run platform:check`、`npm --prefix mobile run typecheck`、`npm --prefix mobile run smoke:web`、`npm --prefix mobile run web:routes:smoke`、`npm --prefix mobile run maestro:check`、shared UI accessibility contract gate |

| 尚未閉環項 | 不得宣稱 |
| --- | --- |
| physical-device native side-effect evidence | M0 release-grade 完整驗收 |
| React Native foreground/background SSE runtime、selected-media native upload、Push token sync、真機 Deep Link / notification landing 證據 | 對應 native adapter 已完整驗收 |
| EAS build / TestFlight / physical device evidence | 可內測發布或 release-ready |

## 4. M1 Quick + Auth

| 項目 | 裁決 |
| --- | --- |
| 目標 | 先完成 App 最低價值閉環：匿名 quick -> result -> claim / login |
| 主要工作 | quick create、collaborative、result、session refresh、AI stream result replay、login/register、claim-session handoff |
| Web 配合 | 將 quick/session/auth API client shape 從 Web service 抽到 shared client |
| Backend / DB | 優先不改；若 App session restore 需要新增 device metadata，先進待辦 |
| 測試 gate | quick flow unit / component、App smoke、session restore smoke、claim-session API smoke |
| 完成定義 | 未登入 App 用戶可提交 quick case、看到 pending / completed result、登入後 claim 不阻斷 auth 主流程 |
| 不得宣稱 | 不能宣稱正式案件、chat 或 push 已完成 |

### 4.1 M1 進度回寫（2026-05-08）

| 已落地項 | 證據 |
| --- | --- |
| `packages/api-client` 已新增 Quick/Auth/Session domain client，覆蓋 quick session、refresh、quick case、case by id/session、login/register、claim-session | `npm run typecheck --workspace @cj/api-client` 已通過 |
| App bundler 已改為 shared package resolution，避免 TypeScript path alias 與 Metro runtime resolution 分叉 | `npm --prefix mobile run smoke:web` 已通過 |
| Quick screen 已接 anonymous session refresh/create、quick case submit、SecureStore session 保存與 result navigation | `npm --prefix mobile run typecheck`、`npm --prefix mobile run smoke:web` 已通過 |
| Quick collaborative screen 已接同機 A/B 兩段式說明、anonymous session create/refresh、case id handoff 與 result navigation | `mobile/__tests__/m1-screens.test.js`、`npm --prefix mobile run test`、web export route smoke 已通過 |
| Quick submit 已新增共用 anonymous session recovery helper；refresh 遇到過期 / 無效 session 時清理本機 session id、重建 quick session 並記 safe telemetry | `mobile/__tests__/m1-screens.test.js`、`npm --prefix mobile run test` 已通過 |
| Quick result 讀取遇到過期 / 無效 session 時會清理本機 session id、顯示明確過期承接文案並記 safe telemetry | `mobile/__tests__/m1-screens.test.js`、`npm --prefix mobile run test` 已通過 |
| Quick result screen 已接 case by id / session lookup、pending / failed / summary 狀態展示與手動刷新 | `npm --prefix mobile run typecheck`、`npm --prefix mobile run smoke:web` 已通過 |
| Quick result pending polling 已接入，僅在 `submitted` / `in_progress` 且尚未有 judgment summary 時定期刷新，完成或失敗即停止 | `mobile/__tests__/m1-screens.test.js`、`npm --prefix mobile run test` 已通過 |
| Quick result 已接通用 AI stream：`/streams/case_judgment/:caseId`、`after_seq` replay、ready snapshots fallback，收到 persisted / completed / failed / cancelled 終態事件後立即 refetch 結果；screen 已改用共用 App AI stream subscription hook，AppState background 會 abort 當前 SSE，foreground 後用最後 `afterSeq` 重連 | `mobile/src/features/m1/api.test.js`、`mobile/__tests__/m1-screens.test.js`、`npm --prefix mobile run test -- --runTestsByPath __tests__/m1-screens.test.js --runInBand` 已通過 |
| Auth screen 已接 login / register、SecureStore token 保存、claim-session、本機會話清理與電子郵件 / 密碼 submit readiness helper | `npm --prefix mobile run typecheck`、`npm --prefix mobile run test -- m1-screens`、`npm --prefix mobile run smoke:web` 已通過 |
| claim-session failure 不再阻斷登入；過期 / 無效 anonymous session 會清理本機 session cache，並只回傳 safe telemetry context | `mobile/__tests__/m1-screens.test.js`、`npm --prefix mobile run test` 已通過 |
| Quick/Auth screen 已建立 React Native Testing Library 最小互動測試，覆蓋 Quick submit disabled / inline helper、anonymous session submit、existing session refresh、expired session recovery、result access expired cleanup、collaborative A/B handoff、result stream persisted refetch、Quick result ready fallback 用戶語義文案、Quick result AppState background abort / foreground `afterSeq` reconnect、Auth email/password disabled / inline helper、login token + claim handoff、claim failure handoff | `npm --prefix mobile run test -- m1-screens` 已通過；M1 16 tests passed |
| Quick/Auth 已建立 Maestro form smoke baseline 與穩定 selector，覆蓋 public home -> quick -> auth 的 native flow artifact | `mobile/maestro/00-public-navigation-smoke.yaml`、`mobile/maestro/10-quick-auth-form-smoke.yaml`、`npm --prefix mobile run maestro:check` 已通過；iOS 26.4.1 / iPhone 17 simulator Release build 的 native Maestro flow 已通過，證據見 `90-證據與盤點/環境與發版驗證/App-Native-Maestro-2026-05-08T16-03-15-803Z.json` |
| Backend smoke test 的 Prisma mock enum 契約已補齊，M1 依賴的 session / auth / quick endpoint 可達性 smoke 已恢復 | `npm --prefix backend test -- --runInBand tests/integration/smoke.test.ts` 已通過 |
| App true-service smoke harness 已建立，預設 dry-run 不打 backend，`--run` 才執行真服務；M1 支援 anonymous quick session -> quick case -> case fetch -> by-session -> judgment poll，並可用 `--bootstrap-local-users` 在本機安全註冊暫時 smoke user 後跑 claim-session；`--m1-stream-replay` 會在 judgment persistence 後訂閱 `/streams/case_judgment/:caseId`，並用 `after_seq=lastSeq-1` 驗證 terminal persisted event 可 replay；`--m1-inject-expired-session` 只允許 local API + local DB，會對 smoke-created quick sessions 注入過期時間，驗證 submit recovery、result access reject 與 expired claim ignored；`--run` 會拒絕 non-local API 或 local API + non-local backend DB，除非顯式 override | `npm --prefix mobile run smoke:true-service -- --dry-run --scope=all --bootstrap-local-users` 已可執行並在非本機 DB 時 blocked；在 `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev` + local backend 下，`npm --prefix mobile run smoke:true-service -- --run --scope=all --bootstrap-local-users --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過，M1 覆蓋 session / quick case / case fetch / by-session / judgment poll / claim-session；`APP_SMOKE_JUDGMENT_POLL_ATTEMPTS=30 APP_SMOKE_JUDGMENT_POLL_INTERVAL_MS=1000 npm --prefix mobile run smoke:true-service -- --run --scope=m1 --bootstrap-local-users --m1-stream-replay --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 `m1.case_judgment_stream_replay`，觀察 `stream.persisted` lastSeq=9 並以 `after_seq=8` 回放 terminal event；`APP_SMOKE_JUDGMENT_POLL_ATTEMPTS=10 APP_SMOKE_JUDGMENT_POLL_INTERVAL_MS=500 npm --prefix mobile run smoke:true-service -- --run --scope=m1 --bootstrap-local-users --m1-inject-expired-session --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 expired submit recovery、expired result `SESSION_EXPIRED` 與 expired claim ignored |

| 尚未閉環項 | 不得宣稱 |
| --- | --- |
| 已建立 true-service smoke 入口，且 M1 anonymous quick create -> result/judgment poll -> bootstrap claim-session 已在本機隔離 Postgres + local backend 下通過；`--m1-stream-replay` 已覆蓋 `case_judgment` SSE `after_seq` replay，local-only `--m1-inject-expired-session` 已覆蓋過期 submit recovery、result `SESSION_EXPIRED` reject 與 expired claim ignored；但 `backend/.env` 的 `DATABASE_URL` 仍可能指向非本機 DB，必須用 `APP_SMOKE_BACKEND_DATABASE_URL` 或等效本機 DB 明確覆蓋；目前已有 RNTL AppState reconnect gate，仍缺 native stream reconnect 真機證據 | M1 App/native flow 完整驗收 |
| 已建立 Maestro flow artifact / selector static gate，且 public / quick / auth native simulator smoke 已通過；RNTL 目前覆蓋 quick/auth 最小互動、quick submit session recovery、result access expired cleanup、collaborative A/B handoff、pending polling、stream persisted refetch、Quick result ready fallback wording、Quick result AppState reconnect 與 claim failure handoff，後端單測覆蓋 expired claim guard，true-service 覆蓋 `case_judgment` replay 與 session expired submit/access/claim；仍未覆蓋 native stream reconnect 真機證據與真機錯誤態 | M1 自動化回歸完成 |
| 尚未建立 native `case_judgment` stream reconnect 真機證據；Quick result 已有 shared hook + RNTL AppState reconnect gate，true-service stream replay 與 session expired 分支已完成 local smoke，但仍缺真機 UX 分支證據 | Quick + Auth alpha 完整完成 |

## 5. M2 Profile + Interview

| 項目 | 裁決 |
| --- | --- |
| 目標 | 承接手機端適合長對話的 profile / interview / my-story |
| 主要工作 | profile home、settings 基線、consent、interview start、respond / skip / end SSE、result、retry、my-story |
| Web 配合 | 抽 interview SSE parse、profile DTO、error normalize、result state helper |
| Backend / DB | 不分叉 profile schema；若 App 需要 lifecycle resume signal，先進待辦 |
| 測試 gate | SSE foreground stream test、interruption recovery inspection、interview result component tests |
| 完成定義 | 登入用戶能從 profile 啟動訪談、完成或恢復訪談、查看 result / my-story |
| 不得宣稱 | 不能用訪談啟動成功代表 profile reuse 效果已量化 |

### 5.1 M2 進度回寫（2026-05-08）

| 已落地項 | 證據 |
| --- | --- |
| `packages/api-client` 已新增 Profile / Psych Profile / Interview domain client，覆蓋 user profile、consent、feedback history、start/resume/get/respond/skip/cancel/end/retry | `npm run test:m2 --workspace @cj/api-client` 已通過 |
| App Profile home 已接 psych profile、resume status、consent + start session、pending session resume 與 My Story entry | `npm --prefix mobile run test`、`npm --prefix mobile run typecheck` 已通過 |
| App Interview screen 已接共用 AI stream subscription hook，覆蓋 foreground parser、ready snapshot、`after_seq` replay、close/error retry、AppState background abort / foreground `afterSeq` reconnect、delta/persisted/failed 狀態、respond/skip/cancel/end 操作，以及同步狀態不向普通用戶顯示 stream 段號 | `mobile/__tests__/m2-screens.test.js`、`mobile/src/features/m2/api.test.js`、`mobile/src/platform/sse/client.test.js`、`npm --prefix mobile run test -- --runTestsByPath __tests__/m2-screens.test.js --runInBand`、`npm --prefix mobile run smoke:web` 已通過 |
| App My Story screen 已接 psych profile、feedback history 與 delete-all-data mutation；feedback card JSON 會轉為可讀摘要 / 鼓勵 / continuation hint，history title 轉為「第 N 次訪談回饋」，不向用戶裸露原始 JSON 或 `session_id` | `mobile/__tests__/m2-screens.test.js`、`npm --prefix mobile run test`、`npm --prefix mobile run smoke:web` 已通過 |
| App failed interview retry 與 partial-success read-only 分支已接線；Profile 可對 `failed_session_id` 重試整理，Interview `processing_failed` session 進入只讀恢復狀態並提供 retry，不允許繼續送新回答，`completed` + `partial_success=true` session 只顯示補齊提示且不提供 retry | `mobile/__tests__/m2-screens.test.js` 已覆蓋 profile retry、interview failed retry state 與 partial-success read-only state |
| Profile / Interview / Story protected deep-link auth-resume Maestro flow artifact 已建立，並補穩定 App `testID` selector | `mobile/maestro/40-profile-interview-auth-gate-smoke.yaml`、`mobile/scripts/check-maestro-flows.mjs`、`npm --prefix mobile run maestro:check` 已通過；iOS simulator native Maestro flow 已通過 |
| App true-service smoke harness 已支援 M2 auth/profile/psych-profile/consent/interview-resume contract probe；`--deep` 顯式跑 5-turn start/respond/background response/end synthetic flow，並輪詢 session `pipeline_step=6`、psych profile narratives 與 feedback history `feedback_card`，證明 My Story 可用 artifacts 已生成；`--m2-inject-failure` 只允許 local API + local DB，會對 smoke-created session 注入 `processing_failed` 並驗證 resume / readback / retry API；`--m2-inject-partial-success` 只允許 local API + local DB，會對 smoke-created session 注入 `completed` + `pipeline_step=6` + `feedback_card=null` 並驗證 `partial_success=true` 與 retry reject；run mode 受 non-local DB 安全閘保護，`--bootstrap-local-users` 僅允許 local API + local DB | `npm --prefix mobile run smoke:true-service -- --dry-run --scope=all --bootstrap-local-users` 已可執行並在非本機 DB 時 blocked；在 local backend + local Postgres 下，`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev npm --prefix mobile run smoke:true-service -- --run --scope=m2 --bootstrap-local-users --deep --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=30000` 已通過 profile / psych-profile / consent / resume / 5-turn interview start/respond/background AI response/end / my-story completion（observed `pipelineStep=6`、`narrativeCount=2`、`feedbackHistoryCount=1`）；`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev npm --prefix mobile run smoke:true-service -- --run --scope=m2 --bootstrap-local-users --m2-inject-failure --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 failed injection session start、local DB injection、`processing_failed` readback、resume `has_failed` 與 retry accept；`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev npm --prefix mobile run smoke:true-service -- --run --scope=m2 --bootstrap-local-users --m2-inject-partial-success --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=25000` 已通過 partial-success injection session start、local DB injection、`partial_success=true` readback 與 retry `VALIDATION_ERROR` reject |

| 尚未閉環項 | 不得宣稱 |
| --- | --- |
| 已建立 true-service smoke 入口，且 local backend + local Postgres + `--bootstrap-local-users --deep` 已跑通 profile -> consent -> 5-turn interview start/respond/background response/end -> my-story artifacts completion；App failed retry UI / partial-success read-only UI / My Story feedback 摘要與 Interview screen background abort / foreground `afterSeq` reconnect 已有 RNTL gate；local-only `--m2-inject-failure` 已驗證 failed resume / retry API，local-only `--m2-inject-partial-success` 已驗證 `partial_success=true` / retry reject；仍缺 native foreground/background interruption 與 Maestro native evidence | M2 完整完成 |
| 已有 AppState recovery hook 與 screen-level RNTL reconnect gate，但尚未建立真機 / 模擬器 foreground/background interruption recovery 證據 | App SSE lifecycle 已完整驗收 |
| 已建立 Profile / Interview / Story protected deep-link auth-resume Maestro flow artifact，且 iOS simulator native smoke 已通過；仍缺登入後 profile / interview lifecycle 真機證據與 native accessibility 證據 | M2 App e2e / native accessibility 完整完成 |
| true-service failed injection 與 partial_success injection 已補；native lifecycle 分支尚未完整驗證 | Profile + Interview alpha 完整完成 |

## 6. M3 Chat

| 項目 | 裁決 |
| --- | --- |
| 目標 | 讓 App 承接高頻「先聊再判」入口 |
| 主要工作 | room entry、invite landing、accept / decline、message list、message stream、request judgment、judgment status、auth handoff |
| Web 配合 | 抽 chat API client、message normalize、stream event reducer、handoff helper |
| Backend / DB | 不改 chat ownership；Push 邀請提醒後置到 M5 |
| 測試 gate | room create / invite / message / request judgment e2e、stream reconnect、handoff auth fallback |
| 完成定義 | App 用戶可完成 chat room 基本互動並在明確請求後轉 judgment |
| 不得宣稱 | 不能讓 chat 自動轉 judgment；不能弱化 included message / visibility_scope 規則 |

### 6.1 M3 進度回寫（2026-05-08）

| 已落地項 | 證據 |
| --- | --- |
| `packages/api-client` 已新增 Chat domain client，覆蓋 room、invite accept/decline、messages、request-judgment、judgment-status、leave / kick | `npm run test:m3 --workspace @cj/api-client` 已通過 |
| App Chat Home 已接匿名 session bootstrap、create room、invite code accept、未登入 invite accept auth resume 與 room deep link entry；普通入口不再要求手填房間代號，只保留新建對話與邀請 / deep link 承接，且首頁可見文案使用「共同整理 / 對話」語義 | `mobile/__tests__/m3-screens.test.js`、`npm --prefix mobile run typecheck` 已通過 |
| App Chat Room 已接 room/messages/status 查詢、actor gate、room event SSE parser、chat-room AI stream draft hook、ready snapshot / `after_seq` replay、AppState background abort / foreground `afterSeq` reconnect、send message、create invite、explicit request judgment、message time formatting / fallback、協調草稿同步狀態不顯示 stream 段號與 visibility_scope / roleB consent UI gate | `mobile/__tests__/m3-screens.test.js`、`mobile/src/features/m3/api.test.js`、`mobile/src/platform/sse/client.test.js`、`npm --prefix mobile run test -- --runTestsByPath __tests__/m3-screens.test.js --runInBand`、`npm --prefix mobile run smoke:web`、web export route smoke 已通過 |
| App Chat Invite landing 已接 code accept/decline；accept invite 在無 token 時會保存 `/chat/invite?code=...`、導向 `/auth?next=...`，登入後由 Auth resume 回到邀請承接頁，不先打 user-only accept API | `mobile/__tests__/m3-screens.test.js`、web export route smoke 已通過 |
| Chat Room 缺少 room context 時會引導回新建對話 / 邀請連結入口，不再要求普通用戶輸入房間代號；Chat Home / Room / Invite runtime copy 已補「房間」負向斷言，避免把內部 room 模型直接暴露為用戶概念 | `mobile/__tests__/m3-screens.test.js` 已覆蓋 missing context copy、回到對話入口、對話狀態 label、invite landing 文案與 `房間` 負向斷言；`npm --prefix mobile run copy:check` 已禁止 `房間代號` 回到 App 可見文案 |
| Chat invite notification landing path 已接安全映射：backend notification `payload.path` 允許 `/chat/invite/:code` / `/chat/invites/:code`，App 解析為 `/chat/invite?code=...`；`/chat/invites/:code/accept` 仍被拒絕或折回，避免 notification 直接觸發 accept side effect | `mobile/src/platform/linking/native.test.js`、`mobile/src/platform/notifications/native.test.js`、`backend/tests/unit/services/notification.service.test.ts` 已通過 |
| Chat entry / invite / room auth-gate Maestro flow artifact 已建立，並補穩定 App `testID` selector | `mobile/maestro/20-chat-entry-auth-gate-smoke.yaml`、`mobile/scripts/check-maestro-flows.mjs`、`npm --prefix mobile run maestro:check` 已通過；iOS simulator native Maestro flow 已通過 |
| App true-service smoke harness 已支援 M3 anonymous room create、message、invite create、message list、judgment status probe；`--bootstrap-local-users` 可在 local API + local DB 下自動註冊 partner，`--request-ai` 會顯式請求 chat judgment；run mode 受 non-local DB 安全閘保護 | `npm --prefix mobile run smoke:true-service -- --dry-run --scope=all --bootstrap-local-users --request-ai` 已可執行並在非本機 DB 時 blocked；在 local backend + local Postgres 下，`npm --prefix mobile run smoke:true-service -- --run --scope=m3 --bootstrap-local-users --request-ai --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=35000` 已通過 room/message/invite/list/status、partner invite accept、B 方 message、A 方 follow-up 與 request-judgment |

| 尚未閉環項 | 不得宣稱 |
| --- | --- |
| 已建立 true-service smoke 入口，且 local backend + local Postgres + `--bootstrap-local-users --request-ai` 已跑通 create room -> send message -> invite -> accept -> 双方 message -> request judgment；notification invite landing resolver / allowlist、Chat Home 無手填房間代號入口、缺少對話上下文 guidance、Chat runtime「房間」負向文案 gate 與 Chat Room AppState reconnect 已有 unit / screen gate，iOS simulator native smoke 已通過；仍缺 native stream reconnect 真機證據與真 push delivery 下的 invite landing 證據 | M3 Chat 完整完成 |
| 已有 AppState recovery hook 與 Chat Room screen-level RNTL reconnect gate，但尚未建立 native foreground/background stream reconnect 與 retry evidence | App Chat stream lifecycle 已完整驗收 |
| 已建立 Chat entry/auth-gate Maestro flow artifact，且 iOS simulator native smoke 已通過；invite landing path 已有 resolver unit gate，但尚未建立真 push / 真機冷啟動 invite landing 證據 | Chat App e2e / handoff 完整完成 |

## 7. M4 Formal Case + Repair

| 項目 | 裁決 |
| --- | --- |
| 目標 | 承接正式處理與修復旅程完整鏈路 |
| 主要工作 | pairing gate、relationship profile、case list/create/detail/review、judgment detail、reconciliation plans、today step、checkin、replan、dashboard |
| Web 配合 | 抽 case / judgment / repair API client、status normalize、AI phase reducer、upload domain helper |
| Backend / DB | 不改 case status machine；任何 new field / response shape 先進 Parity 待辦 |
| 測試 gate | pairing -> case -> judgment -> repair journey regression、upload fallback、replan stream replay |
| 完成定義 | 登入 App 用戶可完成正式案件到 repair journey 的主要閉環 |
| 不得宣稱 | 不能用 single happy path 代表所有 repair journey 狀態完成 |

### 7.1 M4 進度回寫（2026-05-08）

| 已落地項 | 證據 |
| --- | --- |
| `packages/api-client` 已新增 Formal Case / Repair domain client，覆蓋 pairing、case、judgment、reconciliation、execution | `npm run test:m4 --workspace @cj/api-client` 已通過 |
| App Case screen 已接登入 gate、pairing create/join/cancel、case list/create/submit、judgment generate/accept，並補 pairing invite / formal statement readiness helper | `npm --prefix mobile run test -- m4-screens`、`npm --prefix mobile run typecheck` 已通過 |
| App Repair screen 已接登入 gate、execution dashboard、plans get/generate、select plan、confirm execution、checkin，並補 check-in notes helper | `npm --prefix mobile run test -- m4-screens`、`npm --prefix mobile run smoke:web` 已通過 |
| Case accept judgment 會帶入 Repair route context；Repair screen 已移除手填判斷 / 方案 / 旅程識別輸入，改由 route context、方案卡操作與旅程看板選取承接 | `mobile/__tests__/m4-screens.test.js` 已覆蓋接受判斷後 `/repair?judgmentId=...` handoff、Repair 直接生成方案、方案卡選擇 / 開始與 dashboard replan 對象選取；`npm --prefix mobile run copy:check` 已禁止 `判斷代號`、`方案代號`、`修復旅程識別碼` 回到 App 可見文案 |
| App Repair screen 已接 `replanTrack`、mode / reason 控制、`repair_track` AI stream subscription hook、ready snapshot / `after_seq` replay、close/error retry、AppState background abort / foreground `afterSeq` reconnect 與 `stream.persisted` 後 dashboard refresh | `mobile/src/features/m4/api.test.js`、`mobile/__tests__/m4-screens.test.js`、`npm --prefix mobile run test -- --runTestsByPath __tests__/m4-screens.test.js --runInBand`、`npm --prefix mobile run typecheck` 已通過 |
| App Case evidence upload 已要求用戶顯式確認 safety assertion，不再由 App 默認代簽「不含敏感 / 未成年人 / 非自願 / 違法內容」；picker 取消不記成功 telemetry | `mobile/__tests__/m4-screens.test.js` 已覆蓋默認鎖定、敏感資料 acknowledgement、picker cancel 非成功上傳 |
| Case / Repair route 在無 token 時不裸連 backend，符合 deep link auth gate | web export route smoke 已通過 |
| Case / Repair auth-gate Maestro flow artifact 已建立，並補穩定 App `testID` selector | `mobile/maestro/50-case-repair-auth-gate-smoke.yaml`、`mobile/scripts/check-maestro-flows.mjs`、`npm --prefix mobile run maestro:check` 已通過；iOS simulator native Maestro flow 已通過 |
| App true-service smoke harness 已支援 M4 two-token pairing create/join、formal collaborative case create、formal case evidence upload probe、case list、judgment poll、execution dashboard probe、repair plan select、execution confirm、repair track replan accept 與 `repair_track` SSE terminal 等待；`--bootstrap-local-users` 可在 local API + local DB 下自動註冊雙方，`--request-ai` 顯式生成 repair plans 並觸發 replan；run mode 受 non-local DB 安全閘保護 | 在 Homebrew Postgres 16 local cluster + local backend mock AI 下，`APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev npm --prefix mobile run smoke:true-service -- --run --scope=m4 --bootstrap-local-users --request-ai --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=60000` 已通過 safety、bootstrap users、pairing create/join、formal case create、`m4.evidence_upload`、case list、judgment poll、execution dashboard、repair plan generation、`m4.repair_plan_select`、`m4.execution_confirm`、`m4.repair_track_dashboard`、`m4.repair_track_replan_accept` 與 `m4.repair_track_replan_stream`（`stream.persisted`）；同一組 local DB 另以 backend restart 後 `GET /api/v1/streams/repair_track/:id?after_seq=7` 驗證 DB-backed replay 回 `stream.persisted` 且 response 未壓縮；過程中補齊 notification key schema 長度 normalize、AI stream subscription live race gate、AI stream live DB replay / snapshot fallback 與 SSE no-compression backend gate；`node --check mobile/scripts/app-true-service-smoke.mjs` 與 dry-run gate 也已通過 |

| 尚未閉環項 | 不得宣稱 |
| --- | --- |
| 已建立 true-service smoke 入口，且 local backend + local Postgres + `--bootstrap-local-users --request-ai` 已跑通 pairing -> case -> formal evidence upload -> judgment -> plans -> select -> execution confirm -> live replan stream persisted；backend restart 後 DB-backed `after_seq` replay 也已通過，iOS simulator native smoke 已通過；仍缺 native picker、repair journey 全狀態、native foreground/background reconnect 與 persisted 後 canonical dashboard native 證據 | M4 Formal Case + Repair 完整完成 |
| media evidence upload 已接 M5 native upload adapter、shared upload client 與顯式 safety assertion UI，formal case backend upload 已有 local true-service smoke，Android release APK native ImagePicker picker-cancel evidence 已建立，selected-media backend synthetic fixture upload/delete evidence 已建立；尚未跑真機 picker-selected asset、profile media 或 physical device upload evidence | 正式案件證據上傳已完整驗收 |
| `repair_track` replay 已有 App hook / unit gate、screen-level AppState reconnect gate、local true-service live replan stream `stream.persisted` probe 與 backend restart 後 DB-backed replay probe；尚未跑真機 foreground/background reconnect 與 persisted 後 canonical dashboard native evidence | Repair replan stream 已完整驗收 |
| 已建立 Case / Repair auth-gate Maestro flow artifact，且 iOS simulator native smoke 已通過；仍未覆蓋 repair journey 全狀態、真機 picker、native reconnect 與 canonical dashboard native evidence | App repair journey 完整完成 |

## 8. M5 Push + Deep Link

| 項目 | 裁決 |
| --- | --- |
| 目標 | 建立 App 再進場能力，但保持 notification 狀態由 backend 裁決 |
| 主要工作 | permission、device token registration / revoke、logout cleanup、notification list、push landing、deep link parse、read / snooze / act sync |
| Web 配合 | 抽 notification API client、render payload type、target path normalize |
| Backend / DB | 已新增 `push_device_tokens` schema 與 registration / revoke route；`notifications` 已新增 action / priority / group / read / dismiss / act / snooze metadata 與 Expo ticket / receipt tracking 欄位；App registration-time token rotation revoke、Expo push sender、`dispatch_pending_push_notifications` 與 `poll_push_notification_receipts` job 已接線；App-side `push-delivery:smoke` 可直接向 Expo provider 發送 smoke push 並輪詢 receipt，APNs sandbox 與真機 lifecycle evidence 仍需後續 gate |
| 測試 gate | notification action smoke、deep link auth fallback、logout cleanup、device token revoke inspection |
| 完成定義 | Push 或 in-app notification 可把用戶帶到授權後的具體 App target，並同步 backend notification 狀態 |
| 不得宣稱 | 不能把 Push delivery 成功等同於用戶完成行動 |

### 8.1 M5 進度回寫（2026-05-08）

| 已落地項 | 證據 |
| --- | --- |
| `packages/api-client` 已新增 M5 domain client，覆蓋 notification list / unread-count / read-all / read / snooze / dismiss / act、push device token register / revoke，以及 case evidence upload / delete | `npm run test:m5 --workspace @cj/api-client` 已通過 |
| App Notifications screen 已接登入 gate、feed state、unread count、read-all、read、snooze、dismiss、act target landing，通知時間會格式化為用戶可讀文案並對缺失時間提供安全 fallback，且無 token 時不裸連 backend | `npm --prefix mobile run test`、`npm --prefix mobile run smoke:web`、web export route smoke 已通過 |
| App Deep Link resolver 已能把 backend notification target path 映射到 `/repair`、`/case`、`/chat/room?roomId=...`、`/chat/invite?code=...`、`/profile`、`/quick`、`/notifications` 等 App route；Chat invite notification path 只落到 landing，不直接 accept | `mobile/src/platform/linking/native.test.js`、`mobile/src/platform/notifications/native.test.js`、`backend/tests/unit/services/notification.service.test.ts` 已通過 |
| App Notification landing handler 已接 root layout，可處理 last notification response 與 foreground notification response target，並以 request / notification / href 去重後交給 Expo Router | `mobile/src/platform/notifications/native.test.js`、`mobile/src/features/m5/NotificationLandingHandler.test.js`、web export route smoke 已通過 |
| App notification auth fallback 已接安全白名單、pending landing SecureStore 與 `/auth?next=...` post-login resume；未登入受保護 target 不會被當成已授權 target 直接打開 | `mobile/src/platform/linking/authGate.test.js`、`mobile/src/platform/storage/secureStore.test.js`、`mobile/src/features/m5/NotificationLandingHandler.test.js`、`mobile/__tests__/m1-screens.test.js`、static route smoke 已通過 |
| Notification protected deep-link auth-resume Maestro flow artifact 已建立，覆蓋 `cj://notifications` 未登入保存 pending href 並導向登入承接入口 | `mobile/maestro/30-notification-landing-auth-gate-smoke.yaml`、`mobile/scripts/check-maestro-flows.mjs`、`npm --prefix mobile run maestro:check` 已通過；iOS simulator native Maestro flow 已通過，但尚未證明真 push delivery / notification response |
| 非通知 Deep Link handler 已接 root layout，可處理 cold-start / foreground `cj://` target，公開 Quick 子路由直接打開，受保護 route 未登入會保存 pending href 並走 `/auth?next=...` | `mobile/src/platform/linking/native.test.js`、`mobile/src/features/m5/DeepLinkLandingHandler.test.js`、`mobile/maestro/60-deep-link-auth-resume-smoke.yaml`、`npm --prefix mobile run maestro:check` 已通過；iOS simulator native Maestro flow 已通過 |
| Push permission / token helper 已改為 iOS / Android 動態載入 `expo-notifications`，避免 Web static render 觸發 native side effect | `npm --prefix mobile run smoke:web` 已通過 |
| Backend 已新增 `PushDeviceToken` schema、migration、registration / revoke route、validation 與 service；notification read / read-all / snooze / dismiss / act、device token register / revoke 已補 service / controller / route contract 單元 gate；Expo push sender、pending push dispatch job 與 receipt polling job 已接線，會把 pending push notification 轉成 Expo message、保存 ticket、輪詢 receipt、回寫 sent / failed / receipt ok / receipt error 並清洗 token；App push permission 成功後會同步 register token | `npm --prefix backend test -- --runInBand tests/unit/services/push-notification.service.test.ts tests/unit/services/notification.service.test.ts tests/unit/jobs/cleanup.job.test.ts`、`npm --prefix backend test -- --runInBand tests/unit/controllers/notification.controller.test.ts tests/unit/routes/notification.routes.test.ts`、`npm --prefix backend test -- --runInBand tests/integration/smoke.test.ts`、`npm --prefix mobile run test` 已通過 |
| App 本機清理已改為先嘗試 revoke stored push token，再清 token / session / device metadata；revoke 失敗不阻塞登出並只記 safe telemetry | `mobile/src/features/m5/pushLifecycle.test.js`、`mobile/__tests__/m1-screens.test.js` 已通過 |
| App push token 註冊前會比較本機舊 token；若 Expo token 輪換，先嘗試 revoke 舊 token，再註冊與保存新 token；revoke 失敗不阻塞新 token 註冊並只記 safe telemetry | `mobile/src/features/m5/pushLifecycle.test.js`、`npm --prefix mobile run test` 已通過 |
| Upload adapter 已新增 backend-compatible `files` FormData 與 `safety_assertion` 封裝；Case screen 可經 platform adapter 上傳 evidence，且上傳前要求顯式 safety assertion / 敏感資料 acknowledgement，picker cancel 不記成功 | `mobile/src/platform/upload/native.test.js`、`mobile/__tests__/m4-screens.test.js` 已通過 |
| Telemetry adapter 已新增 token / session / secret / password key redaction，並以 non-blocking 方式送出 `POST /api/v1/telemetry/events`；OpenTelemetry exporter 已改送 `POST /api/v1/telemetry/otlp/v1/traces`，backend 端會再次清洗 sensitive key、限制 batch / context shape 並只寫 structured log，通知與上傳錯誤只回傳 safe context | `mobile/src/platform/telemetry/client.test.js`、`backend/tests/unit/services/app-telemetry.service.test.ts`、`backend/tests/unit/routes/app-telemetry.routes.test.ts`、`backend/tests/integration/smoke.test.ts` 已通過 |
| M5 true-service smoke 已補齊真 backend contract gate，覆蓋 push token register/revoke、notification create/list/unread/read/snooze/dismiss/act/read-all、quick session evidence multipart upload/delete 與 App telemetry ingest | `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev npm --prefix mobile run smoke:true-service -- --run --scope=m5 --bootstrap-local-users --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=30000` 已通過；過程中補齊 `20260508150000_add_notification_action_metadata` migration 並加入 release DB parity 清單 |

| 尚未閉環項 | 不得宣稱 |
| --- | --- |
| 已接 Expo push sender、pending dispatch job、receipt polling job 與 `push-delivery:smoke` structured provider runner，但尚未用真 Expo push token 取得 provider accepted ticket + ok receipt，也尚未建立 APNs sandbox 與真機 cold-start notification landing / push lifecycle 證據 | Push delivery / cold-start landing / Push lifecycle 完整完成 |
| iOS simulator 已跑 custom-scheme protected route auth-resume smoke，Android release APK 已跑 ImagePicker picker-cancel side-effect smoke，selected-media backend upload/delete contract 已在 local backend + local DB 通過；尚未跑真 push permission、notification response cold-start landing、真機 picker-selected asset / profile media upload | M5 native App 驗收完成 |
| notification landing 與非通知 Deep Link 已有 handler、unit gate 與 auth-gate / auth-resume Maestro flow artifact，且 iOS simulator native smoke 已通過；backend read/snooze/act、push token register/revoke、pending push dispatch 與 receipt polling 已有 service/controller/route/job contract gate，M5 local true-service smoke 已通過；`push-delivery:smoke` runner 已建立但尚未有 pass JSON；仍缺真 provider delivery、APNs sandbox 與真機 notification lifecycle evidence | Push + Deep Link 完整完成 |

## 9. M6 Release Hardening

| 項目 | 裁決 |
| --- | --- |
| 目標 | 讓 App 具備可內測、可回滾、可留證的發布能力 |
| 主要工作 | EAS Build profile、iOS TestFlight、EAS Update policy、runtime version policy、Android readiness check、crash / error context、manual evidence pack |
| Web 配合 | 不要求 Web 發布流程同步改版；shared package 變更需維持 Web compatibility |
| Backend / DB | App safe telemetry ingest、CJ OTLP JSON trace ingest、最小化 `app_telemetry_events` persistence、Admin 聚合報表與 30d cleanup 已進入 runtime 並回寫接口 / Parity / RTM；App JS fatal / unhandled promise 已納入 crash session 口徑；native crash SDK configuration 與 `native-crash:runtime:smoke` runner 已接入 App，release audit 只接受 production environment native crash runtime evidence，但 pass 狀態 native crash runtime evidence、external tracing backend 或長期 crash-free sessions 基線仍未建立。若 app version endpoint、native crash provider 或 telemetry dashboard 擴大用途，需再補資料治理與證據文檔 |
| 測試 gate | EAS build smoke、simulator smoke、device evidence、manual regression pack、docs:check |
| 完成定義 | iOS TestFlight build 可被產品/QA 使用，Android 不破壞同一 codebase，evidence 可追溯 |
| 不得宣稱 | 不能用本機 Expo Go 成功替代 TestFlight / build artifact evidence |

### 9.1 M6 進度回寫（2026-05-08）

| 已落地項 | 證據 |
| --- | --- |
| `mobile/app.json` 已補 app version、iOS bundle id、Android package、runtimeVersion、updates policy、notification / image-picker plugin 與 iOS photo permission 文案 | `npm --prefix mobile run release:check` 已通過 |
| `mobile/eas.json` 已新增 development / preview / production build profile 與 production submit placeholder | `npm --prefix mobile run release:check` 已通過 |
| `mobile` repo-local shared dependency contract 已顯式化 | `mobile/package.json` 與 `mobile/package-lock.json` 已把 `@cj/api-client` / `@cj/contracts` 聲明為 `file:../packages/*` dependency，`mobile/metro.config.js` 已為兩者建立 alias；`npm --prefix mobile run release:check` 已檢查 package / lockfile link |
| Expo SDK 55 native dependency set 已對齊 expected versions | `npx expo install --check` 已通過；第一次 iOS Release build 暴露 `expo-notifications@55.0.12` / RN 0.83 Swift compile failure（`RCTSharedApplication` 不可見），已改為 `expo-notifications@~55.0.22`、`react-native@0.83.6`、`react-native-worklets@0.7.4`、`@sentry/react-native@~7.11.0` 等 SDK 55 expected versions |
| `mobile/scripts/check-release-readiness.mjs` 已新增 release config preflight，並在缺少 Expo / Apple / EAS project id、EAS CLI 不可用或不可非互動 authenticated、active developer dir 未選 full Xcode 時輸出 warning / info 而非假裝已發布；root workspace 不含 `mobile` 依 ADR 視為 info，不再作假 warning；若以 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` 執行，CLT active path 不再作 warning | `npm --prefix mobile run release:check`、`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run release:check` 已通過；Maestro CLI 2.5.1 已可由 script 透過 Homebrew Java 17 環境調用 |
| `mobile/scripts/check-native-readiness.mjs` 已新增 native readiness gate，區分 non-strict 檢查與 strict 阻塞模式，並支援 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` 臨時使用 full Xcode 而不全局改本機 `xcode-select` | `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run native:check:strict` 已通過，確認 Full Xcode 26.4.1、iOS 26.4.1 runtime、iPhone 17 simulator、Maestro 2.5.1、EAS CLI 18.4.0 可用；仍以 EAS project id / Expo token / Apple credentials 作 release warning |
| `mobile/scripts/check-android-readiness.mjs` 已新增 Android readiness gate，嚴格檢查 Android package、versionCode、adaptive icon assets、push/upload permissions、Expo plugins、EAS Android build profiles、Android SDK root、Java 17、adb、emulator CLI 與 sdkmanager；腳本已支援 Homebrew Java 17 佈局 | `npm --prefix mobile run android:check` 與 `npm --prefix mobile run android:check:strict` 已通過；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-Toolchain-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Android-Toolchain-2026-05-08.md)，仍不替代 Android App install / flow smoke、physical device 或 build artifact |
| `mobile/scripts/run-android-emulator-smoke.mjs` 已新增 Android emulator boot smoke runner，可啟動指定 AVD、等待 `sys.boot_completed=1`、關閉 emulator 並輸出 JSON evidence | `npm --prefix mobile run android:emulator:smoke -- --avd=CJ_Pixel_9_API_36 --timeout-ms=240000` 已通過；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-Emulator-2026-05-08T10-27-04-283Z.json](../90-證據與盤點/環境與發版驗證/App-Android-Emulator-2026-05-08T10-27-04-283Z.json)，仍不替代 Android App APK 安裝 / flow smoke、physical device 或 build artifact |
| `mobile/scripts/run-android-app-smoke.mjs` 已新增 Android app runtime smoke runner，可在已 boot 的 Android device / emulator 上 build release APK、install、resolve `MainActivity`、顯式 launch 並以 `pidof` / `dumpsys activity` / `dumpsys window` 驗證 foreground window | `npm --prefix mobile run android:app:smoke` 已通過；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-App-2026-05-08T10-43-22-314Z.json](../90-證據與盤點/環境與發版驗證/App-Android-App-2026-05-08T10-43-22-314Z.json)，仍不替代 Android full Maestro flow、physical device、EAS Android artifact 或 Play Store artifact；本地 smoke 以 `SENTRY_DISABLE_AUTO_UPLOAD=true` 跳過 source map upload，正式 release 仍需 Sentry org/project/token |
| `mobile/scripts/run-android-maestro-smoke.mjs` 已新增 Android Maestro smoke runner，串接 Maestro static gate、Android strict readiness、Android app runtime gate 與 7 個 Maestro flow | `npm --prefix mobile run android:maestro:smoke -- --skip-build` 已通過；證據見 [../90-證據與盤點/環境與發版驗證/App-Android-Maestro-2026-05-08T16-22-39-948Z.json](../90-證據與盤點/環境與發版驗證/App-Android-Maestro-2026-05-08T16-22-39-948Z.json)，覆蓋 Android emulator 上 M0-M5 7/7 flow；仍不替代 Android physical device、EAS Android artifact 或 Play Store artifact |
| `mobile/scripts/run-maestro-native.mjs` 已新增 native Maestro execution runner，串接 static flow gate、strict native readiness、逐 flow `maestro test` 與 JSON evidence record，並自動套用 Homebrew Java 17 / Maestro CLI 環境 | `npm --prefix mobile run maestro:native:dry-run` 已通過；`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm --prefix mobile run maestro:native -- --evidence-dir=/tmp/cj-maestro-evidence` 已通過 7/7 flow，證據已回存至 `docs/核心開發文件/90-證據與盤點/環境與發版驗證/App-Native-Maestro-2026-05-08T16-03-15-803Z.json` |
| iOS Release simulator build / install / launch 已通過 | `SENTRY_DISABLE_AUTO_UPLOAD=true DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npx expo run:ios --configuration Release --device "iPhone 17" --no-bundler` 已在 iOS 26.4.1 / iPhone 17 simulator 產生 Release-iphonesimulator `CJ.app`、安裝並以 `com.cj.motherbearcourt` 啟動；證據見 [../90-證據與盤點/環境與發版驗證/App-iOS-Release-Simulator-2026-05-08T12-43-46Z.json](../90-證據與盤點/環境與發版驗證/App-iOS-Release-Simulator-2026-05-08T12-43-46Z.json)，不替代 EAS/TestFlight、physical device、Sentry runtime 或 release DB parity |
| App route topology contract 已納入 `release:preflight` 的 static gate | `npm --prefix mobile run routes:check` 已通過，固定 `(public)` / `(app)` / Quick / Auth / Case / Chat / Profile / Notifications / Repair / `modal` route file set、layout registrations、authenticated deep route tab hiding，並阻止 Expo template tabs 回流；不替代 native navigation / Deep Link / auth resume runtime evidence |
| App feature coverage contract 已納入 `release:preflight` 的 static gate | `npm --prefix mobile run features:check` 已通過，固定 M0-M5 screen `testID` / CTA、shared API endpoint、SSE / Deep Link / Push / Upload / Telemetry adapter、Backend notification / telemetry / push provider route source contract；不替代 true-service / native / external provider evidence |
| App true-service smoke harness contract 已納入 `release:preflight` 的 static gate | `npm --prefix mobile run true-service:check` 已建立，固定 `smoke:true-service` 的 M1-M5 / `all` scope、local-only DB-state injection safety、`--bootstrap-local-users`、`--m1-stream-replay`、`--m1-inject-expired-session`、`--m2-inject-failure`、`--m2-inject-partial-success`、`--request-ai` 與 M1-M5 true-service smoke 關鍵步驟（M1 stream replay / expired session、M2 failed / partial-success、M3 request judgment、M4 repair replan stream、M5 telemetry ingest / upload / notification action）；不替代 local backend 實跑、native lifecycle、provider delivery 或外部 release evidence |
| App web route smoke 已納入 `release:preflight` 的 runtime gate | `npm --prefix mobile run web:routes:smoke` 已通過，會在本地 `mobile/dist` static server 上用 Playwright 驗證 16 條 App route 在 desktop + phone viewport 掛載指定 screen marker、body 非空、無 page error，並檢查首頁主 CTA 樣式；不替代 native navigation / Deep Link / auth resume runtime evidence |
| Maestro flow artifact / selector contract 已納入 `release:preflight` 的 static gate | `npm --prefix mobile run maestro:check` 已通過，覆蓋 M0-M5 的 7 個 flow artifact / 29 個 selector id；native Maestro 已完成 iOS simulator smoke，但不替代 TestFlight / 真機 / provider side-effect evidence |
| Release evidence verifier 已納入 `release:preflight` | `npm --prefix mobile run release:evidence:check` 已通過，會校驗 `App-Native-Maestro-2026-05-08T16-03-15-803Z.json` 的 app id、7/7 flow、static gate、native readiness、flow exit code / duration，也會校驗 `App-iOS-Release-Simulator-2026-05-08T12-43-46Z.json` 的 Expo dependency alignment、Release build、simulator install/launch、Sentry bundle/dSYM phases 與“不替代外部證據”聲明，並確認 Roadmap / release evidence doc 引用同一份證據；2026-05-08T23:09Z 起也固定校驗最新 `App-External-Evidence-Status-*.json` / `App-External-Evidence-Handoff-*.json` 的 App identity、credential boolean schema、status `env_files.values_redacted` / `env_files.loaded[]` schema、device counters、known blocker ids、status/handoff blocker 對齊、當前 `release:completion:audit -- --json` 的 `handoff_blocker_ids` 被 handoff items 覆蓋且標為 `release_completion_blocker=true`、prerequisite-only blockers 不被誤認為 completion blocker、status / handoff / source status / source release audit timestamp coherence、iOS / Android platform-specific next commands、final strict gates、secret-safe summary 與 README / release hardening / `/goal` audit / 待辦台帳引用；`npm --prefix mobile run release:evidence-redaction:check` 已納入 preflight，掃描現有 `App-*.json` evidence pack，並支援 `--evidence-dir=<path>` 追加掃描 workflow runner temp prerequisite report 目錄，阻止 raw Bearer / push token / DB URL / JWT / API key / signed URL / EAS raw artifact URL 欄位進入 structured evidence / artifact；`npm --prefix mobile run release:evidence-sanitization:check` 已納入 preflight，用受控假 secret 觸發 EAS / push / native crash blocked evidence，確認 JSON 與 console output 不泄漏 raw token、build id、push token / access token 或 Sentry event id，並用 clean / leak prerequisite report 固化 `--evidence-dir` 追加掃描的正反驗收 |
| Release completion audit gate 已納入 `release:preflight` | `npm --prefix mobile run device-discovery:check` 與 `npm --prefix mobile run release:external-evidence:status` 已納入 preflight，會以不輸出 secrets / Expo account / UDID / DB host / env value 的方式列出 `extra.eas.projectId` 是否為 UUID 形狀、credential presence（含 push delivery token、Sentry runtime query credentials 與 native crash event id presence）、env-file provenance counters、EAS CLI available/authenticated boolean、iOS / Android 真機可見性、type-aware / identity-bound structured evidence candidate、normalized blockers 與下一步命令；status next commands 已固定同時包含 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排入口、iOS signed app path、iOS / Android physical-device smoke、Android `APP_ANDROID_DEVICE_SERIAL` 與 strict audit gate；status 與 strict completion audit 的外部 evidence 核心判定共用 `mobile/scripts/lib/release-evidence-policy.mjs`，防止 dashboard/status 和 release strict gate 規則漂移；`npm --prefix mobile run release:external-evidence:status:contract` 也已納入 preflight，會驗證 status JSON schema、credential booleans（含 Sentry runtime query credentials 與 native crash event id presence）、env-files redaction / loaded provenance schema、EAS CLI tool booleans、device counters、type-aware / identity-bound evidence candidate 狀態、EAS iOS / TestFlight / EAS Android / physical device / push delivery / native crash runtime / release DB parity valid-invalid fixture pairs 不會把 wrong app identity、dry-run、缺 redaction marker 或 migration count 不足標為 `candidate_pass`、normalized blockers 與 controlled fake secrets 不回洩；`npm --prefix mobile run release:external-evidence:handoff:check` 已納入 preflight，會把 normalized blockers 轉成 owner action、required env keys、正式命令、accepted evidence 與 strict gates，並可輸出 `App-External-Evidence-Handoff-*.json` secret-safe handoff artifact；`npm --prefix mobile run release:external-evidence:handoff:contract` 已納入 preflight，會驗證 handoff JSON schema、15 個 known blocker catalog、controlled secret env 不回洩、handoff report 寫入與 generated redaction scan；`npm --prefix mobile run release:external-evidence:fixtures:check` 已納入 preflight，會用受控 fixture 直接驗證 strict audit 對 EAS iOS + TestFlight、EAS Android、physical device、provider delivery、native crash runtime 與 release DB parity evidence 的正反裁決；2026-05-08 本機探測為 iOS physical connected=0 / offline=2、Android physical connected=0，因此 physical device evidence 仍 pending。`npm --prefix mobile run release:external-evidence:dry-run` 也已納入 preflight，會串接 EAS iOS / EAS Android / iOS physical-device / Android physical-device / push delivery / native crash / telemetry runtime / release DB parity runner dry-run，防止外部證據 runner 腳本壞掉但 audit 只檢查名稱；`npm --prefix mobile run release:external-evidence:signoff` 與 `release:external-evidence:signoff:android-dry-run` 也已納入 preflight，會分別 dry-run 外部 sign-off orchestrator 的 iOS 預設 branch 與 Android physical-platform branch，防止個別 runner 可用但編排入口回退；`npm --prefix backend run ops:release-db:dry-run` 只列出 DB parity query plan 與正式 evidence 命令，不連資料庫、不輸出 host / credentials、不產生 pass evidence；`npm --prefix mobile run release:completion:audit` 已通過 non-strict audit；`npm --prefix mobile run release:completion:audit:strict` 已可驗證 iOS simulator Maestro evidence、iOS Release simulator build/install/launch evidence、Android native toolchain、Android emulator runtime evidence、Android app runtime evidence、Android full flow evidence、native ImagePicker picker-cancel evidence、selected-media backend upload evidence、native crash SDK configuration evidence、iOS Sentry native prebuild configuration 與 OTel provider evidence；iOS Release simulator evidence 必須是 structured `App-iOS-Release-Simulator-*.json`，且必須證明 Expo dependency alignment、Release build、simulator install/launch、Sentry bundle/dSYM phases，並明示不替代 EAS/TestFlight/真機/provider/crash/runtime DB parity；EAS iOS build artifact 與 TestFlight evidence 現在必須是 `mobile/scripts/run-eas-ios-release-smoke.mjs` 產出的 structured `App-EAS-iOS-Release-*.json`，且 EAS build 必須證明 run mode、finished iOS store production build、bundle / version / build number match 與 artifact HEAD/range check pass，TestFlight 必須證明 App Store Connect query pass、matching pre-release version / build number、processing state `VALID` 與 not expired；EAS Android artifact 現在必須是 `mobile/scripts/run-eas-android-release-smoke.mjs` 產出的 structured `App-EAS-Android-Release-*.json`，且必須證明 run mode、finished Android store production build、package / version / versionCode match 與 artifact HEAD/range check pass；physical device evidence 現在必須是 `mobile/scripts/run-physical-device-smoke.mjs` 產出的 structured `App-Physical-Device-*.json`，且必須證明真機、platform readiness、App runtime launch 與 M0 Maestro smoke；iOS explicit `--device` 仍須通過 `xcrun xctrace` connected physical device 驗證，simulator UDID 或 offline device 只允許 blocked evidence；provider delivery evidence 現在必須是 `mobile/scripts/run-push-delivery-smoke.mjs` 產出的 structured `App-Push-Delivery-*.json`，且必須是 run mode、Expo ticket accepted、receipt checked 且 receipt ok；native crash runtime evidence 現在必須是 `mobile/scripts/run-native-crash-runtime-smoke.mjs` 產出的 structured `App-Native-Crash-Runtime-*.json`，且必須是 run mode、Sentry event found、release / production environment match、native runtime observed、crash-like event observed、`blocked=false`；release DB parity 現在必須是 `backend/scripts/check-release-db-parity.ts` 產出的 `App-Release-DB-Parity-*.json`，且 target 為 release / production、DB 分類為 non-local、`ok=true`、missing / incomplete / failed migration 均為空；仍會把 UUID 形狀 EAS project id、EAS CLI 可用性、`EXPO_TOKEN`、Apple credentials、EAS/TestFlight artifact、physical device evidence、APNs / provider delivery 與 native crash runtime evidence 逐項列為 blocker，release DB parity 與 telemetry runtime 目前已有 pass evidence，`TODO` / placeholder 字串不能消除 EAS project id blocker |
| Release completion audit JSON contract 已建立 | `npm --prefix mobile run release:completion:audit -- --json` 會輸出 `app-release-completion-audit` structured record；`npm --prefix mobile run release:completion:audit:contract` 已納入 `release:preflight`，會驗證 non-strict / strict JSON schema、required release blocker ids、頂層 `blocker_ids` 與 blocked checks 一致、check-level `handoff_blocker_ids` 只在該 check blocked 時列入 owner action、`checks[].handoff_catalog_ids` 保留靜態 external handoff catalog mapping、頂層 `handoff_blocker_ids` 與 external handoff catalog coverage 一致、summary 計數、`complete` boolean、strict exit-code 與 preflight wiring。該 contract 只讓 release sign-off 狀態可被 CI / dashboard / agent 穩定解析，不替代任何 EAS / TestFlight / 真機 / provider / release DB pass evidence |
| App Store Connect API credentials blocker 已納入 release completion audit | `release:completion:audit` 現在會把 `app_store_connect_api_credentials` 作為獨立 blocker，與 external status / handoff 口徑對齊；缺 issuer id、key id、private key 或 private key path 時，`release:completion:audit:strict` 不允許只靠 TestFlight evidence placeholder 或手寫文檔通過 |
| External release sign-off orchestrator / workflow 已建立 | `npm --prefix mobile run release:external-evidence:signoff` 預設 dry-run 走 iOS physical-device branch，`npm --prefix mobile run release:external-evidence:signoff:android-dry-run` 走 Android physical-platform branch，兩者都已納入 `release:preflight`；orchestrator 可串接 external status、external handoff、EAS iOS + TestFlight、EAS Android、physical device、push provider delivery、native crash runtime、release DB parity、release completion audit 與 `/goal` completion audit；加 `--report-dir=<path>` 時會由 orchestrator 同時輸出 `App-External-Evidence-Status-*.json` 與 `App-External-Evidence-Handoff-*.json` 交接報告；`npm --prefix mobile run release:external-evidence:validate` 是正式 run 前置條件 gate，會在碰 EAS / provider / 真機安裝 / release DB 前列出缺失或非 UUID 形狀的 EAS project id、secrets、真機 inputs、`xcrun xctrace` / `adb devices -l` 真機可見性與 requested-device match 或 DB；`npm --prefix mobile run release:external-evidence:run` 是正式一鍵入口，不允許 `--skip`，必須執行完整外部 evidence steps 與 strict audits；`.github/workflows/app-release-external-signoff.yml` 已提供手動 CI 入口，mode 支援 `dry-run` / `validate` / `run`，可用 `runner_json` 指向可信 self-hosted macOS / device runner；workflow run-mode probe 會按 `physical_platform` 分別要求 iOS `APP_IOS_DEVICE_UDID` + `APP_IOS_DEVICE_APP_PATH` 或 Android `APP_ANDROID_DEVICE_SERIAL`；workflow 在外部 sign-off 前會先跑 App `release:preflight`，並用 step-level `APP_RELEASE_EXTERNAL_SIGNOFF_RUN=false` 防止 `mode=run` 時 preflight 提前觸碰 EAS / provider / 真機 / release DB；之後會跑 `release:evidence-redaction:check` / `release:evidence-sanitization:check` / `release:external-evidence:handoff:contract`，signoff 後再生成 `App-External-Evidence-Handoff-*.json` handoff artifact，再以 `--evidence-dir="${APP_RELEASE_EXTERNAL_SIGNOFF_REPORT_DIR}"` 跑 generated evidence redaction，同時覆蓋 repo evidence 與 runner temp status / handoff / prerequisite report 目錄，只有 redaction step 通過才 upload artifact；`npm --prefix mobile run release:external-evidence:workflow:check` 已納入 `release:preflight`，會固定 mode / runner / physical-platform-specific secret probe / preflight-before-signoff / generated redaction / artifact path / status report / handoff report / prerequisite report artifact contract。但仍必須由各 structured evidence JSON 與 `release:completion:audit:strict` / `goal:completion:audit:strict` 裁決，不能用 orchestrator / workflow 執行成功本身替代外部證據 |
| Prerequisite report contract gate 已建立 | `npm --prefix mobile run release:external-evidence:prereq-report:check` 已納入 `release:preflight`；會用受控假 secret 與缺失 file path 分別觸發 validate-only、正式 run、Android physical-platform validate、`--release-env-file=release.env.example` placeholder validate 與 unsafe release-env-file key 前置失敗，確認 `App-External-Signoff-Prerequisites-*.json` 不含 token / DB URL / push token / UDID / serial / Sentry event id，stdout/stderr 不回洩 raw secret，report 只用 boolean / counter 暴露 EAS CLI availability、credential / device / signed app / push / Sentry / DB input presence、iOS / Android `device_visibility` counters 與 requested-device match，`REPLACE_WITH_...` placeholder 不會被當成已配置，`NODE_OPTIONS` / 非白名單 key 會在 validation 前被拒絕，並為每個 missing prerequisite 輸出 secret-safe `resolution_hints`，且正式 run 前置失敗不會開始 EAS / provider / 真機安裝 / release DB / strict audit steps；同時確認正式 run 拒絕 `--skip`，並驗證 dry-run orchestrator 在 `--report-dir` 下會安全寫出 status + handoff 兩類交接報告，且 dry-run status / handoff 必須包含 `sentry_runtime_query_credentials` 與 `native_crash_event_id` prerequisite-only item，避免跳過外部 evidence、Sentry 查詢 credentials、受控 crash event id 或 strict audit 後誤認為完成。`goal:completion:audit:contract` 已直接檢查該 contract source 固定 Android validate / `android_physical_device_visible` / requested-device match / env-file placeholder guard / key allowlist / `sentry_runtime_query_credentials` 與 `native_crash_event_id` dry-run handoff，不允許 `/goal` audit 只看 script 名稱。該 gate 只驗證 prerequisite / status / handoff report 安全可交接，不替代 EAS / TestFlight / 真機 / provider / release DB pass evidence |
| Release env template contract gate 已建立 | `mobile/release.env.example` 已新增為 external sign-off 本地 env 模板，且全部 secret-like 欄位只保留 `REPLACE_WITH_` placeholder，預設 `APP_RELEASE_EXTERNAL_SIGNOFF_RUN=false`；`npm --prefix mobile run release:external-evidence:env-template:check` 已納入 `release:preflight`，會驗證模板必填 keys、placeholder-only secret value、`/tmp` report dir、`--release-env-file=release.env.local` runbook 命令與交接說明；orchestrator 只解析白名單內 `KEY=value` / `export KEY=value`，不 eval、不輸出 value，且 placeholder 不算已配置，`NODE_OPTIONS` / 非白名單 key 不允許進入 release runner；該 gate 不替代 repository secrets、workflow validate 或 external pass evidence |
| GitHub secret sync dry-run contract 已建立 | `npm --prefix mobile run release:external-evidence:github-secrets:sync:contract` 已納入 `release:preflight`；它用受控臨時 env-file 與 private key fixture 驗證 `github-secrets:sync -- --json` dry-run 在沒有 `gh` / GitHub auth / network 時仍可完成本機 readiness 判定，固定 current completion / evidence refresh secret group 分層、`DATABASE_URL -> APP_RELEASE_DATABASE_URL`、ASC private key path -> `APP_STORE_CONNECT_PRIVATE_KEY`、controlled secret redaction 與 apply-only GitHub dependency。這只保證補 GitHub Environment secrets 前的本機交接工具不回退，不消除 EAS / TestFlight / 真機 / provider / native crash blockers |
| Telemetry runtime release env-file、native CORS 合約與 backend version precheck 已閉環 | `mobile/scripts/run-telemetry-runtime-smoke.mjs` 已支援 `--release-env-file=release.env.local`，可直接讀取 gitignored release input 中的 `APP_TELEMETRY_RUNTIME_API_BASE_URL`，並拒絕未知 key、placeholder 與 raw value output；`release:external-evidence:status`、handoff 與 runbook 的 telemetry 建議命令已改為 env-file 路徑。首輪 non-local release API 實跑回 `CORS_ORIGIN_DENIED`，指出 production CORS 會擋住 native 無 `Origin` telemetry；該 blocked 診斷只記入待辦，不保留在 canonical evidence pack，避免 release audit 將 blocked JSON 視為 invalid candidate。`backend/src/app.ts` 已收窄修正為只允許 `/api/v1/telemetry/events` 與 `/api/v1/telemetry/otlp/v1/traces` 兩個 App native ingest endpoint 接受無 `Origin`，非白名單瀏覽器 `Origin` 與其他 API 無 `Origin` 仍拒絕；`mobile/scripts/run-telemetry-runtime-smoke.mjs` 也已在 run mode 先查 release backend `/version`，要求 `commitSha` 等於 runner 執行當下本地 `HEAD`，不對齊時停止於 event / OTLP POST 之前。audit 允許後續只追加 docs / evidence commit，但若 evidence backend commit 後改過 backend telemetry/version runtime 路徑，舊 evidence 會失效。2026-05-29 canonical `App-Telemetry-Runtime-2026-05-29T16-00-52-498Z.json` 已 `blocked=false`、`backend_version_passed=true`、event / OTLP accepted，`release:completion:audit -- --json` 已把 `telemetry_runtime_evidence` 標為 passed |
| Core docs preflight gate 已納入 App release gate | `mobile/package.json` 已新增 `docs:check` 與 `docs:audit:dry-run:current`，並串入 `release:preflight`；App release gate 現在會同時跑根目錄文檔結構、truth check、過期 App 狀態守衛與 current SSOT metadata dry-run，避免代碼與 release evidence 通過但核心文件進度或現行 SSOT 漂移 |
| App `/goal` completion audit gate 已建立並納入 `release:preflight` | `npm --prefix mobile run goal:completion:audit` 已建立 prompt-to-artifact checklist，覆蓋 stack、iOS-first / Android-compatible、M0-M5 artifact、route topology gate、web route smoke gate、true-service smoke harness contract、UI/UX accessibility / copy gate、測試 gate、`docs:check` / `docs:audit:dry-run:current` core-doc gate、外部 evidence fixture contract、外部 release handoff contract、外部 release env template contract、GitHub secret sync dry-run contract、核心文檔回寫與 release sign-off；當 `release_signoff` 未完成時，`goal:completion:audit -- --json` 會直接把 `release:completion:audit -- --json` 的 `blocker_ids`、`handoff_blocker_ids`、warnings 與 blockers 放入 `release_signoff.caveat`，供 CI / dashboard / agent 交接解析；`npm --prefix mobile run goal:completion:audit:contract` 已驗證 JSON schema、required checklist ids、summary 計數、`complete` boolean、strict 退出碼一致性，並固定 `tests_and_gates` 必須要求 `routes:check`、`features:check`、`true-service:check`、`web:routes:smoke`、`docs:check`、`docs:audit:dry-run:current`、`release:evidence:check`、`release:external-evidence:fixtures:check`、`release:external-evidence:handoff:check`、`release:external-evidence:handoff:contract`、`release:external-evidence:env-template:check` 與 `release:external-evidence:github-secrets:sync:contract`，且固定 `check-app-true-service-smoke-contracts.mjs` 必須保留 M1-M5 true-service smoke contract、local-only DB-state injection safety 與 preflight wiring，固定 `release:evidence:check` source 必須保留 external status / handoff snapshot、status env_files redaction/provenance schema、status-handoff blocker 對齊、current release completion audit `handoff_blocker_ids` coverage、release/prerequisite classification、timestamp coherence、platform-specific commands、final gates 與 docs references，固定 `ui_ux_accessibility` 必須包含 feature label module copy coverage、`copy:check` source 必須掃描 `src/features` / `labels.ts` / PsychDomain raw enum，讓 audit record 可供 CI / dashboard 消費；證據見 [../90-證據與盤點/環境與發版驗證/App-Goal-Completion-Audit-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Goal-Completion-Audit-2026-05-08.md)。`release:preflight` 會跑 non-strict `goal:completion:audit` 與 contract gate 作進度盤點，但不消除任何 release blocker；`goal:completion:audit:strict` 必須在 `release:completion:audit:strict` 也通過後才可視為 `/goal` 完成；外部 release blockers 未清零前不得調用 `/goal` complete |
| App `/goal` completion audit current contract 已對齊 release sign-off 交接 | 2026-05-08T23:29Z 後的 `goal:completion:audit:contract` 會直接檢查 `tests_and_gates` 覆蓋 `features:check`、`release:evidence:check`、`release:completion:audit:contract`、`release:external-evidence:status:contract`、`release:external-evidence:prereq-report:check`、`release:external-evidence:workflow:check` 與 `release:external-evidence:env-template:check`，並檢查 `release:evidence:check` source 固定 `App-External-Evidence-Status-*` / `App-External-Evidence-Handoff-*`、known blocker ids、status env_files redaction/provenance schema、status/handoff 對齊、當前 release completion audit `handoff_blocker_ids` 覆蓋檢查、release/prerequisite classification、timestamp coherence、iOS / Android validate commands、`/goal` audit docs reference 與 Roadmap wildcard reference；也會檢查 release completion audit contract source 固定 `blocker_ids` / status-scoped `handoff_blocker_ids` consistency、`checks[].handoff_catalog_ids` mapping、external handoff coverage、required blocker ids、strict exit-code 與 preflight wiring；當 `release_signoff` 未完成時，contract 會檢查 caveat 包含 `blocker_ids`、`handoff_blocker_ids`、`app_store_connect_api_credentials`，並只在 release DB parity 或 telemetry runtime 仍未解時要求對應 blocker id；當前 non-strict `/goal` audit 為 11 passed / 1 missing，唯一 missing 仍是 `release_signoff` |
| Release sign-off blocker snapshot 已對齊 JSON audit | 當前 `release:completion:audit -- --json` 頂層 `blocker_ids` 為 `eas_project_id`、`expo_token`、`apple_submission_credentials`、`app_store_connect_api_credentials`、`eas_ios_build_artifact`、`testflight_evidence`、`physical_device_evidence`、`eas_android_build_artifact`、`apns_or_provider_delivery_evidence`、`native_crash_runtime_evidence`；頂層 `handoff_blocker_ids` 會映射到 external handoff catalog 的 10 個 owner action，external handoff report 會以 `release_completion_blocker=true` 標示，external status / handoff report 另暴露 Sentry runtime query credentials、native crash event id 與 iOS / Android physical device visibility prerequisite-only blockers。任何不在這些 structured evidence runner 產出的 pass evidence 仍不得消除 blocker |
| External handoff platform command contract 已建立 | `release:external-evidence:handoff:check` 的 handoff catalog 已固定 iOS / Android `release:external-evidence:validate/run -- --physical-platform=...` 編排命令與底層 `physical-device:smoke` 命令；`release:external-evidence:handoff:contract` 會直接檢查 platform-specific validate / run command string，避免外部 owner 只拿到底層 smoke 指令而繞過 prerequisite validation、status / handoff report、redaction 與 strict audit 編排 |
| Physical device structured evidence runner 已建立 | `npm --prefix mobile run physical-device:smoke -- --dry-run --platform=ios` 與 `--platform=android` 已通過；iOS 模式要求 connected / trusted iPhone、已簽名 `.app` 或 `--skip-install`、`devicectl` launch 與 M0 Maestro flow，Android 模式要求 adb 可見非 emulator 實機、release APK install / launch 與 M0 Maestro flow；blocked JSON 或手寫 markdown 不可消除 release blocker |
| EAS iOS / TestFlight structured evidence runner 已建立 | `npm --prefix mobile run eas-ios-release:smoke -- --dry-run` 已通過；正式 run 需要 `EXPO_TOKEN` 查詢 EAS production store build metadata、驗證 bundle id / version / build number / artifact URL HEAD，若加 `--require-testflight` 還需要 App Store Connect API credentials 查詢 TestFlight build availability；`.ipa` path、raw build id、blocked JSON 或手寫 markdown 不可消除 release blocker |
| EAS Android structured evidence runner 已建立 | `npm --prefix mobile run eas-android-release:smoke -- --dry-run` 已通過；正式 run 需要 `EXPO_TOKEN` 查詢 EAS Android production store build metadata、驗證 package / version / versionCode / artifact URL HEAD；本機 APK、emulator smoke、blocked JSON 或手寫 markdown 不可消除 Android EAS artifact blocker |
| Platform side-effect boundary static gate 已納入 `release:preflight` | `npm --prefix mobile run platform:check` 已通過，可阻止 screen / feature 層繞過 `mobile/src/platform` 直接調用 native Expo side effect；不替代真機 native side-effect evidence |
| Native upload picker-cancel evidence 已建立 | `npm --prefix mobile run native:upload:smoke -- --skip-build` 已在 `CJ_Pixel_9_API_36` / Android release APK 上通過 static contract、platform boundary、upload adapter unit、Android readiness、APK install/launch 與 `maestro-side-effects/70-native-upload-picker-smoke.yaml`，證據見 [../90-證據與盤點/環境與發版驗證/App-Native-Upload-2026-05-08T11-18-22-284Z.json](../90-證據與盤點/環境與發版驗證/App-Native-Upload-2026-05-08T11-18-22-284Z.json)；不替代真機 picker-selected asset、profile media、physical device 或 media provider authorization |
| Selected-media backend upload evidence 已建立 | `APP_SMOKE_BACKEND_DATABASE_URL=postgresql://alex@127.0.0.1:5432/cj_app_upload_smoke_20260508193900?schema=public npm --prefix mobile run selected-media:upload:smoke -- --run --api-base-url=http://127.0.0.1:3001/api/v1 --timeout-ms=30000` 已在臨時 Homebrew PostgreSQL DB + local backend 上通過 `platform:check`、upload adapter unit、M5 true-service synthetic PNG upload/delete；證據見 [../90-證據與盤點/環境與發版驗證/App-Selected-Media-Upload-2026-05-08T11-39-17-168Z.json](../90-證據與盤點/環境與發版驗證/App-Selected-Media-Upload-2026-05-08T11-39-17-168Z.json)，臨時 DB 已刪除；不替代真機 picker-selected asset、profile media 或 physical device evidence |
| App release-sensitive migrations 已納入 release DB parity 清單並補清單覆蓋測試 | `npm --prefix backend test -- --runInBand tests/unit/scripts/check-release-db-parity.test.ts` 已通過，覆蓋清單引用的 migration 目錄存在、App migrations 均進清單、missing / failed / rolled back 阻塞，並驗證 release DB parity evidence 只保存 target / provider / local classification / migration report，不保存 `DATABASE_URL` 或 host |
| Local DB release parity gate 已實跑 | 在 `postgresql://cj:cj_dev_pass@127.0.0.1:55432/cj_dev` 上，`npm --prefix backend exec -- prisma migrate status --schema backend/prisma/schema.prisma` 顯示 25 migrations up to date，`npm --prefix backend run ops:release-db:check` 回報 14/14 required migrations applied；仍不替代 release / production DB parity |
| Release / production DB parity structured evidence 已通過 | `npm --prefix backend run ops:release-db:dry-run` 已提供不連 DB 的 runner preflight；正式 evidence 已由明確 release / production `DATABASE_URL` 執行 `npm --prefix backend run ops:release-db:evidence` 產出 `App-Release-DB-Parity-2026-05-16T06-23-53-463Z.json`，且 `release:completion:audit -- --json` 已把 `release_production_db_parity` 標為 passed。local DB、staging DB、raw console output 或手寫 markdown 仍不能替代；後續若新增 release-blocking migration，必須重新產生 fresh non-local structured evidence |
| App telemetry persistence / Admin report / 30d retention first pass 已接入 backend | `backend/prisma/migrations/20260508124000_add_app_telemetry_events/migration.sql`、`backend/src/services/app-telemetry.service.ts`、`GET /api/v1/admin/reports/app-telemetry`、`cleanup_app_telemetry`；`npm --prefix backend test -- --runInBand tests/unit/services/app-telemetry.service.test.ts tests/unit/routes/app-telemetry.routes.test.ts tests/unit/controllers/admin.controller.test.ts tests/unit/routes/admin.routes.test.ts tests/unit/jobs/cleanup.job.test.ts tests/integration/smoke.test.ts` 已通過；仍不替代 native crash runtime evidence / external tracing backend / 長期 crash-free baseline |
| App observability bootstrap 已建立首輪 | `mobile/src/providers/ObservabilityBootstrap.tsx`、`mobile/src/platform/telemetry/observability.ts` 已接 session start、lifecycle transition、global JS fatal / unhandled promise、OpenTelemetry provider span export 與 native crash provider status context，`captureTelemetry` payload 已帶 build number，backend crash-free 聚合納入 `app_js_fatal` / `app_unhandled_promise`，`/telemetry/otlp/v1/traces` 已提供 CJ OTLP JSON trace ingest baseline；`npm --prefix mobile test -- --runInBand src/platform/telemetry/client.test.js src/platform/telemetry/observability.test.js src/providers/AuthSessionBootstrap.test.js` 與 `npm --prefix backend test -- --runInBand tests/unit/services/app-telemetry.service.test.ts` 已通過；仍不替代 native crash runtime evidence、APNs/TestFlight crash-free evidence 或 external tracing backend |
| App-side OpenTelemetry provider evidence 已建立 | [../90-證據與盤點/環境與發版驗證/App-OTel-Provider-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-OTel-Provider-2026-05-08.md) 記錄 `@opentelemetry/api` / `@opentelemetry/sdk-trace-base` / `@opentelemetry/core` 接入、`BasicTracerProvider` + `SimpleSpanProcessor` + safe exporter、CJ OTLP JSON trace ingest、trace/span id safe summary 與 targeted test / typecheck；不替代 native crash runtime evidence、external tracing backend 或 TestFlight crash-free sessions |
| Native crash SDK configuration evidence 已建立 | [../90-證據與盤點/環境與發版驗證/App-Native-Crash-SDK-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Native-Crash-SDK-2026-05-08.md) 記錄 `@sentry/react-native` dependency、Sentry Metro config、DSN-gated native crash adapter、safe `beforeSend` redaction、native crash provider status context、Android native `sentry.gradle` / `sentry.properties` 接線、iOS native `sentry-xcode.sh` bundle phase 與 `sentry-xcode-debug-files.sh` dSYM upload phase；不替代 Sentry DSN / source map upload / native crash runtime evidence / TestFlight crash-free sessions |
| Native crash runtime structured runner 已建立 | `npm --prefix mobile run native-crash:runtime:smoke -- --dry-run` 已通過；正式 run 需要 Sentry org / project / auth token 與受控 native crash event id，並由 `release:completion:audit:strict` 驗證 `App-Native-Crash-Runtime-*.json` 的 provider query、release / production environment match、native runtime signal、crash-like event 與 `blocked=false`；runner 不保存 Sentry auth token 或 event id 原文，不替代 TestFlight crash-free sessions / external tracing backend |
| Android native toolchain evidence 已建立 | [../90-證據與盤點/環境與發版驗證/App-Android-Toolchain-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Android-Toolchain-2026-05-08.md) 記錄 Android SDK root、Java 17、adb、emulator CLI、sdkmanager 與 strict readiness/audit 驗證；不替代 Android App install / flow smoke、physical device 或 build artifact |
| Android emulator runtime evidence 已建立 | [../90-證據與盤點/環境與發版驗證/App-Android-Emulator-2026-05-08T10-27-04-283Z.json](../90-證據與盤點/環境與發版驗證/App-Android-Emulator-2026-05-08T10-27-04-283Z.json) 記錄 `CJ_Pixel_9_API_36` AVD、Android 16 / API 36 arm64 emulator、`sys.boot_completed=1` 與 27.9s boot duration；不替代 Android App APK 安裝 / flow smoke、physical device 或 build artifact |
| Android app runtime evidence 已建立 | [../90-證據與盤點/環境與發版驗證/App-Android-App-2026-05-08T10-43-22-314Z.json](../90-證據與盤點/環境與發版驗證/App-Android-App-2026-05-08T10-43-22-314Z.json) 記錄 release APK `assembleRelease`、APK sha256、`adb install -r`、`com.cj.motherbearcourt/.MainActivity` launch、foreground window 與 process id；不替代 Android full Maestro flow、physical device 或 EAS / store artifact |
| Android full flow evidence 已建立 | [../90-證據與盤點/環境與發版驗證/App-Android-Maestro-2026-05-08T16-22-39-948Z.json](../90-證據與盤點/環境與發版驗證/App-Android-Maestro-2026-05-08T16-22-39-948Z.json) 記錄 Android strict readiness、Android app runtime gate 與 M0-M5 7/7 Maestro flow pass；不替代 Android physical device、EAS Android artifact 或 store artifact |
| 發版證據入口已建立 | [../90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md](../90-證據與盤點/環境與發版驗證/App-Release-Hardening-2026-05-08.md) |
| App export 驗證仍通過 | `npm --prefix mobile run typecheck`、`npm --prefix mobile run smoke:web` 已通過 |

| 尚未閉環項 | 不得宣稱 |
| --- | --- |
| 尚未配置 `extra.eas.projectId`、`EXPO_TOKEN`、Apple submission credentials / App Store Connect API credentials | EAS build / submit / `eas-ios-release:smoke --run --require-testflight` 可在 CI 非交互執行 |
| 本機預設 active developer dir 仍指向 Command Line Tools；以 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` 可使用 Full Xcode 26.4.1、iOS 26.4.1 runtime、iPhone 17 simulator、Maestro CLI 2.5.1 與 Java 17，且 `maestro:native` runner 已產生 iOS simulator pass JSON evidence；`eas-ios-release:smoke`、`eas-android-release:smoke`、`physical-device:smoke` 與 `push-delivery:smoke` runner 已建立但尚未產生 pass JSON；尚未產生 EAS/TestFlight build artifact、physical device evidence、APNs delivery evidence | M6 Release Hardening 完整完成 |
| Android config readiness gate、strict toolchain gate、emulator boot smoke、release APK install/launch smoke、Android Maestro 7/7 flow smoke 與 EAS Android structured runner 已建立並通過 dry-run；本機尚未建立 Android physical device evidence、pass 狀態 EAS Android artifact 或 Play Store artifact | Android readiness 完整完成 |
| 尚未跑 native notification delivery / landing、cold-start Deep Link、真機 picker-selected asset / profile media；Android ImagePicker picker-cancel smoke 與 selected-media backend upload/delete contract 已建立但不替代真機 upload | App release evidence pack 完整完成 |
| native crash SDK configuration first pass 與 `native-crash:runtime:smoke` runner 已接入；CJ OTLP JSON trace ingest baseline 已接入；尚未配置 Sentry DSN / org / project / auth token，尚未取得 pass 狀態的 `App-Native-Crash-Runtime-*.json`、external tracing backend 或 TestFlight crash-free sessions 證據 | Release observability 完整完成 |
| 尚未用 release / production `DATABASE_URL` 實跑 `ops:release-db:check` | Release / Production DB parity 完整完成 |

## 10. 跨里程碑 Gate

每個 milestone 進入實作前，必須核對：

1. 對應需求是否在 [02-App完整版本工程PRD.md](./02-App完整版本工程PRD.md) 有 `CJ-PRD-APP-*`。
2. 對應能力是否在 [../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md](../50-跨端Mapping與Parity/01-App首輪能力與工程落點Mapping.md) 有 Web / Backend / API / DB / shared 對照。
3. 若牽動 API / DB / shared / Push / Deep Link / upload / telemetry，是否已更新待辦與 ADR。
4. 測試是否符合 [../08-測試規範與驗收/03-App測試與證據接入基線.md](../08-測試規範與驗收/03-App測試與證據接入基線.md)。
5. 證據是否能落到 `90-證據與盤點/` 或測試回歸包，而不是只留在口頭結論。

## 11. 推進順序裁決

App 完整版按 M0 -> M1 -> M2 -> M3 -> M4 -> M5 -> M6 推進。不得先做 Push / Deep Link 或完整 repair journey 來繞過 M0 / M1，因為 storage、API adapter、session restore 與 Quick + Auth 是後續所有鏈路的共同風險面。

若需要縮短第一個可測版本，允許交付 M0 + M1 作為 iOS internal alpha，但該版本只能宣稱「Quick + Auth alpha」，不能宣稱完整 App MVP。
