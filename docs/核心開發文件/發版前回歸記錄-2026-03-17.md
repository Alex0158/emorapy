# 發版前回歸記錄（2026-03-17）

**文檔版本**：v1.1  
**最後更新**：2026-03-28  
**定位**：記錄本輪針對核心業務閉環與證據鏈的實際回歸結果。

---

## 1. 回歸範圍

- 快速體驗 `F01`
- 協作聽證 `F02`
- 正式案件/判決 handoff `F03/F04`
- 執行鏈 `F05`
- 訪談/畫像 `F06`
- Chat 與轉判決 `F07`
- 配對/檔案 `F08`
- 認證/帳號 `F09`
- Admin 治理 `F10`

---

## 2. 已執行命令與結果

### 2.1 前端 E2E

```bash
npm exec -- playwright test -c e2e/playwright.config.ts \
  e2e/chat/quick-experience-flow.e2e.ts \
  e2e/chat/quick-experience-claim-session.e2e.ts \
  e2e/chat/judgment-handoff.e2e.ts
```

結果：

- `7 passed`

### 2.2 前端關鍵頁面/路由/狀態恢復

```bash
npm run test -- \
  src/store/authStore.test.ts \
  src/pages/Execution/CheckIn/index.test.tsx \
  src/pages/Chat/Room/index.test.tsx \
  src/router/index.test.tsx
```

```bash
npm run test -- \
  src/pages/Interview/Chat/index.test.tsx \
  src/pages/Interview/Result/index.test.tsx \
  src/pages/Profile/MyStory/index.test.tsx \
  src/pages/Admin/AuditLogs/index.test.tsx \
  src/pages/Admin/Configs/index.test.tsx \
  src/pages/Admin/Users/index.test.tsx
```

```bash
npm run test -- \
  src/pages/QuickExperience/Collaborative/index.test.tsx \
  src/pages/Case/Create/index.test.tsx \
  src/pages/Case/List/index.test.tsx \
  src/pages/Case/Review/index.test.tsx \
  src/pages/Case/Detail/index.test.tsx \
  src/pages/Execution/Dashboard/index.test.tsx \
  src/pages/Profile/Pairing/index.test.tsx \
  src/pages/Judgment/Detail/index.test.tsx
```

結果：

- 共 `544 passed`

### 2.3 後端 integration

```bash
npm test -- --runInBand \
  tests/integration/chat-invite-judgment-flow.test.ts \
  tests/integration/admin-api-flow.test.ts
```

結果：

- `9 passed`

### 2.4 後端 quick-experience flow

```bash
RUN_FLOW_TESTS=true npm test -- --runInBand tests/integration/quick-experience.flow.test.ts
```

結果：

- `38 passed`
- 其中已覆蓋 `F02 協作聽證` 的真 DB 主鏈路、錯誤 session 與重複提交保護

### 2.5 前端聚合收斂回歸

```bash
npm run test -- --run \
  src/pages/QuickExperience/Create/index.test.tsx \
  src/pages/QuickExperience/Result/index.test.tsx \
  src/pages/QuickExperience/Collaborative/index.test.tsx \
  src/pages/Judgment/Detail/index.test.tsx \
  src/pages/Reconciliation/Detail/index.test.tsx \
  src/pages/Execution/Dashboard/index.test.tsx \
  src/pages/Profile/Index/index.test.tsx
```

```bash
npm run test:e2e -- \
  quick-experience-claim-session.e2e.ts \
  judgment-handoff.e2e.ts \
  chat-failure-matrix.e2e.ts \
  execution-flow.e2e.ts \
  interview-recovery-flow.e2e.ts
```

結果：

- 單元/頁面測試：`267 passed`
- Playwright：`19 passed`, `1 skipped`
- 覆蓋 `QuickExperience / Judgment / Reconciliation / Execution / Profile` 收口後的聚合回歸

### 2.6 Production-like smoke

```bash
ADMIN_SEED_EMAIL=admin-smoke@example.com \
ADMIN_SEED_PASSWORD=AdminPass1234 \
npm run prisma:seed
```

```bash
BACKEND_BASE_URL=http://127.0.0.1:3001 \
FRONTEND_BASE_URL=http://127.0.0.1:4173 \
ORIGIN=http://127.0.0.1:4173 \
ADMIN_EMAIL=admin-smoke@example.com \
ADMIN_PASSWORD=AdminPass1234 \
bash ./scripts/smoke-production-like.sh
```

結果：

- `PASS: production-like smoke checks completed`
- 已覆蓋：
  - `/health` / `/health/ready`
  - quick session
  - quick case create + get case detail
  - frontend admin login route
  - admin login API
  - admin me API

### 2.7 Admin 前端真服務 E2E

```bash
E2E_BASE_URL=http://127.0.0.1:5173 \
E2E_ADMIN_EMAIL=admin-smoke@example.com \
E2E_ADMIN_PASSWORD=AdminPass1234 \
npm test -- admin/admin-critical-flow.e2e.ts admin/admin-config-flow.e2e.ts
```

結果：

- `6 passed`
- 已覆蓋：
  - admin login -> ops/configs/reports/audit-logs 頁面覆蓋
  - config feature flag 讀寫
  - admin user create/update/delete 審計留痕
  - 不可自刪 / 不可停用自己 / 不可修改自己角色
  - 停用管理員後既有 token 立即失效

### 2.8 P01 claim-session 真服務預檢

```bash
bash ./scripts/smoke-claim-session-production-like.sh
```

結果：

- `PASS: claim-session production-like preflight completed`
- 已覆蓋：
  - quick session create
  - quick case create
  - register token claim-session
  - DB 取碼後 verify-email
  - verified login
  - login token claim-session 冪等
  - quick case session continuity 讀取
  - DB 綁定驗證：`user.email_verified=true`、`case.plaintiff_id=user.id`

### 2.9 歷史外層 last-run 工件同步（僅歷史記錄）

本段是 2026-03-17 的歷史操作記錄，現行流程已不再依賴外層腳本。

現行口徑：

- 以套件內測試輸出與 `npm run --workspace frontend test:e2e:critical-guard` 為準
- 發版前人工證據以 `manual-regression:*` 系列命令與 `docs/核心開發文件/發版前手動回歸證據/<date>/` 為準

### 2.10 Chat failure matrix（skip 收斂補跑）

```bash
npm exec --workspace frontend -- playwright test -c e2e/playwright.config.ts e2e/chat/chat-failure-matrix.e2e.ts
```

結果：

- `9 passed`
- `0 failed`
- `0 skipped`
- `getChatRoom 失敗時應顯示錯誤、點擊 retry 成功後應顯示聊天室` 已由 skip 轉為正式用例

### 2.11 關鍵 E2E skip 防回退檢查

```bash
npm run --workspace frontend test:e2e:critical-guard
```

結果：

- `[critical-e2e-skip-guard] ok: checked 6 files, no .skip found`
- 已新增 `scripts/check-critical-e2e-skips.mjs`，對關鍵 chat e2e 套件做 `.skip` 阻斷

### 2.12 手動回歸證據門禁與匯總（repo 內腳本）

```bash
npm run manual-regression:check
npm run manual-regression:check:strict
npm run manual-regression:summarize
```

結果（當前）：

- `manual-regression:check` 可正確阻斷 `PENDING/缺欄位`（目前 P01~P05 仍為 `PENDING`）
- `manual-regression:check:strict` 會額外驗證 `截圖/錄屏` 路徑存在且含檔案
- `manual-regression:summarize` 會回寫 `docs/核心開發文件/發版前手動回歸證據/<date>/summary.md`
- `manual-regression:mark` 可命令式回填 `P01-P05/record.md` 單流程欄位
- `manual-regression:gate` 可一鍵串行 `check -> check:strict -> summarize`
- 已新增：
  - `scripts/init-manual-regression-batch.mjs`
  - `scripts/check-manual-regression-status.mjs`
  - `scripts/summarize-manual-regression.mjs`
  - `scripts/mark-manual-regression-result.mjs`
  - `scripts/run-manual-regression-gate.mjs`

### 2.13 Staging smoke gate（workflow + 腳本）

```bash
npm run smoke:staging
```

結果（當前）：

- 若未提供 `BACKEND_BASE_URL/FRONTEND_BASE_URL/STAGING_ADMIN_EMAIL/STAGING_ADMIN_PASSWORD` 會明確 fail-fast
- 已新增：
  - `scripts/smoke-staging.sh`
  - `.github/workflows/staging-smoke.yml`（workflow_dispatch，使用 `STAGING_*` secrets）

### 2.14 Manual regression gate（workflow + strict evidence）

新增：

- `.github/workflows/manual-regression-gate.yml`（workflow_dispatch）

用途：

- 在 CI 執行 `manual-regression:check` + `manual-regression:check:strict`
- 在 CI 以 `manual-regression:gate` 作為單一入口，避免漏步
- 支援 `date` 參數檢查指定批次
- 自動產生並上傳 `summary.md` artifact

---

## 3. 本輪修補點

為使真 DB flow 測試可穩定重跑，本輪補了以下收斂：

- `backend/tests/setup.ts`
  - 測試環境顯式載入 `.env.test.local` / `.env.test` / `.env`
  - 補上 `SKIP_RATE_LIMIT=true`
- `backend/src/middleware/rateLimiter.ts`
  - `NODE_ENV=test` 且 `SKIP_RATE_LIMIT=true` 時跳過限流
- `backend/tests/integration/helpers/test-utils.ts`
  - 清理測試資料前等待 quick judgment 背景任務排空，避免 FK race
- `backend/src/services/judgment.service.ts`
  - collaborative case 生成判決時允許以 session 權限進入
- `e2e/admin/admin-critical-flow.e2e.ts`
  - 對齊 `frontend-admin` 現行文案，並修正 strict-mode 斷言
- `e2e/admin/admin-config-flow.e2e.ts`
  - 對齊 `frontend-admin` 現行配置頁標題文案
- `scripts/smoke-claim-session-production-like.sh`
  - 新增 `P01` 的真服務 + 真 DB 預檢腳本，覆蓋 register/verify/login/claim 一條鏈
- `backend/tests/integration/fixtures/quick-experience.fixtures.ts`
  - 修正過時樣本
- `backend/tests/integration/quick-experience.flow.test.ts`
  - 對齊 `defendant_statement=''` 會被正規化為 `null` 的現行語義
- `backend/tests/integration/admin-api-flow.test.ts`
  - 補 `PUT /api/v1/admin/configs` 的 `config:write` + audit log integration
  - 補 `PATCH /api/v1/admin/users/:userId/status` 的 `users:write` + audit log integration
  - 補 `GET /api/v1/admin/audit-logs` 的成功查詢證據

---

## 4. 結論

- 本輪核心主流程的自動化證據鏈已補齊。
- 前端主鏈頁、關鍵 E2E 與後端 quick-experience 真 DB flow 均已有可複述結果。
- 本輪已補 `F02-真DB驗證記錄-2026-03-17.md` 與 `高風險API抽檢記錄-2026-03-17.md`。
- 本輪已補 `production-like smoke` 與 `admin` 真服務 E2E 的可複述結果。
- 本輪已補 `P01-claim-session真服務預檢記錄-2026-03-17.md`，補足 `claim-session` 的真服務 + 真 DB 預檢證據。
- 本輪已補 `發版前手動回歸包-2026-03-17.md`、`發版前手動回歸執行版-2026-03-17.md`、`發版前手動回歸證據/2026-03-17/`、`scripts/check-manual-regression-status.mjs`、`scripts/init-manual-regression-batch.mjs`、`已知風險清單-2026-03-17.md` 與 `不納入發版項清單-2026-03-17.md` 的正式模板/口徑。
- `chat-failure-matrix.e2e.ts` 的 skip 已收斂為正式回歸用例並完成實跑。
- 目前剩餘缺口主要集中在 `發版前手動回歸包` 的實際人工執行與結果填寫。
- 後續若再做發版評審，可直接以本記錄、`業務缺陷收斂台帳-2026-03-17.md`、`P01-claim-session真服務預檢記錄-2026-03-17.md`、`F02-真DB驗證記錄-2026-03-17.md`、`高風險API抽檢記錄-2026-03-17.md`、`已知風險清單-2026-03-17.md` 與 `不納入發版項清單-2026-03-17.md` 為基線，並補齊手動回歸執行結果。
