# Emorapy 測試覆蓋現狀基線與缺口矩陣

日期：2026-03-07  
用途：在正式補測前，先回答「現有測試資產有哪些、主要覆蓋了什麼、缺什麼、下一步先做哪一批」。

## 一、判定依據與範圍

- `docs/核心開發文件/功能特性清單.md`
- `docs/核心開發文件/業務流程整合.md`
- `docs/核心開發文件/頁面清單.md`
- `docs/核心開發文件/全接口清單-主文檔.md`
- `docs/核心開發文件/接口-功能-頁面-Mapping.md`
- `docs/核心開發文件/接口描述/*`
- `backend/tests/**/*.test.ts`、`frontend/src/**/*.{test,spec}.{ts,tsx}`、`frontend/e2e/**/*.e2e.ts`、`e2e/**/*.e2e.ts`

## 二、現有測試資產快照

- 功能主表：`10` 個主功能（`F01-F10`）+ `4` 個保留能力附錄（`F11-F14`）
- 主流程：`5` 條（P01-P05）
- 路由總數：`29`；其中 `ProtectedRoute=16`、`PublicRoute=3`、`Public=10`
- 接口總數：`101`；其中 `已使用=91`、`候選廢棄=10`
- 測試檔案總數：`278`；測試案例總數：`3475`
- 後端：`97` 檔 / `1491` 案例；前端單元/元件：`173` 檔 / `2100` 案例；E2E 合計：`7` 檔 / `16` 案例

### 2026-03-17 增量同步

本章保留 2026-03-07 基線數字，不直接覆寫為 2026-03-17 的即時總數。若需重算整體檔案數/案例數，應另做全倉統計。

本輪新增的正式同步信息是：

- 前端測試對齊報告已補兩份：
  - `docs/測試/03-失敗分析與修復/Emorapy-前端測試對齊報告-F01-F02-F04-F07-20260317.md`
  - `docs/測試/03-失敗分析與修復/Emorapy-前端測試對齊報告-F03-F05-F06-F08-F10-20260317.md`
- 最新定向驗證結果：
  - F01/F02/F04/F07：`7 passed` E2E、`194 passed` 關鍵頁面/狀態測試
  - F03/F05/F06/F08/F10：`22 files passed`、`515 passed`、`4 skipped`
- 本輪結論：
  - 未新增前端真業務缺陷
  - 已修正測試殼/觀測點噪音，涉及 `ChatRoom`、`authStore`、`CaseDetail`、`ProfilePairing`
  - 測試現況與當前產品行為一致，後續失敗的定位信號更乾淨

## 三、按功能主表的覆蓋現狀

### F01 快速體驗、結果與升格

- 覆蓋判定：`中高`
- 現有測試檔數：`15`；案例數：`240`
- 本輪進展：
  - 已修復 **F01-BUG-003**：結果頁證據上傳成功後若 `fetchCase()` 拋錯，原誤判為上傳失敗；現改為 try/catch 隔離，僅 log 不影響上傳成功態
  - 已補 QuickExperience Result：證據上傳成功後應呼叫 fetchCase 刷新案件、fetchCase 失敗時應保持 success 狀態且不顯示上傳失敗、證據上傳失敗且 message 為空字串時應使用 evidenceUploadFail（F10 邊界）；**證據上傳 FORBIDDEN 且無 message 時應使用 evidenceUploadFail**（F01 權限邊界 fallback）；**證據上傳成功但組件已卸載時不應呼叫 message.success**、**judgment_failed 時 retry 成功但組件已卸載時不應呼叫 message.success**、**輪詢獲取判決成功但組件已卸載時不應 setState**、**fetchCase 成功但組件已卸載時不應 setState**、**fetchCase 返回 NOT_FOUND 但組件已卸載時不應 navigate 或 message**（useMountedRef 回歸：F01-OPT-002，避免 F01-BUG-001 同類問題）
  - `auth.service` / `auth.controller` / `auth.routes` 已補 `claim-session` 護欄並通過定向測試
  - `QuickExperience/Create`、`QuickExperience/Result`、`authStore` 定向 Vitest 已通過；QuickExperience Create createQuickCase 失敗且 message 為空字串時應使用 submitFail（F10 邊界）已補、**createQuickCase FORBIDDEN 且無 message 時應使用 submitFail**（F01 權限邊界 fallback）已補；**createQuickCase 失敗後應仍可再次點擊提交，成功後應跳轉結果頁**（F01 錯誤恢復：失敗不阻塞重試）已補；**原告陳述不足 30 字時下一步按鈕應 disabled**、**原告陳述 exactly 30 字時下一步應 enabled**（邊界：validateStatement 30 字正邊界）、**原告陳述超過 2000 字時下一步應 disabled**、**原告陳述 exactly 2000 字時下一步應 enabled**（邊界：validateStatement 30-2000 規則上界正邊界）已補
  - 已補 QuickExperience Result：getCase 與 getJudgmentByCaseId 皆失敗時應顯示錯誤並可點擊 retry 或返回創建頁、**getCase 與 getJudgmentByCaseId 皆失敗時點擊 retry 應呼叫 startPolling**（F01 錯誤恢復：雙失敗不阻塞導航出口、retry 觸發輪詢）、**getCase 與 getJudgmentByCaseId 皆失敗時點擊 retry 成功後應顯示判決內容**（F01 錯誤恢復：retry 成功閉環）、**長時間等待分支點擊繼續等待成功後應顯示判決內容**（F01 錯誤恢復：繼續等待成功閉環）、**長時間等待分支點擊重新生成成功後應顯示判決內容**（F01 錯誤恢復：重新生成成功閉環）；**judgment_failed 時 retry 快速連點只會送出一次 generateJudgment 請求**（F01 重試節流，retryJudgmentLockRef）
  - 已補 QuickExperience Create：**createSession 失敗時應仍顯示表單且可填寫**（F01 錯誤恢復：session 失敗不阻塞表單）；**createSession 失敗後應仍可提交，createQuickCase 成功後應跳轉結果頁**（F01 錯誤恢復：session 失敗不阻塞提交）；**createQuickCase 失敗時應仍可點擊關閉並導向 /**（F01 錯誤恢復：失敗不阻塞導航出口）；**createQuickCase FORBIDDEN 且無 message 時應使用 submitFail**（F01 權限邊界 fallback）
  - 已補 QuickExperience Result：**證據上傳失敗後應仍可再次點擊上傳，成功後應顯示成功**（F01 錯誤恢復：失敗不阻塞重試）
  - `F01-BUG-001` 已定位為 `useMountedRef` 在 `StrictMode` 下導致成功分支不導航，現已修復並重跑 Playwright 通過
  - `F01-BUG-002` 已定位為 session refresh 未保留舊 session、且未同步更新 `caseSessionMap`，現已修復並補齊定向測試
  - `claim-session` 已新增前端 `auth API` / `authStore` 定向測試，並補後端 flow 測試護欄覆蓋「首次 claim」與「不可覆蓋既有綁定」
  - 已補 `auth.service.claimSession`：**關聯案件不存在（case findUnique 返回 null）**、**關聯案件存在但非 quick 模式** 兩分支分離測試（F01 claim-session 分支覆蓋）
  - 已補 `claimSessionSchema` 驗證測試：session_id 必填、空字串拒絕、超過 100 字元拒絕、剛好 100 字元通過（F01 claim-session 輸入護欄）
  - 已補 **auth API claimSession**：data 為 { case_id: null } 時應正確返回（F01/F09 claim-session 邊界）；**data 為 { case_id: undefined } 時應正規化為 { case_id: null }**（F01/F09 邊界：API 回傳不完整時防禦）
  - 已補 **case API getCase**：回應缺少 case、case 為 null 或 undefined 時應拋錯（F01/F03 邊界：API 回傳不完整時防禦）；**case API createQuickCase/createCase**：後端回傳 case 為 null 時應拋錯（F01/F03 邊界：API 回傳不完整時防禦）；**case API submitCase/updateCase**：後端回傳 case 為 null 時應拋錯（F03 邊界：API 回傳不完整時防禦，提交案件與被告回覆）；**judgment API getJudgmentByCaseId**：後端回傳 200 且 judgment 為 null 或 undefined 時應返回 null（F01/F04 邊界：pending 語義，API 回傳不完整時防禦）
  - 已補 E2E **quick-experience-flow**：createQuickCase 失敗時應顯示錯誤、可再次提交且成功導向 result；Result 頁 getJudgment 失敗時應顯示錯誤、點擊 retry 成功後應顯示判決內容（F01 錯誤恢復）
  - 已補 `storage.caseSessionMap.replaceSession`：oldSessionId 或 newSessionId 為空字串時應直接返回不寫入（F01 邊界，防止誤寫入）；**oldSessionId 等於 newSessionId 時應直接返回不寫入**（F01 邊界：避免冗餘寫入）；**caseSessionMap.set**：caseId 或 sessionId 為空時應直接返回不寫入（F01 邊界：防止無效映射）；**sessionStorage.set**：sessionId 為空時應直接返回不寫入（F01 邊界：與 caseSessionMap 一致，防止無效 session）；**case API getCaseBySessionId**：後端回傳 200 且 case 為 null 或 undefined 時應返回 null（F01 邊界：無關聯案件、API 回傳不完整時防禦）；**案件 NOT_FOUND/HTTP_404 時應顯示 caseNotFoundOrExpired、清理映射並跳轉創建頁**（F01 案件不存在/過期反饋）；**judgment_failed 時 retry，generateJudgment FORBIDDEN 且無 message 時應使用 retryFail**（F01 權限邊界 fallback）；**案件 completed 時不顯示 evidence 上傳區塊**（F01 evidence 回訪邊界：completed 不可上傳）；**證據上傳在 caseSessionMap、sessionStorage、store session 三源都缺失時應顯示 sessionIdMissing 且不調用 uploadEvidence**（F01 邊界：session 缺失防護）
  - 已補 `sessionStore`：createSession/refreshSession 失敗後再次調用應可重試且成功（F01 錯誤恢復：inflight 清除後不阻塞）；**session API createSession/refreshSession**：後端回傳 data 為 null 時應拋錯（F01 邊界：API 回傳不完整時防禦）；**session API createSession/refreshSession**：後端回傳 session_id 為空字串時應拋錯（F01 邊界：空 session 視為無效）
  - 已補 `session.service`：**addCaseToSession/addPairingToSession session 不存在時應拋出 SESSION_EXPIRED**（F01 邊界：與 psych-profile giveConsent 對齊，避免 Prisma P2025 轉成 INTERNAL_ERROR）
  - 已補 E2E `quick-experience-flow`：**createQuickCase 失敗時應顯示錯誤、可再次提交且成功導向 result**（F01 錯誤恢復 E2E）；**content API getContentList**：後端回傳 items 為非陣列時應返回空陣列、**items 為 null 時應返回空陣列**（F01 邊界：API 回傳不完整時防禦）
  - **QuickExperience Result AIAnalyzingAnimation**：tips 為非陣列時應使用 safeTips 防禦，避免 setInterval 內 `(prev+1)%tips.length` 因 undefined 導致 NaN 崩潰（F01 邊界：組件層防禦）
  - **QuickExperience Result**：**getContentList 失敗時應不影響頁面渲染**（tips 非關鍵，F01 邊界：loadTips 有 try/catch，失敗不阻塞主流程）；**getCase 回傳 evidences 為非陣列時應不崩潰且不誤設 success**（F01 邊界：API 回傳不完整時防禦，Array.isArray 護欄）
- 已覆蓋重點：
  - 建立/刷新匿名 session、匿名 quick case、結果頁判決讀取、補證據、註冊提示等主流程已有測試資產。
  - 後端 `quick-experience.flow`、`session.service`、`judgment`/`case` API 層與前端 `Create/Result` 頁已有較多單測。
- 主要缺口：
  - `claim-session` 已明確對齊為 F01/F09 共用後置流程，但仍需補更完整升格真鏈路 E2E。
  - `useMountedRef` 被多個頁面共用，仍需做跨頁回歸確認沒有同類問題。
  - `session refresh` 核心缺陷已修復，但仍需補更完整的結果頁真鏈路與 evidence 回訪回歸。

### F02 協作聽證（同設備雙人）

- 覆蓋判定：`中`
- 現有測試檔數：`1`；案例數：`30`
- 本輪進展：
  - 已建立 `F02` 專項測試設計文檔
  - 已補 E2E **collaborative-flow**：role_b 提交失敗時應顯示錯誤、可再次提交且成功導向 result（F02 錯誤恢復）
  - 已補 `case.service` collaborative 狀態轉移護欄
  - 已補 `case.controller` / `case.routes` 的 `POST /collaborative` 分支
  - 已補 Collaborative 頁對 `sessionStorage` 與 `caseSessionMap` 寫入的前端護欄；role_a/role_b 提交失敗且錯誤無 message 時 message.submitFail；role_a/role_b 提交失敗且 message 為空字串時應使用 submitFail（F10 邊界）；**role_a/role_b createCollaborativeCase FORBIDDEN 且無 message 時應使用 submitFail**（F02 權限邊界 fallback）；**case API createCollaborativeCase**：後端回傳 case 為 null 時應拋錯（F02 邊界：API 回傳不完整時防禦）；**F02-OPT-001**：補 useMountedRef 於 handleRoleBSubmit；**role_b createCollaborativeCase 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
  - 已新增 Playwright `collaborative -> result` 主鏈路 E2E，並驗證通過
  - 已補後端 integration / flow 測試案例（happy path、session mismatch、重複提交），當前環境因 `RUN_FLOW_TESTS` 未開啟，僅完成檔案載入驗證
  - 已定位並修復 `F02-BUG-001`：`collaborative` case 在 `Result` 頁承接時，後端錯把匿名 session 案件當成登入案件，導致 `getCase/getJudgment/uploadEvidence` 可能被 `UNAUTHORIZED/FORBIDDEN`
  - **已修復 F02-BUG-003**：role_b 提交失敗時無導航出口，現已補齊 role_b 返回按鈕（導向 handoff），並補測試「role_b 提交失敗時應仍可點擊返回並導向 handoff」
  - 已補 **role_a 提交失敗後應仍可再次點擊提交，成功後應進入 handoff**、**role_b 提交失敗後應仍可再次點擊提交，成功後應導向 result**（F02 錯誤恢復：失敗不阻塞重試）
  - 已補後端 `case.service` / `judgment.service` / `evidence.controller` 與前端 `QuickExperience/Result` 錯誤矩陣護欄
  - 已優化 Collaborative 測試：移除 constants mock，改用真實 MIN_STATEMENT_LENGTH(30)/MIN_DEFENDANT_LENGTH(10)，補 role_a exactly 29 字邊界、role_b exactly 9 字邊界、**role_b exactly 10 字時提交應 enabled**（MIN_DEFENDANT_LENGTH 正邊界）
  - **F02-BUG-002 補充**：IPV_SIGNAL_REGEX 補「打人」「摔碗」；新增 `safety-routing.service.test.ts`（35 例），驗證打人/動手/推/扇巴掌/拉扯/摔碗等語句必須進入 safety_support、自傷/自殺進入 crisis_support、一般衝突進入 standard（F02 安全分流護欄）
  - 已補 **role_a createCollaborativeCase SESSION_EXPIRED 時應顯示錯誤且可點擊返回導向 intro**（F02 session 過期：role_a 導航出口）；**role_b createCollaborativeCase SESSION_EXPIRED 時應顯示錯誤且可點擊返回導向 handoff**（F02 session 過期：role_b 導航出口）
- 已覆蓋重點：
  - 前端協作頁已有單測；後端 `cases/collaborative` 已在 API 與流程中有證據。
- 主要缺口：
  - 後端 integration / flow 層已補案例，但仍待在可用 DB 環境下完成真實執行驗證。
  - `F02-BUG-001` 已修復，但仍需在真 DB 下驗證 `collaborative -> result` 的 case/judgment/evidence 全鏈路。
  - 已補 **role_a/role_b createCollaborativeCase SESSION_EXPIRED 時應顯示錯誤且可點擊返回導向 intro/handoff**（F02 session 過期：導航出口）；**collaborative 案件在 result 頁應可正確載入**（F02 result 回訪：QuickExperience Result 共用頁補 collaborative 模式測試）

### F03 正式案件管理

- 覆蓋判定：`中高`
- 現有測試檔數：`8`；案例數：`256`
- 本輪進展：
  - 已建立 `F03` 專項測試設計文檔
  - 已定位並修復 `F03-BUG-001`：正式 `collaborative` 建案缺少 `defendant_statement` 仍可直接進 `submitted`
  - 已補 `createCaseSchema` 與 `case.service.createCase()` 對 collaborative 雙方陳述的雙護欄
  - 已補 `CaseDetail` 頁對「等待被告回覆」與「submit -> review」的前端護欄；getCase 失敗且無 message 時 common.getCaseFail、submitCase 失敗且無 message（非 FORBIDDEN）時 message.submitCaseFail、被告回覆 updateCase 失敗且無 message 時 caseDetail.defendantRespondFail、**被告回覆 updateCase FORBIDDEN 且無 message 時應使用 defendantRespondFail**（F03 權限邊界 fallback）、被告回覆 updateCase 失敗且 message 為空字串時應使用 caseDetail.defendantRespondFail（F10 邊界）；completed 案件點擊查看判決 getJudgmentByCaseId 失敗且 message 為空字串時應使用 getJudgmentFail（F10 邊界）、**completed 案件點擊查看判決 getJudgmentByCaseId FORBIDDEN 且無 message 時應使用 getJudgmentFail**（F03 權限邊界 fallback）
  - 已補 `CaseReview` 對 `draft -> detail redirect` 與 `judgment_failed -> retry` 的前端護欄；**F03-OPT-003**：補 useMountedRef 於 handleRetryJudgment、**generateJudgment 成功但組件已卸載時不應呼叫 message.success**（useMountedRef 回歸）
  - 已補 `Case Create`：pairing pending/active、提交 disabled、表單填妥提交成功導向 `/case/:id`、**無證據時提交，createCase 成功後不應調用 uploadEvidence**（F03 證據邊界）、**createCase 成功但證據上傳失敗時應顯示 warning 且仍導向案件詳情**（F03 證據失敗恢復）；**F03-OPT-002**：補 useMountedRef 於 handleSubmit、**F03-OPT-004**：補 useMountedRef 於 checkPairing；**createCase 成功但組件已卸載時不應呼叫 message.success 或 navigate**、**checkPairing 成功/失敗但組件已卸載時不應 setState**（useMountedRef 回歸）
  - 已補 `Case List`：getCaseList 錯誤時 message.error；錯誤無 message 時使用 getCaseListFail 文案；getCaseList 失敗時仍可點擊建立案件按鈕導向 /case/create（F03 錯誤恢復：失敗不阻塞建立入口）；**getCaseList 回傳 cases 為 undefined 或非陣列時應顯示空狀態不崩潰**（F03 邊界：API 回傳不完整時不崩潰，Array.isArray 防禦）；**case API getCaseList**：後端回傳 data 為 null 時應拋錯（F03 邊界：API 回傳不完整時防禦）；**case API getCaseList**：後端回傳 cases 為非陣列時應返回空陣列（F03 邊界：API 層防禦）；**case API getCaseList**：後端回傳 pagination 為 null/undefined 或部分欄位缺失時應返回預設 pagination（F03 邊界：API 回傳不完整時防禦）；**case API getCaseList**：後端回傳 pagination 為非物件（如字串）時應返回預設 pagination（F03 邊界：API 回傳不完整時防禦）；**case API uploadEvidence**：後端回傳 evidences 為 null 時應拋錯、evidences 為非陣列時應返回空陣列（F03/F05 邊界：API 回傳不完整時防禦）
  - 已補 `Case Detail`：**getCase 失敗時應仍可點擊 backList 導向 /case/list**（F03 錯誤恢復：失敗不阻塞導航出口）；**getCase 回傳 NOT_FOUND 時應顯示錯誤 Alert 並可點擊 backList 或 retry**（F03 錯誤恢復：與其他失敗一致，失敗不阻塞導航出口）；**getCase 失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示案件內容**（F03 錯誤恢復：失敗不阻塞重試）；**getCase 失敗時 retry 再次失敗應顯示該次錯誤訊息**、**getCase 失敗時 retry 再次失敗且 message 為空字串應使用 common.getCaseFail**（F03 重試錯誤反饋、F10 邊界）；**submitCase 失敗後應仍可再次點擊提交，成功後應導向 review**（F03 錯誤恢復：失敗不阻塞重試）；**submitCase 提交按鈕快速連點只會送出一次 submitCase 請求**（F03 提交節流）；**被告回覆 updateCase 失敗後應仍可再次點擊回覆，成功後應導向 review**（F03 錯誤恢復：失敗不阻塞重試）；**completed 案件點擊查看判決失敗後應仍可再次點擊，成功後應導向判決頁**（F03 錯誤恢復：getJudgmentByCaseId 失敗不阻塞重試）；**被告陳述 exactly 29 字時回覆按鈕應 disabled**、**被告陳述 exactly 30 字時回覆按鈕應 enabled**（邊界：validateStatement 30 字規則）
  - 已補 `Case Review`：getCase FORBIDDEN/NOT_FOUND、getCase 失敗且非 FORBIDDEN/NOT_FOUND 且無 message 時 common.getCaseFail、getCase 失敗且 message 為空字串時應使用 common.getCaseFail（F10 邊界）、completed 有判決時顯示並導向判決頁、judgment_failed 重試失敗顯示錯誤並重拉案件、重試失敗且錯誤無 message 時 review.retryFail；**judgment_failed 重試 generateJudgment FORBIDDEN 且無 message 時應使用 review.retryFail**（F03 權限邊界 fallback）；judgment_failed 重試 JUDGMENT_EXISTS 時 getJudgmentByCaseId 失敗且 message 為空字串應使用 review.retryFail（F10 邊界）；**getCase 失敗時應仍可點擊 backList 導向 /case/list**（F03 錯誤恢復：失敗不阻塞導航出口）；**getCase 回傳 NOT_FOUND 時應顯示錯誤 Alert 並可點擊 backList 或 retry**（F03 錯誤恢復：與其他失敗一致，失敗不阻塞導航出口）；**getCase 失敗時點擊 retry 應重新呼叫 getCase**、**getCase 失敗時 retry 再次失敗應顯示該次錯誤訊息**、**getCase 失敗時 retry 再次失敗且 message 為空字串應使用 common.getCaseFail**（F03 getCase 重試分支、F10 邊界）；**getCase 失敗時 retry 快速連點只會送出一次 getCase 請求**（F03 重試節流，fetchLockRef）；**getCase 失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示審理中畫面**（F03 錯誤恢復：失敗不阻塞重試）；**已修復 F03-BUG-005**：cancelled 案件進入 review 時原誤顯示審理中，現已補 cancelled 分支；**cancelled 案件進入 review 時應提示並導回案件列表**（F03 狀態邊界）
  - 已補 `Case List`：變更狀態篩選時以新參數重新調用 getCaseList；**getCaseList FORBIDDEN 且無 message 時應使用 getCaseListFail**（F03 權限邊界 fallback）；**已修復 F03-BUG-002**：getCaseList 失敗時現有 retry 按鈕，**getCaseList 失敗時應仍可點擊 retry 重新呼叫，成功後應顯示案件列表**、**retry 失敗後應仍可再次點擊 retry**（F03 錯誤恢復：失敗不阻塞重試）；**getCaseList 失敗後變更狀態篩選應重新呼叫 getCaseList，成功後應顯示案件列表**（F03 錯誤恢復：失敗不阻塞重試）；**已優化**：Case List retry 新增 fetchLockRef 防止快速連點重複請求，與 Case Review、Profile Pairing 等對齊；**getCaseList 失敗時 retry 快速連點只會送出一次 getCaseList 請求**（F03 重試節流）
  - 已補 `Case Create`：createCase 失敗且錯誤無 message 時 message.createCaseFail、createCase 失敗且 message 為空字串時應使用 message.createCaseFail（F10 邊界）、**createCase FORBIDDEN 且無 message 時應使用 createCaseFail**（F03 權限邊界 fallback）、**createCase 成功但證據上傳失敗時應顯示 warning 且仍導向案件詳情**、證據上傳失敗且錯誤無 message 時 message.evidenceUploadFailCaseCreated、**證據上傳失敗且 message 為空字串時應使用 evidenceUploadFailCaseCreated（F10 邊界）**（F03 證據失敗恢復）；**createCase 成功但證據上傳 FORBIDDEN 且無 message 時應使用 evidenceUploadFailCaseCreated**（F03 權限邊界 fallback）；**createCase 失敗後應仍可重新提交，表單不鎖死**（F03 錯誤恢復：失敗不阻塞重試）；**已修復 F03-BUG-003**：getPairingStatus 失敗時現有 retry 按鈕、**getPairingStatus 失敗時應顯示錯誤與 retry、點擊 retry 成功後應顯示建立表單**（F03 錯誤恢復：失敗不阻塞重試）、**getPairingStatus FORBIDDEN 且無 message 時應使用 getPairingFail**（F03 權限邊界 fallback）；**remote 模式下原告不足 30 字時提交應 disabled**、**remote 模式下原告剛好 30 字時提交應 enabled**（正邊界）、**collaborative 模式下原告 exactly 29 字時提交應 disabled**（邊界：validateStatement 30 字規則）
  - 已補 `Case Detail`：**draft collaborative 案件且雙方皆有陳述時應顯示提交按鈕且可提交**、**draft collaborative 案件且僅原告有陳述時不應顯示提交按鈕**（F03 draft 邊界、F03-BUG-004）；**draft remote 案件且被告已有陳述時應顯示提交按鈕且可提交**（F03 draft 邊界，remote 與 collaborative 對稱）；**已修復 F03-BUG-004**：collaborative 僅原告有陳述時原仍顯示提交按鈕，現已擴展 needsDefendantResponse 涵蓋 collaborative，後端 submitCase 亦補 defendant_statement 驗證；**F03-OPT-001**：補 useMountedRef 於 handleSubmit、handleDefendantRespond、查看判決 onClick，**submitCase 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）；**submitCase 回傳 CASE_NOT_EDITABLE 時應顯示 submitCaseFail**（F03 draft 邊界：案已提交 race condition）
  - 已補 `FileUpload`：**caseId 時刪除證據應二次確認，確認後呼叫 deleteEvidence 並更新列表**（F03 證據刪除流程）；**deleteEvidence 失敗時應顯示錯誤且不更新列表**（F03 證據刪除錯誤處理）
- 已補 `validate` 工具：**validateStatement 長度剛好 29/30 字正邊界**（支援 Case Create 30 字規則驗證）；**validateStatement 對 null/undefined 應視為空字串不拋錯**（防禦邊界，避免 StatementInput/表單傳入異常時崩潰）；**已修復 VALIDATION-BUG-001**：validation.ts validateStatement/validatePassword/validateEmail 傳入 null/undefined 時原會崩潰，現已補防禦（statement ?? ''、password ?? ''、email ?? ''）；**validateSessionId/validateInviteCode 對 null/undefined 應視為無效不拋錯**（F01/F08 邊界：session 與邀請碼防禦）；**validateSessionId 長度剛好 11 字應返回 true**（正邊界：guest_ 6 字 + 5 字 = 11 > 10，F01 session 格式護欄）；**validateSessionId 長度剛好 10 字應返回 false**（負邊界：10 不滿足 > 10，F01 session 格式護欄）；**validateSessionId 空字串應返回 false**（F01/F09 邊界：session 格式驗證）；**validateUrl 空字串應返回 false**（F01/F05 邊界：evidence URL 等格式驗證）；**validatePassword 空字串應返回 valid: false**（F09 邊界：登入/註冊表單防禦）
- 已補 `case.service.submitCase`：**remote/collaborative 模式缺少 defendant_statement 應拋 VALIDATION_ERROR**（F03-BUG-004 後端護欄）
- 已覆蓋重點：
  - 案件列表、建案、詳情、審閱、提交在前後端皆已有單測/整合測試。
- 主要缺口：
  - 已補 submitCase CASE_NOT_EDITABLE 前端處理（F03 draft 邊界：案已提交 race condition）、FileUpload 刪除證據二次確認與失敗處理（F03 證據刪除流程）；draft 狀態邊界與證據失敗恢復已較完整，頁面級可收尾。

### F04 判決生成、查看與接受

- 覆蓋判定：`中高`
- 現有測試檔數：`6`；案例數：`104`
- 本輪進展：
  - 已補 `clinical-quality.service.test.ts`：**recordPostResponseMetrics cache 為空時應以預設值聚合**（F04 邊界：首次記錄不崩潰）；**cache 有既有資料時應正確累加**（F04 邊界：聚合邏輯）；**route/caseType 未提供時應使用 fallback**（F04 邊界：輸入防禦）（3 例）
  - 已補 `rupture-repair.service.test.ts`：**repair userFeedback 為空時 repairType 應為 validation**（F04 邊界：空反饋防禦）；**含責備/沒懂關鍵字時 repairType 應為 apology_tone_fix/strategy_reset**（F04 邊界：反饋分類）；**judgmentContent 超過 4000 字時應截斷**（F04 邊界：prompt 長度防禦）（4 例）
  - 已補 Judgment Detail：getJudgment 失敗無 message 時 getJudgmentDetailFail、拒絕判決流程 acceptJudgment(accepted: false)、接受/生成和好方案失敗錯誤提示、generatePlans 失敗且無 message 時 message.generatePlansFail、**generatePlans FORBIDDEN 且無 message 時應使用 message.generatePlansFail**（F04 權限邊界 fallback）、接受/拒絕判決失敗且錯誤無 message 時 message.operationFail、**acceptJudgment 接受/拒絕 FORBIDDEN 且無 message 時應使用 operationFail**（F04 權限邊界 fallback）、**getJudgment 失敗時點擊 retry 應重新呼叫 getJudgment 並成功顯示判決內容**（F04 重試分支）；**getJudgment 失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示判決內容**（F04 錯誤恢復：失敗不阻塞重試）；**getJudgment 失敗時 retry 再次失敗應顯示該次錯誤訊息**、**getJudgment 失敗時 retry 再次失敗且 message 為空字串應使用 getJudgmentDetailFail**（F04 重試錯誤反饋、F10 邊界）；**acceptJudgment 成功後 fetchJudgment 失敗應保留 judgment 並顯示錯誤提示，不應顯示 judgmentNotFound**、**acceptJudgment(accepted: false) 成功後 fetchJudgment 失敗應保留 judgment（reject 對稱）**（F04 刷新失敗不覆蓋成功）；**generatePlans 失敗後應仍可再次點擊生成和好方案，成功後應導向 reconciliation**（F04 錯誤恢復：失敗不阻塞重試）；**acceptJudgment(accepted: true/false) 失敗後應仍可再次點擊 modal OK，成功後應顯示成功**（F04 錯誤恢復：失敗不阻塞重試）；**判決已拒絕（user1_acceptance: false）時接受與拒絕按鈕應 disabled 且點擊不應呼叫 acceptJudgment**、**判決已接受（user1_acceptance: true）時接受與拒絕按鈕應 disabled 且點擊不應呼叫 acceptJudgment（與拒絕對稱）**（F04 業務規則：已決不可重選）；**getJudgment NOT_FOUND 時應顯示錯誤 Alert 並可點擊 retry 或返回**（F04 錯誤恢復：與其他失敗一致，失敗不阻塞導航出口）；**已修復 F04-BUG-005**：judgment_content 為 null 時原會崩潰，現已補 fallback；**getJudgment 回傳 judgment_content 為 null 時應不崩潰並顯示判決區塊**（F04 邊界：API 回傳不完整時不崩潰）；**judgment API generateJudgment/getJudgment/acceptJudgment**：後端回傳 judgment 為 null 時應拋錯（F04 邊界：API 回傳不完整時防禦）；**ResponsibilityRatio**：plaintiff/defendant 為 undefined 或 NaN 時應不渲染（F04 邊界：API 回傳不完整時不崩潰）
  - **已修復 F04-BUG-004**：accept/reject 成功後 fetchJudgment 失敗時按鈕仍可點，現已樂觀更新 judgment，**accept/reject 成功後 fetch 失敗時接受與拒絕按鈕應 disabled**（F04 業務規則：已決不可重選）
  - 已補 **getJudgment FORBIDDEN 時應仍可點擊返回並導向上一頁**（F04 錯誤恢復：權限失敗不阻塞導航出口）
  - **F04-OPT-001**：補 useMountedRef 於 handleAccept、handleReject、handleGeneratePlans；**acceptJudgment 成功但組件已卸載時不應呼叫 message.success**、**acceptJudgment(accepted: false) 成功但組件已卸載時不應呼叫 message.success**、**generatePlans 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
- 已覆蓋重點：
  - 判決生成、讀取、接受/拒絕、和好方案生成在前後端均已有測試。
- 主要缺口：
  - 需補與 F03/F07 的跨功能銜接、權限邊界與重試節流驗證。

### F05 和好方案與執行追蹤

- 覆蓋判定：`中`
- 現有測試檔數：`12`；案例數：`262`
- 本輪進展：
  - 已補 Reconciliation List：getPlans 錯誤無 message 時 getPlansFail、**getPlans FORBIDDEN 且無 message 時應使用 getPlansFail**（F05 權限邊界 fallback）、空列表時 generatePlans 失敗、**generatePlans FORBIDDEN 且無 message 時應使用 generatePlansFail**（F05 權限邊界 fallback）、generatePlans/selectPlan 失敗且無 message 時 message.generatePlansFail / message.selectPlanFail，selectPlan 失敗錯誤提示；**已修復 F05-BUG-003**：getPlans 失敗時現有 retry 按鈕，**getPlans 失敗時應仍可點擊 retry 重新呼叫，成功後應顯示方案列表**、**retry 失敗後應仍可再次點擊 retry**（F05 錯誤恢復：失敗不阻塞重試）；**getPlans 失敗時應仍可點擊生成方案按鈕，generatePlans 成功後應顯示方案列表**（F05 錯誤恢復：失敗不阻塞生成入口）；**getPlans NOT_FOUND 時應顯示空狀態並可點擊生成方案，generatePlans 成功後應顯示方案列表**（F05 錯誤恢復：NOT_FOUND 不阻塞生成入口）；**generatePlans 失敗後應仍可再次點擊生成方案，成功後應顯示方案列表**、**selectPlan 失敗後應仍可再次點擊選擇方案，成功後應導向詳情頁**（F05 錯誤恢復：失敗不阻塞重試）；**getPlans 失敗後變更難度篩選應重新呼叫 getPlans，成功後應顯示方案列表**（F05 錯誤恢復：失敗不阻塞重試）；**已優化**：Reconciliation List retry 新增 fetchLockRef 防止快速連點重複請求；**getPlans 失敗時 retry 快速連點只會送出一次 getPlans 請求**（F05 重試節流）；**方案已選（user1_selected/user2_selected）時選擇方案按鈕應 disabled 且點擊不應呼叫 selectPlan**（F05 業務規則：已選不可重選、對方已選不可再選）；**F05-OPT-001**：getPlans 失敗時補齊導航出口、**getPlans 失敗時應仍可點擊返回判決導向 /judgment/:judgmentId**（F05 錯誤恢復：失敗不阻塞導航出口）；**F05-OPT-003**：補 useMountedRef 於 handleGeneratePlans、handleSelectPlan；**generatePlans/selectPlan 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）；**getPlans 回傳 plan 的 plan_content 為 null 時應不崩潰並顯示方案卡片**（F05 邊界：API 回傳不完整時不崩潰）；**已修復 F05-BUG-004**：getPlans 回傳 undefined 時原會崩潰，現已補 `Array.isArray` 防禦；**getPlans 回傳 undefined 時應不崩潰並顯示空狀態**（F05 邊界：API 回傳不完整時不崩潰）；**handleGeneratePlans 回傳 undefined 或非陣列時應不崩潰並顯示空狀態**（F05 邊界：generatePlans API 回傳不完整時不崩潰，Array.isArray 防禦）
  - 已補 Execution Dashboard：getAllExecutionStatuses 失敗且無 message 時 getExecutionStatusFail 與 retry 按鈕、**getAllExecutionStatuses FORBIDDEN 且無 message 時應使用 getExecutionStatusFail**（F05 權限邊界 fallback）、點擊 retry 重新呼叫 getAllExecutionStatuses；retry 再次失敗應顯示該次錯誤、retry 再次失敗且 message 為空字串應使用 getExecutionStatusFail（F05 重試錯誤反饋、F10 邊界）；**getAllExecutionStatuses 失敗時應仍可點擊前往案件列表按鈕並導向 /case/list**（F05 錯誤恢復：失敗不阻塞導航出口）；**getAllExecutionStatuses 失敗後點擊 retry 成功應顯示執行資料**、**getAllExecutionStatuses 失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示執行資料**（F05 錯誤恢復：失敗不阻塞重試）；**execution 的 plan_summary 為 null 或空物件 {} 時應使用 planFallbackTitle 顯示標題**（F05 邊界：API 回傳不完整時不崩潰）；**execution 的 plan_summary 為非物件（如字串）時應不崩潰並使用 planFallbackTitle**（F05 邊界：API 回傳不完整時防禦）；**execution 的 status 為 undefined 時應不崩潰，該項不顯示於進行中或已完成區塊**（F05 邊界：API 回傳不完整時防禦）；**已修復 F05-BUG-005**：getAllExecutionStatuses 回傳 undefined 或非陣列時原會崩潰，現已補 `Array.isArray` 防禦；**getAllExecutionStatuses 回傳 undefined 或非陣列時應不崩潰並顯示空狀態**（F05 邊界：API 回傳不完整時不崩潰）；**execution API getAllExecutionStatuses**：後端回傳 executions 為非陣列時應返回空陣列（F05 邊界：API 層防禦）；**getAllExecutionStatuses 回傳 executions 內項目 records 為 null 或非陣列時應正規化為空陣列**（F05 邊界：與 getExecutionStatus 防禦一致，避免未來消費者崩潰）；**execution API getExecutionStatus**：回應 data 為 null 時應拋錯（F05 邊界：API 回傳不完整時防禦）；**已修復 F05-BUG-007**：getExecutionStatus 回傳 records 為 null 或非陣列時原會導致 CheckIn 崩潰，現已補 `Array.isArray` 防禦；**getExecutionStatus 後端回傳 records 為 null 或非陣列時應返回空陣列**（F05 邊界：避免 CheckIn 崩潰）
  - 已補 Execution CheckIn：getExecutionStatus 失敗且無 message 時 getExecutionStatusFail、**getExecutionStatus FORBIDDEN 且無 message 時應使用 getExecutionStatusFail**（F05 權限邊界 fallback）、打卡提交失敗且無 message 時 message.checkinFail、**checkin FORBIDDEN 且無 message 時應使用 checkinFail**（F05 權限邊界 fallback）；retry 再次失敗應顯示該次錯誤、retry 再次失敗且 message 為空字串應使用 getExecutionStatusFail（F05 重試錯誤反饋、F10 邊界）；**execution API confirmExecution/checkin**：後端回傳 execution 為 null 或 undefined 時應拋錯（F05 邊界：API 回傳不完整時防禦）
  - 已優化 Execution CheckIn 錯誤恢復：getExecutionStatus 失敗時原僅有返回按鈕，現已補齊 retry 與「前往執行看板」按鈕，與 Dashboard/Pairing 模式對齊
  - 已補 `execution.service.checkin`：**photos 為 undefined 時應以空陣列寫入 photos_urls**（F05 邊界：API 回傳不完整時防禦）、**photos 含有效 URL 時應正確寫入**、**photos 含非 http/https URL 應被過濾**、**photos 超過 20 個應只取前 20 個**、**notes 含 script/iframe 應被移除（XSS 防護）**、**notes 超過 500 字應截斷**（F05 照片上傳與安全邊界）
  - 已補 `execution.service`：**plan_content 為 null 時 getExecutionStatus 應返回 plan_summary 且 title 為空字串**（F05 邊界：extractPlanSummary 防禦）; **getAllExecutionStatuses plan_content 為 null 或無效 JSON 時應返回 plan_summary 且 title 為空字串**（F05 邊界：DB/API 回傳不完整時防禦）
  - 已補 `checkinSchema` 驗證測試：plan_id 必填/UUID、notes 500 字邊界、photos 上限 3/URI 格式（F05 打卡輸入護欄）；**reconciliation.service getPlansByJudgmentId**：判決不存在應拋出 NOT_FOUND（F05 邊界）；**reconciliation API**：generatePlans 後端回傳 plans 為 null、undefined 或非陣列時應拋錯、getPlans 後端回傳 plans 為 undefined 或 data 無 plans 時應返回空陣列、**getPlans 後端回傳 plans 為 null 或非陣列時應返回空陣列**（F05 邊界：API 回傳不完整時防禦，F05-BUG-006 修復）、**getPlanById 回應 plan 為 null 或 undefined 時應拋錯**（F05 邊界：API 回傳不完整時防禦）、**selectPlan 回應 plan 為 null 或 undefined 時應拋錯**（F05 邊界：API 回傳不完整時防禦）
  - 已補 Execution CheckIn：**getExecutionStatus 失敗時應仍可點擊 retry 或前往執行看板導向 /execution/dashboard**、**getExecutionStatus 失敗時點擊 retry 應重新呼叫 getExecutionStatus**（F05 錯誤恢復：失敗不阻塞導航出口）；**getExecutionStatus 失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示執行進度**（F05 錯誤恢復：失敗不阻塞重試）；**getExecutionStatus 失敗時 retry 快速連點只會送出一次 getExecutionStatus 請求**（F05 重試節流）；**已修復 F05-BUG-002**：打卡成功後 refresh 失敗會覆蓋成功狀態，現改為 fetchExecution({ isRefresh: true }) 失敗時保留 execution 並顯示 execCheckIn.refreshFail；**打卡成功後 refresh 失敗應保留 execution 並顯示 refreshFail，不顯示 notFound**；**打卡失敗後應仍可再次點擊打卡，成功後應顯示成功**（F05 錯誤恢復：失敗不阻塞重試）；**打卡成功但組件已卸載時不應呼叫 message.success**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）；**無照片時打卡不應調用 uploadEvidence**（F05 照片邊界：與 Case Create 無證據不調用對齊）；**照片上傳成功但 checkin 失敗後應仍可再次點擊打卡，成功後應顯示成功**（F05 錯誤恢復：checkin 失敗不阻塞重試）；**照片上傳失敗且 checkin 失敗後應仍可再次點擊打卡，第二次上傳成功時應將 photoUrls 傳給 checkin 並顯示成功**（F05 錯誤恢復：照片上傳失敗不阻塞重試）；**有照片時 uploadEvidence FORBIDDEN 且無 message 應使用 photoUploadFailContinue 並以空 photos 繼續打卡**（F05 權限邊界 fallback）；**有照片時打卡快速連點只會送出一次 checkin 請求**（F05 提交節流）；**有照片時 getPlanById 回傳 null 應顯示 photoUploadFailContinue 且以空 photos 繼續打卡**（F05 邊界：plan 不存在時不崩潰）；**F05-BUG-007 組件層防禦**：CheckIn 對 execution.records 使用 Array.isArray 護欄，records 為 null 或非陣列時不崩潰；**execution.records 為 null 或非陣列時應不崩潰並顯示進度與表單**（F05-BUG-007 組件層防禦）；**有照片時 uploadEvidence 回傳空陣列應以空 photos 繼續打卡且顯示成功**（F05 邊界：API 回傳空陣列時不崩潰）
  - 已補 Reconciliation Detail：getPlanById+getPlans 皆失敗時 getPlanDetailFail、getPlanById 失敗且 getPlans 失敗且 message 為空字串時應使用 getPlanDetailFail（F10 邊界）、**getPlanById 失敗且 getPlans FORBIDDEN 且無 message 時應使用 getPlanDetailFail**（F05 權限邊界 fallback：fetchPlan 雙 API 皆失敗）、getPlans 失敗但有 message 時顯示該 message、selectPlan/confirmExecution 失敗無 message 時 selectPlanFail/startExecutionFail、**selectPlan FORBIDDEN 且無 message 時應使用 selectPlanFail**（F05 權限邊界 fallback）；selectPlan 成功時顯示 selectPlanSuccess 並重新拉取方案、confirmExecution 成功時顯示 startExecutionSuccess 並導向 checkin、點擊返回按鈕 navigate(-1)；**已修復 F05-BUG-001**：getPlanById 與 getPlans 皆失敗時原無返回/重試按鈕，現已補齊；**F05-OPT-002**：planNotFound 時補齊「返回判決」按鈕導向 /judgment/:judgmentId（與 List 導航慣例對齊）；**getPlanById 與 getPlans 皆失敗時應仍可點擊返回判決導向 /judgment/:judgmentId**、**getPlanById 與 getPlans 皆失敗時點擊 retry 應重新呼叫 fetchPlan**、**getPlanById 與 getPlans 皆失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示方案詳情**（F05 錯誤恢復：失敗不阻塞重試）；**retry 再次失敗應顯示該次錯誤訊息**、**retry 再次失敗且 message 為空字串應使用 getPlanDetailFail**（F05 重試分支、F10 邊界）；**已優化**：fetchPlan 新增 fetchLockRef 防止 retry 快速連點重複請求，已補「retry 快速連點只會送出一次 fetchPlan 請求」測試（F05 重試節流）；**selectPlan 成功後 fetchPlan 失敗應保留 plan 並顯示錯誤提示，不應顯示 planNotFound**（F05 刷新失敗不覆蓋成功）；**confirmExecution 失敗後應仍可再次點擊開始執行，成功後應導向 checkin**（F05 錯誤恢復：失敗不阻塞重試）；**selectPlan 失敗後應仍可再次點擊選擇方案，成功後應顯示成功並重新拉取**（F05 錯誤恢復：失敗不阻塞重試）；**方案已選（user1_selected 或 user2_selected）時不應顯示選擇方案按鈕，應顯示開始執行按鈕**（F05 業務規則：已選不可重選，與 List 對齊）；**F05-OPT-004**：補 useMountedRef 於 handleSelect、handleStartExecution；**selectPlan/confirmExecution 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）；**getPlanById 回傳 plan_content 為 null 時應不崩潰並顯示方案詳情**（F05 邊界：API 回傳不完整時不崩潰）；**getPlanById 回傳 plan 的 time_cost/money_cost/emotion_cost 為 undefined 時應不崩潰並顯示方案詳情**（F05 邊界：API 回傳不完整時防禦，組件層 cost 欄位 fallback）
- 已覆蓋重點：
  - 和好方案列表/詳情、執行確認與打卡已有前後端測試。
- 主要缺口：
  - 照片上傳三段鏈路、失敗降級與 dashboard 回寫需補更完整流程測試。

### F06 心理訪談、結果與心理畫像

- 覆蓋判定：`中高`
- 現有測試檔數：`24`；案例數：`265`
- 本輪進展：
  - 已建立 F06 專項測試設計文檔（Emorapy-F06-心理訪談與心理畫像測試設計與開發拆解-20260310.md）
  - 已補 `psych-profile.service.test.ts`：giveConsent、getProfile、getFeedbackHistory、deleteAllData 的 happy path、NOT_FOUND、空陣列邊界、DB 交互（9 例）；**giveConsent userId 不存在時應拋出 NOT_FOUND**（F06 邊界：與 getProfile/deleteAllData 對齊）
  - 已補 `profile-richness.service.test.ts`：**calculateRichness 無最新敘事時應返回 0**（F06 邊界：空陣列不崩潰）；**completeness 為 null 時應以 0 計入**（F06 邊界：防禦性計算）；**未知 domain 時應以權重 1 計入**（F06 邊界：防禦性計算）；**getRichnessLevel 分數 < 0.05 應返回 L0、0.05-0.2 應返回 L1**（5 例）
  - 已補 `profile-snapshot.service.test.ts`：**createSnapshot 無敘事與洞見時應仍寫入快照**（F06 邊界：空陣列不崩潰）；**ai_summary 為 null 時應使用 raw_narrative 前 500 字**（F06 邊界：防禦性 fallback）；**getSnapshot 快照不存在時應返回 null**（F06 邊界：NOT_FOUND 語義）（3 例）
  - 已補 `async-pipeline.service.test.ts`：**process session 不存在或狀態非 PROCESSING 時應不執行 pipeline**（F06 邊界：pipeline 前置條件、狀態護欄）；**resume session 不存在或狀態非 PROCESSING/PROCESSING_FAILED 時應拋出 VALIDATION_ERROR**（F06 邊界：retry 前置條件）（4 例）
  - 已補 `insight-extraction.service.test.ts`：**extractInsights 無最新敘事時應不呼叫 AI 且正常返回**（F06 邊界：空陣列不崩潰）；**敘事皆低於 100 字時應不呼叫 AI 且正常返回**（F06 邊界：qualifiedNarratives 空陣列）（2 例）
  - 已補 `domain-classification.service.test.ts`：**batchClassify session 不存在時應不呼叫 AI 且正常返回**（F06 邊界：pipeline 前置條件）；**無有效 turns 時應不呼叫 AI 且正常返回**（F06 邊界：空陣列不崩潰）（2 例）
  - 已補 `narrative.service`：**extractNarratives 訪談不存在應拋出 NOT_FOUND**（F06 邊界：pipeline 前置條件）；**extractNarratives 訪談存在時應萃取敘事並寫入 ProfileNarrative**（F06 成功路徑）；**summarizeNarratives 無最新敘事時應不呼叫 AI 且正常返回**（F06 邊界：空陣列不崩潰）；**summarizeNarratives 有最新敘事時應呼叫 AI 並更新 ai_summary**（F06 成功路徑）
  - 已補 `psych-profile.routes.test.ts`：GET /、GET /feedback、POST /consent、DELETE / 的 API 層驗證（4 例）；**錯誤傳遞**：getProfile、getFeedbackHistory、giveConsent、deleteAllData 調用 next(error) 時應返回 500（+4 例）
  - 已補 `interview.routes.test.ts`：start、respond、end、skip、resume、getSession、retry 的 API 層驗證（7 例）；**錯誤傳遞**：startSession、getSession、respond、endSession、skip、checkResume、retryFailed 調用 next(error) 時應返回 500（+7 例）
  - 已擴充 `interview.service.test.ts`：CONSENT_REQUIRED、MAX_TURNS_REACHED、TURN_TOO_FAST、SESSION_COMPLETED、NOT_FOUND（session 不存在/屬其他用戶）限流與異常分支（+4 例，共 21 例）；**endSession 狀態轉移**：NOT_FOUND、SESSION_COMPLETED、內容不足（turns<5 或總字數<50）→ COMPLETED 且不觸發 pipeline、內容足夠→ PROCESSING 且觸發 pipeline（+6 例）；**retryFailed**：NOT_FOUND、VALIDATION_ERROR、PROCESSING_FAILED→PROCESSING 且觸發 resume（+4 例）；**startSession 每小時限額與舊 session 處理**：每小時 RATE_LIMIT_EXCEEDED、舊 IN_PROGRESS turns≥5→PROCESSING、turns<5→ABANDONED（+3 例，共 34 例）
  - 已補 Profile MyStory：retryFailed 快速連點節流（retryLockRef）、重試失敗 session 時 retryFailed 快速連點只會送出一次請求（F06 重試節流）；startSession/retryFailed/deleteAllData 失敗且 message 為空字串時應使用對應 fallback（F10 邊界）；**storeError 時點擊 retry 應呼叫 fetchProfile**（F06 錯誤恢復：重試載入）；**已修復 F06-BUG-004**：storeError 時現含「前往個人資料」導航出口、**storeError 時應仍可點擊 retry 或前往個人資料導向 /profile/index**（F06 錯誤恢復：失敗不阻塞導航出口）；**startSession 失敗後應仍可再次點擊繼續聊天，成功後應導航**、**retryFailed 失敗後應仍可再次點擊 retry，成功後應清除 failed 狀態**、**deleteAllData 失敗後應仍可再次點擊確認刪除，成功後應導航**（F06 錯誤恢復：失敗不阻塞重試）；**startSession/retryFailed/deleteAllData FORBIDDEN 且無 message 時應使用 interview.startFail/retryFail/psychProfile.deleteFail**（F06 權限邊界 fallback）；**F06-OPT-001**：補 useMountedRef 於 handleStartChat、handleRetryFailed、handleDelete；**deleteAllData/startSession/retryFailed 成功但組件已卸載時不應呼叫 message.success、message.info 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）；**profile.narratives 或 insights 為非陣列時應不崩潰並顯示空狀態**（F06 邊界：API 回傳不完整時防禦，Array.isArray 於 MyStory 與 Profile Index）；**psychProfileStore fetchFeedbackHistory**：API 回傳 history 為非陣列時應設置空陣列（F06 邊界：API 回傳不完整時防禦）；**psychProfile API getProfile**：後端回傳 data 為 null 時應正常返回不拋錯（F06 邊界：API 回傳不完整時由 store 防禦）；**psychProfile API getFeedbackHistory**：後端回傳 data 為 null 時應正常返回不拋錯（F06 邊界：API 回傳不完整時由 store 防禦）；**interview API getSession**：後端回傳 200 且 data 為 null 時應正常返回不拋錯（F06 邊界：API 回傳不完整時由 store 防禦）；**interview API checkResume**：後端回傳 200 且 data 為 null 時應正常返回不拋錯（F06 邊界：API 回傳不完整時由 store 防禦）
  - 已補 Interview Result：retry 快速連點節流（retryLockRef）、processing_failed 時 retry 快速連點只會送出一次 retryFailed 請求（F06 重試節流）；retryFailed 成功但 getSession 失敗且 message 為空字串時應使用 retryFail（F10 邊界）；**retryFailed FORBIDDEN 且無 message 時應使用 retryFail**（F06 權限邊界 fallback）；**getSession 失敗且 currentSession 為 null 且無 storeError 時應顯示完成結果與返回個人資料按鈕**（F06 錯誤恢復：失敗不阻塞導航）；**已修復 F06-BUG-003**：getSession 失敗且 currentSession 為 null 且有 storeError 時現有 retry 與返回個人資料、**getSession 失敗 storeError 時應顯示錯誤與 retry**（F06 錯誤恢復：失敗不阻塞重試）；**retryFailed 成功但組件已卸載時不應呼叫 message.info**（useMountedRef 回歸）；**retryFailed 失敗後應仍可再次點擊 retry，成功時應顯示 retryProcessing**（F06 錯誤恢復：失敗不阻塞重試）
  - 已補 Interview Chat：getSession 失敗且 message 為空字串時應使用 interview.loadFail（F10 邊界）、endSession 失敗且 message 為空字串時應使用 interview.endFail（F10 邊界）；**endSession FORBIDDEN 且無 message 時應使用 interview.endFail**（F06 權限邊界 fallback）；**getSession 失敗 FORBIDDEN 且無 message 時應使用 interview.loadFail**（F06 權限邊界 fallback）；**RATE_LIMIT_EXCEEDED 時仍可透過 header 返回按鈕導向 /profile/index**（F06 錯誤恢復：無專用按鈕時仍有導航出口）；**已修復 F06-BUG-002**：未知 errorCode 時現有 reload 按鈕、**errorCode 為未知時應顯示重新載入按鈕，點擊應呼叫 getSession**（F06 錯誤恢復：未知錯誤提供 retry 出口）；**errorCode 為未知時 reload 快速連點只會送出一次 getSession 請求**（F06 重試節流，reloadLockRef）；**暫停對話時 endSession 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）；**暫停對話時 endSession 失敗後應仍可再次點擊暫停對話，成功後應導向 result**（F06 錯誤恢復：失敗不阻塞重試）
- 已覆蓋重點：
  - 訪談啟動、SSE 回答/跳題/結束、retry、psych profile 讀寫已有較多前後端測試。
- 主要缺口：
  - 限流分支（CONSENT_REQUIRED、MAX_TURNS_REACHED、TURN_TOO_FAST）已補；psych-profile 與 interview API 層已補；**end 後狀態轉移（IN_PROGRESS→PROCESSING/COMPLETED）、retryFailed PROCESSING_FAILED→PROCESSING、start 舊 IN_PROGRESS→PROCESSING/ABANDONED** 已補。
  - 仍可補：pipeline 失敗恢復、跨頁回流測試。

### F07 Chat 房間、邀請與轉判決

- 覆蓋判定：`中高`
- 現有測試檔數：`8`；案例數：`101+`
- 本輪進展：
  - 已補 Chat Room F10 邊界：createChatRoom/declineChatInvite/acceptChatInvite/sendChatMessage/createChatInvite/getChatRoom/requestChatJudgment/leaveChatRoom/kickChatParticipantB 失敗且 message 為空字串時應使用對應 fallback（空 message 視為無）；loadMoreHistory（listChatMessages 帶 cursor）失敗且 message 為空字串時應使用 loadMoreFail（F10 邊界）、**loadMoreHistory FORBIDDEN 且無 message 時應使用 loadMoreFail**（F07 權限邊界 fallback）；**chat API listChatMessages**：後端回傳 messages 為非陣列時應返回空陣列、**nextCursor 為 undefined 時應正規化為 null**、**data 為 null 時應拋錯**（F07 邊界：API 回傳不完整時防禦）；**chat API sendChatMessage**：後端回傳 message 為 null 或 undefined 時應拋錯（F07 邊界：API 回傳不完整時防禦）；**chat API requestChatJudgment/getChatJudgmentStatus**：後端回傳 data 為 null 時應拋錯（F07 邊界：API 回傳不完整時防禦，聊天轉判決）；**chat API getChatRoom/createChatRoom/createChatInvite/acceptChatInvite/declineChatInvite/leaveChatRoom/kickChatParticipantB**：後端回傳 room/invite 為 null 時應拋錯（F07 邊界：API 回傳不完整時防禦）；**createChatRoom**：後端回傳 room 為 null 時應拋錯（F07 邊界：與 getChatRoom 一致）**getChatRoom FORBIDDEN 時若有 message 應顯示該 message**、**getChatRoom FORBIDDEN 且無 message 時應使用 loadFail**（F07 權限邊界）；**createChatRoom FORBIDDEN 且無 message 時應使用 createRoomFail**、**requestChatJudgment FORBIDDEN 且無 message 時應使用 judgmentFail**（F07 權限邊界 fallback）；**sendChatMessage FORBIDDEN 且無 message 時應使用 chat.message.forbidden（專用發言權限提示）**、**createChatInvite/kickChatParticipantB FORBIDDEN 且無 message 時應使用 createInviteFail/kickFail**；**declineChatInvite/acceptChatInvite FORBIDDEN 且無 message 時應使用 declineFail/joinFail**；**已修復 F07-BUG-001**：getChatRoom 失敗時現有 retry 按鈕，**getChatRoom 失敗時應仍可點擊 retry 重新呼叫，成功後應顯示聊天室**、**retry 失敗後應仍可再次點擊 retry**、**retry 快速連點節流**（F07 錯誤恢復、重試節流）；**createChatRoom 失敗後應仍可再次點擊建立，成功後應導向聊天室**、**requestChatJudgment 失敗後應仍可再次點擊確認發起判決**、**sendChatMessage 失敗後應仍可再次發送訊息**（F07 錯誤恢復：失敗不阻塞重試）；**declineChatInvite/acceptChatInvite/leaveChatRoom/kickChatParticipantB/createChatInvite 失敗後應仍可再次點擊，成功後應顯示成功**（F07 錯誤恢復：失敗不阻塞重試）；已補 E2E **chat-failure-matrix**：**getChatRoom 失敗時應顯示錯誤、點擊 retry 成功後應顯示聊天室**、**createChatRoom 失敗時應顯示錯誤、可再次點擊建立且成功導向聊天室**（F07 錯誤恢復 E2E）；**F07-OPT-001**：補 useMountedRef 於 handleCreateRoom、handleAcceptInvite、handleDeclineInvite、handleCreateInvite、handleRequestJudgment、tryStartJudgmentPolling、leaveChatRoom、kickChatParticipantB、showRoomStatusNotice；**createChatRoom 成功但組件已卸載時不應呼叫 message.success 或 navigate**、**acceptChatInvite 成功但組件已卸載時不應呼叫 message.success**、**declineChatInvite 成功但組件已卸載時不應呼叫 message.success**、**createChatInvite 成功但組件已卸載時不應呼叫 message.success**、**kickChatParticipantB 成功但組件已卸載時不應呼叫 message.success**、**requestChatJudgment 成功但組件已卸載時不應呼叫 message.success**、**leaveChatRoom 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
- 已覆蓋重點：
  - 聊天室、邀請、訊息、判決請求已有單測、整合與 E2E。
- 主要缺口：
  - 需持續補併發、冪等、房間過期、owner session 匹配與 invite 身份矩陣的真實主鏈路回歸。

### F08 配對與關係檔案

- 覆蓋判定：`中`
- 現有測試檔數：`8`；案例數：`123`
- 本輪進展：
  - 已補 Profile Pairing：getPairingStatus 失敗/無 message 時 getPairingFail、createPairing/joinPairing/cancelPairing 失敗且無 message 時 message.createPairingFail/joinPairingFail/cancelPairingFail、getRelationshipProfile 失敗、upsertRelationshipProfile 失敗及失敗無 message 時 message.relationshipProfileSaveFail；**pairing API createPairing/joinPairing/cancelPairing**：後端回傳 pairing 為 null 時應拋錯（F08 邊界：API 回傳不完整時防禦）；**pairing API getPairingStatus**：後端回傳 200 且 pairing 為 null 或 undefined 時應返回 null（F08 邊界：無配對狀態、API 回傳不完整時防禦）；**profile API upsertRelationshipProfile**：後端回傳 profile 為 null 時應拋錯；**getRelationshipProfile**：data 或 profile 缺失時應返回 null（F08 邊界：API 回傳不完整時防禦）；**validation.validateInviteCode**：空字串應返回 false（F08 邊界：配對邀請碼格式驗證）
  - 已補 Profile Pairing F10 邊界：getPairingStatus/createPairing/joinPairing/cancelPairing/getRelationshipProfile/upsertRelationshipProfile 失敗且 message 為空字串時應使用對應 fallback（空 message 視為無）；同意後 startSession 失敗且 message 為空字串時應使用 interview.startFail（F10 邊界）；**同意後 startSession FORBIDDEN 且無 message 時應使用 interview.startFail**（F06/F08 權限邊界 fallback）
  - 已修 Profile Pairing 測試穩定性：關係檔案表單需等 getRelationshipProfile 載入完成後才渲染，三處用例改為 waitFor 表單 placeholder 出現後再互動
  - **已修復 F08-BUG-001**：Profile Pairing getPairingStatus 失敗時原無 loadError Alert、retry 與導航出口，現已補齊 loadError Alert、重試與前往個人設定按鈕
  - 已補 Profile Pairing：**getPairingStatus 失敗時應仍可點擊 retry 或前往個人設定導向 /profile/settings**、**getPairingStatus 失敗時點擊 retry 應重新呼叫 getPairingStatus**（F08 錯誤恢復：失敗不阻塞導航出口）；**getPairingStatus 失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示配對狀態**（F08 錯誤恢復：失敗不阻塞重試）；retry 再次失敗應顯示該次錯誤、retry 再次失敗且 message 為空字串應使用 getPairingFail（F08 重試錯誤反饋、F10 邊界）；**getPairingStatus 失敗時 retry 快速連點只會送出一次 getPairingStatus 請求**（F08 重試節流）
  - 已補 Profile Pairing：**createPairing 失敗後應仍可再次點擊建立配對，成功後應顯示配對狀態**、**joinPairing 失敗後應仍可再次點擊加入，成功後應顯示配對狀態**、**cancelPairing 失敗後應仍可再次點擊取消，成功後應顯示成功**、**upsertRelationshipProfile 失敗後應仍可再次點擊保存，成功後應顯示成功**（F08 錯誤恢復：失敗不阻塞重試）；已修 join 測試穩定性（userEvent.type + waitFor 確保 inviteCode 已寫入後再點擊）；**createPairing/joinPairing/cancelPairing/upsertRelationshipProfile FORBIDDEN 且無 message 時應使用對應 fallback**（F08 權限邊界 fallback）、**getRelationshipProfile FORBIDDEN 且無 message 時應使用 relationshipProfileLoadFail**（F08 權限邊界 fallback）；**F08-OPT-001**：補 useMountedRef 於 handleCreatePairing、handleJoinPairing、handleCancelPairing、handleSaveRelationshipProfile、startInterviewFlow；**createPairing/joinPairing/cancelPairing/saveRelationshipProfile/startInterviewFlow 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
  - 已補 Profile Index：getProfile 失敗與失敗無 message 時 getProfileIndexFail、getProfile 失敗且 message 為空字串時應使用 getProfileIndexFail（F10 邊界）；**getProfile 失敗時若有 psychProfile 應仍可點擊管理我的數據導向 /profile/my-story**（錯誤恢復：失敗不阻塞導航）；**getProfile 失敗後應仍可填寫表單並提交，updateProfile 成功應顯示成功訊息**（F08/F09 錯誤恢復：失敗不阻塞表單操作）；**startSession 失敗後應仍可再次點擊繼續聊天，成功後應導航**、**updateProfile 失敗後應仍可再次點擊儲存，成功後應顯示成功**（F08 錯誤恢復：失敗不阻塞重試）；**updateProfile FORBIDDEN 且無 message 時應使用 updateFail**（F08 權限邊界 fallback）；**已修復 F08-BUG-002**：Profile Index 載入失敗時現有 retry 按鈕、**getProfile 失敗時應仍可點擊 retry 重新呼叫**、**retry 失敗後應仍可再次點擊 retry**（F08 錯誤恢復：失敗不阻塞重試）；**getProfile 失敗時 retry 快速連點只會送出一次 getProfile 請求**（F08/F09 重試節流）；**F08-OPT-002**：補 useMountedRef 於 handleSubmit、頭像上傳、繼續聊天、onConsent；**updateProfile/繼續聊天 startSession 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
- 已覆蓋重點：
  - 配對 create/join/status/cancel 與 relationship profile 已有前後端測試。
- 主要缺口：
  - 缺少更完整的關係檔案權限矩陣與配對後續流程串接回歸。

### F09 身份認證與個人資料管理

- 覆蓋判定：`中高`
- 現有測試檔數：`15`；案例數：`227`
- 本輪進展：
  - 已補 Login：登入失敗無 message 時 loginFail、EMAIL_NOT_VERIFIED 時 sendVerificationCode 失敗顯示 resendVerifyFail、sendVerificationCode 失敗且 message 為空字串時應使用 resendVerifyFail（F10 邊界）；**登入 FORBIDDEN 且無 message 時應使用 loginFail**（F09 權限邊界 fallback）；**登入失敗時應仍可點擊註冊並導向 /auth/register**、**登入失敗時應仍可點擊忘記密碼並導向 /auth/forgot-password**（F09 錯誤恢復：失敗不阻塞導航出口）；**EMAIL_NOT_VERIFIED 且 sendVerificationCode 失敗時應仍可點擊註冊並導向 /auth/register**（F09 錯誤恢復：雙失敗不阻塞導航出口）；**登入失敗後應仍可再次點擊登入，成功後應導航**（F09 錯誤恢復：失敗不阻塞重試）；**F09-OPT-001**：補 useMountedRef 於 handleSubmit；**登入成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）；**auth API login/register 回應 token 為空字串時應拋錯**（F09 邊界：空 token 視為無效）
  - 已補 ForgotPassword：sendResetEmail/confirmResetPassword 失敗且無 message 時使用 sendResetFail/resetFail；**sendResetEmail/confirmResetPassword FORBIDDEN 且無 message 時應使用 sendResetFail/resetFail**（F09 權限邊界 fallback）；**sendResetEmail 失敗時應仍可點擊返回登入並導向 /auth/login**（F09 錯誤恢復：失敗不阻塞導航出口）；**confirmResetPassword 失敗時應仍可點擊返回登入並導向 /auth/login**（F09 錯誤恢復：Step 2 失敗不阻塞導航出口）；**sendResetEmail/confirmResetPassword 失敗後應仍可再次點擊重試**（F09 錯誤恢復：失敗不阻塞重試）；**F09-OPT-003**：補 useMountedRef 於 handleSendResetEmail、handleResetPassword；**confirmResetPassword 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
  - 已補 Register：sendCode/verifyEmail/register 失敗且無 message 時使用 sendCodeFail/verifyFail/registerFail；**sendCode FORBIDDEN 且無 message 時應使用 sendCodeFail**（F09 權限邊界 fallback）、**verifyEmail/register FORBIDDEN 且無 message 時應使用 verifyFail/registerFail**（F09 權限邊界 fallback）；**sendCode 失敗時應仍可點擊登入並導向 /auth/login**（F09 錯誤恢復：失敗不阻塞導航出口）；**verifyEmail 失敗時應仍可點擊登入並導向 /auth/login**、**register 失敗時應仍可點擊登入並導向 /auth/login**（F09 錯誤恢復：Step 1/2 失敗不阻塞導航出口）；**sendCode 失敗後應仍可再次點擊發送，成功後應轉到驗證步驟**、**verifyEmail 失敗後應仍可再次點擊驗證，成功後應轉到密碼步驟**、**register 失敗後應仍可再次點擊註冊，成功後應導航**（F09 錯誤恢復：失敗不阻塞重試）；**F09-OPT-002**：補 useMountedRef 於 handleSendCode、handleVerifyCode、handleSubmit；**register 成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
  - 已補 Profile Settings：getProfile 失敗且無 message 時 getProfileFail；已修 **F09-BUG-001**：retry 再次失敗時未顯示錯誤；已補 retry 再次失敗有 message/空 message F10 邊界、retry 重新呼叫 getProfile、**getProfile 失敗時 retry 快速連點只會送出一次 getProfile 請求**；**已修復 F09-BUG-002**：getProfile 失敗時無導航出口，現已補齊「前往個人資料」按鈕，並補測試「getProfile 失敗時應仍可點擊 retry 或前往個人資料導向 /profile/index」；**getProfile 失敗時 retry 失敗後應仍可再次點擊 retry，成功後應顯示表單**、**updateProfile 失敗後應仍可再次點擊儲存，成功後應顯示成功**（F09 錯誤恢復：失敗不阻塞重試）；**updateProfile FORBIDDEN 且無 message 時應使用 saveFail**（F09 權限邊界 fallback）；**F09-OPT-004**：補 useMountedRef 於 handleSubmit；**updateProfile 成功但組件已卸載時不應呼叫 message.success**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
  - 已補 Profile Index：updateProfile 失敗且錯誤無 message 時 message.updateFail、繼續聊天 startSession 失敗且 message 為空字串時應使用 interview.startFail（F10 邊界）；**繼續聊天 startSession FORBIDDEN 且無 message 時應使用 interview.startFail**（F06/F08 權限邊界 fallback）
  - 已補 **authStore checkAuth**：getProfile 失敗時應同時清除 localStorage 與 sessionStorage 的 token（F09 守衛：token 失效時徹底清理雙儲存）；**user API getProfile**：後端回傳 user 為 null 或 undefined 時應拋錯（F09 邊界：profile/me 回傳不完整時防禦）；**user API updateProfile**：後端回傳 user 為 null 時應拋錯（F09 邊界：API 回傳不完整時防禦）
- 已覆蓋重點：
  - 登入/註冊/重置密碼/守衛已有前後端單測。
- 本輪進展（續）：
  - 已補 ProtectedRoute：未認證重定向時應傳遞 `state.from` 供登入後回跳（F09 回跳鏈路護欄）
  - 已修復 **AUTH-OPT-001**：`getAuthUserId` 在 req.user 缺失時原拋 plain Error 導致 500，現改為拋 Errors.UNAUTHORIZED 回傳 401
- 主要缺口：
  - 需把 `claim-session` 與 `profile/me` 一併納入 F09/F01 共用回歸；PublicRoute/登入後回跳單測已補（Login 有 from 回跳、ProtectedRoute 有 state.from 傳遞）。

### F10 Admin 運維與治理

- 覆蓋判定：`中高`
- 現有測試檔數：`27`；案例數：`216`
- 本輪進展：
  - 已補 Admin Health：useQuery 回傳 error 時顯示 loadFailed Alert；**已修復 F10-BUG-001**：載入失敗時現有 retry 按鈕，**useQuery 回傳 error 時應仍可點擊 retry 重新拉取**（F10 錯誤恢復：失敗不阻塞重試）
  - 已補 Admin Reports：任一 useQuery 回傳 error 時顯示 admin.reports.loadFailed Alert
  - 已補 Admin Configs：listQuery/runtimeQuery error 時顯示 admin.configs.loadFailed Alert
  - 已補 Admin Jobs：jobsQuery error 時顯示 admin.jobs.loadFailed Alert
  - 已補 Admin Users：usersQuery error 時顯示 admin.users.loadFailed Alert
  - 已補 Admin AuditLogs：query error 時顯示 admin.audit.loadFailed Alert
  - 已補 Admin Settings：adminUsersQuery/configsQuery error 時顯示 admin.settings.loadFailed Alert；**已修復 F10-BUG-002**：Admin Reports/Jobs/Users/Configs/AuditLogs/Settings 載入失敗時現有 retry 按鈕、**應仍可點擊 retry 重新拉取**（F10 錯誤恢復：失敗不阻塞重試）
  - 已補 Admin OpsJobs：stats 載入失敗（statsQuery.error）時顯示 admin.ops.loadFailed Alert；**已修復 F10-BUG-003**：loadFailed Alert 現含 retry 按鈕、**stats 載入失敗時 loadFailed Alert 內應有 retry 按鈕，點擊應觸發 refetch**（F10 錯誤恢復：失敗不阻塞重試）
  - 已補 AdminPermissionRoute：adminMe 錯誤碼 UNAUTHORIZED/INVALID_TOKEN 時應顯示對應 identityFailed description、未知 code 時使用 identityFailedDesc（F10 權限矩陣錯誤碼映射）
  - 已補 AdminPermissionRoute：adminMe 錯誤碼 NOT_FOUND 時應顯示 identityFailed 且 description 為 common.notFound（F10 權限矩陣錯誤碼映射）；adminMe 錯誤為 { code: '', message: '' } 時應使用 identityFailedDesc（F10 邊界）；requiredPermissions 為空陣列時應渲染 children（F10 權限邊界：空需求即放行）
  - 已補 **useAdminAccess**：permissions 為非陣列時應正規化為空陣列不崩潰、permissions 為 null 時應正規化為空陣列、permissions 含非字串項時應過濾掉、**admin 為 null 時應正規化為空陣列不崩潰**（F10 邊界：API 回傳不完整時防禦）；**mode=any 時具備任一權限應返回 true**（F10 權限矩陣 any 模式）；**enabled=false 時 useAdminMe 不請求，permissions 為空應返回 hasPermission=false**（F10 權限矩陣：disabled 時不放行）；已優化 useAdminAccess 對 admin?.permissions 的 optional chaining 與 Array.isArray 防禦；**admin API getMe**：後端回傳 admin 為 null 或 undefined 時應拋錯（F10 邊界：API 回傳不完整時防禦）
  - 已補 Admin Login：登入失敗且有 message 顯示該 message、登入失敗且 message 為空字串時應使用 admin.login.failed（F10 邊界）、登入失敗且無 message 時 admin.login.failed、**登入 FORBIDDEN 且無 message 時應使用 admin.login.failed**（F10 權限邊界 fallback）；**登入失敗後應仍可再次點擊登入，成功後應導向 admin ops**（F10 錯誤恢復：失敗不阻塞重試）；**F10-OPT-001**：補 useMountedRef 於 onFinish；**登入成功但組件已卸載時不應呼叫 message.success 或 navigate**（useMountedRef 回歸：避免 F01-BUG-001 同類問題）
  - 已補 `adminAuth`：**token iat 早於 admin.updated_at 時應回 UNAUTHORIZED**（F10 權限：帳號變更後舊 token 失效）
- 已覆蓋重點：
  - Admin 前端頁、後端 admin service/routes、外部 e2e 已具基礎覆蓋。
- 主要缺口：
  - 需進一步補權限矩陣、審計落庫、失敗回滾與配置生效驗證。

## 四、按主流程的覆蓋現狀

| 流程ID | 流程名稱 | 覆蓋判定 | 說明 |
|---|---|---|---|
| P01 | 快速體驗閉環 | 中高 | `Create -> Result` 與 session refresh 核心缺陷均已修復；升格語義與更完整結果頁真實鏈路仍需加強 |
| P02 | 正式案件閉環 | 中 | 主功能點已有覆蓋，跨頁與狀態矩陣仍不足 |
| P03 | 心理訪談閉環 | 中高 | 訪談、result 與畫像已有較多測試，但限流/恢復分支仍需補強 |
| P04 | 聊天轉判決閉環 | 中高 | 單測與 E2E 已起步，但 owner session、併發與錯誤回復仍是高風險 |
| P05 | Admin 運維閉環 | 中 | 核心頁與部分 E2E 已有，但權限矩陣與落庫驗證仍需補完 |

## 五、高風險區域專章

- Session/JWT 雙憑證共存與 refresh 重試邏輯
- `claim-session` 的弱依賴語義與升格後回流
- SSE 類流程（interview、chat stream）
- 文件上傳鏈路（quick evidence、execution checkin photo）
- 聊天轉判決的併發、冪等、鎖與 owner session 匹配
- Admin 權限矩陣與審計/配置落庫

## 六、缺口優先級

- `P0`：F01 快速體驗閉環（先從主入口與 session/claim-session 風險開始）
- `P1`：F03-F05 正式案件到執行主鏈路
- `P1`：F09 認證與守衛、F10 Admin 權限矩陣
- `P2`：F02/F06/F07/F08 的擴充分支、候選能力清理

## 七、當前結論

- **本輪補測（narrative.service summarizeNarratives 空陣列邊界）**：未發現業務缺陷。
- **本輪補測（narrative.service extractNarratives/summarizeNarratives 成功路徑）**：未發現業務缺陷。
- **本輪補測（notification.service createIfEnabled 用戶不存在邊界）**：未發現業務缺陷。
- **本輪補測（system-config.service getConfigValue 成功路徑與 prisma 拋錯邊界）**：未發現業務缺陷。
- **本輪補測（errorMessages getErrorMessage FORBIDDEN/JUDGMENT_NOT_FOUND 映射）**：未發現業務缺陷。
- **本輪補測（psych-profile.service giveConsent 用戶不存在邊界）**：發現並修復 **F06-BUG-005**：`giveConsent` 未檢查 user 存在性，對不存在的 userId 直接 update 會導致 Prisma P2025 拋錯；現已加入 findUnique 檢查並拋出 NOT_FOUND，與 getProfile/deleteAllData 對齊。
- **本輪補測（helpers generateCaseTitle/extractKeywords/sanitizeText null/undefined 邊界）**：未發現業務缺陷；已於 helpers.ts 加入 `?? ''` 防禦，傳入 null/undefined 時視為空字串不拋錯，與 validateStatement 防禦模式對齊。
- **本輪補測（helpers isValidEmail/isValidUrl null/undefined 邊界）**：未發現業務缺陷；已於 helpers.ts 加入 `?? ''` 防禦，傳入 null/undefined 時視為空字串返回 false。
- **本輪補測（session.service addCaseToSession/addPairingToSession session 不存在邊界）**：發現並修復 **F01-BUG-006**：`addCaseToSession`/`addPairingToSession` 未檢查 session 存在性，對不存在的 sessionId 直接 update 會導致 Prisma P2025 拋錯並轉成 INTERNAL_ERROR；現已加入 findUnique 檢查並拋出 SESSION_EXPIRED，與 psych-profile giveConsent 對齊。
- **本輪補測（helpers formatDateTime null/undefined 邊界）**：未發現業務缺陷；已於 helpers.ts 加入 null/undefined 防禦，傳入時返回空字串不拋錯。
- **本輪補測（helpers calculatePagination total 負數邊界）**：發現並修復 **HELPERS-BUG-002**：`calculatePagination` 在 `total` 為負數時會回傳負數 total 與異常 total_pages/has_more 語義；已於 helpers.ts 加入 safeTotal 防禦，負數視為 0，與 pageSize=0 防禦（HELPERS-BUG-001）對齊。
- **本輪補測（F02 result 回訪）**：未發現業務缺陷；已補 QuickExperience Result **collaborative 案件（mode=collaborative）在 result 頁應可正確載入**（F02 result 回訪：與 F01 共用 Result 頁，確保 collaborative 流程承接無誤）。
- **本輪補測（email.service sendJudgmentNotification 用戶不存在邊界）**：未發現業務缺陷；已補 **用戶不存在時應靜默返回**（候選功能邊界：findUnique 返回 null 時 `!user?.email` 已防禦，不拋錯不發送）。
- **本輪補測（file.service validateFile originalname 空字串邊界）**：未發現業務缺陷；已補 **originalname 為空字串時應拋出 INVALID_FILE_TYPE**（候選功能邊界：無擴展名防禦，F03/F05 證據與照片上傳共用）。
- **本輪補測（notification.service markAsSent/markFailed notification 不存在邊界）**：未發現業務缺陷；已補 **markAsSent/markFailed 當 notification 不存在時應拋錯**（候選功能邊界：Prisma P2025 會由 errorHandler 轉為 404）。
- **本輪補測（email.service sendPairingNotification findMany 空陣列邊界）**：未發現業務缺陷；已補 **findMany 返回空陣列時應不發送且不拋錯**（候選功能邊界：雙方用戶皆不存在時靜默返回，F08 配對通知）。
- **本輪補測（system-config.service getBooleanConfig/getNumberConfig value 為 null 邊界）**：未發現業務缺陷；已補 **getBooleanConfig/getNumberConfig 當 value 為 null 時應返回 fallback**（F10 邊界：DB 存 null 防禦）。
- **本輪補測（admin.service bootstrap roleKey 不存在邊界）**：未發現業務缺陷；已補 **bootstrap roleKey 對應角色不存在時應拋出 VALIDATION_ERROR**（F10 邊界：管理員初始化角色防禦）。
- **本輪補測（content.service getRecommendations 無關聯邊界）**：未發現業務缺陷；已補 **無關聯時應返回空陣列（F01/F05 邊界：案件無推薦內容）**（候選功能邊界：findMany 返回空陣列時 result 應為 []）。
- **本輪補測（profile.service getRelationshipProfile 無關係檔案邊界）**：未發現業務缺陷；已補 **配對存在但無關係檔案時應返回 null（F08 邊界：尚未填寫關係檔案）**（候選功能邊界：relationshipProfile.findUnique 返回 null 時正常返回 null）。
- **本輪補測（notification.service list 空陣列邊界）**：未發現業務缺陷；已補 **無通知時應返回空陣列（F09/F10 邊界：用戶尚未收到任何通知）**（候選功能邊界：findMany 返回空陣列時 result 應為 []）。
- **本輪補測（content.service listContent 空陣列邊界）**：未發現業務缺陷；已補 **無內容時應返回空陣列（F01 邊界：content list 空結果）**（候選功能邊界：findMany 返回空陣列時 result 應為 []）。
- **本輪補測（notification.service getPending 空陣列邊界）**：未發現業務缺陷；已補 **無待處理通知時應返回空陣列（F10 邊界：queue 為空）**（候選功能邊界：findMany 返回空陣列時 result 應為 []）。
- **本輪補測（reconciliation.service getPlansByJudgmentId 空陣列邊界）**：未發現業務缺陷；已補 **判決存在但尚無方案時應返回空陣列（F05 邊界：尚未生成方案）**（和好方案列表空結果防禦）。
- **本輪補測（admin.service listAdminUsers 空陣列邊界）**：未發現業務缺陷；已補 **無管理員時應返回 items 空陣列與 total 0（F10 邊界）**（管理員列表空結果防禦）。
- **本輪補測（admin.service listAuditLogs 空陣列邊界）**：未發現業務缺陷；已補 **無審計時應返回 items 空陣列與 total 0（F10 邊界）**（審計日誌列表空結果防禦）。
- **本輪補測（chat.service listMessages 空陣列邊界）**：未發現業務缺陷；已補 **無訊息時應返回 messages 空陣列與 nextCursor null（F07 邊界）**（聊天室訊息列表空結果防禦）。
- **本輪補測（admin.controller listConfigs 空陣列邊界）**：未發現業務缺陷；已補 **無配置時應返回 items 空陣列與 total 0（F10 邊界）**（系統配置列表空結果防禦）。
- **本輪補測（admin.controller listUsers 空陣列邊界）**：未發現業務缺陷；已補 **無用戶時應返回 items 空陣列與 total 0（F10 邊界）**（用戶列表空結果防禦）。
- **本輪補測（execution.service getExecutionStatus）**：未發現業務缺陷；已強化 **無記錄時應返回 records 空陣列、進度 0、狀態 pending（F05 邊界）**（執行打卡記錄空結果防禦）。
- **本輪補測（notification.controller list 空陣列邊界）**：未發現業務缺陷；已補 **無通知時應返回 notifications 空陣列（F09/F10 邊界）**（通知列表空結果防禦）。
- **本輪補測（content.controller list/recommendations 空陣列邊界）**：未發現業務缺陷；已補 **無內容時應返回 items 空陣列（F01 邊界）**、**無推薦時應返回 items 空陣列（F01/F05 邊界）**（內容列表與推薦空結果防禦）。
- **本輪補測（reconciliation.controller getPlans、execution.controller getAllExecutionStatuses 空陣列邊界）**：未發現業務缺陷；已補 **無方案時應返回 plans 空陣列（F05 邊界）**、**無執行狀態時應返回 executions 空陣列（F05 邊界）**（和好方案與執行看板空結果防禦）。
- **本輪補測（case.controller getCaseList 空陣列邊界）**：未發現業務缺陷；已補 **無案件時應返回 cases 空陣列與 pagination total 0（F03 邊界）**（案件列表空結果防禦）。
- **本輪補測（admin.controller listJobs 空陣列邊界）**：未發現業務缺陷；已補 **無日誌時應返回 jobs 空陣列（F10 邊界）**（定時任務列表空結果防禦）。
- **本輪補測（admin.controller listAdminUsers / listAuditLogs 空陣列邊界）**：未發現業務缺陷；已補 **無管理員時應返回 items 空陣列與 total 0（F10 邊界）**、**無審計時應返回 items 空陣列與 total 0（F10 邊界）**（管理員列表與審計日誌空結果防禦）。
- **本輪補測（psych-profile.controller、case.service 空陣列邊界）**：未發現業務缺陷；已補 **無反饋時應返回 history 空陣列（F06 邊界）**（心理畫像反饋歷史空結果防禦）、**無案件時應返回 cases 空陣列與 pagination total 0（F03 邊界）**（案件列表空結果防禦）。
- **本輪補測（psych-profile getProfile 空陣列邊界）**：未發現業務缺陷；已補 **無敘事與洞見時應返回 narratives、insights 空陣列（F06 邊界）**（心理畫像 profile 空結果防禦）。
- **本輪補測（chat-routes messages 空陣列邊界）**：未發現業務缺陷；已補 **無訊息時應返回 messages 空陣列與 nextCursor null（F07 邊界）**（Chat 訊息列表空結果防禦，整合層）。
- **本輪補測（psych-profile.routes 空陣列邊界）**：未發現業務缺陷；已補 **GET /psych-profile 無敘事與洞見時應返回 narratives、insights 空陣列（F06 邊界）**、**GET /psych-profile/feedback 無反饋時應返回 history 空陣列（F06 邊界）**（心理畫像 profile 與反饋歷史空結果防禦，路由層）。
- **本輪補測（admin.routes 空陣列邊界）**：未發現業務缺陷；已補 **GET /jobs 無日誌時應返回 jobs 空陣列（F10 邊界）**、**GET /configs 無配置時應返回 items 空陣列與 total 0（F10 邊界）**、**GET /users 無用戶時應返回 items 空陣列與 total 0（F10 邊界）**、**GET /admin-users 無管理員時應返回 items 空陣列與 total 0（F10 邊界）**、**GET /audit-logs 無審計時應返回 items 空陣列與 total 0（F10 邊界）**（Admin 列表空結果防禦，路由層）。
- **本輪補測（admin.routes 錯誤傳遞）**：未發現業務缺陷；已補 **listUsers、listAdminUsers、listAuditLogs 調用 next(error) 時應返回 500**（F10 錯誤傳遞，與 listJobs/listConfigs 對齊）。
- **本輪補測（execution.routes、reconciliation.routes 錯誤傳遞）**：未發現業務缺陷；已補 **execution.routes** confirmExecution、getExecutionStatus、getAllExecutionStatuses 調用 next(error) 時應返回 500（F05 錯誤傳遞）；**reconciliation.routes** generatePlans、getPlanById、selectPlan 調用 next(error) 時應返回 500（F05 錯誤傳遞）。
- **本輪補測（content.routes 錯誤傳遞）**：未發現業務缺陷；已補 **recommendations、link 調用 next(error) 時應返回 500**（候選功能/F01 錯誤傳遞，與 list 對齊）。
- **本輪補測（auth.routes 錯誤傳遞）**：未發現業務缺陷；已補 **register、sendVerificationCode、verifyEmail、resetPassword、confirmResetPassword 調用 next(error) 時應返回 500**（F09 錯誤傳遞，與 login/claimSession 對齊，全端點覆蓋）。
- **本輪補測（profile.routes 錯誤傳遞）**：未發現業務缺陷；已補 **upsertUserProfile、upsertRelationshipProfile 調用 next(error) 時應返回 500**（F08 錯誤傳遞，與 getUserProfile/getRelationshipProfile 對齊，全端點覆蓋）。
- **本輪補測（psych-profile.controller、judgment.controller 錯誤傳遞）**：未發現業務缺陷；已補 **psych-profile.controller** getProfile、giveConsent、deleteAllData 拋錯時應 next(error)（F06 錯誤傳遞，與 getFeedbackHistory 對齊，全端點覆蓋）；**judgment.controller** getJudgmentById 當 prisma.findUnique 拋錯或 getJudgmentByCaseId 拋錯（非 FORBIDDEN）時應 next(error)、**repairJudgment、recordClinicalMetrics** 拋錯時應 next(error)（F04 錯誤傳遞，全端點覆蓋）。
- **本輪補測（case.controller 錯誤傳遞）**：未發現業務缺陷；已補 **createCollaborativeCase、getCaseBySessionId** 拋錯時應 next(error)（F01/F02/F03 錯誤傳遞，與 createQuickCase/getCaseById 對齊）。
- **本輪補測（evidence.controller 錯誤傳遞）**：未發現業務缺陷；已補 **uploadEvidence** preAuthorizeUpload 當 prisma.case.findUnique 拋錯時應 next(error)、**deleteEvidence** 當 prisma.evidence.findUnique 拋錯時應 next(error)（F01/F03/F05 證據邊界：DB 異常時錯誤傳遞）。
- **本輪補測（judgment.controller getJudgmentById 權限分支）**：未發現業務缺陷；已補 **getJudgmentByCaseId 拋錯（FORBIDDEN）時應 next(NOT_FOUND)**（F04 權限邊界：無權限時轉為判決不存在，避免洩漏權限資訊）。
- **本輪補測（evidence.controller sessionService 拋錯）**：未發現業務缺陷；已補 **uploadEvidence** preAuthorizeUpload 當 sessionService.getSession 拋錯時應 next(error)、**deleteEvidence** 當 sessionService.getSession 拋錯時應 next(error)（F01/F03/F05 證據邊界：session 服務異常時錯誤傳遞）。
- **本輪補測（evidence.controller fileService.deleteFile 拋錯）**：未發現業務缺陷；已補 **deleteEvidence** 當 fileService.deleteFile 拋錯時應 next(error)（F01/F03/F05 證據邊界：文件刪除異常時錯誤傳遞）。
- **本輪補測（chat.routes 錯誤傳遞）**：未發現業務缺陷；已新增 `chat.routes.test.ts`，補 **createRoom、getRoom、createInvite、acceptInvite、declineInvite、listMessages、sendMessage、requestJudgment、leaveRoom、kickParticipantB、getJudgmentStatus** 拋錯時應 next(error) 返回 500（F07 錯誤傳遞，與 chat-routes.smoke 對齊）。
- **本輪補測（chat.routes stream 端點錯誤傳遞）**：未發現業務缺陷；已補 **GET /rooms/:roomId/stream** 當 getRoom 拋錯時應 next(error) 返回 500（F07 SSE 錯誤傳遞）。
- **本輪補測（pairing.routes 空結果邊界）**：未發現業務缺陷；已補 **GET /status 無配對時應返回 pairing null（F08 邊界）**（配對狀態空結果防禦，與 profile getRelationshipProfile 對齊）。
- **本輪補測（session.routes createSession 成功邊界）**：未發現業務缺陷；已補 **POST /quick 成功時應返回 session_id 與 expires_at 非空（F01 邊界）**（快速體驗 session 創建回應結構防禦）。
- **本輪補測（profile.routes getRelationshipProfile 空結果邊界）**：未發現業務缺陷；已補 **getRelationshipProfile 無關係檔案時應返回 profile null（F08 邊界）**（關係檔案空結果防禦，與 profile.service 對齊）。
- **本輪補測（session.routes refreshSession 成功邊界）**：未發現業務缺陷；已補 **POST /refresh 成功時應返回 session_id 與 expires_at 非空（F01 邊界）**（session 刷新回應結構防禦，與 createSession 對齊）。
- **本輪補測（user.routes getProfile 成功邊界）**：未發現業務缺陷；已補 **getProfile 成功時應返回 data.user（F09 邊界）**（個人資料 API 回應結構防禦，與 user API getProfile 對齊）。
- **本輪補測（health.routes 邊界）**：未發現業務缺陷；已補 **lock backend 為 simple-lock-degraded 時應標記 degraded（F10 邊界）**（健康檢查鎖後端降級防禦）；**health/ready 在 development 時 DB 失敗 error 應為實際錯誤訊息（F10 邊界）**（就緒檢查錯誤訊息環境差異）。
- **本輪補測（metrics.routes 邊界）**：未發現業務缺陷；已補 **development 且 METRICS_ENABLED 時應返回 200（F10 邊界）**（監控端點開發環境成功路徑防禦）。
- **本輪補測（profile.routes getRelationshipProfile 成功邊界）**：未發現業務缺陷；已補 **getRelationshipProfile 成功時應返回 data.profile 含關係檔案結構（F08 邊界）**（關係檔案 API 回應結構防禦，與 profile null 空結果對稱）。
- **本輪補測（judgment.routes、execution.routes 成功邊界）**：未發現業務缺陷；已補 **getJudgmentById 成功時應返回 data.judgment（F04 邊界）**（判決 API 回應結構防禦）；**getExecutionStatus 成功時應返回 data 含 plan_id、records、progress（F05 邊界）**（執行狀態 API 回應結構防禦）。
- **本輪補測（reconciliation.routes、case.routes 成功邊界）**：未發現業務缺陷；已補 **getPlanById 成功時應返回 data.plan（F05 邊界）**（和好方案 API 回應結構防禦）；**getCaseById 成功時應返回 data.case（F03 邊界）**（案件 API 回應結構防禦）。
- **本輪補測（auth.routes、pairing.routes 成功邊界）**：未發現業務缺陷；已補 **login 成功時應返回 data.user、data.token（F09 邊界）**（登入 API 回應結構防禦）；**createPairing 成功時應返回 data.pairing 含 invite_code（F08 邊界）**（配對 API 回應結構防禦）。
- **本輪補測（psych-profile.routes、interview.routes 成功邊界）**：未發現業務缺陷；已補 **getProfile 成功時應返回 data 含 narratives、insights、richness_score（F06 邊界）**（心理畫像 API 回應結構防禦）；**getSession 成功時應返回 data 含 id、status、turns（F06 邊界）**（訪談 session API 回應結構防禦）。
- **本輪補測（chat.routes、admin.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **chat.routes** getRoom、createInvite、acceptInvite、declineInvite、sendMessage、requestJudgment、leaveRoom、kickParticipantB、getJudgmentStatus 成功時應返回 data.room / data.invite / data.message / data 含 status-caseId-judgmentId-linkId / data.roomStatus（F07 邊界）；**admin.routes** login、me、listJobs、listConfigs、getUserDetail、listAdminUsers 成功時應返回 data.token / data.admin / data.jobs / data.items / data.user（F10 邊界）。
- **本輪補測（content.routes、notification.routes、case.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **content.routes** link 成功時應返回 data.link（F01 邊界）；**notification.routes** list 成功時應返回 data 含 notifications（F09/F10 邊界）；**case.routes** createQuickCase 成功時應返回 data.case、data.session_id（F01 邊界）、getCaseList 成功時應返回 data.cases、data.pagination（F03 邊界）、getJudgmentByCaseId 成功時應返回 data.judgment（F04 邊界）。
- **本輪補測（reconciliation.routes、execution.routes、auth.routes、pairing.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **reconciliation.routes** generatePlans、getPlans 成功時應返回 data.plans，selectPlan 成功時應返回 data.plan（F05 邊界）；**execution.routes** confirmExecution、checkin 成功時應返回 data.execution，getAllExecutionStatuses 成功時應返回 data.executions（F05 邊界）；**auth.routes** register 成功時應返回 data.user、data.token，claimSession 成功時應返回 data.case_id（F01/F09 邊界）；**pairing.routes** joinPairing、cancelPairing 成功時應返回 data.pairing（F08 邊界）。
- **本輪補測（profile.routes、user.routes、judgment.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **profile.routes** getUserProfile、upsertUserProfile、upsertRelationshipProfile 成功時應返回 data.profile（F08 邊界）；**user.routes** updateProfile 成功時應返回 data.user（F09 邊界）；**judgment.routes** generateJudgment、acceptJudgment 成功時應返回 data.judgment（F04 邊界）。
- **本輪補測（case.routes、content.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **case.routes** createCase、createCollaborativeCase、submitCase、updateCase 成功時應返回 data.case（F02/F03 邊界），uploadEvidence 成功時應返回 data.evidences（F03 邊界）；**content.routes** list 成功時應返回 data.items（F01 邊界），recommendations 成功時應返回 data.items（F01/F05 邊界）。
- **本輪補測（interview.routes、pairing.routes、psych-profile.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **interview.routes** startSession 成功時應返回 data 含 id、status、turns（F06 邊界），checkResume 成功時應返回 data（F06 邊界）；**pairing.routes** getPairingStatus 有配對時應返回 data.pairing（F08 邊界）；**psych-profile.routes** getFeedbackHistory 成功時應返回 data.history（F06 邊界）。
- **本輪補測（case.routes、chat.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **case.routes** getCaseBySessionId 成功時應返回 data.case（F01/F03 邊界）；**chat.routes** createRoom 成功時應返回 data.room（F07 邊界）、listMessages 成功時應返回 data.messages、data.nextCursor（F07 邊界）。
- **本輪補測（admin.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **admin.routes** listUsers、listAuditLogs 成功時應返回 data.items（F10 邊界）。
- **本輪補測（auth.routes 成功回應結構邊界）**：未發現業務缺陷；已補 **auth.routes** sendVerificationCode 成功時應返回 data.expires_in、verifyEmail 成功時應返回 data.verified、resetPassword 成功時應返回 data.expires_in、confirmResetPassword 成功時應返回 data（F09 邊界）。
- **本輪補測（case.routes deleteEvidence 成功回應結構邊界）**：未發現業務缺陷；已補 **case.routes** deleteEvidence 成功時應返回 data（F03 邊界）。
- **本輪補測（judgment.routes repairJudgment、recordClinicalMetrics 成功回應結構邊界）**：未發現業務缺陷；已補 **judgment.routes** repairJudgment 成功時應返回 data、recordClinicalMetrics 成功時應返回 data（F04 邊界）。
- **本輪補測（admin.routes triggerJob、getJobStats、upsertConfig、updateUserStatus、getInterviewRuntimeConfig 成功回應結構邊界）**：未發現業務缺陷；已補 **admin.routes** triggerJob 成功時應返回 data.status、getJobStats 成功時應返回 data.perJob、upsertConfig 成功時應返回 data.item、updateUserStatus 成功時應返回 data.user、getInterviewRuntimeConfig 成功時應返回 data（F10 邊界）。
- **本輪補測（admin.routes createAdminUser、updateAdminUser、deleteAdminUser 成功回應結構邊界）**：未發現業務缺陷；已補 **admin.routes** createAdminUser 成功時應返回 data.item、updateAdminUser 成功時應返回 data.item、deleteAdminUser 成功時應返回 data.item（F10 邊界）。
- **本輪補測（admin.routes 錯誤傳遞）**：未發現業務缺陷；已補 **admin.routes** triggerJob、getJobStats、upsertConfig、getUserDetail、updateUserStatus、createAdminUser、updateAdminUser、deleteAdminUser 調用 next(error) 時應返回 500（F10 錯誤傳遞）。
- **本輪補測（admin.routes bootstrap 成功回應結構與錯誤傳遞）**：未發現業務缺陷；已補 **admin.routes** bootstrap 成功時應返回 data（F10 邊界）、bootstrap 調用 next(error) 時應返回 500（F10 錯誤傳遞）。
- **本輪補測（judgment.controller repairJudgment/recordClinicalMetrics 邊界）**：未發現業務缺陷；已補 **repairJudgment、recordClinicalMetrics** header/query session 衝突時應 next(INVALID_SESSION_ID)（F04 邊界：與 generateJudgment/getJudgmentById 對齊）。
- **本輪補測（reconciliation.routes、execution.routes、notification.routes 空陣列邊界）**：未發現業務缺陷；已補 **無方案時應返回 plans 空陣列（F05 邊界）**、**無執行狀態時應返回 executions 空陣列（F05 邊界）**、**無通知時應返回 notifications 空陣列（F09/F10 邊界）**（路由層空結果防禦）。
- **本輪補測（case.routes、content.routes 空陣列邊界）**：未發現業務缺陷；已補 **無案件時應返回 cases 空陣列與 pagination total 0（F03 邊界）**、**無內容時應返回 items 空陣列（F01 邊界）**、**無推薦時應返回 items 空陣列（F01/F05 邊界）**（案件列表與內容列表空結果防禦，路由層）。
- **本輪補測（profile-richness.service 豐富度計算邊界）**：未發現業務缺陷。
- **本輪補測（profile-snapshot.service、insight-extraction.service pipeline 邊界）**：未發現業務缺陷。
- **本輪補測（case-context.service loadCaseContext quick/無原告邊界）**：未發現業務缺陷。
- **本輪補測（domain-classification.service batchClassify 邊界）**：未發現業務缺陷。
- **本輪補測（async-pipeline.service process/resume 邊界）**：未發現業務缺陷。
- **本輪補測（case-context.service formatForEmotionalAnalysis/formatForResponsibilityRatio/formatForSummary 空 ctx 邊界）**：未發現業務缺陷。
- **本輪補測（system-config.service getConfigValue/getBooleanConfig/getNumberConfig 邊界）**：未發現業務缺陷。
- **本輪補測（rupture-repair.service repair 反饋分類與 prompt 長度邊界）**：未發現業務缺陷。
- **本輪補測（clinical-quality.service recordPostResponseMetrics 邊界）**：未發現業務缺陷。
- **本輪補測（prompt fenceUserInput prompt injection 防禦）**：未發現業務缺陷。
- **本輪補測（judgment.utils normalizeJudgment、helpers calculatePagination/extractKeywords/sanitizeText 邊界）**：發現並修復 **HELPERS-BUG-001**：`calculatePagination` 在 `pageSize=0` 時導致 `total_pages` 為 NaN（total=0）或 Infinity（total>0），已於 `helpers.ts` 加入 `safePageSize` 防禦。
- **本輪補測（ValidationUtils 邊界）**：未發現業務缺陷；已優化 `validateResponsibilityRatio` 對 null/undefined 改拋 VALIDATION_ERROR 而非 TypeError。
- **本輪補測（ai.types isResponsibilityRatio/isReconciliationPlanContent、retry retryWithBackoff 邊界）**：未發現業務缺陷。
- **本輪補測（request getSessionIdFromSources、jwt verifyToken、constants 邊界）**：未發現業務缺陷。
- **本輪補測（password validatePasswordStrength 邊界）**：未發現業務缺陷。
- **本輪補測（session validateSessionId、errors v2.0 訪談錯誤工廠）**：未發現業務缺陷。
- **本輪補測（admin-jwt、cache generateKey/generateHashKey、lock release/getBackendStatus 邊界）**：未發現業務缺陷。
- **本輪補測（errorHandler MulterError、err null/非 Error 邊界；prompt fenceUserInput label/text 空字串邊界）**：未發現業務缺陷。
- **本輪補測（responseFormatter data null/undefined/success:false 邊界）**：未發現業務缺陷。
- **本輪補測（validator 空 schema、query undefined 邊界）**：未發現業務缺陷。
- **本輪補測（performance 生產環境不記錄正常請求；helpers generateCaseTitle 空字串邊界）**：未發現業務缺陷。
- **本輪補測（cache Redis set 失敗降級；judgment normalizeJudgment undefined 邊界）**：未發現業務缺陷。
- **本輪補測（constants CASE_STATUS 完整狀態、LOCK_TTL 業務鎖）**：未發現業務缺陷。
- **本輪補測（metrics.routes IP 白名單、jwt verifyToken 邊界、request getSessionIdFromSources 邊界）**：發現並修復 **REQUEST-BUG-001**：`getSessionIdFromSources` 在 `req.query` 為 undefined 時拋錯（`Cannot read properties of undefined (reading 'session_id')`），已於 `request.ts` 加入 `req.query != null` 防禦。
- **本輪補測（storage caseSessionMap 空 sid、env METRICS_ALLOWED_IPS 邊界）**：未發現業務缺陷。
- **本輪補測（content.controller list tags 空字串、query 完全為空邊界；health.routes getBackendStatus 拋錯 500）**：未發現業務缺陷。
- **本輪補測（content.controller list limit clamp/is_active false、link 拋錯；content.service listContent tags 空陣列/limit 0；content.routes 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（session.controller refreshSession 無 session/僅 query 邊界；session.routes 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（user.controller updateProfile 拋錯；user.routes、notification.routes 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（case.routes createQuickCase/getCaseById 錯誤傳遞；auth.routes login/claimSession 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（execution.routes checkin、reconciliation.routes getPlans、judgment.routes acceptJudgment 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（profile.routes getUserProfile/getRelationshipProfile、admin.routes listJobs/listConfigs 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（interview.routes、psych-profile.routes 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（case.routes 全端點錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（admin.routes login/me 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（admin.routes healthDetailed、exportAuditLogsCsv、reportOverview、reportFunnel、reportCosts、exportOverviewCsv、customReport、getInterviewRuntimeConfig、upsertAlertRules、setFeatureFlags 錯誤傳遞）**：未發現業務缺陷。
- **本輪補測（路由層單元測試盤點）**：admin、case、judgment、auth、chat、interview、psych-profile、pairing、execution、reconciliation、profile、user、content、session、notification、health、metrics 等路由錯誤傳遞與成功回應結構已全覆蓋；下一批優先為 E2E/flow（八、下一步建議 第1-2項）。
- **本輪補測（smoke.test claim-session 端點可達性）**：未發現業務缺陷。
- **本輪補測（smoke.test sessions/quick、sessions/refresh 端點可達性）**：未發現業務缺陷。
- **本輪補測（smoke.test cases/quick plaintiff_statement 不足 30 字驗證邊界）**：未發現業務缺陷。
- **本輪補測（smoke.test health/live 端點可達性）**：未發現業務缺陷。
- **本輪補測（smoke.test content-items 端點可達性）**：未發現業務缺陷；已補 **GET /api/v1/content-items 應返回 200 且含 items**（F01 content tips 端點可達）。
- **本輪補測（smoke.test content-items/recommendations 端點可達性）**：未發現業務缺陷；已補 **GET /api/v1/content-items/recommendations/:caseId 無效 UUID 時應返回驗證錯誤**（F01/F05 推薦端點可達與驗證）。
- **本輪補測（smoke.test health/ready 端點可達性）**：未發現業務缺陷；已補 **GET /health/ready 應返回 200 或 503**（K8s 就緒探針端點可達，F10 健康檢查）。
- **本輪補測（smoke.test auth 端點可達性）**：未發現業務缺陷；已補 **POST /api/v1/auth/send-verification-code 無效 email 時應返回驗證錯誤**、**POST /api/v1/auth/login 缺少必填字段時應返回驗證錯誤**（F09 認證端點可達）。
- **本輪補測（smoke.test auth/register、pairing 端點可達性）**：未發現業務缺陷；已補 **POST /api/v1/auth/register 缺少必填字段時應返回驗證錯誤**（F09 端點可達）、**GET /api/v1/pairing/status 無 token 時應返回 401**（F08 配對端點可達）。
- **本輪補測（smoke.test auth reset-password、verify-email 端點可達性）**：未發現業務缺陷；已補 **POST /api/v1/auth/reset-password 缺少必填字段時應返回驗證錯誤**、**POST /api/v1/auth/verify-email 缺少必填字段時應返回驗證錯誤**（F09 忘記密碼/驗證端點可達）。
- **本輪補測（smoke.test auth reset-password-confirm、user/profile 端點可達性）**：未發現業務缺陷；已補 **POST /api/v1/auth/reset-password-confirm 缺少必填字段時應返回驗證錯誤**、**GET /api/v1/user/profile 無 token 時應返回 401**（F09 確認重置/個人資料端點可達）。
- **本輪補測（smoke.test cases、notifications、profile/me 端點可達性）**：未發現業務缺陷；已補 **GET /api/v1/cases 無 token 時應返回 401**（F03 案件列表端點可達）、**GET /api/v1/notifications 無 token 時應返回 401**（F09 通知端點可達）、**GET /api/v1/profile/me 無 token 時應返回 401**（F08/F09 個人資料端點可達）。
- **本輪補測（smoke.test psych-profile、execution 端點可達性）**：未發現業務缺陷；已補 **GET /api/v1/execution/dashboard 無 token 時應返回 401**、**GET /api/v1/execution/status 無 token 時應返回 401**（F05 執行端點可達）、**GET /api/v1/psych-profile 無 token 時應返回 401**（F06 心理畫像端點可達）。
- **本輪補測（smoke.test interview、judgment、reconciliation 端點可達性）**：未發現業務缺陷；已補 **POST /api/v1/interview/start 無 token 時應返回 401**、**GET /api/v1/interview/resume 無 token 時應返回 401**（F06 訪談端點可達）、**POST /api/v1/judgments/:id/accept 無 token 時應返回 401**（F04 接受判決端點可達）、**GET /api/v1/judgments/:id/reconciliation-plans 無 token 時應返回 401**、**GET /api/v1/reconciliation-plans/:id 無 token 時應返回 401**（F05 和好方案端點可達）；smoke.test.ts 案例數 32→37。
- Emorapy 已有相當數量的測試資產，但目前更接近「多點存在」而非「按功能完整閉環」。
- 當前最需要的不是盲目加案例數，而是把測試重新對齊到功能主表、主流程與鐵律。
- 第一波應從 F01 入手，先完成最有轉化與跨層風險的閉環。

## 八、下一步建議

1. **優先**：持續維護 F01 專項測試設計文檔，補升格與回訪真鏈路（E2E/flow）。
2. **優先**：補 `claim-session`、session refresh、owner session invite 與 result 頁真實鏈路（E2E/flow）。
3. 每完成一輪功能，就同步更新本文件與任務拆解表。
4. **路由層單元測試**：已全覆蓋（錯誤傳遞、成功回應結構），可收尾。
5. 本輪已補：**smoke.test** POST /api/v1/auth/claim-session 無 token 時應返回 401、sessions/quick、sessions/refresh、cases/quick plaintiff_statement 不足 30 字驗證邊界、GET /health/live 應返回 200 alive、**GET /health/ready 應返回 200 或 503**、**GET /api/v1/content-items 應返回 200 且含 items**、**GET /api/v1/content-items/recommendations/:caseId 無效 UUID 時應返回驗證錯誤**、**POST /api/v1/auth/send-verification-code 無效 email 時應返回驗證錯誤**、**POST /api/v1/auth/login 缺少必填字段時應返回驗證錯誤**、**POST /api/v1/auth/register 缺少必填字段時應返回驗證錯誤**、**POST /api/v1/auth/reset-password 缺少必填字段時應返回驗證錯誤**、**POST /api/v1/auth/verify-email 缺少必填字段時應返回驗證錯誤**、**POST /api/v1/auth/reset-password-confirm 缺少必填字段時應返回驗證錯誤**、**GET /api/v1/user/profile 無 token 時應返回 401**、**GET /api/v1/pairing/status 無 token 時應返回 401**、**GET /api/v1/cases 無 token 時應返回 401**、**GET /api/v1/notifications 無 token 時應返回 401**、**GET /api/v1/profile/me 無 token 時應返回 401**、**GET /api/v1/execution/dashboard 無 token 時應返回 401**、**GET /api/v1/execution/status 無 token 時應返回 401**、**GET /api/v1/psych-profile 無 token 時應返回 401**、**POST /api/v1/interview/start 無 token 時應返回 401**、**GET /api/v1/interview/resume 無 token 時應返回 401**、**POST /api/v1/judgments/:id/accept 無 token 時應返回 401**、**GET /api/v1/judgments/:id/reconciliation-plans 無 token 時應返回 401**、**GET /api/v1/reconciliation-plans/:id 無 token 時應返回 401**（F01 升格/session/cases/health/content、F03 案件、F04 判決、F05 執行/和好方案、F06 心理畫像/訪談、F08 配對、F09 認證端點可達）；**admin.routes** healthDetailed、exportAuditLogsCsv、reportOverview、reportFunnel、reportCosts、exportOverviewCsv、customReport、getInterviewRuntimeConfig、upsertAlertRules、setFeatureFlags 調用 next(error) 時應返回 500（F10 錯誤傳遞）；**admin.routes** login、me 調用 next(error) 時應返回 500（F10 錯誤傳遞）；**admin.routes** bootstrap 成功時應返回 data、bootstrap 調用 next(error) 時應返回 500（F10 邊界）；**admin.routes** triggerJob、getJobStats、upsertConfig、getUserDetail、updateUserStatus、createAdminUser、updateAdminUser、deleteAdminUser 調用 next(error) 時應返回 500（F10 錯誤傳遞）；**admin.routes** createAdminUser、updateAdminUser、deleteAdminUser 成功時應返回 data.item（F10 邊界）；**admin.routes** triggerJob、getJobStats、upsertConfig、updateUserStatus、getInterviewRuntimeConfig 成功時應返回 data.status/data.perJob/data.item/data.user/data（F10 邊界）；**judgment.routes** repairJudgment、recordClinicalMetrics 成功時應返回 data（F04 邊界）；**case.routes** deleteEvidence 成功時應返回 data（F03 邊界）；**auth.routes** sendVerificationCode、verifyEmail、resetPassword、confirmResetPassword 成功時應返回 data.expires_in/data.verified/data（F09 邊界）；**admin.routes** listUsers、listAuditLogs 成功時應返回 data.items（F10 邊界）；**chat.routes** listMessages 成功時應返回 data.messages、data.nextCursor（F07 邊界）；**case.routes** getCaseBySessionId 成功時應返回 data.case（F01/F03 邊界）；**chat.routes** createRoom 成功時應返回 data.room（F07 邊界）；**interview.routes** startSession、checkResume 成功時應返回 data 含 id/status/turns 或 data（F06 邊界）；**pairing.routes** getPairingStatus 有配對時應返回 data.pairing（F08 邊界）；**psych-profile.routes** getFeedbackHistory 成功時應返回 data.history（F06 邊界）；**case.routes** createCase、createCollaborativeCase、submitCase、updateCase 成功時應返回 data.case，uploadEvidence 成功時應返回 data.evidences（F02/F03 邊界）；**content.routes** list、recommendations 成功時應返回 data.items（F01/F05 邊界）；**profile.routes** getUserProfile/upsertUserProfile/upsertRelationshipProfile、**user.routes** updateProfile、**judgment.routes** generateJudgment/acceptJudgment 成功回應結構；**reconciliation.routes** generatePlans/getPlans/selectPlan、**execution.routes** confirmExecution/checkin/getAllExecutionStatuses、**auth.routes** register/claimSession、**pairing.routes** joinPairing/cancelPairing 成功回應結構；**content.routes** link data.link、**notification.routes** list data.notifications、**case.routes** createQuickCase/getCaseList/getJudgmentByCaseId 成功回應結構；**chat.routes、admin.routes** 成功回應結構邊界（data.room/invite/message、data.token/admin/jobs/items/user）；**case.routes** getCaseBySessionId、createQuickCase、createCollaborativeCase、createCase、getCaseList、getCaseById、getJudgmentByCaseId、submitCase、updateCase、uploadEvidence、deleteEvidence 調用 next(error) 時應返回 500（F01/F03 錯誤傳遞）；**interview.routes** startSession、getSession、respond、endSession、skip、checkResume、retryFailed 調用 next(error) 時應返回 500（F06 錯誤傳遞）；**psych-profile.routes** getProfile、getFeedbackHistory、giveConsent、deleteAllData 調用 next(error) 時應返回 500（F06 錯誤傳遞）；**profile.routes** getUserProfile/getRelationshipProfile 調用 next(error) 時應返回 500（F08 錯誤傳遞）；**admin.routes** listJobs/listConfigs 調用 next(error) 時應返回 500（F10 錯誤傳遞）；**execution.routes** checkin 調用 next(error) 時應返回 500（F05 錯誤傳遞）；**reconciliation.routes** getPlans 調用 next(error) 時應返回 500（F05 錯誤傳遞）；**judgment.routes** acceptJudgment 調用 next(error) 時應返回 500（F04 錯誤傳遞）；**case.routes** createQuickCase/getCaseById 調用 next(error) 時應返回 500（F01/F03 錯誤傳遞）；**auth.routes** login/claimSession 調用 next(error) 時應返回 500（F09 錯誤傳遞）；**user.controller** updateProfile 拋錯時應 next(error)；**user.routes** getProfile/updateProfile 調用 next(error) 時應返回 500（錯誤傳遞）；**notification.routes** list 調用 next(error) 時應返回 500（錯誤傳遞）；**session.controller refreshSession** header/query 皆無 session 時應調用 refreshSession(undefined) 等同創建新 session（F01 邊界）、僅 query 有 session 時應使用 query session_id；**session.routes** createSession/refreshSession 調用 next(error) 時應返回 500（錯誤傳遞）；**content.controller list** limit 超過 100 時應 clamp 到 100（F01 MAX_CONTENT_LIMIT）、is_active 為 'false' 時應傳 false、**link** 拋錯時應 next(error)；**content.service listContent** tags 為空陣列時應不設置 where.tags、limit 為 0 時應使用 20（防禦性）；**content.routes** list 調用 next(error) 時應返回 500（錯誤傳遞）；**content.controller list** tags 為空字串時應傳空陣列（F01 邊界：防禦性）、query 完全為空時應使用預設 limit 20 與 is_active true；**health.routes** getBackendStatus 拋錯時應返回 500（候選功能 / 通用基礎 health handler 異常分支）；`utils/url`、…、`BackToTop`；`useAsync`；`accessibility`；`errorHandler`；**apiError.getErrorMessage**（含 **FORBIDDEN 且無 message 屬性時應使用 fallback** 權限邊界慣例、**message 為 null 或 undefined 時應使用 fallback** F10 邊界）；**validator**；**validate.validateStatement**（含 **長度剛好 2000 字應返回 valid: true** 上界正邊界）；**lock.withLock**；**retry** maxRetries 0；**getAuthUserId** 在 req.user 缺失時改拋 Errors.UNAUTHORIZED（AUTH-OPT-001，401 語義對齊）；**execution.service** plan_content 為 null 或無效 JSON 時 extractPlanSummary 應返回 title 為空字串（F05 邊界：DB/API 回傳不完整時防禦）；**narrative.service** extractNarratives 訪談不存在應拋出 NOT_FOUND、summarizeNarratives 無最新敘事時應不呼叫 AI（F06 邊界：pipeline 前置條件、空陣列不崩潰）；**case-context.service** loadCaseContext preloadedCase mode 為 quick 或 plaintiff_id 為 null 時應返回 null（F01/F05 邊界：quick 模式與無原告不載入 context）；**case-context.service** formatForEmotionalAnalysis/formatForResponsibilityRatio/formatForSummary 空 ctx 時應返回 null（F04/F05 邊界：判決與和好方案 context 空值防禦）；**judgment.utils** normalizeJudgment plaintiff_ratio/defendant_ratio 為 undefined 或 0 時邊界；**helpers** calculatePagination pageSize=0 防禦（HELPERS-BUG-001 修復）、extractKeywords 空字串/count=0、sanitizeText 空字串；**ValidationUtils** validateStatement/validateEvidenceUrls/validateUUID/validateEmail/validatePassword/validateResponsibilityRatio null/undefined 邊界、validateEvidenceUrls http 拒絕與空陣列、validateResponsibilityRatio 0+100 邊界；**ai.types** isResponsibilityRatio 負數/缺欄位/非數字/0+100、isReconciliationPlanContent 缺欄位/steps 非陣列/空 steps；**retry** retryWithBackoff 401 不重試、error 無 status 時重試；**request** getSessionIdFromSources 皆無/query 陣列/相同時 hasConflict；**jwt** verifyToken 空字串；**constants** EVIDENCE_UPLOAD_ALLOWED_STATUSES、PAGINATION；**password** validatePasswordStrength 常見弱密碼/長度/字母/數字/8/128 邊界；**session** validateSessionId undefined/8字/7字邊界；**errors** v2.0 訪談錯誤工廠（CONSENT_REQUIRED、PROCESSING_FAILED 等）；**admin-jwt** verifyAdminToken 空字串/無效/過期/簽名錯誤邊界；**cache** generateKey 空 parts、generateHashKey 空 content；**lock** release 冪等、getBackendStatus；**errorHandler** MulterError LIMIT_FILE_SIZE/LIMIT_FILE_COUNT/LIMIT_UNEXPECTED_FILE、err null/非 Error 邊界；**prompt** fenceUserInput label/text 空字串邊界；**responseFormatter** data null/undefined/success:false 邊界；**validator** 空 schema、query undefined 邊界；**performance** 生產環境不記錄正常請求；**helpers** generateCaseTitle 空字串邊界；**cache** Redis set 失敗降級；**judgment** normalizeJudgment undefined 邊界；**constants** CASE_STATUS 完整狀態、LOCK_TTL 業務鎖；**metrics.routes** IP 白名單（trust proxy + X-Forwarded-For）、IP 不在白名單 403；**jwt** verifyToken undefined、過期 token TOKEN_EXPIRED；**request** getSessionIdFromSources req.query 為 undefined 時不崩潰（REQUEST-BUG-001 修復）；**storage** caseSessionMap get 當 entry sid 為空字串時 compact 過濾返回 null；**env** METRICS_ALLOWED_IPS 空字串返回空陣列、逗號分隔正確解析 trim。
