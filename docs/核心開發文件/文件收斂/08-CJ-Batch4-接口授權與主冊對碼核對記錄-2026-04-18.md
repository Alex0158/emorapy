# CJ Batch 4 接口授權與主冊對碼核對記錄（2026-04-18）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：治理記錄
**來源時間**：2026-04-18
**上下文**：非現行 SSOT；僅作文檔治理歷史記錄
**SSOT 屬性**：非現行 SSOT（僅作證據/歷史/治理參考）
**最後核驗 Commit**：`8147ac3`
**最後核驗日期**：`2026-04-19`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 範圍

本次核對 Batch 4 接口文檔與主冊：

1. `全接口清單-主文檔.md`
2. `接口-功能-頁面-Mapping.md`
3. `06-接口描述/*`

## 取證入口

1. `backend/src/app.ts`
2. `backend/src/routes/*.ts`
3. `backend/src/middleware/auth.ts`
4. `backend/src/middleware/adminAuth.ts`
5. `scripts/lib/core-docs-truth.mjs`
6. `scripts/check-docs-truth.mjs`

## 本輪發現與處置

| 編號 | 發現 | 影響 | 處置 |
|---|---|---|---|
| B4-01 | 主冊已校驗端點存在與狀態，但對 `Auth` 欄位與路由授權模式缺少機器約束 | 可能出現接口仍存在但授權口徑漂移 | 在 `docs:check:truth` 新增 `Auth` 欄位對齊守衛（`User` 需 `是/必須`，`Admin` 不得標成 `否`） |
| B4-02 | 初版嘗試用「端點類型」直接對齊路由 authMode，對 `bootstrap/login/metrics` 與 `accept invite` 等語義分類產生誤報 | 守衛噪聲上升，降低可用性 | 收斂到授權欄位最小充分校驗，保留語義分層由 `06-接口描述` 人工語義描述承接 |

## 守衛補強

本輪新增以下接口級守衛：

1. `全接口清單-主文檔.md` 每個後端註冊 endpoint 的 `Auth` 欄位與後端授權模式最低一致性校驗。
2. `User` 授權端點：主冊 `Auth` 欄位必須含 `是` 或 `必須`。
3. `Admin` 授權端點：主冊 `Auth` 欄位不得為 `否`。
4. 保留既有端點存在性、狀態桶、API/Mapping 對齊、stream 展開完整性守衛不變。

## 驗證結果

執行命令：

```bash
npm run docs:check
```

結果：

1. `docs:check:structure` PASS
2. `docs:check:truth` PASS（`126 endpoints / 30 frontend routes / 9 admin routes`）

## 2026-04-19 再核驗（逐文件對碼）

本次對 `06-接口描述` 全 11 份文檔執行再核驗，並與下列現碼入口交叉比對：

1. `backend/src/routes/{auth,session,user,profile,pairing,case,judgment,reconciliation,execution,interview,psych-profile,chat,content,notification,admin,health,metrics,meta}.routes.ts`
2. `frontend/src/services/api/*.ts`
3. `frontend-admin/src/services/api/admin.ts`
4. `docs/核心開發文件/全接口清單-主文檔.md`

再核驗結論：

1. 未發現 endpoint 註冊、授權模式與前後台 API 客戶端調用之間的新漂移。
2. `GET /api/v1/version` 仍為活躍兼容入口，狀態口徑維持「候選廢棄（兼容保留）」；`GET /version` 維持已使用主入口。
3. 已補齊 `06-接口描述` 子域文檔核驗元數據，統一到本輪基準 commit。

## 2026-04-19 補充回寫（現碼增量語義）

在保持接口主冊與授權守衛綠燈的前提下，本輪補記 2 條已落地但文檔缺少細節的現碼語義：

1. `06-interview-psych-profile` 補充有界 canonical 自愈輪詢規則：`isStreaming` 期間每 `2500ms` 靜默同步一次，最多 `24` 次，並以 lock 防止重入，用於 SSE 漏 terminal 事件時的自恢復。
2. `07-chat` 補充 `included_message_ids` 白名單契約：僅 `user_text + visibility_scope=all`，且受 `history_visibility_mode + roleB.joined_at` 時間窗約束；越界 ID 由後端以 `NOT_FOUND` 收口。

## 結論

1. Batch 4 在不引入語義誤報的前提下，補上了接口授權欄位的機器約束。
2. `全接口清單-主文檔` 的主干事實（端點、狀態、授權最低口徑）已可由守衛持續監測。
3. 後續若授權模式改動但主冊未回寫，`docs:check:truth` 會直接報錯。
