# CJ Batch 1 旗艦文檔對碼核對記錄（2026-04-18）

<!-- CORE_DOC_AUDIT_METADATA:START -->
**文檔類型**：治理記錄
**來源時間**：2026-04-18
**上下文**：非現行 SSOT；僅作文檔治理歷史記錄
**SSOT 屬性**：非現行 SSOT（僅作證據/歷史/治理參考）
**最後核驗 Commit**：`bd66c2d`
**最後核驗日期**：`2026-04-18`
<!-- CORE_DOC_AUDIT_METADATA:END -->

## 範圍

本次僅核對根層 7 份旗艦文檔：

1. `README.md`
2. `功能特性清單.md`
3. `頁面清單.md`
4. `全接口清單-主文檔.md`
5. `接口-功能-頁面-Mapping.md`
6. `業務流程整合.md`
7. `術語表.md`

## 取證入口

1. `backend/src/app.ts`
2. `backend/src/routes/**/*.ts`
3. `backend/prisma/schema.prisma`
4. `frontend/src/router/index.tsx`
5. `frontend-admin/src/router.tsx`
6. `frontend/src/services/api/**/*.ts`
7. `frontend-admin/src/services/api/**/*.ts`
8. `scripts/check-docs-truth.mjs`

## 本輪發現與處置

| 編號 | 發現 | 影響 | 處置 |
|---|---|---|---|
| B1-01 | `全接口清單-主文檔.md` 快照統計中的狀態數與實際端點狀態行不一致（原為 `已使用 99 / 候選廢棄 10`） | 旗艦主冊統計失真，可能誤導回歸範圍估算 | 依現碼端點逐條核對後修正為 `已使用 115 / 候選廢棄 11` |
| B1-02 | `docs:check:truth` 對 endpoint 行匹配使用字符串包含，`/version` 可能被 `/api/v1/version` 誤命中 | 守衛存在誤判空間，難以及時發現統計與狀態漂移 | 改為 endpoint 精確匹配，並新增狀態統計自洽與 API/Mapping 狀態對齊校驗 |

## 守衛補強

本輪新增以下真實性守衛（已進入 `npm run docs:check:truth`）：

1. `全接口清單-主文檔.md` 端點行精確匹配（避免 path 子串誤命中）。
2. 端點狀態匯總值與主冊明文統計值一致性校驗（`已使用/候選廢棄/已確認廢棄`）。
3. `全接口清單-主文檔.md` 與 `接口-功能-頁面-Mapping.md` 端點狀態逐條對齊。
4. 主冊與 Mapping 的額外端點行（非後端註冊、非允許的 stream 展開別名）檢測。
5. `GET /api/v1/streams/:scopeType/:scopeId` 的四條展開路徑在主冊與 Mapping 的完整性校驗。

## 驗證結果

執行命令：

```bash
npm run docs:check
```

結果：

1. `docs:check:structure` PASS
2. `docs:check:truth` PASS（`126 endpoints / 30 frontend routes / 9 admin routes`）

## 結論

1. Batch 1 旗艦文檔與現碼主鏈路在路由、端點、守衛、枚舉與流程口徑上保持一致。
2. 本輪已修復一項真實漂移與一項守衛精度問題。
3. 後續若端點狀態或主冊統計再漂移，CI 會直接失敗，不再依賴人工抽查發現。
